# {{FEATURE}} — <topic> schema spec

Owner standard:
- `docs/project_management/system/standards/planning/PLANNING_SPEC_DETERMINATION_STANDARD.md`

## Scope
- This spec is authoritative for the <topic> data schema / file format.

## Format
- Format: JSON | YAML | TOML | other
- Canonical file name(s):
- Canonical location(s):

## Compatibility policy (explicit)
- Forward compatibility:
- Backward compatibility:
- Unknown fields handling:
- Deprecation policy:

## Schema (authoritative)

### Top-level
- Fields:
  - `<field>`:
    - Type:
    - Required: yes/no
    - Allowed values / constraints:
    - Default:
    - Absence semantics (what happens if missing):

### Canonicalization rules (explicit)
- Normalization:
- Sorting:
- Deterministic serialization expectations (if any):

## Examples (authoritative)
- Minimal valid:
- Full valid:
- Invalid (with expected error):

## Error model (explicit)
- Parsing errors:
- Validation errors:
- Exit code mapping (if CLI-facing):

## Security / redaction (explicit)
- Sensitive fields:
- Logging rules:

## Acceptance criteria (testable)
- Schema validation gates:
- Round-trip/canonicalization assertions:

