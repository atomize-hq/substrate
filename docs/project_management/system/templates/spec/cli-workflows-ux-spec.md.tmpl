# {{FEATURE}} — <topic> CLI workflows / output UX spec

Owner standard:
- `docs/project_management/system/standards/planning/PLANNING_SPEC_DETERMINATION_STANDARD.md`

## Scope
- This spec is authoritative for the end-to-end operator workflows and user-facing outputs introduced or changed by <topic>.

## Non-goals
- <explicitly list what is out of scope (e.g., internal refactors, non-user-facing logs, unrelated commands)>

## CLI surface inventory (authoritative)

List every CLI surface touched by this work (commands, flags, env vars, config keys, defaults).

| Surface type | Name | Scope | Default | Notes |
| --- | --- | --- | --- | --- |
| command | `<cmd>` | <platforms> | n/a | |
| flag | `--<flag>` | <platforms> | `<value>` | |
| env var | `<ENV_VAR>` | <platforms> | `<value>` | |
| config key | `<key>` | <platforms> | `<value>` | |

## Workflow catalog (authoritative)

Catalog should cover ~70–80% of expected real-world usage for the change.

### WF-01 — <short name>
- Goal:
- Preconditions:
- Commands (ordered; copy/paste):
  - `...`
- Expected outputs (explicit; authoritative):
  - stdout:
    - <lines or shape>
  - stderr:
    - <lines or shape>
  - `--json` (if applicable):
    - Shape/schema: <inline or link to schema spec>
    - Required fields:
    - Optional fields (only if absence semantics are explicit):
- Exit codes (explicit):
  - `0`:
  - `2`:
  - <etc; must align with `EXIT_CODE_TAXONOMY.md`>

### WF-02 — <short name>
- <repeat>

## Output conventions (authoritative)

### Human-readable output rules
- Required line(s) / prefix conventions:
- Redaction requirements:
- Ordering/stability guarantees (if any):
- Warnings/errors:
  - Required guidance elements (what must be included):
  - Prohibited content (privacy / secrets):

### `--json` output rules (if applicable)
- Stable schema versioning:
- Field naming conventions:
- Backwards/forwards compatibility:

## UX consistency rules (authoritative)

Define cross-workflow consistency requirements such as:
- Flag naming and semantics consistency across commands.
- Default behavior consistency (and explicit exceptions).
- Error phrasing conventions (actionable, includes remediation).
- Deterministic ordering rules where output is list-like.

## Examples (copy/paste; authoritative)
- Minimal examples:
- Common examples:
- Failure examples (with expected outputs and exit codes):

## Validation evidence (required)

Define how this spec is validated:
- Manual workflow scripts to run (paths + commands):
- Automated tests / golden tests:
  - Which workflow(s) are covered:
  - What is asserted (exact output vs shape vs key elements):

