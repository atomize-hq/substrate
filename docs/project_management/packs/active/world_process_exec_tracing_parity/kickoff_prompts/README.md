# Kickoff prompts — world_process_exec_tracing_parity

Each task has an authoritative kickoff prompt that defines:
- scope boundaries,
- required commands, and
- the “no docs edits in worktrees” guardrail.

