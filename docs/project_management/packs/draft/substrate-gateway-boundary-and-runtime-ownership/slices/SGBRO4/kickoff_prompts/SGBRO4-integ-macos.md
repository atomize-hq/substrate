# Kickoff: SGBRO4-integ-macos (integration macOS)

## Scope
- macOS platform-fix validation after `CP1-ci-checkpoint`.
- Spec: `docs/project_management/packs/draft/substrate-gateway-boundary-and-runtime-ownership/slices/SGBRO4/SGBRO4-spec.md`

## Start Checklist
1. Read `plan.md`, `tasks.json`, `session_log.md`, the spec, and this prompt.
2. Verify the task worktree exists before editing task-owned files.
3. Merge `SGBRO4-integ-core` before validation.

## End Checklist
1. Run the platform smoke and required checks.
2. Run `make triad-task-finish TASK_ID="SGBRO4-integ-macos"`.
