# Kickoff: SGBRO0-integ (integration)

## Scope
- Merge the `SGBRO0` planning slice and keep the accepted five-slice order coherent.
- Spec: `docs/project_management/packs/draft/substrate-gateway-boundary-and-runtime-ownership/slices/SGBRO0/SGBRO0-spec.md`

## Start Checklist
1. Read `plan.md`, `tasks.json`, `session_log.md`, the spec, and this prompt.
2. Verify the task worktree exists before editing task-owned files.
3. Keep changes inside the current slice boundary.

## End Checklist
1. Run the required integration checks.
2. Run `make triad-task-finish TASK_ID="SGBRO0-integ"`.
