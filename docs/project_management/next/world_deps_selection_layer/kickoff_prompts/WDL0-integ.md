# WDL0-integ kickoff — Selection config + UX integration

Merge WDL0 code + tests, reconcile to `S0`, then run:
- `cargo fmt`
- `cargo clippy --workspace --all-targets -- -D warnings`
- Relevant `cargo test ...` suites
- `make preflight`

Capture outputs in PR/testing notes.

