# Kickoff prompts — llm_and_agent_config_policy_surface

Every task in `tasks.json` has a matching kickoff prompt in this directory.

Sentinel rule:
- Every kickoff prompt MUST include the exact line: `Do not edit planning docs inside the worktree.`

