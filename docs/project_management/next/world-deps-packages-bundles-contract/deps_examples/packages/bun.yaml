version: 1
name: bun
description: Bun runtime (script install into world-deps prefix).
runnable: true
entrypoints: ["bun"]
install:
  method: script
  script_path: ../scripts/bun.sh
probe:
  command: "bun --version"

