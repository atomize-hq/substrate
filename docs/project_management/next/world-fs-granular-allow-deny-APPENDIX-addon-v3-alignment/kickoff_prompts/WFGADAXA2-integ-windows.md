# Kickoff: WFGADAXA2-integ-windows (integration — Windows parity fix)

## Scope
- Runs after CP1 completes.
- Apply fixes only if Windows compile parity / CI testing failed at CP1.

## Requirements
- Do not edit planning docs inside the worktree.
- Use CP1 evidence (run id/URL) to scope work; rerun parity gate(s) until green.

