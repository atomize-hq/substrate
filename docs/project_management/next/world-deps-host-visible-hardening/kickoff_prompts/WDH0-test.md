# Kickoff: WDH0-test (test)

## Scope
- Tests only (unit/integration tests as defined by the spec).
- Spec: `docs/project_management/next/world-deps-host-visible-hardening/WDH0-spec.md`

## Requirements
- Add tests asserting PATH is sanitized and prefixed correctly for both PTY and non-PTY request builders.
- Do not edit planning docs inside the worktree.

