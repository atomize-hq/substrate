# {{FEATURE}} — plan

## Scope
- Feature directory: `{{FEATURE_DIR}}`
- Orchestration branch: `feat/{{FEATURE}}`
- Spec ownership map: `{{FEATURE_DIR}}/spec_manifest.md`
- Impact map: `{{FEATURE_DIR}}/impact_map.md`

## Goal
- Produce shippable, test-backed behavior aligned to the specs in this directory.

## Guardrails (non-negotiable)
- Specs are the single source of truth.
- Planning Pack docs are edited only on the orchestration branch.
- Do not edit planning docs inside the worktree.

## Triads
- {{SLICE_ID}}: init slice (code/test/integ)
- Prefer more (smaller) triads over fewer (larger) ones; avoid “grab bag” slices that mix multiple independent behavior changes.
