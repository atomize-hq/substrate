# spec_manifest — llm_gateway_in_world

This is a lightweight placeholder manifest for ADR-0023 planning outputs. It will be expanded during planning pack hardening.

Required (v1):
- `contract.md` (operator-facing)
- `decision_register.md` (A/B decisions)
- `specs/env_injection.md` (secret delivery; no secrets in Substrate YAML)

Future (when implementation starts):
- HTTP contract spec(s) for supported endpoint subsets + streaming semantics
- Trace/event family spec(s) for LLM request lifecycle (aligned in Phase 8 with ADR-0028)
- Smoke + manual testing playbooks
