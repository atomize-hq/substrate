OpenAI Codex v0.77.0 (research preview)
--------
workdir: /home/spenser/__Active_code/substrate/wt/workspace_config_policy_unification-wcu4-test
model: gpt-5.2
provider: openai
approval: never
sandbox: danger-full-access
reasoning effort: high
reasoning summaries: auto
session id: 019bc72d-282e-71a0-b4ca-1aab07e6c246
--------
user
# Kickoff: WCU4-test (test)

## Scope
- Tests only; no production code.
- Spec: `docs/project_management/next/workspace-config-policy-unification/WCU4-spec.md`
- ADR: `docs/project_management/adrs/implemented/ADR-0008-workspace-config-policy-scope-and-dot-substrate-unification.md`
- Execution workflow standard: `docs/project_management/standards/TASK_TRIADS_WORKTREE_EXECUTION_STANDARD.md`

Do not edit planning docs inside the worktree.

## Start Checklist
1. Verify you are in the task worktree `wt/workspace_config_policy_unification-wcu4-test` on branch `workspace_config_policy_unification-wcu4-test` and that `.taskmeta.json` exists.
2. Read: WCU4-spec, ADR-0008, plan/tasks/session log, this prompt.
3. If `.taskmeta.json` is missing or mismatched, stop and ask the operator to run:
   - `make triad-task-start-pair FEATURE_DIR="docs/project_management/next/workspace-config-policy-unification" SLICE_ID="WCU4"`

## Requirements
- Add/modify tests that enforce WCU4 acceptance criteria in `tasks.json`.
- Run: `cargo fmt`, plus targeted tests you add/touch.

## End Checklist
1. From inside the worktree, run: `make triad-task-finish TASK_ID="WCU4-test"`

mcp: deepwiki starting
mcp: deepwiki ready
mcp startup: ready: deepwiki
exec
/bin/bash -lc 'pwd
git rev-parse --abbrev-ref HEAD
git status --porcelain
ls -a
test -f .taskmeta.json && echo ".taskmeta.json: present" || echo ".taskmeta.json: MISSING"' in /home/spenser/__Active_code/substrate/wt/workspace_config_policy_unification-wcu4-test succeeded in 47ms:
/home/spenser/__Active_code/substrate/wt/workspace_config_policy_unification-wcu4-test
workspace_config_policy_unification-wcu4-test
.
..
AGENTS.md
Cargo.lock
Cargo.toml
CHANGELOG.md
config
CONTRIBUTING.md
crates
Cross.toml
deny.toml
dist
dist-workspace.toml
docs
.git
.github
.gitignore
LICENSE
exec
/bin/bash -lc 'cat .taskmeta.json 2>/dev/null || true' in /home/spenser/__Active_code/substrate/wt/workspace_config_policy_unification-wcu4-test succeeded in 39ms:
{
  "created_at_utc": "2026-01-16T14:19:43Z",
  "created_from_sha": "cbfa6ad466a36afd5cd493e8c980cde4efca5559",
  "feature_dir": "docs/project_management/next/workspace-config-policy-unification",
  "orchestration_branch": "feat/workspace-config-policy-unification",
  "schema_version": 1,
  "task_branch": "workspace_config_policy_unification-wcu4-test",
  "task_id": "WCU4-test"
}
exec
/bin/bash -lc 'ls docs/project_management/next/workspace-config-policy-unification || true
ls docs/project_management/next || true' in /home/spenser/__Active_code/substrate/wt/workspace_config_policy_unification-wcu4-test succeeded in 35ms:
decision_register.md
execution_preflight_report.md
integration_map.md
kickoff_prompts
manual_testing_playbook.md
PHASE_A_B_GATES_ADR_0012.md
plan.md
quality_gate_report.md
session_log.md
smoke
tasks.json
WCU1-closeout_report.md
WCU1-spec.md
WCU2-closeout_report.md
WCU2-spec.md
WCU3-closeout_report.md
WCU3-spec.md
WCU4-closeout_report.md
WCU4-spec.md
WCU5-closeout_report.md
exec
/bin/bash -lc 'ls docs/project_management/next/workspace-config-policy-unification/WCU4-spec.md 2>/dev/null && echo "WCU4 spec present" || echo "WCU4 spec missing"' in /home/spenser/__Active_code/substrate/wt/workspace_config_policy_unification-wcu4-test succeeded in 40ms:
docs/project_management/next/workspace-config-policy-unification/WCU4-spec.md
WCU4 spec present
exec
/bin/bash -lc "sed -n '1,200p' docs/project_management/next/workspace-config-policy-unification/WCU4-spec.md" in /home/spenser/__Active_code/substrate/wt/workspace_config_policy_unification-wcu4-test succeeded in 40ms:
# WCU4 Spec — Installer/dev env cleanup (stop exporting `SUBSTRATE_OVERRIDE_*` by default)

References:
- `docs/project_management/adrs/implemented/ADR-0008-workspace-config-policy-scope-and-dot-substrate-unification.md`
- `docs/project_management/standards/EXIT_CODE_TAXONOMY.md`

## Scope
- Remove default exporting of `SUBSTRATE_OVERRIDE_*` from install/dev environment scripts so overrides are treated as explicit one-off operator input.

## Non-goals
- Changing override semantics themselves (overrides remain supported when explicitly provided by the operator).

## Authoritative contract
- Install/dev scripts MUST NOT export any `SUBSTRATE_OVERRIDE_*` environment variables by default.
- Operators may still set `SUBSTRATE_OVERRIDE_*` explicitly in their shell/session; Substrate respects them as highest-precedence inputs.

## Validation requirements (authoritative)
- Tests and/or integration checks must demonstrate:
  - a clean environment has no `SUBSTRATE_OVERRIDE_*` exports after running the relevant dev/install scripts, and
  - `config current show` behavior is not silently affected by unintended override exports.
exec
/bin/bash -lc "sed -n '1,220p' docs/project_management/next/workspace-config-policy-unification/ADR-0008-workspace-config-policy-scope-and-dot-substrate-unification.md" in /home/spenser/__Active_code/substrate/wt/workspace_config_policy_unification-wcu4-test exited 2 in 40ms:
sed: can't read docs/project_management/next/workspace-config-policy-unification/ADR-0008-workspace-config-policy-scope-and-dot-substrate-unification.md: No such file or directory
exec
/bin/bash -lc "sed -n '1,220p' docs/project_management/next/workspace-config-policy-unification/plan.md" in /home/spenser/__Active_code/substrate/wt/workspace_config_policy_unification-wcu4-test succeeded in 44ms:
# Workspace Config/Policy Unification (ADR-0008) — Plan

## Context (why this exists)
This Planning Pack implements the operator-facing contract defined by:
- `docs/project_management/adrs/implemented/ADR-0008-workspace-config-policy-scope-and-dot-substrate-unification.md`

This body of work also **must** implement the cross-cutting refinement defined by:
- `docs/project_management/adrs/implemented/ADR-0012-config-schema-per-key-merge-and-provenance.md`

Non-negotiable additional gating for this pack:
- `docs/project_management/next/workspace-config-policy-unification/PHASE_A_B_GATES_ADR_0012.md`

## Execution model (triads + automation)
- Standards:
  - `docs/project_management/standards/TASK_TRIADS_AND_FEATURE_SETUP.md`
  - `docs/project_management/standards/TASK_TRIADS_WORKTREE_EXECUTION_STANDARD.md`
  - `docs/project_management/standards/EXECUTION_PREFLIGHT_GATE_STANDARD.md` (because `tasks.json` enables execution gates)
- Orchestration branch: `feat/workspace-config-policy-unification`
- Docs discipline:
  - planning docs (`plan.md`, `tasks.json`, `session_log.md`, specs, prompts) are edited only on the orchestration branch
exec
/bin/bash -lc 'cat docs/project_management/next/workspace-config-policy-unification/tasks.json' in /home/spenser/__Active_code/substrate/wt/workspace_config_policy_unification-wcu4-test succeeded in 42ms:
{
  "meta": {
    "schema_version": 3,
    "feature": "workspace_config_policy_unification",
    "cross_platform": true,
    "behavior_platforms_required": [
      "linux",
      "macos",
      "windows"
    ],
    "ci_parity_platforms_required": [
      "linux",
      "macos",
      "windows"
    ],
    "execution_gates": true,
    "automation": {
      "enabled": true,
      "orchestration_branch": "feat/workspace-config-policy-unification"
    }
exec
/bin/bash -lc "sed -n '1,220p' docs/project_management/next/workspace-config-policy-unification/session_log.md" in /home/spenser/__Active_code/substrate/wt/workspace_config_policy_unification-wcu4-test succeeded in 38ms:
# workspace-config-policy-unification — session log

## START — 2026-01-14T00:00:00Z — planning — Phase A/B gate scaffolding
- Feature: `docs/project_management/next/workspace-config-policy-unification/`
- Branch: `feat/workspace-config-policy-unification`
- Goal: Add Phase A/B gates (ADR-0012) scaffolding and ensure it is mechanically encoded in the Planning Pack.
- Inputs to read end-to-end:
  - `docs/project_management/standards/TASK_TRIADS_AND_FEATURE_SETUP.md`
  - `docs/project_management/standards/PLANNING_RESEARCH_AND_ALIGNMENT_STANDARD.md`
  - `docs/project_management/adrs/implemented/ADR-0008-workspace-config-policy-scope-and-dot-substrate-unification.md`
  - `docs/project_management/adrs/implemented/ADR-0012-config-schema-per-key-merge-and-provenance.md`
- Commands planned (if any):
  - `make planning-lint FEATURE_DIR="docs/project_management/next/workspace-config-policy-unification"`

## END — 2026-01-14T00:00:00Z — planning — Phase A/B gate scaffolding
- Summary of changes (exhaustive):
  - Added explicit Phase A/B gate file for ADR-0012 and wired it as a non-negotiable Planning Pack input
  - Added Planning Pack scaffolding files (`plan.md`, `tasks.json`, `integration_map.md`, gate report templates, smoke stubs)
- Files created/modified:
  - `docs/project_management/next/workspace-config-policy-unification/PHASE_A_B_GATES_ADR_0012.md`


thinking
**Reading project documents**

I realized I didn't read ADR-0008 because the path differs. It’s located in docs/project_management/next/workspace-config-policy-unification. For WCU4, I need to access the correct file path, which I mistakenly omitted earlier. Now I have the right file name and will read it. I also need to check the task triads standard document and the kickoff prompt—I might skip the prompt for now. Additionally, I'll ensure the tests enforce that dev/install scripts don’t set certain environment variables.
thinking
**Implementing test checks**

The spec indicates that integration tests need to show there are no environment variables left after running the relevant scripts. I'll focus on scripts likely found in the `scripts/` directory, like `scripts/dev-entrypoint.sh`, to ensure exports are removed. The code should already be changed in the WCU4-code branch, so I'll just need to add tests to verify the current behavior. I plan to inspect existing scripts for any "SUBSTRATE_OVERRIDE_" exports and frequent test patterns, possibly in Bash or Rust that run shell processes.
exec
/bin/bash -lc "sed -n '1,260p' docs/project_management/next/workspace-config-policy-unification/ADR-0008-workspace-config-policy-scope-and-dot-substrate-unification.md" in /home/spenser/__Active_code/substrate/wt/workspace_config_policy_unification-wcu4-test exited 2 in 39ms:
sed: can't read docs/project_management/next/workspace-config-policy-unification/ADR-0008-workspace-config-policy-scope-and-dot-substrate-unification.md: No such file or directory
exec
/bin/bash -lc "sed -n '1,220p' docs/project_management/standards/TASK_TRIADS_WORKTREE_EXECUTION_STANDARD.md" in /home/spenser/__Active_code/substrate/wt/workspace_config_policy_unification-wcu4-test succeeded in 38ms:
# Task Triads — Worktree Execution Standard (Automation + Concurrent Code/Test)

This standard is for **execution-time** triad work when tasks are started via triad automation (preferred), and the agent is already running **inside a task worktree**.

It is a focused companion to:
- `docs/project_management/standards/TASK_TRIADS_AND_FEATURE_SETUP.md` (full planning pack + task schema + integration model)

## Operating assumptions (the situation you are in)

When this standard applies, the operator started you with one of:
- `make triad-task-start-pair FEATURE_DIR="docs/project_management/next/<feature>" SLICE_ID="<slice>" LAUNCH_CODEX=1` (preferred)
- `make triad-task-start FEATURE_DIR="docs/project_management/next/<feature>" TASK_ID="<task-id>" LAUNCH_CODEX=1`

You are already:
- in a git worktree (usually under `wt/...`)
- on a task branch (not the orchestration branch)
- expected to find `.taskmeta.json` at the worktree root (task id, orchestration branch, etc.)

## Non-negotiable safety rule

exec
/bin/bash -lc 'rg -n "SUBSTRATE_OVERRIDE_" -S .' in /home/spenser/__Active_code/substrate/wt/workspace_config_policy_unification-wcu4-test succeeded in 62ms:
./docs/CONFIGURATION.md:59:- `SUBSTRATE_OVERRIDE_*`: operator-provided override inputs (read by the effective-config resolver).
./docs/CONFIGURATION.md:62:### Override inputs (`SUBSTRATE_OVERRIDE_*`)
./docs/CONFIGURATION.md:66:| `SUBSTRATE_OVERRIDE_WORLD` | Override `world.enabled` | *unset* | `disabled` |
./docs/CONFIGURATION.md:67:| `SUBSTRATE_OVERRIDE_ANCHOR_MODE` | Override `world.anchor_mode` (`workspace`, `follow-cwd`, `custom`) | *unset* | `workspace` |
./docs/CONFIGURATION.md:68:| `SUBSTRATE_OVERRIDE_ANCHOR_PATH` | Override `world.anchor_path` (paired with `custom`) | *unset* | `/workspaces/substrate` |
./docs/CONFIGURATION.md:69:| `SUBSTRATE_OVERRIDE_CAGED` | Override `world.caged` (boolean) | *unset* | `0` |
./docs/CONFIGURATION.md:70:| `SUBSTRATE_OVERRIDE_POLICY_MODE` | Override `policy.mode` (`disabled`, `observe`, `enforce`) | *unset* | `enforce` |
./docs/CONFIGURATION.md:71:| `SUBSTRATE_OVERRIDE_SYNC_AUTO_SYNC` | Override `sync.auto_sync` (boolean) | *unset* | `1` |
./docs/CONFIGURATION.md:72:| `SUBSTRATE_OVERRIDE_SYNC_DIRECTION` | Override `sync.direction` (`from_world`, `from_host`, `both`) | *unset* | `from_host` |
./docs/CONFIGURATION.md:73:| `SUBSTRATE_OVERRIDE_SYNC_CONFLICT_POLICY` | Override `sync.conflict_policy` (`prefer_host`, `prefer_world`, `abort`) | *unset* | `abort` |
./docs/CONFIGURATION.md:74:| `SUBSTRATE_OVERRIDE_SYNC_EXCLUDE` | Override `sync.exclude` (comma-separated list) | *unset* | `.git,node_modules` |
./docs/CONFIGURATION.md:77:- When `<workspace_root>/.substrate/workspace.yaml` exists, `SUBSTRATE_OVERRIDE_*` is ignored for effective config.
./docs/CONFIGURATION.md:108:3. Environment overrides: `SUBSTRATE_OVERRIDE_*`
./docs/CONFIGURATION.md:156:  or `SUBSTRATE_OVERRIDE_WORLD=disabled` disables it; metadata stays unchanged and `--no-world` still wins when provided.
./docs/internals/env/generation.md:20:   - Ensure `SUBSTRATE_OVERRIDE_*` reserved names are excluded unless the repo actually reads/writes them.
./docs/internals/env/inventory.md:23:- `SUBSTRATE_OVERRIDE_*`: operator/test override inputs to effective-config resolution (separate from exported state).
./docs/internals/env/inventory.md:32:- `override input`: `SUBSTRATE_OVERRIDE_*` names for config-shaped override inputs (ADR-0006).
./docs/internals/env/inventory.md:40:- Planned config override inputs use the `SUBSTRATE_OVERRIDE_*` prefix (ADR-0006).
./docs/internals/env/inventory.md:46:## Config Override Inputs (`SUBSTRATE_OVERRIDE_*`, ADR-0006)
./docs/internals/env/inventory.md:50:- `SUBSTRATE_OVERRIDE_WORLD`
exec
/bin/bash -lc 'rg -n "config current show" -S crates tests scripts src | head' in /home/spenser/__Active_code/substrate/wt/workspace_config_policy_unification-wcu4-test succeeded in 49ms:
crates/shell/tests/config_world_deps_phase_b.rs:172:        cmd.output().expect("run config current show --json")
crates/shell/tests/config_world_deps_phase_b.rs:238:        "config current show should succeed: {current:?}"
crates/shell/tests/config_world_deps_phase_b.rs:383:        "config current show should succeed: {current:?}"
crates/shell/tests/config_world_deps_phase_b.rs:424:        "config current show should succeed after reset: {current:?}"
crates/shell/tests/config_world_deps_phase_b.rs:485:        "config current show should succeed: {current:?}"
exec
/bin/bash -lc 'rg -n "dev-entrypoint|world-provision|install" scripts docs -S | head -n 50' in /home/spenser/__Active_code/substrate/wt/workspace_config_policy_unification-wcu4-test succeeded in 40ms:
docs/REPLAY.md:94:- Linux replays attempt to install nftables policy inside a per-replay netns. When `ip netns add` fails (missing CAP_NET_ADMIN, stale namespaces, etc.), `--replay-verbose` prints a warning and falls back to socket cgroup matching. The fallback scopes nft rules to `/sys/fs/cgroup/substrate/<WORLD_ID>`, keeping host traffic untouched even though rules run in the host namespace.
docs/REPLAY.md:109:- If the replayed command modifies files (e.g., `npm install`), run on a disposable project copy or in a clean world.
scripts/linux/world-provision.sh:5:# installs it under /usr/local/bin, writes both the .service and .socket unit
scripts/linux/world-provision.sh:9:# Usage: scripts/linux/world-provision.sh [--profile <name>] [--skip-build] [--dry-run]
scripts/linux/world-provision.sh:130:install_unit() {
scripts/linux/world-provision.sh:134:        printf '[dry-run] install unit %s\n' "${destination}"
scripts/linux/world-provision.sh:140:    sudo install -Dm0644 "${tmp}" "${destination}"
scripts/linux/world-provision.sh:146:Usage: scripts/linux/world-provision.sh [options]
scripts/linux/world-provision.sh:253:[Install]
scripts/linux/world-provision.sh:271:[Install]
scripts/linux/world-provision.sh:275:echo "==> Installing world-agent to /usr/local/bin (sudo will prompt if needed)"
scripts/linux/world-provision.sh:276:run_cmd sudo install -Dm0755 "${BIN_PATH}" /usr/local/bin/substrate-world-agent
scripts/linux/world-provision.sh:279:run_cmd sudo install -d -m0750 /run/substrate
scripts/linux/world-provision.sh:280:run_cmd sudo install -d -m0750 /var/lib/substrate
scripts/linux/world-provision.sh:283:install_unit "${SERVICE_PATH}" "${SERVICE_UNIT_CONTENT}"
scripts/linux/world-provision.sh:284:install_unit "${SOCKET_PATH}" "${SOCKET_UNIT_CONTENT}"
docs/VISION.md:78:  - "pip install *"
docs/VISION.md:79:  - "npm install *"
scripts/linux/agent-hub-isolation-verify.sh:102:  - Linux: run scripts/linux/world-provision.sh (installs systemd socket/service)
scripts/linux/world-socket-verify.sh:8:Provision the systemd-managed substrate world-agent socket via scripts/linux/world-provision.sh,
exec
/bin/bash -lc 'ls -1 docs/project_management/next/workspace-config-policy-unification | rg "ADR-0008" || true
ls -1 docs/project_management/next/workspace-config-policy-unification | head' in /home/spenser/__Active_code/substrate/wt/workspace_config_policy_unification-wcu4-test succeeded in 41ms:
decision_register.md
execution_preflight_report.md
integration_map.md
kickoff_prompts
manual_testing_playbook.md
PHASE_A_B_GATES_ADR_0012.md
plan.md
quality_gate_report.md
session_log.md
smoke


thinking
**Investigating ADR file issues**

I'm exploring why ADRs aren't showing up. Earlier, I noted items like ADR-0008 after WCU specs, but now the head listing shows only the first 10 items. Maybe the ADR file is located deeper down, or `rg` didn’t match properly. I recall `ls docs/project_management/next/workspace-config-policy-unification` did list ADR-0008, but now it doesn’t. I wonder if the directory path issue could be due to using hyphens instead of underscores, which might explain the missing files.
thinking
**Checking directory outputs**

Okay, I'm wondering about the output inconsistency. It seems that files like decision_register.md might have been affected by how `ls -1` sorts items, potentially putting ADRs after, but since `rg` didn't show them, maybe they aren't present at all. It's possible the previous output got truncated or combined with another directory list. To get clarity, I’ll rerun the command `ls docs/project_management/next/workspace-config-policy-unification | tail` and see what I find there.
exec
/bin/bash -lc 'ls -1 docs/project_management/next/workspace-config-policy-unification | tail -n 40' in /home/spenser/__Active_code/substrate/wt/workspace_config_policy_unification-wcu4-test succeeded in 41ms:
decision_register.md
execution_preflight_report.md
integration_map.md
kickoff_prompts
manual_testing_playbook.md
PHASE_A_B_GATES_ADR_0012.md
plan.md
quality_gate_report.md
session_log.md
smoke
tasks.json
WCU1-closeout_report.md
WCU1-spec.md
WCU2-closeout_report.md
WCU2-spec.md
WCU3-closeout_report.md
WCU3-spec.md
WCU4-closeout_report.md
WCU4-spec.md
WCU5-closeout_report.md
exec
/bin/bash -lc "sed -n '1,260p' docs/project_management/adrs/implemented/ADR-0008-workspace-config-policy-scope-and-dot-substrate-unification.md" in /home/spenser/__Active_code/substrate/wt/workspace_config_policy_unification-wcu4-test succeeded in 44ms:
# ADR-0008 — Workspace Config/Policy Scopes + `.substrate/` Unification (Patch Files)

## Status
- Status: Approved
- Date (UTC): 2026-01-10
- Owner(s): spenser

## Scope
- Feature directory: `docs/project_management/next/workspace-config-policy-unification/`
- Sequencing spine: `docs/project_management/next/sequencing.json`
- Standards:
  - `docs/project_management/standards/ADR_STANDARD_AND_TEMPLATE.md`
  - `docs/project_management/standards/PLANNING_RESEARCH_AND_ALIGNMENT_STANDARD.md`
  - `docs/project_management/standards/TASK_TRIADS_AND_FEATURE_SETUP.md`
  - `docs/project_management/standards/TASK_TRIADS_WORKTREE_EXECUTION_STANDARD.md`
  - `docs/project_management/standards/EXIT_CODE_TAXONOMY.md`

## Related Docs
- Prior ADRs:
  - `docs/project_management/adrs/queued/ADR-0003-policy-and-config-mental-model-simplification.md`
exec
/bin/bash -lc "jq '.tasks[] | select(.id==\"WCU4-test\")' docs/project_management/next/workspace-config-policy-unification/tasks.json" in /home/spenser/__Active_code/substrate/wt/workspace_config_policy_unification-wcu4-test succeeded in 44ms:
{
  "id": "WCU4-test",
  "name": "WCU4 — Installer/dev env cleanup (test)",
  "type": "test",
  "phase": "WCU4",
  "status": "in_progress",
  "description": "Add/adjust tests for installer/env-script behavior so override exports are not required for correctness.",
  "references": [
    "docs/project_management/adrs/implemented/ADR-0008-workspace-config-policy-scope-and-dot-substrate-unification.md",
    "docs/project_management/next/workspace-config-policy-unification/decision_register.md (DR-0007)",
    "docs/project_management/next/workspace-config-policy-unification/plan.md",
    "docs/project_management/next/workspace-config-policy-unification/WCU4-spec.md",
    "docs/project_management/standards/TASK_TRIADS_WORKTREE_EXECUTION_STANDARD.md"
  ],
  "acceptance_criteria": [
    "Tests prove effective config does not require `SUBSTRATE_OVERRIDE_*` exports to behave correctly"
  ],
  "start_checklist": [
    "Run: make triad-orch-ensure FEATURE_DIR=\"docs/project_management/next/workspace-config-policy-unification\"",
    "Read: ADR-0008, plan.md, kickoff prompt",

thinking
**Inspecting scripts for overrides**

I see that the specification states a clean environment should not have any `SUBSTRATE_OVERRIDE_*` exports after running the relevant installation scripts. To address this, I need to run those scripts in a subshell and verify the output environment. However, the scripts might be designed to be sourced, not executed directly. I'll check both `install-substrate.sh` and `dev-entrypoint.sh` to search for any override exports and see what those scripts contain.
exec
/bin/bash -lc 'rg -n "SUBSTRATE_OVERRIDE_" scripts/substrate/install-substrate.sh scripts/dev-entrypoint.sh scripts -S || true' in /home/spenser/__Active_code/substrate/wt/workspace_config_policy_unification-wcu4-test succeeded in 60ms:
scripts/substrate/install-substrate.sh:923:export SUBSTRATE_OVERRIDE_WORLD=${world_literal}
scripts/substrate/install-substrate.sh:924:export SUBSTRATE_OVERRIDE_CAGED=1
scripts/substrate/install-substrate.sh:925:export SUBSTRATE_OVERRIDE_ANCHOR_MODE=${anchor_mode_literal}
scripts/substrate/install-substrate.sh:926:export SUBSTRATE_OVERRIDE_ANCHOR_PATH=${anchor_path_literal}
scripts/substrate/install-substrate.sh:927:export SUBSTRATE_OVERRIDE_POLICY_MODE=${policy_mode_literal}
scripts/substrate/install-substrate.sh:923:export SUBSTRATE_OVERRIDE_WORLD=${world_literal}
scripts/substrate/install-substrate.sh:924:export SUBSTRATE_OVERRIDE_CAGED=1
scripts/substrate/install-substrate.sh:925:export SUBSTRATE_OVERRIDE_ANCHOR_MODE=${anchor_mode_literal}
scripts/substrate/install-substrate.sh:926:export SUBSTRATE_OVERRIDE_ANCHOR_PATH=${anchor_path_literal}
scripts/substrate/install-substrate.sh:927:export SUBSTRATE_OVERRIDE_POLICY_MODE=${policy_mode_literal}
scripts/substrate/dev-install-substrate.sh:156:export SUBSTRATE_OVERRIDE_WORLD=${world_literal}
scripts/substrate/dev-install-substrate.sh:157:export SUBSTRATE_OVERRIDE_CAGED=${caged_literal}
scripts/substrate/dev-install-substrate.sh:158:export SUBSTRATE_OVERRIDE_ANCHOR_MODE=${anchor_mode_literal}
scripts/substrate/dev-install-substrate.sh:159:export SUBSTRATE_OVERRIDE_ANCHOR_PATH=${anchor_path_literal}
scripts/substrate/dev-install-substrate.sh:160:export SUBSTRATE_OVERRIDE_POLICY_MODE=${policy_mode_literal}
exec
/bin/bash -lc "nl -ba scripts/substrate/install-substrate.sh | sed -n '860,980p'" in /home/spenser/__Active_code/substrate/wt/workspace_config_policy_unification-wcu4-test succeeded in 46ms:
   860	if [[ -n "\${SUBSTRATE_MANAGER_ENV_ACTIVE:-}" ]]; then
   861	  return 0
   862	fi
   863	export SUBSTRATE_MANAGER_ENV_ACTIVE=1
   864	
   865	substrate_home="\${SUBSTRATE_HOME:-}"
   866	if [[ -z "\${substrate_home}" ]]; then
   867	  substrate_home="\$(cd "\$(dirname "\${BASH_SOURCE[0]}")" && pwd)"
   868	fi
   869	
   870	substrate_env="\${substrate_home}/env.sh"
   871	if [[ -f "\${substrate_env}" ]]; then
   872	  # shellcheck disable=SC1090
   873	  source "\${substrate_env}"
   874	fi
   875	
   876	manager_init_path="\${substrate_home}/manager_init.sh"
   877	if [[ -f "\${manager_init_path}" ]]; then
   878	  # shellcheck disable=SC1090
   879	  source "\${manager_init_path}"
exec
/bin/bash -lc "nl -ba scripts/dev-entrypoint.sh | sed -n '1,240p'" in /home/spenser/__Active_code/substrate/wt/workspace_config_policy_unification-wcu4-test succeeded in 42ms:
     1	#!/usr/bin/env bash
     2	set -euo pipefail
     3	
     4	# Purpose: prepare the container for substrate privileged tests when run with --privileged
     5	# - try to enable user namespaces (best-effort)
     6	# - ensure cgroup v2 is mounted
     7	# - ensure tracefs/debugfs are mounted (for extra debugging if needed)
     8	# - try to relax dmesg restrictions for nft LOG checks
     9	# - load relevant kernel modules if possible (best-effort)
    10	
    11	echo "[entrypoint] Starting privileged test environment setup"
    12	
    13	export HOME=${HOME:-/root}
    14	export RUST_LOG=${RUST_LOG:-info}
    15	
    16	# Best-effort sysctls (may require --privileged and host to allow)
    17	for kv in \
    18	  kernel.unprivileged_userns_clone=1 \
    19	  kernel.dmesg_restrict=0 \
    20	  net.ipv4.ip_unprivileged_port_start=0 \
exec
/bin/bash -lc 'ls -1 scripts/substrate
ls -1 scripts | head' in /home/spenser/__Active_code/substrate/wt/workspace_config_policy_unification-wcu4-test succeeded in 45ms:
dev-install-substrate.sh
dev-shim-bootstrap.sh
dev-uninstall-substrate.sh
install.sh
install-substrate.sh
loader
uninstall.sh
uninstall-substrate.sh
world-deps.yaml
world-enable.sh
check-container-prereqs.sh
check-host-prereqs.sh
ci
dev
dev-entrypoint.sh
e2e
linux
mac
planning
substrate
exec
/bin/bash -lc 'rg -n "override" scripts/dev-entrypoint.sh scripts/substrate/install-substrate.sh | head -n 100' in /home/spenser/__Active_code/substrate/wt/workspace_config_policy_unification-wcu4-test succeeded in 57ms:



thinking
**Checking environment scripts**

I think it's acceptable to check if the WCU4-code is already implemented on a separate branch, as our worktree might not include it. I'll create tests to ensure `env.sh` doesn't contain overrides during script execution. There’s a script for dev installations that might also need an update. I’ll investigate the "relevant dev/install scripts" so we can ensure they write to `env.sh`. The tests need to avoid relying on the system path or root access, possibly using Rust in a temporary directory.
thinking
**Exploring script options**

Okay, it looks like `write_env_sh_script` uses `ENV_SH_PATH`, which might come from a prefix set earlier. The install script likely relies on `PREFIX` for the output path, and I think we can run it with the `--prefix` option. I should check the available script options, though it might be lengthy. I wonder if there are tests in the repo for the install scripts? I’ll search for `install-substrate.sh` in the crate tests to find out.
exec
/bin/bash -lc "rg -n \"install-substrate\\.sh\" crates tests scripts | head -n 200" in /home/spenser/__Active_code/substrate/wt/workspace_config_policy_unification-wcu4-test succeeded in 55ms:
tests/mac/installer_parity_fixture.sh:419:  if ! "${REPO_ROOT}/scripts/substrate/install-substrate.sh" \
tests/mac/installer_parity_fixture.sh:491:  if ! "${REPO_ROOT}/scripts/substrate/install-substrate.sh" \
tests/mac/installer_parity_fixture.sh:536:  if ! "${REPO_ROOT}/scripts/substrate/uninstall-substrate.sh" --cleanup-state >"${log}" 2>&1; then
scripts/linux/world-socket-verify.sh:149:  log "Cleaning up via uninstall-substrate.sh"
scripts/linux/world-socket-verify.sh:150:  run "${REPO_ROOT}/scripts/substrate/uninstall-substrate.sh"
tests/installers/install_smoke.sh:729:    "${REPO_ROOT}/scripts/substrate/install-substrate.sh" "${args[@]}" \
tests/installers/install_smoke.sh:744:    "${REPO_ROOT}/scripts/substrate/uninstall-substrate.sh" \
tests/installers/install_smoke.sh:772:    "${REPO_ROOT}/scripts/substrate/dev-install-substrate.sh" "${args[@]}" \
tests/installers/install_state_smoke.sh:663:  "${REPO_ROOT}/scripts/substrate/install-substrate.sh" \
tests/installers/install_state_smoke.sh:692:  "${REPO_ROOT}/scripts/substrate/install-substrate.sh" \
tests/installers/install_state_smoke.sh:778:  "${REPO_ROOT}/scripts/substrate/uninstall-substrate.sh" \
tests/installers/install_state_smoke.sh:836:  "${REPO_ROOT}/scripts/substrate/uninstall-substrate.sh" \
tests/installers/install_state_smoke.sh:862:  "${REPO_ROOT}/scripts/substrate/uninstall-substrate.sh" \
scripts/substrate/uninstall-substrate.sh:38:  uninstall-substrate.sh [--cleanup-state] [--auto-cleanup] [-h|--help]
scripts/substrate/dev-install-substrate.sh:19:  dev-install-substrate.sh [--prefix <path>] [--profile <debug|release>] [--version-label <name>] [--no-world] [--anchor-mode <mode>] [--anchor-path <path>] [--caged|--uncaged] [--no-shims]
scripts/substrate/dev-install-substrate.sh:20:  dev-install-substrate.sh --help
scripts/substrate/dev-install-substrate.sh:484:  local -a helper_files=("world-enable.sh" "install-substrate.sh")
scripts/substrate/uninstall.sh:27:  if [[ -x "${root}/versions/current/scripts/substrate/uninstall-substrate.sh" ]]; then
scripts/substrate/uninstall.sh:28:    printf '%s\n' "${root}/versions/current/scripts/substrate/uninstall-substrate.sh"
scripts/substrate/uninstall.sh:53:      if [[ -n "${version_dir}" && -x "${version_dir}/scripts/substrate/uninstall-substrate.sh" ]]; then

thinking
**Inspecting test scripts**

I’m focusing on the test crate and thinking about how to run it with `cargo test -p shell`. There are scripts in the repository that can possibly spawn bash to execute them. I wonder if it should be cross-platform, especially since tests in `crates/shell` may only work on Linux. I’ll check the existing tests in `crates/shell/tests`, like `config_show.rs` and `config_world_deps_phase_b.rs`, for any useful patterns in how they execute scripts.
exec
/bin/bash -lc 'ls -1 crates/shell/tests' in /home/spenser/__Active_code/substrate/wt/workspace_config_policy_unification-wcu4-test succeeded in 32ms:
common.rs
config_init.rs
config_set.rs
config_show.rs
config_world_deps_phase_b.rs
doctor_scopes_ds0.rs
ev0_override_split.rs
fail_closed_semantics.rs
integration
logging.rs
manager_init.rs
needs_shell.rs
playbook_alignment.rs
policy_discovery.rs
policy_routing_semantics.rs
profile_guardrails.rs
redaction.rs
replay_world.rs
scripts.rs
shell_behavior.rs
exec
/bin/bash -lc "sed -n '1,260p' crates/shell/tests/config_show.rs" in /home/spenser/__Active_code/substrate/wt/workspace_config_policy_unification-wcu4-test succeeded in 47ms:
#![cfg(unix)]

mod common;

use assert_cmd::Command;
use common::{substrate_shell_driver, temp_dir};
use serde_json::Value as JsonValue;
use serde_yaml::Value as YamlValue;
use std::fs;
use std::path::{Path, PathBuf};
use tempfile::TempDir;

struct ConfigShowFixture {
    _temp: TempDir,
    home: PathBuf,
    substrate_home: PathBuf,
    workspace_root: PathBuf,
}

impl ConfigShowFixture {
exec
/bin/bash -lc "sed -n '1,260p' crates/shell/tests/config_world_deps_phase_b.rs" in /home/spenser/__Active_code/substrate/wt/workspace_config_policy_unification-wcu4-test succeeded in 44ms:
#![cfg(unix)]

mod common;

use assert_cmd::Command;
use common::{substrate_shell_driver, temp_dir};
use serde_json::Value as JsonValue;
use serde_yaml::Value as YamlValue;
use std::fs;
use std::path::{Path, PathBuf};
use tempfile::TempDir;

struct WorldDepsConfigFixture {
    _temp: TempDir,
    home: PathBuf,
    substrate_home: PathBuf,
    workspace_root: PathBuf,
}

impl WorldDepsConfigFixture {
exec
/bin/bash -lc 'rg -n "tempdir|TempDir" crates/shell/tests | head -n 100' in /home/spenser/__Active_code/substrate/wt/workspace_config_policy_unification-wcu4-test succeeded in 45ms:
crates/shell/tests/doctor_scopes_ds0.rs:157:        .tempdir_in("/tmp")
crates/shell/tests/doctor_scopes_ds0.rs:158:        .expect("create ds0 socket tempdir");
crates/shell/tests/redaction.rs:9:fn setup_isolated_home(temp: &tempfile::TempDir) -> PathBuf {
crates/shell/tests/policy_discovery.rs:10:use tempfile::TempDir;
crates/shell/tests/policy_discovery.rs:13:    _temp: TempDir,
crates/shell/tests/config_set.rs:11:use tempfile::TempDir;
crates/shell/tests/config_set.rs:14:    _temp: TempDir,
crates/shell/tests/logging.rs:15:fn setup_isolated_home(temp: &tempfile::TempDir) -> PathBuf {
crates/shell/tests/logging.rs:243:        .tempdir_in("/tmp")
crates/shell/tests/logging.rs:244:        .expect("failed to create socket tempdir");
crates/shell/tests/workspace_init.rs:10:use tempfile::TempDir;
crates/shell/tests/workspace_init.rs:13:    _temp: TempDir,
crates/shell/tests/config_show.rs:11:use tempfile::TempDir;
crates/shell/tests/config_show.rs:14:    _temp: TempDir,
crates/shell/tests/socket_activation.rs:11:use tempfile::{Builder, TempDir};
crates/shell/tests/socket_activation.rs:15:    _socket_dir: TempDir,
crates/shell/tests/socket_activation.rs:24:            .tempdir_in("/tmp")
crates/shell/tests/socket_activation.rs:25:            .expect("failed to create socket tempdir");
crates/shell/tests/support/mod.rs:10:use tempfile::TempDir;
crates/shell/tests/support/mod.rs:24:    _temp: TempDir,
exec
/bin/bash -lc "rg -n \"Command::new\\(\\\"bash\\\"\\)|/bin/bash|shell\" crates/shell/tests | head -n 100" in /home/spenser/__Active_code/substrate/wt/workspace_config_policy_unification-wcu4-test succeeded in 53ms:
crates/shell/tests/shim_deployment.rs:6:    use substrate_shell::shim_deploy::{DeploymentStatus, ShimDeployer};
crates/shell/tests/world_verify.rs:35:        let mut cmd = support::substrate_shell_driver();
crates/shell/tests/config_set.rs:6:use common::{substrate_shell_driver, temp_dir};
crates/shell/tests/config_set.rs:38:        let mut cmd = substrate_shell_driver();
crates/shell/tests/shim_doctor.rs:19:      shell: |
crates/shell/tests/shim_doctor.rs:29:      shell: |
crates/shell/tests/shim_doctor.rs:138:      shell: |
crates/shell/tests/shim_doctor.rs:147:      shell: |
crates/shell/tests/shim_doctor.rs:223:      shell: |
crates/shell/tests/shim_doctor.rs:233:      shell: |
crates/shell/tests/shim_doctor.rs:367:      shell: |
crates/shell/tests/shim_doctor.rs:481:      shell: |
crates/shell/tests/shim_doctor.rs:566:      shell: "echo skip"
crates/shell/tests/shim_doctor.rs:616:      shell: |
crates/shell/tests/shim_doctor.rs:689:      shell: |
crates/shell/tests/world_deps_layering.rs:9:use common::substrate_shell_driver;
crates/shell/tests/world_deps_layering.rs:147:        let mut cmd = substrate_shell_driver();
crates/shell/tests/workspace_init.rs:6:use common::{substrate_shell_driver, temp_dir};
crates/shell/tests/workspace_init.rs:33:        let mut cmd = substrate_shell_driver();
crates/shell/tests/shell_behavior.rs:12:use substrate_shell::needs_shell;





thinking
**Verifying configuration behavior**

I see there's already a test called config_show using SUBSTRATE_OVERRIDE_* environment variables. The WCU4 spec notes that these won't be exported by default, but they can still be used if explicitly provided, so those tests are fine. However, the acceptance criteria state that effective config shouldn't require those exports. That implies I need to check if the tests validate behavior when environment variables are absent. I want to make sure commands like `config current show` return correct defaults without relying on env.sh exports.
thinking
**Testing installation scripts**

I’m thinking about testing the installation scripts to create a SUBSTRATE_HOME environment with `env.sh` and then using `substrate config current show` to check values from the global config. Since I can't easily "source env.sh" in Rust tests, I’d prefer to write an integration test in crates/shell/tests. I’ll create a temp directory for SUBSTRATE_HOME, run the install script, and then confirm that the generated `env.sh` doesn't include any `SUBSTRATE_OVERRIDE_` lines before asserting that the effective config meets expectations.
thinking
**Examining install scripts and config behavior**

I'm considering whether the install scripts might still create an install_config or write to config.yaml. The `write_install_config` function takes care of writing install metadata in YAML format, but not directly into config.yaml. For testing the "config current show" behavior, I can simulate this by setting SUBSTRATE_HOME to a prefix, applying a global config patch, and then running the command to ensure it reflects our changes rather than any unintended default environment exports from the installer. It’s a bit complex, especially with the precedence of environment overrides.
thinking
**Testing global config patch and overrides**

I'm thinking about creating a global config patch with policy.mode set to disabled and then running `config current show`. If environment overrides are still active, it would display "observe" instead of "disabled," which isn't what I want. I need to ensure that the override exports are absent. I can check that env.sh doesn't set them, but I wonder if the install script only sets them there. When running substrate with SUBSTRATE_HOME, env.sh isn’t automatically sourced, unless there’s a wrapper. The current configuration reads these overrides directly from the process environment instead.
thinking
**Testing env.sh exports**

I need to check if the install script writes `env.sh` with `SUBSTRATE_OVERRIDE_*` exports. If it does, sourcing `env.sh` in the user's shell would set these variables globally, affecting subsequent runs of substrate. It's crucial to test that `env.sh` doesn’t contain those exports, ensuring a clean environment. I’m also considering how to handle `dev-install-substrate.sh`, which builds from source and could be resource-heavy, so I'll look for a `--skip-build` option. I'll quickly inspect the usage and options by checking the first 80 lines of both scripts with `sed`.
exec
/bin/bash -lc "sed -n '1,120p' scripts/substrate/install-substrate.sh" in /home/spenser/__Active_code/substrate/wt/workspace_config_policy_unification-wcu4-test succeeded in 40ms:
#!/usr/bin/env bash
set -euo pipefail

if [[ -z "${INSTALLER_NAME:-}" ]]; then
  INSTALLER_NAME="substrate-install"
fi
readonly INSTALLER_NAME
# shellcheck disable=SC2034 # used for release metadata
readonly INSTALLER_VERSION="0.1.0-dev"
readonly DEFAULT_FALLBACK_VERSION="0.2.2"
readonly LATEST_RELEASE_API="${SUBSTRATE_INSTALL_LATEST_API:-https://api.github.com/repos/atomize-hq/substrate/releases/latest}"
readonly DEFAULT_PREFIX="${HOME}/.substrate"
readonly DEFAULT_BASE_URL="https://github.com/atomize-hq/substrate/releases/download"

VERSION_RAW=""
VERSION=""
VERSION_TAG=""
PREFIX="$DEFAULT_PREFIX"
NO_WORLD=0
NO_SHIMS=0
exec
/bin/bash -lc "sed -n '1,220p' scripts/substrate/dev-install-substrate.sh" in /home/spenser/__Active_code/substrate/wt/workspace_config_policy_unification-wcu4-test succeeded in 45ms:
#!/usr/bin/env bash
set -euo pipefail

SCRIPT_NAME="dev-install-substrate"

log()   { printf '[%s] %s\n' "${SCRIPT_NAME}" "$1"; }
warn()  { printf '[%s][WARN] %s\n' "${SCRIPT_NAME}" "$1" >&2; }
fatal() { printf '[%s][ERROR] %s\n' "${SCRIPT_NAME}" "$1" >&2; exit 1; }

usage() {
  cat <<'USAGE'
Substrate Dev Installer

Build Substrate from the current repository and wire development shims to the
freshly built binaries. This is intended for local iteration after removing any
production installation.

Usage:
  dev-install-substrate.sh [--prefix <path>] [--profile <debug|release>] [--version-label <name>] [--no-world] [--anchor-mode <mode>] [--anchor-path <path>] [--caged|--uncaged] [--no-shims]
  dev-install-substrate.sh --help
exec
/bin/bash -lc 'bash scripts/substrate/install-substrate.sh --help | head -n 120' in /home/spenser/__Active_code/substrate/wt/workspace_config_policy_unification-wcu4-test succeeded in 41ms:
Substrate Installer
Usage:
  curl -fsSL https://raw.githubusercontent.com/atomize-hq/substrate/main/scripts/substrate/install-substrate.sh | bash
  # (Windows host) powershell -ExecutionPolicy Bypass -File install-substrate.ps1

Options:
  --version <semver>   Install a specific release (default: latest GitHub release)
  --prefix <path>      Installation prefix (default: ~/.substrate)
  --no-world           Skip world backend provisioning
  --no-shims           Skip shim deployment
  --sync-deps          Run 'substrate world deps sync' after provisioning completes
  --dry-run            Print actions without executing
  --artifact-dir <dir> Use pre-downloaded host bundle + SHA256SUMS
  --archive <dir>      Alias for --artifact-dir (deprecated)
  -h, --help           Show this message
exec
/bin/bash -lc 'bash scripts/substrate/dev-install-substrate.sh --help | head -n 120' in /home/spenser/__Active_code/substrate/wt/workspace_config_policy_unification-wcu4-test succeeded in 46ms:
Substrate Dev Installer

Build Substrate from the current repository and wire development shims to the
freshly built binaries. This is intended for local iteration after removing any
production installation.

Usage:
  dev-install-substrate.sh [--prefix <path>] [--profile <debug|release>] [--version-label <name>] [--no-world] [--anchor-mode <mode>] [--anchor-path <path>] [--caged|--uncaged] [--no-shims]
  dev-install-substrate.sh --help

Options:
  --prefix <path>           Installation prefix for shims/env helper (default: ~/.substrate)
  --profile <name>          Cargo profile to build (debug or release; default: debug)
  --version-label <name>    Version directory label under <prefix>/versions (default: dev)
  --no-world                Mark install metadata as world_disabled (skips provisioning entirely)
  --anchor-mode <mode>      Default anchor mode (workspace|follow-cwd|custom; default: workspace)
  --anchor-path <path>      Default anchor path (for custom mode)
  --caged                   Write caged=true to install metadata (default)
  --uncaged                 Write caged=false to install metadata
  --no-shims                Skip shim deployment (only run cargo build)


thinking
**Exploring installation script options**

I’m considering how to call `install-substrate.sh` with `--artifact-dir` but realize we don’t have any artifacts. Another idea is to try `SUBSTRATE_INSTALL_ARTIFACT_DIR`, but if we want to avoid network calls, I can use `--dry-run`, though it won't create your environment variables. There are existing tests in `tests/installers/install_smoke.sh`, but they don’t seem to trigger cargo tests. Maybe I could write Rust tests to verify the script without executing it. The goal is to ensure the clean environment meets the acceptance criteria.
thinking
**Inspecting test infrastructure**

I’m exploring how to write tests for `install-substrate.sh --dry-run` to confirm it aims to generate an `env.sh`, but without creating an actual file. I could check the `write_env_sh_script` function to see if it emits exports, but that feels a bit too internal-focused. Instead, the existing installer tests in `tests/installers/` might leverage in-repo artifacts. The requirement implies we need to run cargo tests, but I’m wondering if the triad-test-checks might include running those scripts. I’ll need to check the `Makefile` for specific tasks to clarify.
exec
/bin/bash -lc 'rg -n "triad-test-checks" -S Makefile scripts tools docs | head -n 50
make -n triad-test-checks 2>/dev/null | head -n 200' in /home/spenser/__Active_code/substrate/wt/workspace_config_policy_unification-wcu4-test succeeded in 117ms:
Makefile:270:.PHONY: triad-test-checks
Makefile:271:triad-test-checks:
docs/project_management/_archived/policy_and_config_precedence/tasks.json:128:      "required_make_targets": ["triad-test-checks"],
scripts/planning/new_feature.sh:321:        task["required_make_targets"] = ["triad-test-checks"]
docs/project_management/_archived/world-overlayfs-enumeration/tasks.json:133:        "triad-test-checks"
scripts/planning/new_feature.ps1:225:$test.required_make_targets = if ($Automation.IsPresent) { @("triad-test-checks") } else { $null }
docs/project_management/_archived/doctor_scopes/tasks.json:136:        "triad-test-checks"
docs/project_management/_archived/env_var_taxonomy_and_override_split/tasks.json:150:        "triad-test-checks"
docs/project_management/next/workspace-config-policy-unification/tasks.json:157:        "triad-test-checks"
docs/project_management/next/workspace-config-policy-unification/tasks.json:522:        "triad-test-checks"
docs/project_management/next/workspace-config-policy-unification/tasks.json:885:        "triad-test-checks"
docs/project_management/next/workspace-config-policy-unification/tasks.json:1230:        "triad-test-checks"
docs/project_management/next/workspace-config-policy-unification/tasks.json:1577:        "triad-test-checks"
docs/project_management/next/world_deps_selection_layer/tasks.json:132:      "required_make_targets": ["triad-test-checks"]
docs/project_management/next/world_deps_selection_layer/tasks.json:440:      "required_make_targets": ["triad-test-checks"]
docs/project_management/next/world_deps_selection_layer/tasks.json:750:      "required_make_targets": ["triad-test-checks"]
docs/project_management/next/world_deps_selection_layer/session_log.md:173:  - `CHECKS=make triad-test-checks`
make[1]: Entering directory '/home/spenser/__Active_code/substrate/wt/workspace_config_policy_unification-wcu4-test'
cargo fmt
make[1]: Leaving directory '/home/spenser/__Active_code/substrate/wt/workspace_config_policy_unification-wcu4-test'

thinking
**Clarifying command function**

When I run `make -n triad-test-checks`, it only shows `cargo fmt`. This might be because I'm in a sandbox environment. The `-n` flag prints commands, so it seems the only requirement is to run `cargo fmt`. It looks like triad-test-checks may just execute formatting, leaving other tests for us to manage. I’ll need to check the Makefile regarding triad-test-checks to confirm how it’s structured.
exec
/bin/bash -lc "nl -ba Makefile | sed -n '240,310p'" in /home/spenser/__Active_code/substrate/wt/workspace_config_policy_unification-wcu4-test succeeded in 43ms:
   240	# =========================
   241	
   242	TASK_ID ?=
   243	TASK_PLATFORM ?=
   244	SLICE_ID ?=
   245	CODE_TASK_ID ?=
   246	TEST_TASK_ID ?=
   247	PLATFORMS ?=
   248	SMOKE_RUN_ID ?=
   249	
   250	LAUNCH_CODEX ?= 0
   251	CODEX_PROFILE ?=
   252	CODEX_MODEL ?=
   253	CODEX_JSONL ?= 0
   254	
   255	VERIFY_ONLY ?= 0
   256	NO_COMMIT ?= 0
   257	SMOKE ?= 0
   258	
   259	REMOVE_WORKTREES ?= 0
exec
/bin/bash -lc 'cat crates/shell/Cargo.toml' in /home/spenser/__Active_code/substrate/wt/workspace_config_policy_unification-wcu4-test succeeded in 46ms:
[package]
name = "substrate-shell"
version = "0.2.5"
edition = "2021"
rust-version = "1.89"
authors = ["Atomize"]
description = "Secure execution layer for AI agent development workflows with comprehensive tracing and isolation"
license = "MIT"
repository = "https://github.com/atomize-hq/substrate"
keywords = ["ai", "agent", "security", "tracing", "shell"]
categories = ["command-line-utilities", "development-tools"]
readme = "../../README.md"

[dependencies]
anyhow = "1.0"
clap = { version = "4.5", features = ["derive"] }
serde_json = "1.0"
serde_yaml = "0.9"
chrono = { version = "0.4", features = ["serde"] }
uuid = { version = "1.10", features = ["v7"] }
exec
/bin/bash -lc "sed -n '1,220p' crates/shell/tests/common.rs" in /home/spenser/__Active_code/substrate/wt/workspace_config_policy_unification-wcu4-test succeeded in 27ms:
#![cfg(unix)]

use assert_cmd::Command;
use serde_json::{json, Value};
use std::fs::{self, OpenOptions};
use std::io::Write;
use std::path::{Path, PathBuf};
use std::process::Command as StdCommand;
use std::sync::OnceLock;
use tempfile::{Builder, TempDir};

/// Return an `assert_cmd::Command` pre-configured to run `substrate`
/// in host-only mode. This helper ensures we reuse the already-built binary,
/// sets a shared TMPDIR for deterministic fixtures, and propagates the
/// `SUBSTRATE_OVERRIDE_WORLD=disabled` override that bypasses world init.
pub fn substrate_shell_driver() -> Command {
    ensure_substrate_built();

    let mut cmd = Command::new(binary_path());
    cmd.env("TMPDIR", shared_tmpdir());



thinking
**Evaluating installation scripts**

I'm considering using `--no-world` to avoid issues with the world context, which should work. The acceptance criteria states that a clean environment shouldn't have `SUBSTRATE_OVERRIDE_*` exports after running the installation scripts. I'm looking for a way to run `install-substrate.sh` without a network, since it downloads a large release, which isn’t practical for unit tests. Maybe testing `dev-install-substrate.sh --no-shims` could work since it just runs cargo build and generates the `env.sh`, but I’m worried about building substrate twice. I'll have to weigh the overhead.
thinking
**Considering testing strategies**

I realize that while the approach might be expensive, it could still be manageable within tests, especially for CI. Instead of running the actual scripts, I could inspect them as text to check for the absence of overrides, which is a cheaper method. However, the specification states that tests should verify behavior after running scripts, and that static checks alone might not be sufficient. 

To address that, I’m thinking of a hybrid approach. I could check that the `install-substrate.sh` and `dev-install-substrate.sh` scripts don't export overrides by scanning for `SUBSTRATE_OVERRIDE_`. Running `dev-install-substrate` locally seems viable since it doesn’t require a network connection and will generate the actual `env.sh`. For `install-substrate`, using `--dry-run` might let me verify its output without creating the file.
thinking
**Exploring installation options**

I'm considering another approach: the `install-substrate.sh` has a `--artifact-dir` option that allows it to use a pre-downloaded host bundle. It might be possible to direct it to the `dist/` directory or some fixture. I notice that in this repo, there's a `dist/` folder and a `dist-workspace.toml`. I wonder if there are local artifacts available for use in CI, possibly in `dist/host`. I’ll need to inspect the `dist` directory and make a list of its contents.
exec
/bin/bash -lc 'ls -R dist | head -n 200' in /home/spenser/__Active_code/substrate/wt/workspace_config_policy_unification-wcu4-test succeeded in 32ms:
dist:
cargo-dist.toml
release-template.md
scripts

dist/scripts:
assemble-release-bundles.sh
collect-supporting-artifacts.sh

thinking
**Inspecting installation scripts**

I'm realizing that there are no local bundles available. I think the tests or installers might be using `cargo dist` or might have a specific build host bundle. To figure this out, I'll take a look at `tests/installers/install_smoke.sh` to see how they manage it. My plan is to quickly check sections of the script related to building artifacts, especially around line 700. Let's dive into this!
exec
/bin/bash -lc "nl -ba tests/installers/install_smoke.sh | sed -n '680,820p'" in /home/spenser/__Active_code/substrate/wt/workspace_config_policy_unification-wcu4-test succeeded in 45ms:
   680	if caged is None:
   681	    raise SystemExit("world.caged missing in config.yaml")
   682	if caged is not True:
   683	    raise SystemExit(f"world.caged={caged} (expected true)")
   684	PY
   685	
   686	  log "Verified install config at ${config} (world_enabled=${expected_flag}; anchor_mode=project anchor_path=\"\" root_mode=project root_path=\"\" caged=true)"
   687	}
   688	
   689	assert_manifest_present() {
   690	  local manifest="${PREFIX}/versions/${FAKE_VERSION}/config/manager_hooks.yaml"
   691	  if [[ ! -f "${manifest}" ]]; then
   692	    fatal "manager manifest missing after install: ${manifest}"
   693	  fi
   694	  log "Verified manager manifest at ${manifest}"
   695	}
   696	
   697	run_health_smoke() {
   698	  local substrate_bin="${PREFIX}/bin/substrate"
   699	  local output





thinking
**Evaluating test scope**

I’m thinking about tests in the `tests/` root, which are probably run by CI, so they should be in scope. The instructions suggest I should modify tests enforcing WCU4 acceptance criteria in tasks.json, including shell scripts under tests/installers, which feels appropriate for testing installer scripts. 

I can see that we might need to adjust some existing integration tests and add checks to ensure certain environmental variables aren’t being exported after installations. However, there might be some complications if the scripts fail before the code changes.
thinking
**Considering targeted tests and scripts**

I need to consider running the targeted tests we modified, particularly the scripts. The prompt does ask for running `cargo fmt`, but that seems irrelevant for shell script tests. So, a possible approach is to run `cargo fmt` and then execute the installer smoke test with `bash tests/installers/install_smoke.sh`. 

These scripts likely require a Linux environment and might take some time to complete, but they should work within our setup. The acceptance criteria focus on ensuring that certain overrides aren’t exported, which makes the installer smoke test suitable. We can also check that the `config current show` behavior remains unaffected after sourcing `env.sh`.
thinking
**Setting up environment checks**

I need to ensure that the new behavior doesn’t affect the test outcomes. After running the install script, I can `source prefix/env.sh` in a subshell and then run `substrate config current show --json` to verify that the global settings work.

However, I noted that the installer doesn’t currently check the content of `env.sh`. The `install_smoke` checks for `install_config` and hints but misses this. I'll need to identify the path for `env.sh`, which is likely `${PREFIX}/env.sh`, and add checks in `install_smoke` to verify that it doesn’t contain any `SUBSTRATE_OVERRIDE_` lines after sourcing it in a clean shell.
thinking
**Creating temporary configuration checks**

I need to ensure that `config current show` is unaffected during testing. To do this, I can create a temporary global configuration file at `${PREFIX}/config.yaml`. The effective configuration actually uses `$SUBSTRATE_HOME/config.yaml`, which likely gets set by sourcing `env.sh`. 

After sourcing it, I can run the installed substrate binary to read this configuration. To prevent overrides from affecting the output, I should create the config file with `policy.mode=disabled` and verify that `substrate --no-world config current show --json` reflects this. If any overrides are set, the output may still show as "observe," which is what I want to catch.
thinking
**Verifying command existence and tests**

I need to confirm that the `substrate config current show` command exists, as it was added in WCU3. It should be present in the installed version of substrate, and we can quickly verify this by running `cargo run -p substrate -- config current show --help`. However, tests in `crates/shell` already use this command, so it's likely valid.

Still, I wonder if the installed binary from the installer smoke script built from a fake release will include this command. Although it uses artifacts from our repo, the install smoke test compiles everything, so it should be fine. I could explore direct tests in Rust, but the script tests align better with acceptance criteria. We can consider running `bash tests/installers/install_smoke.sh` with specific scenarios; I'll check the script's header for its usage.
exec
/bin/bash -lc "sed -n '1,120p' tests/installers/install_smoke.sh" in /home/spenser/__Active_code/substrate/wt/workspace_config_policy_unification-wcu4-test succeeded in 33ms:
#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
REPO_ROOT="$(cd "${SCRIPT_DIR}/../.." && pwd)"

log() {
  printf '[install-smoke] %s\n' "$*" >&2
}

fatal() {
  log "ERROR: $*"
  exit 1
}

usage() {
  cat <<'USAGE' >&2
Usage: tests/installers/install_smoke.sh --scenario <prod|prod-no-world|dev> [--keep-root]

Scenarios:



thinking
**Analyzing installer smoke tests**

I'm considering how the installer smoke tests relate to systemd and Linux. In our environment, we might not have systemd, and if it skips tests, it passes with an exit code of zero, which could be a concern. The acceptance criteria want tests to run, and while it works in CI with systemd, our setup may not cater to that. The WCU4 spec mentions testing behavior and configurations, but it doesn't seem platform-specific. It's safe to add to the installer smoke script, taking cross-platform compatibility into account.
thinking
**Exploring Rust tests**

I'm thinking about creating Rust tests that don't rely on systemd, which might be a better approach for our environment. It seems like doing both could be beneficial as well, but we primarily need effective tests. I'd like to test the env.sh template creation function, but that requires running the install script. For the dev-install-substrate, it might handle provisioning without needing systemd, especially if using the `--no-world` option. Still, I'll need to look into whether it creates groups or checks systemctl, and I'll check the script functions further to be sure.
thinking
**Checking env.sh build process**

I'm thinking that the build process should just create the env.sh file. However, there's a chance it might still try to create a group, depending on how it's set up. I plan to read through the script to see what happens with the `--no-world` option; it should skip the privileged steps. I’ll look for variables like "WORLD_ENABLED" and check for uses of `run_privileged`. I'll use the `rg` command in the dev-install-substrate directory to search for "systemctl" as well.
exec
/bin/bash -lc 'rg -n "systemctl|world-provision|run_privileged" scripts/substrate/dev-install-substrate.sh | head -n 200' in /home/spenser/__Active_code/substrate/wt/workspace_config_policy_unification-wcu4-test succeeded in 43ms:
36:run_privileged() {
418:    if run_privileged groupadd --system "${target_group}"; then
440:  if run_privileged usermod -aG "${target_group}" "${invoking_user}"; then
452:  if ! command -v systemctl >/dev/null 2>&1; then
453:    warn "systemctl not found; verify /run/substrate.sock is root:substrate 0660 after provisioning."
458:    warn "Socket unit missing at ${socket_unit}; rerun scripts/linux/world-provision.sh to install it."
465:    if ! run_privileged sed -i 's/^SocketGroup=.*/SocketGroup=substrate/' "${socket_unit}"; then
466:      warn "Failed to update ${socket_unit}; edit it manually so SocketGroup=substrate and rerun 'sudo systemctl daemon-reload'."
472:  run_privileged systemctl stop substrate-world-agent.service substrate-world-agent.socket || true
473:  run_privileged rm -f /run/substrate.sock || true
474:  run_privileged systemctl daemon-reload || true
475:  run_privileged systemctl start substrate-world-agent.socket || true
476:  run_privileged systemctl start substrate-world-agent.service || true
782:  PROVISION_SCRIPT="${REPO_ROOT}/scripts/linux/world-provision.sh"
791:      warn "world-provision script reported an error; rerun ${PROVISION_SCRIPT} manually to enable the world-agent service."
800:    warn "Linux world-provision script missing at ${PROVISION_SCRIPT}; world-agent service not configured."
858:    $SUDO_CMD systemctl restart dnsmasq 2>/dev/null || true
859:    $SUDO_CMD systemctl restart systemd-resolved 2>/dev/null || true
937:  limactl shell substrate sudo systemctl daemon-reload
938:  limactl shell substrate sudo systemctl enable substrate-world-agent.service

thinking
**Considering installation options**

I'm thinking about how `dev-install` interacts with `systemctl`. It seems this might only happen if `WORLD_ENABLED` is set. To make testing easier, I could potentially run it with `--no-world` to skip those actions. However, some of the `systemctl` commands might still depend on whether world is enabled, so I should double-check those specific lines. I might use `sed` around lines 730-840 to clarify.
exec
/bin/bash -lc "nl -ba scripts/substrate/dev-install-substrate.sh | sed -n '720,860p'" in /home/spenser/__Active_code/substrate/wt/workspace_config_policy_unification-wcu4-test succeeded in 37ms:
   720	# Place per-manager snippets here if you need them for debugging.
   721	EOF
   722	mv "${MANAGER_INIT_PATH}.tmp" "${MANAGER_INIT_PATH}"
   723	chmod 0644 "${MANAGER_INIT_PATH}" || true
   724	
   725	# Write install metadata (install + world mappings) like the production installer.
   726	write_install_metadata "${WORLD_ENABLED}"
   727	write_env_sh_script "${WORLD_ENABLED}"
   728	write_manager_env_script "${WORLD_ENABLED}"
   729	
   730	shim_note=""
   731	if [[ ${DEPLOY_SHIMS} -eq 1 ]]; then
   732	  log "Deploying shims via ${SUBSTRATE_BIN}"
   733	  if ! SHIM_ORIGINAL_PATH="${PATH}" SUBSTRATE_ROOT="${PREFIX}" "${SUBSTRATE_BIN}" --shim-deploy; then
   734	    fatal "Shim deployment failed"
   735	  fi
   736	  shim_note="Dev shims deployed to ${SHIMS_DIR}."
   737	else
   738	  warn "Shim deployment skipped (--no-shims)."
   739	  shim_note="Shims were not deployed (--no-shims). Binaries are available under ${BIN_DIR}."


thinking
**Evaluating script process**

I'm determining the steps for creating an `env.sh` file. First, I need to read the file to ensure there's no "SUBSTRATE_OVERRIDE_" present. Then, I'll run bash to source it and print the environment with grep, avoiding variable names. I also want to write a `config.yaml` and run a specific command in a clean environment without any overrides. There's a potential issue if the script writes to `env.sh` even on failures. Although tests shouldn't depend on code changes, they might encounter build errors. However, writing Rust tests is permissible, as long as I adhere to the intended platform and export behavior for Unix.
thinking
**Clarifying test environment**

I'm considering how to ensure that the tests don't run on macOS or Windows. Actually, maybe I should include Windows in the tests since the acceptance criteria for packing might expect cross-platform functionality. WCU4 especially focuses on bash scripts. I remember there's a Windows PowerShell install script—though it might not be necessary to mention. Still, I could refer to "install/dev scripts" that might include PowerShell. Ultimately, I want to test bash scripts, so I'll search for `install-substrate.ps1` to find it. I'll use `rg 'install-substrate.ps1'` for that.
exec
/bin/bash -lc "rg -n \"install-substrate\\.ps1|SUBSTRATE_OVERRIDE_\" scripts/substrate | head -n 200" in /home/spenser/__Active_code/substrate/wt/workspace_config_policy_unification-wcu4-test succeeded in 51ms:
scripts/substrate/install-substrate.sh:60:  # (Windows host) powershell -ExecutionPolicy Bypass -File install-substrate.ps1
scripts/substrate/install-substrate.sh:923:export SUBSTRATE_OVERRIDE_WORLD=${world_literal}
scripts/substrate/install-substrate.sh:924:export SUBSTRATE_OVERRIDE_CAGED=1
scripts/substrate/install-substrate.sh:925:export SUBSTRATE_OVERRIDE_ANCHOR_MODE=${anchor_mode_literal}
scripts/substrate/install-substrate.sh:926:export SUBSTRATE_OVERRIDE_ANCHOR_PATH=${anchor_path_literal}
scripts/substrate/install-substrate.sh:927:export SUBSTRATE_OVERRIDE_POLICY_MODE=${policy_mode_literal}
scripts/substrate/dev-install-substrate.sh:156:export SUBSTRATE_OVERRIDE_WORLD=${world_literal}
scripts/substrate/dev-install-substrate.sh:157:export SUBSTRATE_OVERRIDE_CAGED=${caged_literal}
scripts/substrate/dev-install-substrate.sh:158:export SUBSTRATE_OVERRIDE_ANCHOR_MODE=${anchor_mode_literal}
scripts/substrate/dev-install-substrate.sh:159:export SUBSTRATE_OVERRIDE_ANCHOR_PATH=${anchor_path_literal}
scripts/substrate/dev-install-substrate.sh:160:export SUBSTRATE_OVERRIDE_POLICY_MODE=${policy_mode_literal}





thinking
**Considering test setup and dependencies**

I can't find the `install-substrate.ps1` file, so I'm wondering if it might be in the release asset. That aside, I think the `common::substrate_shell_driver` sets an override we shouldn't use in our new tests. We’ll write them independently and use `serde_json` for parsing outputs. I also need `tempfile` for creating directories. I’ll stick with `std::process::Command` for running the scripts. It's crucial to keep tests deterministic and avoid any network calls. I'll examine the previous build commands for insight!
thinking
**Evaluating test execution and environment**

In the tests, I’m considering whether to avoid running `dev-install-substrate` due to time constraints and instead test the file content. However, the acceptance criteria state that a clean environment should have no exports after running scripts. Running `dev-install-substrate` gives more direct results, but that could make `cargo test` heavy with increased run time. Maybe marking the test as ignored will help. I’m also thinking to use `#[test]` with `#[cfg(target_os="linux")]` to ensure compatibility since the script uses `ln -sfn`, which might differ on macOS.
thinking
**Confirming script compatibility**

I think the script might be using `realpath`, so I need to check that. It starts with `#!/usr/bin/env bash` and uses `set -euo pipefail`, which should work on macOS, although the environment inside `cargo test` may differ since they use bash 3.2. The WCU4 test file is behind `#![cfg(unix)]`, but it should still run on macOS. I'm relieved the tests can execute on both Linux and mac, and I want to avoid editing planning docs, keeping modifications confined to `crates/shell/tests`.
thinking
**Planning new test implementation**

I need to run `cargo fmt` and targeted tests at the end. They asked for modifications to enforce WCU4 acceptance criteria in `tasks.json`, so it makes sense to implement a new test file and maybe update existing ones. The criteria highlight no override exports, so adding tests will help enforce this. I'm thinking I should create a file named `installer_env_wcu4.rs` in `crates/shell/tests`, as other naming conventions seem to fit. 

Now I’ll proceed with implementing the pseudo code!
thinking
**Finalizing test plan steps**

First, I need to clarify that the repository root is actually `../../` from `crates/shell`. I'll use `fs::canonicalize` later for paths. I’ll create the installation script path and read its string content to check that it doesn't contain "export SUBSTRATE_OVERRIDE_". For the development test, I'll manage temporary directories for home and prefix, ensuring to set up the environment variables correctly while running the script. It’s fine to capture the status after executing to confirm these variables don’t export unexpectedly. 

Finally, I’ll include the test steps in my plan, including implementing tests and running `cargo fmt` to maintain code style. Let’s get this organized!
Plan update
  ✓ Verify task state and specs
  → Add WCU4 installer env tests
  • Run fmt and targeted tests
  • Run triad task finisher

thinking
**Exploring cargo build process**

I’m going to confirm by searching for "cargo build" lines since it’s a significant topic. I wonder if it utilizes `SUBSTRATE_TEST_CARGO_TARGET_ROOT` for building and creating symlinks. To get a clearer picture, I’ll check quickly and search around the term "SUBSTRATE_TEST_CARGO_TARGET_ROOT". There’s a lot to uncover here, and I want to ensure I’m getting the details right!
exec
/bin/bash -lc 'rg -n "SUBSTRATE_TEST_CARGO_TARGET_ROOT" -n scripts/substrate/dev-install-substrate.sh' in /home/spenser/__Active_code/substrate/wt/workspace_config_policy_unification-wcu4-test exited 1 in 43ms:







thinking
**Evaluating build approaches**

I'm thinking about whether to trigger more builds since that might be flaky. An alternative approach could be to test the file contents statically, but acceptance requires running the script. I could check the writing function in env.sh, but static checks are still valid. Using an env variable with the script might allow me to skip builds, like `--no-shims`, which still runs cargo build. Setting `CARGO_TARGET_DIR` to a temporary location would isolate builds and avoid interference. However, I’m noticing that dev-install-substrate might create symlinks ignoring my `CARGO_TARGET_DIR`.
thinking
**Assessing build outputs**

If I set `CARGO_TARGET_DIR`, the build output will go to the temp directory, but symlink creation could fail since it expects binaries in repo_root/target. The script doesn’t seem to use `CARGO_TARGET_DIR`, instead relying on `TARGET_DIR` from somewhere like PROFILE. I could consider running the script from a copied repo, but that feels complicated. Instead, I’m realizing a simpler approach might be to check the content of the `write_env_sh_script` function to ensure it doesn’t have override exports; that's a static check.
thinking
**Verifying config effect**

I’m thinking about how to test that the `config current show` is unaffected by exports. I could create a string representing an env.sh snippet, but the specs require tests to ensure a clean environment after running scripts—meaning no overrides should persist. A straightforward static test like `assert!(!contents.contains("export SUBSTRATE_OVERRIDE_"))` would confirm that. If the code changes not to export, the test would pass. However, this alone won’t ensure the `config current show` function is unaffected, so I need to validate it under clean conditions as well.
thinking
**Creating integration tests**

I can add a small test by creating a global `config.yaml` with `policy.mode=disabled` and then running `config current show` to verify that it correctly shows disabled without any overrides. This is mostly reliable, but I also need to check that neither the `install-substrate` nor the `dev-install-substrate` scripts export any overrides. This approach is efficient and avoids heavy script execution. However, the specs emphasize that a clean environment should have no `SUBSTRATE_OVERRIDE_*` exports after running relevant scripts, which means an integration check might be more appropriate than a static one.
thinking
**Planning testing strategy**

I'm considering how to implement more behavior by simulating a `source` without executing a full install. One idea is to create sample content for `env.sh` but that’s tricky since the script is responsible for writing it. I can't run the script since it isn’t modular, but maybe I could just run `bash -lc 'source scripts/substrate/dev-install-substrate.sh; ...'`. Instead, I think I'll focus on static tests for both scripts and a small runtime test for `config current show` in a clean environment without overrides. 

I need to ensure that install scripts don’t export overrides, so I'll create assertions for both. The acceptance criteria state that tests should confirm the config behaves correctly without needing `SUBSTRATE_OVERRIDE` exports. I’ll also create Rust tests to check that the install scripts do not export those variables and validate that the behavior remains correct in a clean environment. 

I’ll add checks in `tests/installers/install_smoke.sh` to confirm that after running the installer, the generated `env.sh` does not contain any `SUBSTRATE_OVERRIDE_` exports.

It sounds like a good plan to keep things systematic and efficient!
thinking
**Implementing override checks**

I plan to create a function called `assert_no_override_exports` to read `env.sh` and ensure that it doesn't contain any exports. Next, I can extend this to check that `config current show` is unaffected by any overrides. I’ll create a `config.yaml` with `policy.mode=disabled`, source `env.sh` to set `SUBSTRATE_HOME`, and then run `${PREFIX}/bin/substrate --no-world config current show --json` to parse the output. 

I think this will effectively confirm that the disabled setting matches expected behavior. However, I may need to skip this check in non-systemd environments, especially for production. It seems like it also might skip checks if systemd is missing. I’ll take a closer look at that part.
exec
/bin/bash -lc 'rg -n "maybe_skip_platform" -n tests/installers/install_smoke.sh' in /home/spenser/__Active_code/substrate/wt/workspace_config_policy_unification-wcu4-test succeeded in 32ms:
43:maybe_skip_platform() {
124:maybe_skip_platform
exec
/bin/bash -lc "sed -n '1,120p' tests/installers/install_state_smoke.sh
rg -n \"maybe_skip_platform|systemd\" tests/installers/install_state_smoke.sh | head -n 100" in /home/spenser/__Active_code/substrate/wt/workspace_config_policy_unification-wcu4-test succeeded in 47ms:
#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
REPO_ROOT="$(cd "${SCRIPT_DIR}/../.." && pwd)"
HOST_PATH="${PATH}"
STUB_BIN=""

log() {
  printf '[install-state] %s\n' "$*" >&2
}

fatal() {
  log "ERROR: $*"
  exit 1
}

usage() {
  cat <<'USAGE' >&2
Usage: tests/installers/install_state_smoke.sh [--scenario <all|metadata|cleanup|missing>] [--keep-root]
exec
/bin/bash -lc "rg -n \"env\\.sh|ENV_SH\" tests/installers/install_state_smoke.sh | head -n 200" in /home/spenser/__Active_code/substrate/wt/workspace_config_policy_unification-wcu4-test succeeded in 41ms:

exec
/bin/bash -lc "rg -n \"install-substrate\\.sh\" tests/installers/install_state_smoke.sh | head -n 200" in /home/spenser/__Active_code/substrate/wt/workspace_config_policy_unification-wcu4-test succeeded in 53ms:
663:  "${REPO_ROOT}/scripts/substrate/install-substrate.sh" \
692:  "${REPO_ROOT}/scripts/substrate/install-substrate.sh" \
778:  "${REPO_ROOT}/scripts/substrate/uninstall-substrate.sh" \
836:  "${REPO_ROOT}/scripts/substrate/uninstall-substrate.sh" \
862:  "${REPO_ROOT}/scripts/substrate/uninstall-substrate.sh" \
exec
/bin/bash -lc "nl -ba tests/installers/install_state_smoke.sh | sed -n '620,740p'" in /home/spenser/__Active_code/substrate/wt/workspace_config_policy_unification-wcu4-test succeeded in 33ms:
   620	  fi
   621	
   622	  SKIP_LOG="${work_root}/skip.log"
   623	  if [[ "$(uname -s)" != "Linux" ]]; then
   624	    record_skip "metadata scenario requires Linux (detected $(uname -s))"
   625	    exit 0
   626	  fi
   627	  if ! command -v python3 >/dev/null 2>&1; then
   628	    record_skip "python3 not available for metadata assertions"
   629	    exit 0
   630	  fi
   631	
   632	  local prefix="${work_root}/home/.substrate"
   633	  local home_dir="${work_root}/home"
   634	  local artifacts="${work_root}/artifacts"
   635	  local stub_bin="${work_root}/stub-bin"
   636	  local fake_root="${work_root}/fakeroot"
   637	  local systemctl_log="${work_root}/systemctl.log"
   638	  local group_log="${work_root}/group-ops.log"
   639	  local linger_log="${work_root}/linger.log"


thinking
**Implementing checks in script**

I need to be careful with `install_state_smoke.sh` since it has `set -euo pipefail`, meaning that any failure will exit the script. I’ll add assertions with helpful error messages without modifying production scripts.

For `assert_env_sh_no_override_exports`, I’ll check for the existence of `env.sh`. If an override exists, it will be flagged. For `assert_config_current_show_not_affected_by_overrides`, I’ll create a config file and run it in a clean environment to ensure the correct settings are in place before verifying with Python.
thinking
**Checking substrate binary installation**

I plan to embed a small Python snippet but need to ensure that the substrate binary is installed and exists. It’s likely located in `${prefix}/bin/substrate`. To confirm this, I'll check for existing functions in the script and see if there are any references to usage of `prefix/bin/substrate`. I want to make sure that everything is set up properly before proceeding with anything else.
exec
/bin/bash -lc 'rg -n "bin/substrate" tests/installers/install_state_smoke.sh | head -n 50' in /home/spenser/__Active_code/substrate/wt/workspace_config_policy_unification-wcu4-test succeeded in 16ms:
446:  write_stub_binary "${bundle_root}/bin/substrate"

thinking
**Inspecting build process**

I’m checking `build_fake_release` in `install_state_smoke`. It writes stub binaries into `bundle_root/bin/substrate`, which makes me wonder if it’s creating simple stubs. But wait, it seems like it’s writing a stub binary that might use an actual compiled version. I'll need to investigate around line 400 to understand this better. There’s definitely a lot to unpack here! I’m curious about the details and how this all works together.
exec
/bin/bash -lc "nl -ba tests/installers/install_state_smoke.sh | sed -n '360,520p'" in /home/spenser/__Active_code/substrate/wt/workspace_config_policy_unification-wcu4-test succeeded in 38ms:
   360	    *) fatal "Unsupported architecture for harness: ${arch}" ;;
   361	  esac
   362	}
   363	
   364	compute_sha256() {
   365	  local path="$1"
   366	  python3 - <<'PY' "${path}"
   367	import hashlib, pathlib, sys
   368	path = pathlib.Path(sys.argv[1])
   369	h = hashlib.sha256()
   370	with path.open('rb') as f:
   371	    for chunk in iter(lambda: f.read(1024 * 1024), b''):
   372	        h.update(chunk)
   373	print(h.hexdigest())
   374	PY
   375	}
   376	
   377	write_stub_binary() {
   378	  local path="$1"
   379	  cat >"${path}" <<'EOF_STUB'


thinking
**Evaluating stub functionality**

I’m realizing that the stub substrate binary doesn't support the `config current show` command, so that's a limitation for checking configurations. Since install_state_smoke uses this stub, it can't check either. I think about modifying the `write_stub_binary` to support `config current show --json`, but that’s test code. I want to ensure that the behavior isn’t silently overridden, so I’ll focus instead on adding tests in Rust with the real substrate binary instead of relying on the stub.
thinking
**Combining test strategies**

I'm thinking about combining two testing strategies: first, Rust tests to ensure the `config current show` command isn't affected by overrides. Then, shell installer tests to confirm that `env.sh` doesn't export overrides by default. To simplify things and avoid systemd, I could modify the `install_state_smoke` to check only the content of `env.sh`. I want to assert it doesn't contain `SUBSTRATE_OVERRIDE_`. I also need to ensure we cover the dev-install script, so I’ll check the relevant files for usage of the real substrate binary.
exec
/bin/bash -lc "rg -n \"write_stub_binary|build_fake_release\" tests/installers/install_smoke.sh | head -n 120
nl -ba tests/installers/install_smoke.sh | sed -n '200,360p'" in /home/spenser/__Active_code/substrate/wt/workspace_config_policy_unification-wcu4-test succeeded in 41ms:
493:write_stub_binary() {
549:build_fake_release() {
558:  write_stub_binary "${bundle_root}/bin/substrate"
559:  write_stub_binary "${bundle_root}/bin/host-proxy"
560:  write_stub_binary "${bundle_root}/bin/world-agent"
798:  build_fake_release
   200	  cat >"${STUB_BIN}/${name}" <<'EOF'
   201	#!/usr/bin/env bash
   202	exit 0
   203	EOF
   204	  chmod +x "${STUB_BIN}/${name}"
   205	}
   206	
   207	write_stub_jq() {
   208	  cat >"${STUB_BIN}/jq" <<'EOF'
   209	#!/usr/bin/env bash
   210	cat
   211	EOF
   212	  chmod +x "${STUB_BIN}/jq"
   213	}






thinking
**Modifying Tests for Config Check**

I see that the install_smoke uses a stub substrate binary, which means we can’t test the config current show directly. Instead, I'll lean on Rust tests for that. For the installer scripts, I’ll check the env.sh content via shell tests. So, I'll modify the scripts in `tests/installers/install_state_smoke.sh` and `install_smoke.sh` to include env.sh checks, while keeping in mind that the user only wants tests, not any production changes. Also, we should add a new Rust integration test to ensure that scripts don’t export overrides and check that `substrate config current show` respects the global config. This way, we keep everything on track!
thinking
**Implementing Targeted Tests**

Okay, since the scope specifies tests only, I’ll focus on implementing those, even if some might fail at first. I need to ensure I run targeted tests like `cargo test -p substrate-shell --test wcu4_installer_env`. Though if I modify shell scripts, I can't run them via Cargo. I can run them directly instead, but they may skip. 

Now, should I include acceptance criteria about having no `SUBSTRATE_OVERRIDE_*` exports in a clean environment after running the relevant scripts? I plan to assert that `env.sh` doesn’t contain any override exports. I’ll incorporate this check in the scripts, calling a new function `assert_env_sh_no_override_exports` after every install scenario.
thinking
**Clarifying Installation Details**

In the `install_state_smoke`, the prefix is `${work_root}/home/.substrate`, placing `env.sh` at `${prefix}/env.sh`. I should remember that `install-substrate.sh` may write `env.sh` to a path defined by `ENV_SH_PATH`, which is likely `$PREFIX/env.sh`. So, I need to check that location.

I also have to consider that `env.sh` might not exist if an installation fails earlier, but the script would indicate failure.

Now, I’ll implement the `assert_env_sh_no_override_exports` function. It checks the existence of `env.sh`, then looks for any override exports. If anything is found, I’ll handle that situation with appropriate error messages.
thinking
**Refining the Environment Check**

I realize it’s important to check for any `SUBSTRATE_OVERRIDE_` mentions in `env.sh`, even if they're not exported. Since `env.sh` is a script to be sourced, any mention could imply that a variable is set or unset. However, I want to be extra cautious and look for occurrences.

I also need to consider future cases where there might be things like `unset SUBSTRATE_OVERRIDE_POLICY_MODE`, which would technically keep the "no exports" rule while still being acceptable. But to keep it aligned with the spec, I’ll focus on checking for `export SUBSTRATE_OVERRIDE_`. 

To ensure that sourcing `env.sh` results in no variable names, I’ll add a check using the command `env -i bash -lc "source \"$env_sh\"; if env | grep -q '^SUBSTRATE_OVERRIDE_'; then env | grep '^SUBSTRATE_OVERRIDE_' >&2; exit 1; fi"`. This will confirm that the variable isn't exported.
thinking
**Implementing Modifications to Tests and Scripts**

Okay, it seems I need to focus on checking exported environment variables in `env.sh`, rather than just their mentions. Next, I’ll create a Rust test file to evaluate the installation scripts and modify the shell scripts accordingly. 

I plan to run targeted tests like `cargo test -p substrate-shell --test installer_env_wcu4` and then execute the script with a specific scenario—preferably `--scenario metadata`. The full script might be heavy, but since it uses stubs, it should be manageable. It’s expected that there will be some failures initially, which is okay.

After modifying the shell scripts, specifically adding the function `assert_env_sh_no_override_exports`, I’ll ensure I capture the original `PATH` through `HOST_PATH` for the tests. This will help when running commands in a sanitized environment. 

Let’s proceed with these adjustments!
thinking
**Implementing Test Modifications**

Since this is a test script, it's okay to set variables. I’ll call the `assert_env_sh_no_override_exports` function after installations — specifically after `run_prod_install` and `run_dev_install`. The spec requires that a clean environment has no exports after running the relevant scripts, so this check will be essential.

I’ll also ensure that the `PREFIX` variable is referenced correctly within the function. 

For `install_state_smoke.sh`, I’ll add the assert function after each install, particularly in the metadata scenario, where installs occur twice. It’s also necessary to verify that the `env` command exists, so I'll use `env -i PATH="${HOST_PATH}"` as part of the checks. 

Now it's time to implement these changes and patch everything up!
file update
A /home/spenser/__Active_code/substrate/wt/workspace_config_policy_unification-wcu4-test/crates/shell/tests/installer_env_wcu4.rs
#![cfg(unix)]

mod common;

use common::{binary_path, ensure_substrate_built, shared_tmpdir, temp_dir};
use serde_json::Value as JsonValue;
use std::fs;
use std::path::PathBuf;
use std::process::Command;

fn repo_root() -> PathBuf {
    PathBuf::from(env!("CARGO_MANIFEST_DIR"))
        .join("../..")
        .canonicalize()
        .expect("canonicalize repo root")
}

fn read_repo_file(relative_path: &str) -> String {
    fs::read_to_string(repo_root().join(relative_path))
        .unwrap_or_else(|err| panic!("read {relative_path}: {err}"))
}

#[test]
fn installer_scripts_do_not_export_substrate_override_by_default() {
    for script in [
        "scripts/substrate/install-substrate.sh",
        "scripts/substrate/dev-install-substrate.sh",
    ] {
        let contents = read_repo_file(script);
        assert!(
            !contents.contains("export SUBSTRATE_OVERRIDE_"),
            "{script} must not export SUBSTRATE_OVERRIDE_* by default (WCU4)"
        );
    }
}

#[test]
fn config_current_show_is_not_affected_without_override_inputs() {
    ensure_substrate_built();
    let temp = temp_dir("substrate-wcu4-clean-env-");
    let home = temp.path().join("home");
    let substrate_home = temp.path().join("substrate-home");
    let cwd = temp.path().join("cwd");
    fs::create_dir_all(&home).expect("create HOME");
    fs::create_dir_all(&substrate_home).expect("create SUBSTRATE_HOME");
    fs::create_dir_all(&cwd).expect("create cwd");

    fs::write(
        substrate_home.join("config.yaml"),
        "policy:\n  mode: disabled\n",
    )
    .expect("write config.yaml");

    let host_path = std::env::var("PATH").unwrap_or_else(|_| "/usr/bin:/bin".to_string());
    let output = Command::new(binary_path())
        .env_clear()
        .env("PATH", host_path)
        .env("TMPDIR", shared_tmpdir())
        .env("HOME", &home)
        .env("USERPROFILE", &home)
        .env("SUBSTRATE_HOME", &substrate_home)
        .current_dir(&cwd)
        .arg("--no-world")
        .args(["config", "current", "show", "--json"])
        .output()
        .expect("run substrate config current show --json");
    assert!(
        output.status.success(),
        "config current show should succeed: {output:?}"
    );

    let json: JsonValue = serde_json::from_slice(&output.stdout).expect("current JSON parse");
    assert_eq!(
        json.pointer("/policy/mode").and_then(|v| v.as_str()),
        Some("disabled"),
        "policy.mode mismatch: {json}"
    );
}

M /home/spenser/__Active_code/substrate/wt/workspace_config_policy_unification-wcu4-test/tests/installers/install_smoke.sh
@@ -5,2 +5,3 @@
 REPO_ROOT="$(cd "${SCRIPT_DIR}/../.." && pwd)"
+HOST_PATH="${PATH}"
 
@@ -696,2 +697,18 @@
 
+assert_env_sh_has_no_override_exports() {
+  local env_sh="${PREFIX}/env.sh"
+  if [[ ! -f "${env_sh}" ]]; then
+    fatal "env.sh missing after install: ${env_sh}"
+  fi
+  if grep -Eq '^export[[:space:]]+SUBSTRATE_OVERRIDE_' "${env_sh}"; then
+    log "env.sh contains SUBSTRATE_OVERRIDE_* exports:"
+    sed -n '1,120p' "${env_sh}" >&2 || true
+    fatal "env.sh must not export SUBSTRATE_OVERRIDE_* by default (WCU4)"
+  fi
+  if env -i PATH="${HOST_PATH}" bash -lc "source \"${env_sh}\"; env | grep -q '^SUBSTRATE_OVERRIDE_'" >/dev/null 2>&1; then
+    fatal "Sourcing env.sh must not export SUBSTRATE_OVERRIDE_* (WCU4)"
+  fi
+  log "Verified env.sh does not export SUBSTRATE_OVERRIDE_* (${env_sh})"
+}
+
 run_health_smoke() {
@@ -796,10 +813,11 @@
 
-if [[ "${SCENARIO_KIND}" == "prod" ]]; then
-  build_fake_release
-  run_prod_install
-  install_systemctl_log="$(capture_systemctl_log install)"
-  assert_config_init_hint_logged
-  assert_install_config
-  assert_manifest_present
-  run_health_smoke
+  if [[ "${SCENARIO_KIND}" == "prod" ]]; then
+    build_fake_release
+    run_prod_install
+    assert_env_sh_has_no_override_exports
+    install_systemctl_log="$(capture_systemctl_log install)"
+    assert_config_init_hint_logged
+    assert_install_config
+    assert_manifest_present
+    run_health_smoke
   if [[ "${SCENARIO_WORLD_ENABLED}" -eq 1 ]]; then
@@ -812,9 +830,10 @@
   assert_systemctl_log "uninstall" "${uninstall_systemctl_log}" 1
-elif [[ "${SCENARIO_KIND}" == "dev" ]]; then
-  run_dev_install
-  install_systemctl_log="$(capture_systemctl_log install)"
-  assert_systemctl_log "install" "${install_systemctl_log}" 1
-else
-  fatal "Unsupported scenario kind: ${SCENARIO_KIND}"
-fi
+  elif [[ "${SCENARIO_KIND}" == "dev" ]]; then
+    run_dev_install
+    assert_env_sh_has_no_override_exports
+    install_systemctl_log="$(capture_systemctl_log install)"
+    assert_systemctl_log "install" "${install_systemctl_log}" 1
+  else
+    fatal "Unsupported scenario kind: ${SCENARIO_KIND}"
+  fi
 
M /home/spenser/__Active_code/substrate/wt/workspace_config_policy_unification-wcu4-test/tests/installers/install_state_smoke.sh
@@ -671,2 +671,3 @@
 
+  assert_env_sh_has_no_override_exports "${prefix}" "${HOST_PATH}"
   assert_metadata_file "${state_path}" "${fake_user}" "true" "true" "no"
@@ -700,2 +701,3 @@
 
+  assert_env_sh_has_no_override_exports "${prefix}" "${HOST_PATH}"
   assert_metadata_upgraded "${state_path}" "${fake_user}"
@@ -788,2 +790,20 @@
 
+assert_env_sh_has_no_override_exports() {
+  local prefix="$1"
+  local host_path="$2"
+  local env_sh="${prefix}/env.sh"
+  if [[ ! -f "${env_sh}" ]]; then
+    fatal "env.sh missing after install: ${env_sh}"
+  fi
+  if grep -Eq '^export[[:space:]]+SUBSTRATE_OVERRIDE_' "${env_sh}"; then
+    log "env.sh contains SUBSTRATE_OVERRIDE_* exports:"
+    sed -n '1,120p' "${env_sh}" >&2 || true
+    fatal "env.sh must not export SUBSTRATE_OVERRIDE_* by default (WCU4)"
+  fi
+  if env -i PATH="${host_path}" bash -lc "source \"${env_sh}\"; env | grep -q '^SUBSTRATE_OVERRIDE_'" >/dev/null 2>&1; then
+    fatal "Sourcing env.sh must not export SUBSTRATE_OVERRIDE_* (WCU4)"
+  fi
+  log "Verified env.sh does not export SUBSTRATE_OVERRIDE_* (${env_sh})"
+}
+
 run_missing_metadata_scenario() (
apply_patch(auto_approved=true) exited 0 in 19ms:
Success. Updated the following files:
A crates/shell/tests/installer_env_wcu4.rs
M tests/installers/install_smoke.sh
M tests/installers/install_state_smoke.sh
file update:
diff --git a/crates/shell/tests/installer_env_wcu4.rs b/crates/shell/tests/installer_env_wcu4.rs
new file mode 100644
index 0000000000000000000000000000000000000000..db04cdab3350394dfb4a2ca524933d459d6b89a1
--- /dev/null
+++ b/crates/shell/tests/installer_env_wcu4.rs
@@ -0,0 +1,79 @@
+#![cfg(unix)]
+
+mod common;
+
+use common::{binary_path, ensure_substrate_built, shared_tmpdir, temp_dir};
+use serde_json::Value as JsonValue;
+use std::fs;
+use std::path::PathBuf;
+use std::process::Command;
+
+fn repo_root() -> PathBuf {
+    PathBuf::from(env!("CARGO_MANIFEST_DIR"))
+        .join("../..")
+        .canonicalize()
+        .expect("canonicalize repo root")
+}
+
+fn read_repo_file(relative_path: &str) -> String {
+    fs::read_to_string(repo_root().join(relative_path))
+        .unwrap_or_else(|err| panic!("read {relative_path}: {err}"))
+}
+
+#[test]
+fn installer_scripts_do_not_export_substrate_override_by_default() {
+    for script in [
+        "scripts/substrate/install-substrate.sh",
+        "scripts/substrate/dev-install-substrate.sh",
+    ] {
+        let contents = read_repo_file(script);
+        assert!(
+            !contents.contains("export SUBSTRATE_OVERRIDE_"),
+            "{script} must not export SUBSTRATE_OVERRIDE_* by default (WCU4)"
+        );
+    }
+}
+
+#[test]
+fn config_current_show_is_not_affected_without_override_inputs() {
+    ensure_substrate_built();
+    let temp = temp_dir("substrate-wcu4-clean-env-");
+    let home = temp.path().join("home");
+    let substrate_home = temp.path().join("substrate-home");
+    let cwd = temp.path().join("cwd");
+    fs::create_dir_all(&home).expect("create HOME");
+    fs::create_dir_all(&substrate_home).expect("create SUBSTRATE_HOME");
+    fs::create_dir_all(&cwd).expect("create cwd");
+
+    fs::write(
+        substrate_home.join("config.yaml"),
+        "policy:\n  mode: disabled\n",
+    )
+    .expect("write config.yaml");
+
+    let host_path = std::env::var("PATH").unwrap_or_else(|_| "/usr/bin:/bin".to_string());
+    let output = Command::new(binary_path())
+        .env_clear()
+        .env("PATH", host_path)
+        .env("TMPDIR", shared_tmpdir())
+        .env("HOME", &home)
+        .env("USERPROFILE", &home)
+        .env("SUBSTRATE_HOME", &substrate_home)
+        .current_dir(&cwd)
+        .arg("--no-world")
+        .args(["config", "current", "show", "--json"])
+        .output()
+        .expect("run substrate config current show --json");
+    assert!(
+        output.status.success(),
+        "config current show should succeed: {output:?}"
+    );
+
+    let json: JsonValue = serde_json::from_slice(&output.stdout).expect("current JSON parse");
+    assert_eq!(
+        json.pointer("/policy/mode").and_then(|v| v.as_str()),
+        Some("disabled"),
+        "policy.mode mismatch: {json}"
+    );
+}
+
diff --git a/tests/installers/install_smoke.sh b/tests/installers/install_smoke.sh
index db133b5c269c4ff975491691e4fb17fc2e2a6e46..76b8a4df24bcd3b603ea8df1c25de70ace488c18
--- a/tests/installers/install_smoke.sh
+++ b/tests/installers/install_smoke.sh
@@ -3,6 +3,7 @@
 
 SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
 REPO_ROOT="$(cd "${SCRIPT_DIR}/../.." && pwd)"
+HOST_PATH="${PATH}"
 
 log() {
   printf '[install-smoke] %s\n' "$*" >&2
@@ -694,6 +695,22 @@
   log "Verified manager manifest at ${manifest}"
 }
 
+assert_env_sh_has_no_override_exports() {
+  local env_sh="${PREFIX}/env.sh"
+  if [[ ! -f "${env_sh}" ]]; then
+    fatal "env.sh missing after install: ${env_sh}"
+  fi
+  if grep -Eq '^export[[:space:]]+SUBSTRATE_OVERRIDE_' "${env_sh}"; then
+    log "env.sh contains SUBSTRATE_OVERRIDE_* exports:"
+    sed -n '1,120p' "${env_sh}" >&2 || true
+    fatal "env.sh must not export SUBSTRATE_OVERRIDE_* by default (WCU4)"
+  fi
+  if env -i PATH="${HOST_PATH}" bash -lc "source \"${env_sh}\"; env | grep -q '^SUBSTRATE_OVERRIDE_'" >/dev/null 2>&1; then
+    fatal "Sourcing env.sh must not export SUBSTRATE_OVERRIDE_* (WCU4)"
+  fi
+  log "Verified env.sh does not export SUBSTRATE_OVERRIDE_* (${env_sh})"
+}
+
 run_health_smoke() {
   local substrate_bin="${PREFIX}/bin/substrate"
   local output
@@ -794,14 +811,15 @@
 prepare_stub_bin
 reset_systemctl_log
 
-if [[ "${SCENARIO_KIND}" == "prod" ]]; then
-  build_fake_release
-  run_prod_install
-  install_systemctl_log="$(capture_systemctl_log install)"
-  assert_config_init_hint_logged
-  assert_install_config
-  assert_manifest_present
-  run_health_smoke
+  if [[ "${SCENARIO_KIND}" == "prod" ]]; then
+    build_fake_release
+    run_prod_install
+    assert_env_sh_has_no_override_exports
+    install_systemctl_log="$(capture_systemctl_log install)"
+    assert_config_init_hint_logged
+    assert_install_config
+    assert_manifest_present
+    run_health_smoke
   if [[ "${SCENARIO_WORLD_ENABLED}" -eq 1 ]]; then
     assert_systemctl_log "install" "${install_systemctl_log}" 1
   else
@@ -810,13 +828,14 @@
   run_prod_uninstall
   uninstall_systemctl_log="$(capture_systemctl_log uninstall)"
   assert_systemctl_log "uninstall" "${uninstall_systemctl_log}" 1
-elif [[ "${SCENARIO_KIND}" == "dev" ]]; then
-  run_dev_install
-  install_systemctl_log="$(capture_systemctl_log install)"
-  assert_systemctl_log "install" "${install_systemctl_log}" 1
-else
-  fatal "Unsupported scenario kind: ${SCENARIO_KIND}"
-fi
+  elif [[ "${SCENARIO_KIND}" == "dev" ]]; then
+    run_dev_install
+    assert_env_sh_has_no_override_exports
+    install_systemctl_log="$(capture_systemctl_log install)"
+    assert_systemctl_log "install" "${install_systemctl_log}" 1
+  else
+    fatal "Unsupported scenario kind: ${SCENARIO_KIND}"
+  fi
 
 log "Scenario ${SCENARIO} completed using PREFIX=${PREFIX}"
 log "Temp root: ${WORK_ROOT}"
diff --git a/tests/installers/install_state_smoke.sh b/tests/installers/install_state_smoke.sh
index 88cecedffc5a6cbba88a97c65c1dea1f9dcb12b5..e94e9017e822b20b2e6dfba5a894e29228c877bc
--- a/tests/installers/install_state_smoke.sh
+++ b/tests/installers/install_state_smoke.sh
@@ -669,6 +669,7 @@
       fatal "metadata install failed (see ${install_log})"
     }
 
+  assert_env_sh_has_no_override_exports "${prefix}" "${HOST_PATH}"
   assert_metadata_file "${state_path}" "${fake_user}" "true" "true" "no"
 
   # Simulate legacy metadata and rerun install to ensure upgrade path rewrites it.
@@ -698,6 +699,7 @@
       fatal "metadata upgrade install failed (see ${install_log})"
     }
 
+  assert_env_sh_has_no_override_exports "${prefix}" "${HOST_PATH}"
   assert_metadata_upgraded "${state_path}" "${fake_user}"
 
   log "Metadata scenario completed (state: ${state_path})"
@@ -786,6 +788,24 @@
   log "Cleanup scenario completed (state: ${state_path})"
 )
 
+assert_env_sh_has_no_override_exports() {
+  local prefix="$1"
+  local host_path="$2"
+  local env_sh="${prefix}/env.sh"
+  if [[ ! -f "${env_sh}" ]]; then
+    fatal "env.sh missing after install: ${env_sh}"
+  fi
+  if grep -Eq '^export[[:space:]]+SUBSTRATE_OVERRIDE_' "${env_sh}"; then
+    log "env.sh contains SUBSTRATE_OVERRIDE_* exports:"
+    sed -n '1,120p' "${env_sh}" >&2 || true
+    fatal "env.sh must not export SUBSTRATE_OVERRIDE_* by default (WCU4)"
+  fi
+  if env -i PATH="${host_path}" bash -lc "source \"${env_sh}\"; env | grep -q '^SUBSTRATE_OVERRIDE_'" >/dev/null 2>&1; then
+    fatal "Sourcing env.sh must not export SUBSTRATE_OVERRIDE_* (WCU4)"
+  fi
+  log "Verified env.sh does not export SUBSTRATE_OVERRIDE_* (${env_sh})"
+}
+
 run_missing_metadata_scenario() (
   local work_root
   work_root="$(mktemp -d "/tmp/substrate-install-state.missing.XXXXXX")"

file update:
diff --git a/crates/shell/tests/installer_env_wcu4.rs b/crates/shell/tests/installer_env_wcu4.rs
new file mode 100644
index 0000000000000000000000000000000000000000..db04cdab3350394dfb4a2ca524933d459d6b89a1
--- /dev/null
+++ b/crates/shell/tests/installer_env_wcu4.rs
@@ -0,0 +1,79 @@
+#![cfg(unix)]
+
+mod common;
+
+use common::{binary_path, ensure_substrate_built, shared_tmpdir, temp_dir};
+use serde_json::Value as JsonValue;
+use std::fs;
+use std::path::PathBuf;
+use std::process::Command;
+
+fn repo_root() -> PathBuf {
+    PathBuf::from(env!("CARGO_MANIFEST_DIR"))
+        .join("../..")
+        .canonicalize()
+        .expect("canonicalize repo root")
+}
+
+fn read_repo_file(relative_path: &str) -> String {
+    fs::read_to_string(repo_root().join(relative_path))
+        .unwrap_or_else(|err| panic!("read {relative_path}: {err}"))
+}
+
+#[test]
+fn installer_scripts_do_not_export_substrate_override_by_default() {
+    for script in [
+        "scripts/substrate/install-substrate.sh",
+        "scripts/substrate/dev-install-substrate.sh",
+    ] {
+        let contents = read_repo_file(script);
+        assert!(
+            !contents.contains("export SUBSTRATE_OVERRIDE_"),
+            "{script} must not export SUBSTRATE_OVERRIDE_* by default (WCU4)"
+        );
+    }
+}
+
+#[test]
+fn config_current_show_is_not_affected_without_override_inputs() {
+    ensure_substrate_built();
+    let temp = temp_dir("substrate-wcu4-clean-env-");
+    let home = temp.path().join("home");
+    let substrate_home = temp.path().join("substrate-home");
+    let cwd = temp.path().join("cwd");
+    fs::create_dir_all(&home).expect("create HOME");
+    fs::create_dir_all(&substrate_home).expect("create SUBSTRATE_HOME");
+    fs::create_dir_all(&cwd).expect("create cwd");
+
+    fs::write(
+        substrate_home.join("config.yaml"),
+        "policy:\n  mode: disabled\n",
+    )
+    .expect("write config.yaml");
+
+    let host_path = std::env::var("PATH").unwrap_or_else(|_| "/usr/bin:/bin".to_string());
+    let output = Command::new(binary_path())
+        .env_clear()
+        .env("PATH", host_path)
+        .env("TMPDIR", shared_tmpdir())
+        .env("HOME", &home)
+        .env("USERPROFILE", &home)
+        .env("SUBSTRATE_HOME", &substrate_home)
+        .current_dir(&cwd)
+        .arg("--no-world")
+        .args(["config", "current", "show", "--json"])
+        .output()
+        .expect("run substrate config current show --json");
+    assert!(
+        output.status.success(),
+        "config current show should succeed: {output:?}"
+    );
+
+    let json: JsonValue = serde_json::from_slice(&output.stdout).expect("current JSON parse");
+    assert_eq!(
+        json.pointer("/policy/mode").and_then(|v| v.as_str()),
+        Some("disabled"),
+        "policy.mode mismatch: {json}"
+    );
+}
+
diff --git a/tests/installers/install_smoke.sh b/tests/installers/install_smoke.sh
index db133b5c269c4ff975491691e4fb17fc2e2a6e46..76b8a4df24bcd3b603ea8df1c25de70ace488c18
--- a/tests/installers/install_smoke.sh
+++ b/tests/installers/install_smoke.sh
@@ -3,6 +3,7 @@
 
 SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
 REPO_ROOT="$(cd "${SCRIPT_DIR}/../.." && pwd)"
+HOST_PATH="${PATH}"
 
 log() {
   printf '[install-smoke] %s\n' "$*" >&2
@@ -694,6 +695,22 @@
   log "Verified manager manifest at ${manifest}"
 }
 
+assert_env_sh_has_no_override_exports() {
+  local env_sh="${PREFIX}/env.sh"
+  if [[ ! -f "${env_sh}" ]]; then
+    fatal "env.sh missing after install: ${env_sh}"
+  fi
+  if grep -Eq '^export[[:space:]]+SUBSTRATE_OVERRIDE_' "${env_sh}"; then
+    log "env.sh contains SUBSTRATE_OVERRIDE_* exports:"
+    sed -n '1,120p' "${env_sh}" >&2 || true
+    fatal "env.sh must not export SUBSTRATE_OVERRIDE_* by default (WCU4)"
+  fi
+  if env -i PATH="${HOST_PATH}" bash -lc "source \"${env_sh}\"; env | grep -q '^SUBSTRATE_OVERRIDE_'" >/dev/null 2>&1; then
+    fatal "Sourcing env.sh must not export SUBSTRATE_OVERRIDE_* (WCU4)"
+  fi
+  log "Verified env.sh does not export SUBSTRATE_OVERRIDE_* (${env_sh})"
+}
+
 run_health_smoke() {
   local substrate_bin="${PREFIX}/bin/substrate"
   local output
@@ -794,14 +811,15 @@
 prepare_stub_bin
 reset_systemctl_log
 
-if [[ "${SCENARIO_KIND}" == "prod" ]]; then
-  build_fake_release
-  run_prod_install
-  install_systemctl_log="$(capture_systemctl_log install)"
-  assert_config_init_hint_logged
-  assert_install_config
-  assert_manifest_present
-  run_health_smoke
+  if [[ "${SCENARIO_KIND}" == "prod" ]]; then
+    build_fake_release
+    run_prod_install
+    assert_env_sh_has_no_override_exports
+    install_systemctl_log="$(capture_systemctl_log install)"
+    assert_config_init_hint_logged
+    assert_install_config
+    assert_manifest_present
+    run_health_smoke
   if [[ "${SCENARIO_WORLD_ENABLED}" -eq 1 ]]; then
     assert_systemctl_log "install" "${install_systemctl_log}" 1
   else
@@ -810,13 +828,14 @@
   run_prod_uninstall
   uninstall_systemctl_log="$(capture_systemctl_log uninstall)"
   assert_systemctl_log "uninstall" "${uninstall_systemctl_log}" 1
-elif [[ "${SCENARIO_KIND}" == "dev" ]]; then
-  run_dev_install
-  install_systemctl_log="$(capture_systemctl_log install)"
-  assert_systemctl_log "install" "${install_systemctl_log}" 1
-else
-  fatal "Unsupported scenario kind: ${SCENARIO_KIND}"
-fi
+  elif [[ "${SCENARIO_KIND}" == "dev" ]]; then
+    run_dev_install
+    assert_env_sh_has_no_override_exports
+    install_systemctl_log="$(capture_systemctl_log install)"
+    assert_systemctl_log "install" "${install_systemctl_log}" 1
+  else
+    fatal "Unsupported scenario kind: ${SCENARIO_KIND}"
+  fi
 
 log "Scenario ${SCENARIO} completed using PREFIX=${PREFIX}"
 log "Temp root: ${WORK_ROOT}"
diff --git a/tests/installers/install_state_smoke.sh b/tests/installers/install_state_smoke.sh
index 88cecedffc5a6cbba88a97c65c1dea1f9dcb12b5..e94e9017e822b20b2e6dfba5a894e29228c877bc
--- a/tests/installers/install_state_smoke.sh
+++ b/tests/installers/install_state_smoke.sh
@@ -669,6 +669,7 @@
       fatal "metadata install failed (see ${install_log})"
     }
 
+  assert_env_sh_has_no_override_exports "${prefix}" "${HOST_PATH}"
   assert_metadata_file "${state_path}" "${fake_user}" "true" "true" "no"
 
   # Simulate legacy metadata and rerun install to ensure upgrade path rewrites it.
@@ -698,6 +699,7 @@
       fatal "metadata upgrade install failed (see ${install_log})"
     }
 
+  assert_env_sh_has_no_override_exports "${prefix}" "${HOST_PATH}"
   assert_metadata_upgraded "${state_path}" "${fake_user}"
 
   log "Metadata scenario completed (state: ${state_path})"
@@ -786,6 +788,24 @@
   log "Cleanup scenario completed (state: ${state_path})"
 )
 
+assert_env_sh_has_no_override_exports() {
+  local prefix="$1"
+  local host_path="$2"
+  local env_sh="${prefix}/env.sh"
+  if [[ ! -f "${env_sh}" ]]; then
+    fatal "env.sh missing after install: ${env_sh}"
+  fi
+  if grep -Eq '^export[[:space:]]+SUBSTRATE_OVERRIDE_' "${env_sh}"; then
+    log "env.sh contains SUBSTRATE_OVERRIDE_* exports:"
+    sed -n '1,120p' "${env_sh}" >&2 || true
+    fatal "env.sh must not export SUBSTRATE_OVERRIDE_* by default (WCU4)"
+  fi
+  if env -i PATH="${host_path}" bash -lc "source \"${env_sh}\"; env | grep -q '^SUBSTRATE_OVERRIDE_'" >/dev/null 2>&1; then
+    fatal "Sourcing env.sh must not export SUBSTRATE_OVERRIDE_* (WCU4)"
+  fi
+  log "Verified env.sh does not export SUBSTRATE_OVERRIDE_* (${env_sh})"
+}
+
 run_missing_metadata_scenario() (
   local work_root
   work_root="$(mktemp -d "/tmp/substrate-install-state.missing.XXXXXX")"






thinking
**Clarifying bash function definitions**

I'm thinking about the assert_env_sh function in install_state_smoke, which we added after the run_cleanup_scenario call. But I noticed it's also called earlier in run_metadata_scenario; usually, bash requires functions to be defined before they're used. Actually, bash reads the entire script first and knows the function definitions, so it seems like it can work even if the function is defined later. I’m pretty sure about this, but I want to double-check my understanding!
thinking
**Ensuring function definitions and paths**

To be safe, I should really define functions before using them; it's good practice. In bash, calling a function before its definition works since it reads the entire file. But with `set -e`, it's different. We placed the `assert_env_sh_has_no_override_exports` function near the bottom after run_cleanup_scenario, so it should be accessible. Now, in our Rust test file, we're referencing `shared_tmpdir`, and it seems fine since `Command::env` accepts `Path`. However, I need to make sure the `repo_root` exists, as it might fail if it's missing!
thinking
**Verifying modifications with cargo fmt**

I need to make sure our modifications don’t break cargo fmt. We added env_sh assertion functions to scripts that use `sed`, and I’m checking if that's stubbed. In install_smoke, we set PATH to include stub_bin and host, but I realized we didn’t stub sed, so it will use the real version. In harness, the PATH might have stubs, but sed will still come from the host path. I also need to confirm if `bash` resolves correctly when executing commands from the script!
thinking
**Checking environment and PATH configurations**

After the `env` command starts, it runs `bash` using the path according to the environment's PATH variable. I believe `bash` should be located in /usr/bin/bash, which is good. The install_state_smoke already sets `HOST_PATH="${PATH}"` at the top. When we call a function with this HOST_PATH, it’s essential to check if stub_bin is included. In the metadata scenario, harness_path is stub_bin plus HOST_PATH, but we want to ensure that `grep`, `sed`, and `env` are resolved correctly, as they need to come from the host path. I also need to confirm that the function defined after hasn't caused any issues with editing planning docs.
thinking
**Preparing to run tests and formatting**

I need to run cargo fmt and targeted tests now. But first, I want to double-check there are no other instructions or tasks pending. I also have to ensure that the test file uses 4-space indentation — it’s important for consistency. I think it’s time to update the plan: step 2 should be marked as in progress or possibly completed, so we can proceed to step 3. I’ll make sure to document these updates!
Plan update
  ✓ Verify task state and specs
  ✓ Add WCU4 installer env tests
  → Run fmt and targeted tests
  • Run triad task finisher
file update:
diff --git a/crates/shell/tests/installer_env_wcu4.rs b/crates/shell/tests/installer_env_wcu4.rs
new file mode 100644
index 0000000000000000000000000000000000000000..db04cdab3350394dfb4a2ca524933d459d6b89a1
--- /dev/null
+++ b/crates/shell/tests/installer_env_wcu4.rs
@@ -0,0 +1,79 @@
+#![cfg(unix)]
+
+mod common;
+
+use common::{binary_path, ensure_substrate_built, shared_tmpdir, temp_dir};
+use serde_json::Value as JsonValue;
+use std::fs;
+use std::path::PathBuf;
+use std::process::Command;
+
+fn repo_root() -> PathBuf {
+    PathBuf::from(env!("CARGO_MANIFEST_DIR"))
+        .join("../..")
+        .canonicalize()
+        .expect("canonicalize repo root")
+}
+
+fn read_repo_file(relative_path: &str) -> String {
+    fs::read_to_string(repo_root().join(relative_path))
+        .unwrap_or_else(|err| panic!("read {relative_path}: {err}"))
+}
+
+#[test]
+fn installer_scripts_do_not_export_substrate_override_by_default() {
+    for script in [
+        "scripts/substrate/install-substrate.sh",
+        "scripts/substrate/dev-install-substrate.sh",
+    ] {
+        let contents = read_repo_file(script);
+        assert!(
+            !contents.contains("export SUBSTRATE_OVERRIDE_"),
+            "{script} must not export SUBSTRATE_OVERRIDE_* by default (WCU4)"
+        );
+    }
+}
+
+#[test]
+fn config_current_show_is_not_affected_without_override_inputs() {
+    ensure_substrate_built();
+    let temp = temp_dir("substrate-wcu4-clean-env-");
+    let home = temp.path().join("home");
+    let substrate_home = temp.path().join("substrate-home");
+    let cwd = temp.path().join("cwd");
+    fs::create_dir_all(&home).expect("create HOME");
+    fs::create_dir_all(&substrate_home).expect("create SUBSTRATE_HOME");
+    fs::create_dir_all(&cwd).expect("create cwd");
+
+    fs::write(
+        substrate_home.join("config.yaml"),
+        "policy:\n  mode: disabled\n",
+    )
+    .expect("write config.yaml");
+
+    let host_path = std::env::var("PATH").unwrap_or_else(|_| "/usr/bin:/bin".to_string());
+    let output = Command::new(binary_path())
+        .env_clear()
+        .env("PATH", host_path)
+        .env("TMPDIR", shared_tmpdir())
+        .env("HOME", &home)
+        .env("USERPROFILE", &home)
+        .env("SUBSTRATE_HOME", &substrate_home)
+        .current_dir(&cwd)
+        .arg("--no-world")
+        .args(["config", "current", "show", "--json"])
+        .output()
+        .expect("run substrate config current show --json");
+    assert!(
+        output.status.success(),
+        "config current show should succeed: {output:?}"
+    );
+
+    let json: JsonValue = serde_json::from_slice(&output.stdout).expect("current JSON parse");
+    assert_eq!(
+        json.pointer("/policy/mode").and_then(|v| v.as_str()),
+        Some("disabled"),
+        "policy.mode mismatch: {json}"
+    );
+}
+
diff --git a/tests/installers/install_smoke.sh b/tests/installers/install_smoke.sh
index db133b5c269c4ff975491691e4fb17fc2e2a6e46..76b8a4df24bcd3b603ea8df1c25de70ace488c18
--- a/tests/installers/install_smoke.sh
+++ b/tests/installers/install_smoke.sh
@@ -3,6 +3,7 @@
 
 SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
 REPO_ROOT="$(cd "${SCRIPT_DIR}/../.." && pwd)"
+HOST_PATH="${PATH}"
 
 log() {
   printf '[install-smoke] %s\n' "$*" >&2
@@ -694,6 +695,22 @@
   log "Verified manager manifest at ${manifest}"
 }
 
+assert_env_sh_has_no_override_exports() {
+  local env_sh="${PREFIX}/env.sh"
+  if [[ ! -f "${env_sh}" ]]; then
+    fatal "env.sh missing after install: ${env_sh}"
+  fi
+  if grep -Eq '^export[[:space:]]+SUBSTRATE_OVERRIDE_' "${env_sh}"; then
+    log "env.sh contains SUBSTRATE_OVERRIDE_* exports:"
+    sed -n '1,120p' "${env_sh}" >&2 || true
+    fatal "env.sh must not export SUBSTRATE_OVERRIDE_* by default (WCU4)"
+  fi
+  if env -i PATH="${HOST_PATH}" bash -lc "source \"${env_sh}\"; env | grep -q '^SUBSTRATE_OVERRIDE_'" >/dev/null 2>&1; then
+    fatal "Sourcing env.sh must not export SUBSTRATE_OVERRIDE_* (WCU4)"
+  fi
+  log "Verified env.sh does not export SUBSTRATE_OVERRIDE_* (${env_sh})"
+}
+
 run_health_smoke() {
   local substrate_bin="${PREFIX}/bin/substrate"
   local output
@@ -794,14 +811,15 @@
 prepare_stub_bin
 reset_systemctl_log
 
-if [[ "${SCENARIO_KIND}" == "prod" ]]; then
-  build_fake_release
-  run_prod_install
-  install_systemctl_log="$(capture_systemctl_log install)"
-  assert_config_init_hint_logged
-  assert_install_config
-  assert_manifest_present
-  run_health_smoke
+  if [[ "${SCENARIO_KIND}" == "prod" ]]; then
+    build_fake_release
+    run_prod_install
+    assert_env_sh_has_no_override_exports
+    install_systemctl_log="$(capture_systemctl_log install)"
+    assert_config_init_hint_logged
+    assert_install_config
+    assert_manifest_present
+    run_health_smoke
   if [[ "${SCENARIO_WORLD_ENABLED}" -eq 1 ]]; then
     assert_systemctl_log "install" "${install_systemctl_log}" 1
   else
@@ -810,13 +828,14 @@
   run_prod_uninstall
   uninstall_systemctl_log="$(capture_systemctl_log uninstall)"
   assert_systemctl_log "uninstall" "${uninstall_systemctl_log}" 1
-elif [[ "${SCENARIO_KIND}" == "dev" ]]; then
-  run_dev_install
-  install_systemctl_log="$(capture_systemctl_log install)"
-  assert_systemctl_log "install" "${install_systemctl_log}" 1
-else
-  fatal "Unsupported scenario kind: ${SCENARIO_KIND}"
-fi
+  elif [[ "${SCENARIO_KIND}" == "dev" ]]; then
+    run_dev_install
+    assert_env_sh_has_no_override_exports
+    install_systemctl_log="$(capture_systemctl_log install)"
+    assert_systemctl_log "install" "${install_systemctl_log}" 1
+  else
+    fatal "Unsupported scenario kind: ${SCENARIO_KIND}"
+  fi
 
 log "Scenario ${SCENARIO} completed using PREFIX=${PREFIX}"
 log "Temp root: ${WORK_ROOT}"
diff --git a/tests/installers/install_state_smoke.sh b/tests/installers/install_state_smoke.sh
index 88cecedffc5a6cbba88a97c65c1dea1f9dcb12b5..e94e9017e822b20b2e6dfba5a894e29228c877bc
--- a/tests/installers/install_state_smoke.sh
+++ b/tests/installers/install_state_smoke.sh
@@ -669,6 +669,7 @@
       fatal "metadata install failed (see ${install_log})"
     }
 
+  assert_env_sh_has_no_override_exports "${prefix}" "${HOST_PATH}"
   assert_metadata_file "${state_path}" "${fake_user}" "true" "true" "no"
 
   # Simulate legacy metadata and rerun install to ensure upgrade path rewrites it.
@@ -698,6 +699,7 @@
       fatal "metadata upgrade install failed (see ${install_log})"
     }
 
+  assert_env_sh_has_no_override_exports "${prefix}" "${HOST_PATH}"
   assert_metadata_upgraded "${state_path}" "${fake_user}"
 
   log "Metadata scenario completed (state: ${state_path})"
@@ -786,6 +788,24 @@
   log "Cleanup scenario completed (state: ${state_path})"
 )
 
+assert_env_sh_has_no_override_exports() {
+  local prefix="$1"
+  local host_path="$2"
+  local env_sh="${prefix}/env.sh"
+  if [[ ! -f "${env_sh}" ]]; then
+    fatal "env.sh missing after install: ${env_sh}"
+  fi
+  if grep -Eq '^export[[:space:]]+SUBSTRATE_OVERRIDE_' "${env_sh}"; then
+    log "env.sh contains SUBSTRATE_OVERRIDE_* exports:"
+    sed -n '1,120p' "${env_sh}" >&2 || true
+    fatal "env.sh must not export SUBSTRATE_OVERRIDE_* by default (WCU4)"
+  fi
+  if env -i PATH="${host_path}" bash -lc "source \"${env_sh}\"; env | grep -q '^SUBSTRATE_OVERRIDE_'" >/dev/null 2>&1; then
+    fatal "Sourcing env.sh must not export SUBSTRATE_OVERRIDE_* (WCU4)"
+  fi
+  log "Verified env.sh does not export SUBSTRATE_OVERRIDE_* (${env_sh})"
+}
+
 run_missing_metadata_scenario() (
   local work_root
   work_root="$(mktemp -d "/tmp/substrate-install-state.missing.XXXXXX")"


thinking
**Executing test commands**

I know running `cargo test` will fail because of a new Rust test assertion. That’s expected, but I want to ensure it fails for the right reasons—mainly assertions. First, I’ll run `cargo fmt` to format the code, and then I’ll execute the test with `cargo test -p substrate-shell --test installer_env_wcu4 -- --nocapture`. After that, I'll run the necessary scripts using shell commands. Let's keep everything organized!
exec
/bin/bash -lc 'cargo fmt' in /home/spenser/__Active_code/substrate/wt/workspace_config_policy_unification-wcu4-test succeeded in 558ms:

file update:
diff --git a/crates/shell/tests/installer_env_wcu4.rs b/crates/shell/tests/installer_env_wcu4.rs
new file mode 100644
index 0000000000000000000000000000000000000000..fed36ed1072e3738bc17f7a1232648f324e1b336
--- /dev/null
+++ b/crates/shell/tests/installer_env_wcu4.rs
@@ -0,0 +1,78 @@
+#![cfg(unix)]
+
+mod common;
+
+use common::{binary_path, ensure_substrate_built, shared_tmpdir, temp_dir};
+use serde_json::Value as JsonValue;
+use std::fs;
+use std::path::PathBuf;
+use std::process::Command;
+
+fn repo_root() -> PathBuf {
+    PathBuf::from(env!("CARGO_MANIFEST_DIR"))
+        .join("../..")
+        .canonicalize()
+        .expect("canonicalize repo root")
+}
+
+fn read_repo_file(relative_path: &str) -> String {
+    fs::read_to_string(repo_root().join(relative_path))
+        .unwrap_or_else(|err| panic!("read {relative_path}: {err}"))
+}
+
+#[test]
+fn installer_scripts_do_not_export_substrate_override_by_default() {
+    for script in [
+        "scripts/substrate/install-substrate.sh",
+        "scripts/substrate/dev-install-substrate.sh",
+    ] {
+        let contents = read_repo_file(script);
+        assert!(
+            !contents.contains("export SUBSTRATE_OVERRIDE_"),
+            "{script} must not export SUBSTRATE_OVERRIDE_* by default (WCU4)"
+        );
+    }
+}
+
+#[test]
+fn config_current_show_is_not_affected_without_override_inputs() {
+    ensure_substrate_built();
+    let temp = temp_dir("substrate-wcu4-clean-env-");
+    let home = temp.path().join("home");
+    let substrate_home = temp.path().join("substrate-home");
+    let cwd = temp.path().join("cwd");
+    fs::create_dir_all(&home).expect("create HOME");
+    fs::create_dir_all(&substrate_home).expect("create SUBSTRATE_HOME");
+    fs::create_dir_all(&cwd).expect("create cwd");
+
+    fs::write(
+        substrate_home.join("config.yaml"),
+        "policy:\n  mode: disabled\n",
+    )
+    .expect("write config.yaml");
+
+    let host_path = std::env::var("PATH").unwrap_or_else(|_| "/usr/bin:/bin".to_string());
+    let output = Command::new(binary_path())
+        .env_clear()
+        .env("PATH", host_path)
+        .env("TMPDIR", shared_tmpdir())
+        .env("HOME", &home)
+        .env("USERPROFILE", &home)
+        .env("SUBSTRATE_HOME", &substrate_home)
+        .current_dir(&cwd)
+        .arg("--no-world")
+        .args(["config", "current", "show", "--json"])
+        .output()
+        .expect("run substrate config current show --json");
+    assert!(
+        output.status.success(),
+        "config current show should succeed: {output:?}"
+    );
+
+    let json: JsonValue = serde_json::from_slice(&output.stdout).expect("current JSON parse");
+    assert_eq!(
+        json.pointer("/policy/mode").and_then(|v| v.as_str()),
+        Some("disabled"),
+        "policy.mode mismatch: {json}"
+    );
+}
diff --git a/tests/installers/install_smoke.sh b/tests/installers/install_smoke.sh
index db133b5c269c4ff975491691e4fb17fc2e2a6e46..76b8a4df24bcd3b603ea8df1c25de70ace488c18
--- a/tests/installers/install_smoke.sh
+++ b/tests/installers/install_smoke.sh
@@ -3,6 +3,7 @@
 
 SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
 REPO_ROOT="$(cd "${SCRIPT_DIR}/../.." && pwd)"
+HOST_PATH="${PATH}"
 
 log() {
   printf '[install-smoke] %s\n' "$*" >&2
@@ -694,6 +695,22 @@
   log "Verified manager manifest at ${manifest}"
 }
 
+assert_env_sh_has_no_override_exports() {
+  local env_sh="${PREFIX}/env.sh"
+  if [[ ! -f "${env_sh}" ]]; then
+    fatal "env.sh missing after install: ${env_sh}"
+  fi
+  if grep -Eq '^export[[:space:]]+SUBSTRATE_OVERRIDE_' "${env_sh}"; then
+    log "env.sh contains SUBSTRATE_OVERRIDE_* exports:"
+    sed -n '1,120p' "${env_sh}" >&2 || true
+    fatal "env.sh must not export SUBSTRATE_OVERRIDE_* by default (WCU4)"
+  fi
+  if env -i PATH="${HOST_PATH}" bash -lc "source \"${env_sh}\"; env | grep -q '^SUBSTRATE_OVERRIDE_'" >/dev/null 2>&1; then
+    fatal "Sourcing env.sh must not export SUBSTRATE_OVERRIDE_* (WCU4)"
+  fi
+  log "Verified env.sh does not export SUBSTRATE_OVERRIDE_* (${env_sh})"
+}
+
 run_health_smoke() {
   local substrate_bin="${PREFIX}/bin/substrate"
   local output
@@ -794,14 +811,15 @@
 prepare_stub_bin
 reset_systemctl_log
 
-if [[ "${SCENARIO_KIND}" == "prod" ]]; then
-  build_fake_release
-  run_prod_install
-  install_systemctl_log="$(capture_systemctl_log install)"
-  assert_config_init_hint_logged
-  assert_install_config
-  assert_manifest_present
-  run_health_smoke
+  if [[ "${SCENARIO_KIND}" == "prod" ]]; then
+    build_fake_release
+    run_prod_install
+    assert_env_sh_has_no_override_exports
+    install_systemctl_log="$(capture_systemctl_log install)"
+    assert_config_init_hint_logged
+    assert_install_config
+    assert_manifest_present
+    run_health_smoke
   if [[ "${SCENARIO_WORLD_ENABLED}" -eq 1 ]]; then
     assert_systemctl_log "install" "${install_systemctl_log}" 1
   else
@@ -810,13 +828,14 @@
   run_prod_uninstall
   uninstall_systemctl_log="$(capture_systemctl_log uninstall)"
   assert_systemctl_log "uninstall" "${uninstall_systemctl_log}" 1
-elif [[ "${SCENARIO_KIND}" == "dev" ]]; then
-  run_dev_install
-  install_systemctl_log="$(capture_systemctl_log install)"
-  assert_systemctl_log "install" "${install_systemctl_log}" 1
-else
-  fatal "Unsupported scenario kind: ${SCENARIO_KIND}"
-fi
+  elif [[ "${SCENARIO_KIND}" == "dev" ]]; then
+    run_dev_install
+    assert_env_sh_has_no_override_exports
+    install_systemctl_log="$(capture_systemctl_log install)"
+    assert_systemctl_log "install" "${install_systemctl_log}" 1
+  else
+    fatal "Unsupported scenario kind: ${SCENARIO_KIND}"
+  fi
 
 log "Scenario ${SCENARIO} completed using PREFIX=${PREFIX}"
 log "Temp root: ${WORK_ROOT}"
diff --git a/tests/installers/install_state_smoke.sh b/tests/installers/install_state_smoke.sh
index 88cecedffc5a6cbba88a97c65c1dea1f9dcb12b5..e94e9017e822b20b2e6dfba5a894e29228c877bc
--- a/tests/installers/install_state_smoke.sh
+++ b/tests/installers/install_state_smoke.sh
@@ -669,6 +669,7 @@
       fatal "metadata install failed (see ${install_log})"
     }
 
+  assert_env_sh_has_no_override_exports "${prefix}" "${HOST_PATH}"
   assert_metadata_file "${state_path}" "${fake_user}" "true" "true" "no"
 
   # Simulate legacy metadata and rerun install to ensure upgrade path rewrites it.
@@ -698,6 +699,7 @@
       fatal "metadata upgrade install failed (see ${install_log})"
     }
 
+  assert_env_sh_has_no_override_exports "${prefix}" "${HOST_PATH}"
   assert_metadata_upgraded "${state_path}" "${fake_user}"
 
   log "Metadata scenario completed (state: ${state_path})"
@@ -786,6 +788,24 @@
   log "Cleanup scenario completed (state: ${state_path})"
 )
 
+assert_env_sh_has_no_override_exports() {
+  local prefix="$1"
+  local host_path="$2"
+  local env_sh="${prefix}/env.sh"
+  if [[ ! -f "${env_sh}" ]]; then
+    fatal "env.sh missing after install: ${env_sh}"
+  fi
+  if grep -Eq '^export[[:space:]]+SUBSTRATE_OVERRIDE_' "${env_sh}"; then
+    log "env.sh contains SUBSTRATE_OVERRIDE_* exports:"
+    sed -n '1,120p' "${env_sh}" >&2 || true
+    fatal "env.sh must not export SUBSTRATE_OVERRIDE_* by default (WCU4)"
+  fi
+  if env -i PATH="${host_path}" bash -lc "source \"${env_sh}\"; env | grep -q '^SUBSTRATE_OVERRIDE_'" >/dev/null 2>&1; then
+    fatal "Sourcing env.sh must not export SUBSTRATE_OVERRIDE_* (WCU4)"
+  fi
+  log "Verified env.sh does not export SUBSTRATE_OVERRIDE_* (${env_sh})"
+}
+
 run_missing_metadata_scenario() (
   local work_root
   work_root="$(mktemp -d "/tmp/substrate-install-state.missing.XXXXXX")"

exec
/bin/bash -lc 'cargo test -p substrate-shell --test installer_env_wcu4 -- --nocapture' in /home/spenser/__Active_code/substrate/wt/workspace_config_policy_unification-wcu4-test exited 101 in 21.99s:
   Compiling libc v0.2.178
   Compiling proc-macro2 v1.0.103
   Compiling unicode-ident v1.0.22
   Compiling quote v1.0.42
   Compiling cfg-if v1.0.4
   Compiling serde_core v1.0.228
   Compiling memchr v2.7.6
   Compiling smallvec v1.15.1
   Compiling log v0.4.29
   Compiling autocfg v1.5.0
   Compiling itoa v1.0.16
   Compiling parking_lot_core v0.9.12
   Compiling pin-project-lite v0.2.16
   Compiling serde v1.0.228
   Compiling scopeguard v1.2.0
   Compiling bytes v1.11.0
   Compiling once_cell v1.21.3
   Compiling regex-syntax v0.8.8
   Compiling getrandom v0.3.4
   Compiling equivalent v1.0.2
file update:
diff --git a/crates/shell/tests/installer_env_wcu4.rs b/crates/shell/tests/installer_env_wcu4.rs
new file mode 100644
index 0000000000000000000000000000000000000000..fed36ed1072e3738bc17f7a1232648f324e1b336
--- /dev/null
+++ b/crates/shell/tests/installer_env_wcu4.rs
@@ -0,0 +1,78 @@
+#![cfg(unix)]
+
+mod common;
+
+use common::{binary_path, ensure_substrate_built, shared_tmpdir, temp_dir};
+use serde_json::Value as JsonValue;
+use std::fs;
+use std::path::PathBuf;
+use std::process::Command;
+
+fn repo_root() -> PathBuf {
+    PathBuf::from(env!("CARGO_MANIFEST_DIR"))
+        .join("../..")
+        .canonicalize()
+        .expect("canonicalize repo root")
+}
+
+fn read_repo_file(relative_path: &str) -> String {
+    fs::read_to_string(repo_root().join(relative_path))
+        .unwrap_or_else(|err| panic!("read {relative_path}: {err}"))
+}
+
+#[test]
+fn installer_scripts_do_not_export_substrate_override_by_default() {
+    for script in [
+        "scripts/substrate/install-substrate.sh",
+        "scripts/substrate/dev-install-substrate.sh",
+    ] {
+        let contents = read_repo_file(script);
+        assert!(
+            !contents.contains("export SUBSTRATE_OVERRIDE_"),
+            "{script} must not export SUBSTRATE_OVERRIDE_* by default (WCU4)"
+        );
+    }
+}
+
+#[test]
+fn config_current_show_is_not_affected_without_override_inputs() {
+    ensure_substrate_built();
+    let temp = temp_dir("substrate-wcu4-clean-env-");
+    let home = temp.path().join("home");
+    let substrate_home = temp.path().join("substrate-home");
+    let cwd = temp.path().join("cwd");
+    fs::create_dir_all(&home).expect("create HOME");
+    fs::create_dir_all(&substrate_home).expect("create SUBSTRATE_HOME");
+    fs::create_dir_all(&cwd).expect("create cwd");
+
+    fs::write(
+        substrate_home.join("config.yaml"),
+        "policy:\n  mode: disabled\n",
+    )
+    .expect("write config.yaml");
+
+    let host_path = std::env::var("PATH").unwrap_or_else(|_| "/usr/bin:/bin".to_string());
+    let output = Command::new(binary_path())
+        .env_clear()
+        .env("PATH", host_path)
+        .env("TMPDIR", shared_tmpdir())
+        .env("HOME", &home)
+        .env("USERPROFILE", &home)
+        .env("SUBSTRATE_HOME", &substrate_home)
+        .current_dir(&cwd)
+        .arg("--no-world")
+        .args(["config", "current", "show", "--json"])
+        .output()
+        .expect("run substrate config current show --json");
+    assert!(
+        output.status.success(),
+        "config current show should succeed: {output:?}"
+    );
+
+    let json: JsonValue = serde_json::from_slice(&output.stdout).expect("current JSON parse");
+    assert_eq!(
+        json.pointer("/policy/mode").and_then(|v| v.as_str()),
+        Some("disabled"),
+        "policy.mode mismatch: {json}"
+    );
+}
diff --git a/tests/installers/install_smoke.sh b/tests/installers/install_smoke.sh
index db133b5c269c4ff975491691e4fb17fc2e2a6e46..76b8a4df24bcd3b603ea8df1c25de70ace488c18
--- a/tests/installers/install_smoke.sh
+++ b/tests/installers/install_smoke.sh
@@ -3,6 +3,7 @@
 
 SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
 REPO_ROOT="$(cd "${SCRIPT_DIR}/../.." && pwd)"
+HOST_PATH="${PATH}"
 
 log() {
   printf '[install-smoke] %s\n' "$*" >&2
@@ -694,6 +695,22 @@
   log "Verified manager manifest at ${manifest}"
 }
 
+assert_env_sh_has_no_override_exports() {
+  local env_sh="${PREFIX}/env.sh"
+  if [[ ! -f "${env_sh}" ]]; then
+    fatal "env.sh missing after install: ${env_sh}"
+  fi
+  if grep -Eq '^export[[:space:]]+SUBSTRATE_OVERRIDE_' "${env_sh}"; then
+    log "env.sh contains SUBSTRATE_OVERRIDE_* exports:"
+    sed -n '1,120p' "${env_sh}" >&2 || true
+    fatal "env.sh must not export SUBSTRATE_OVERRIDE_* by default (WCU4)"
+  fi
+  if env -i PATH="${HOST_PATH}" bash -lc "source \"${env_sh}\"; env | grep -q '^SUBSTRATE_OVERRIDE_'" >/dev/null 2>&1; then
+    fatal "Sourcing env.sh must not export SUBSTRATE_OVERRIDE_* (WCU4)"
+  fi
+  log "Verified env.sh does not export SUBSTRATE_OVERRIDE_* (${env_sh})"
+}
+
 run_health_smoke() {
   local substrate_bin="${PREFIX}/bin/substrate"
   local output
@@ -794,14 +811,15 @@
 prepare_stub_bin
 reset_systemctl_log
 
-if [[ "${SCENARIO_KIND}" == "prod" ]]; then
-  build_fake_release
-  run_prod_install
-  install_systemctl_log="$(capture_systemctl_log install)"
-  assert_config_init_hint_logged
-  assert_install_config
-  assert_manifest_present
-  run_health_smoke
+  if [[ "${SCENARIO_KIND}" == "prod" ]]; then
+    build_fake_release
+    run_prod_install
+    assert_env_sh_has_no_override_exports
+    install_systemctl_log="$(capture_systemctl_log install)"
+    assert_config_init_hint_logged
+    assert_install_config
+    assert_manifest_present
+    run_health_smoke
   if [[ "${SCENARIO_WORLD_ENABLED}" -eq 1 ]]; then
     assert_systemctl_log "install" "${install_systemctl_log}" 1
   else
@@ -810,13 +828,14 @@
   run_prod_uninstall
   uninstall_systemctl_log="$(capture_systemctl_log uninstall)"
   assert_systemctl_log "uninstall" "${uninstall_systemctl_log}" 1
-elif [[ "${SCENARIO_KIND}" == "dev" ]]; then
-  run_dev_install
-  install_systemctl_log="$(capture_systemctl_log install)"
-  assert_systemctl_log "install" "${install_systemctl_log}" 1
-else
-  fatal "Unsupported scenario kind: ${SCENARIO_KIND}"
-fi
+  elif [[ "${SCENARIO_KIND}" == "dev" ]]; then
+    run_dev_install
+    assert_env_sh_has_no_override_exports
+    install_systemctl_log="$(capture_systemctl_log install)"
+    assert_systemctl_log "install" "${install_systemctl_log}" 1
+  else
+    fatal "Unsupported scenario kind: ${SCENARIO_KIND}"
+  fi
 
 log "Scenario ${SCENARIO} completed using PREFIX=${PREFIX}"
 log "Temp root: ${WORK_ROOT}"
diff --git a/tests/installers/install_state_smoke.sh b/tests/installers/install_state_smoke.sh
index 88cecedffc5a6cbba88a97c65c1dea1f9dcb12b5..e94e9017e822b20b2e6dfba5a894e29228c877bc
--- a/tests/installers/install_state_smoke.sh
+++ b/tests/installers/install_state_smoke.sh
@@ -669,6 +669,7 @@
       fatal "metadata install failed (see ${install_log})"
     }
 
+  assert_env_sh_has_no_override_exports "${prefix}" "${HOST_PATH}"
   assert_metadata_file "${state_path}" "${fake_user}" "true" "true" "no"
 
   # Simulate legacy metadata and rerun install to ensure upgrade path rewrites it.
@@ -698,6 +699,7 @@
       fatal "metadata upgrade install failed (see ${install_log})"
     }
 
+  assert_env_sh_has_no_override_exports "${prefix}" "${HOST_PATH}"
   assert_metadata_upgraded "${state_path}" "${fake_user}"
 
   log "Metadata scenario completed (state: ${state_path})"
@@ -786,6 +788,24 @@
   log "Cleanup scenario completed (state: ${state_path})"
 )
 
+assert_env_sh_has_no_override_exports() {
+  local prefix="$1"
+  local host_path="$2"
+  local env_sh="${prefix}/env.sh"
+  if [[ ! -f "${env_sh}" ]]; then
+    fatal "env.sh missing after install: ${env_sh}"
+  fi
+  if grep -Eq '^export[[:space:]]+SUBSTRATE_OVERRIDE_' "${env_sh}"; then
+    log "env.sh contains SUBSTRATE_OVERRIDE_* exports:"
+    sed -n '1,120p' "${env_sh}" >&2 || true
+    fatal "env.sh must not export SUBSTRATE_OVERRIDE_* by default (WCU4)"
+  fi
+  if env -i PATH="${host_path}" bash -lc "source \"${env_sh}\"; env | grep -q '^SUBSTRATE_OVERRIDE_'" >/dev/null 2>&1; then
+    fatal "Sourcing env.sh must not export SUBSTRATE_OVERRIDE_* (WCU4)"
+  fi
+  log "Verified env.sh does not export SUBSTRATE_OVERRIDE_* (${env_sh})"
+}
+
 run_missing_metadata_scenario() (
   local work_root
   work_root="$(mktemp -d "/tmp/substrate-install-state.missing.XXXXXX")"


thinking
**Executing tests on Linux**

The test fails as I expected, which is great! Now, I need to run the shell scripts that I've updated. The scenario metadata for `tests/installers/install_state_smoke.sh` is specific to Linux, and since we're probably running on Linux, I’ll go ahead and execute it. I anticipate it will fail because of environment exports, but that’s okay; I still want to proceed and see what happens. I'll also run the install_smoke, although I might skip it due to systemd considerations, but I think I'll run it anyway.
exec
/bin/bash -lc 'bash tests/installers/install_state_smoke.sh --scenario metadata' in /home/spenser/__Active_code/substrate/wt/workspace_config_policy_unification-wcu4-test exited 1 in 909ms:
[install-state] env.sh contains SUBSTRATE_OVERRIDE_* exports:
#!/usr/bin/env bash
export SUBSTRATE_HOME=/tmp/substrate-install-state.metadata.R3It6t/home/.substrate
export SUBSTRATE_OVERRIDE_WORLD=enabled
export SUBSTRATE_OVERRIDE_CAGED=1
export SUBSTRATE_OVERRIDE_ANCHOR_MODE=workspace
export SUBSTRATE_OVERRIDE_ANCHOR_PATH=''
export SUBSTRATE_OVERRIDE_POLICY_MODE=observe
[install-state] ERROR: env.sh must not export SUBSTRATE_OVERRIDE_* by default (WCU4)
file update:
diff --git a/crates/shell/tests/installer_env_wcu4.rs b/crates/shell/tests/installer_env_wcu4.rs
new file mode 100644
index 0000000000000000000000000000000000000000..fed36ed1072e3738bc17f7a1232648f324e1b336
--- /dev/null
+++ b/crates/shell/tests/installer_env_wcu4.rs
@@ -0,0 +1,78 @@
+#![cfg(unix)]
+
+mod common;
+
+use common::{binary_path, ensure_substrate_built, shared_tmpdir, temp_dir};
+use serde_json::Value as JsonValue;
+use std::fs;
+use std::path::PathBuf;
+use std::process::Command;
+
+fn repo_root() -> PathBuf {
+    PathBuf::from(env!("CARGO_MANIFEST_DIR"))
+        .join("../..")
+        .canonicalize()
+        .expect("canonicalize repo root")
+}
+
+fn read_repo_file(relative_path: &str) -> String {
+    fs::read_to_string(repo_root().join(relative_path))
+        .unwrap_or_else(|err| panic!("read {relative_path}: {err}"))
+}
+
+#[test]
+fn installer_scripts_do_not_export_substrate_override_by_default() {
+    for script in [
+        "scripts/substrate/install-substrate.sh",
+        "scripts/substrate/dev-install-substrate.sh",
+    ] {
+        let contents = read_repo_file(script);
+        assert!(
+            !contents.contains("export SUBSTRATE_OVERRIDE_"),
+            "{script} must not export SUBSTRATE_OVERRIDE_* by default (WCU4)"
+        );
+    }
+}
+
+#[test]
+fn config_current_show_is_not_affected_without_override_inputs() {
+    ensure_substrate_built();
+    let temp = temp_dir("substrate-wcu4-clean-env-");
+    let home = temp.path().join("home");
+    let substrate_home = temp.path().join("substrate-home");
+    let cwd = temp.path().join("cwd");
+    fs::create_dir_all(&home).expect("create HOME");
+    fs::create_dir_all(&substrate_home).expect("create SUBSTRATE_HOME");
+    fs::create_dir_all(&cwd).expect("create cwd");
+
+    fs::write(
+        substrate_home.join("config.yaml"),
+        "policy:\n  mode: disabled\n",
+    )
+    .expect("write config.yaml");
+
+    let host_path = std::env::var("PATH").unwrap_or_else(|_| "/usr/bin:/bin".to_string());
+    let output = Command::new(binary_path())
+        .env_clear()
+        .env("PATH", host_path)
+        .env("TMPDIR", shared_tmpdir())
+        .env("HOME", &home)
+        .env("USERPROFILE", &home)
+        .env("SUBSTRATE_HOME", &substrate_home)
+        .current_dir(&cwd)
+        .arg("--no-world")
+        .args(["config", "current", "show", "--json"])
+        .output()
+        .expect("run substrate config current show --json");
+    assert!(
+        output.status.success(),
+        "config current show should succeed: {output:?}"
+    );
+
+    let json: JsonValue = serde_json::from_slice(&output.stdout).expect("current JSON parse");
+    assert_eq!(
+        json.pointer("/policy/mode").and_then(|v| v.as_str()),
+        Some("disabled"),
+        "policy.mode mismatch: {json}"
+    );
+}
diff --git a/tests/installers/install_smoke.sh b/tests/installers/install_smoke.sh
index db133b5c269c4ff975491691e4fb17fc2e2a6e46..76b8a4df24bcd3b603ea8df1c25de70ace488c18
--- a/tests/installers/install_smoke.sh
+++ b/tests/installers/install_smoke.sh
@@ -3,6 +3,7 @@
 
 SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
 REPO_ROOT="$(cd "${SCRIPT_DIR}/../.." && pwd)"
+HOST_PATH="${PATH}"
 
 log() {
   printf '[install-smoke] %s\n' "$*" >&2
@@ -694,6 +695,22 @@
   log "Verified manager manifest at ${manifest}"
 }
 
+assert_env_sh_has_no_override_exports() {
+  local env_sh="${PREFIX}/env.sh"
+  if [[ ! -f "${env_sh}" ]]; then
+    fatal "env.sh missing after install: ${env_sh}"
+  fi
+  if grep -Eq '^export[[:space:]]+SUBSTRATE_OVERRIDE_' "${env_sh}"; then
+    log "env.sh contains SUBSTRATE_OVERRIDE_* exports:"
+    sed -n '1,120p' "${env_sh}" >&2 || true
+    fatal "env.sh must not export SUBSTRATE_OVERRIDE_* by default (WCU4)"
+  fi
+  if env -i PATH="${HOST_PATH}" bash -lc "source \"${env_sh}\"; env | grep -q '^SUBSTRATE_OVERRIDE_'" >/dev/null 2>&1; then
+    fatal "Sourcing env.sh must not export SUBSTRATE_OVERRIDE_* (WCU4)"
+  fi
+  log "Verified env.sh does not export SUBSTRATE_OVERRIDE_* (${env_sh})"
+}
+
 run_health_smoke() {
   local substrate_bin="${PREFIX}/bin/substrate"
   local output
@@ -794,14 +811,15 @@
 prepare_stub_bin
 reset_systemctl_log
 
-if [[ "${SCENARIO_KIND}" == "prod" ]]; then
-  build_fake_release
-  run_prod_install
-  install_systemctl_log="$(capture_systemctl_log install)"
-  assert_config_init_hint_logged
-  assert_install_config
-  assert_manifest_present
-  run_health_smoke
+  if [[ "${SCENARIO_KIND}" == "prod" ]]; then
+    build_fake_release
+    run_prod_install
+    assert_env_sh_has_no_override_exports
+    install_systemctl_log="$(capture_systemctl_log install)"
+    assert_config_init_hint_logged
+    assert_install_config
+    assert_manifest_present
+    run_health_smoke
   if [[ "${SCENARIO_WORLD_ENABLED}" -eq 1 ]]; then
     assert_systemctl_log "install" "${install_systemctl_log}" 1
   else
@@ -810,13 +828,14 @@
   run_prod_uninstall
   uninstall_systemctl_log="$(capture_systemctl_log uninstall)"
   assert_systemctl_log "uninstall" "${uninstall_systemctl_log}" 1
-elif [[ "${SCENARIO_KIND}" == "dev" ]]; then
-  run_dev_install
-  install_systemctl_log="$(capture_systemctl_log install)"
-  assert_systemctl_log "install" "${install_systemctl_log}" 1
-else
-  fatal "Unsupported scenario kind: ${SCENARIO_KIND}"
-fi
+  elif [[ "${SCENARIO_KIND}" == "dev" ]]; then
+    run_dev_install
+    assert_env_sh_has_no_override_exports
+    install_systemctl_log="$(capture_systemctl_log install)"
+    assert_systemctl_log "install" "${install_systemctl_log}" 1
+  else
+    fatal "Unsupported scenario kind: ${SCENARIO_KIND}"
+  fi
 
 log "Scenario ${SCENARIO} completed using PREFIX=${PREFIX}"
 log "Temp root: ${WORK_ROOT}"
diff --git a/tests/installers/install_state_smoke.sh b/tests/installers/install_state_smoke.sh
index 88cecedffc5a6cbba88a97c65c1dea1f9dcb12b5..e94e9017e822b20b2e6dfba5a894e29228c877bc
--- a/tests/installers/install_state_smoke.sh
+++ b/tests/installers/install_state_smoke.sh
@@ -669,6 +669,7 @@
       fatal "metadata install failed (see ${install_log})"
     }
 
+  assert_env_sh_has_no_override_exports "${prefix}" "${HOST_PATH}"
   assert_metadata_file "${state_path}" "${fake_user}" "true" "true" "no"
 
   # Simulate legacy metadata and rerun install to ensure upgrade path rewrites it.
@@ -698,6 +699,7 @@
       fatal "metadata upgrade install failed (see ${install_log})"
     }
 
+  assert_env_sh_has_no_override_exports "${prefix}" "${HOST_PATH}"
   assert_metadata_upgraded "${state_path}" "${fake_user}"
 
   log "Metadata scenario completed (state: ${state_path})"
@@ -786,6 +788,24 @@
   log "Cleanup scenario completed (state: ${state_path})"
 )
 
+assert_env_sh_has_no_override_exports() {
+  local prefix="$1"
+  local host_path="$2"
+  local env_sh="${prefix}/env.sh"
+  if [[ ! -f "${env_sh}" ]]; then
+    fatal "env.sh missing after install: ${env_sh}"
+  fi
+  if grep -Eq '^export[[:space:]]+SUBSTRATE_OVERRIDE_' "${env_sh}"; then
+    log "env.sh contains SUBSTRATE_OVERRIDE_* exports:"
+    sed -n '1,120p' "${env_sh}" >&2 || true
+    fatal "env.sh must not export SUBSTRATE_OVERRIDE_* by default (WCU4)"
+  fi
+  if env -i PATH="${host_path}" bash -lc "source \"${env_sh}\"; env | grep -q '^SUBSTRATE_OVERRIDE_'" >/dev/null 2>&1; then
+    fatal "Sourcing env.sh must not export SUBSTRATE_OVERRIDE_* (WCU4)"
+  fi
+  log "Verified env.sh does not export SUBSTRATE_OVERRIDE_* (${env_sh})"
+}
+
 run_missing_metadata_scenario() (
   local work_root
   work_root="$(mktemp -d "/tmp/substrate-install-state.missing.XXXXXX")"


thinking
**Considering installation process**

Alright, I'm looking at the installation process and thinking about running the install_smoke command. I might skip some steps, but it seems like we should run the scenario prod-no-world anyway — even if it might skip for systemd. But I'm leaning towards proceeding; doing it could give us insights we need. So, I'll go ahead with running it and see how things turn out!
exec
/bin/bash -lc 'bash tests/installers/install_smoke.sh --scenario prod-no-world' in /home/spenser/__Active_code/substrate/wt/workspace_config_policy_unification-wcu4-test exited 1 in 528ms:
[substrate-install] Using requested version 0.0.0-test.
[substrate-install] Checksum verified for substrate-v0.0.0-test-linux_x86_64.tar.gz
[substrate-install] Installing to /tmp/substrate-installer-prod-no-world.wfIoWg/prefix/versions/0.0.0-test
[substrate-install] Deploying shims...
[substrate-install] Skipping world provisioning (--no-world).
[substrate-install] Doctor PATH: /tmp/substrate-installer-prod-no-world.wfIoWg/prefix/bin:/tmp/substrate-installer-prod-no-world.wfIoWg/stub-bin:/home/spenser/.bun/bin:/tmp/.tmpwTcJhi:/home/spenser/.config/nvm/versions/node/v24.11.0/lib/node_modules/@openai/codex/vendor/x86_64-unknown-linux-musl/path:/home/spenser/.bun/bin:/tmp/.tmpsV9OdP:/home/spenser/.config/nvm/versions/node/v24.11.0/lib/node_modules/@openai/codex/vendor/x86_64-unknown-linux-musl/path:/home/spenser/.substrate/bin:/home/spenser/.config/nvm/versions/node/v24.11.0/bin:/home/spenser/.pyenv/shims:/home/spenser/.pyenv/bin:/home/spenser/.var/app/com.visualstudio.code/data/../bin:/home/spenser/.cargo/bin:/home/spenser/.local/bin:/usr/local/bin:/usr/bin:/bin:/usr/local/sbin:/var/lib/flatpak/exports/bin:/usr/lib/jvm/default/bin:/usr/bin/site_perl:/usr/bin/vendor_perl:/usr/bin/core_perl:/var/lib/snapd/snap/bin:/var/lib/snapd/snap/bin
[substrate-install] Skipping world doctor (--no-world).
[substrate-install] Installation complete. /tmp/substrate-installer-prod-no-world.wfIoWg/prefix/bin was added to your shell PATH.
[substrate-install] manager_init placeholder: /tmp/substrate-installer-prod-no-world.wfIoWg/prefix/manager_init.sh
[substrate-install] manager_env script: /tmp/substrate-installer-prod-no-world.wfIoWg/prefix/manager_env.sh
[substrate-install] config manifests: /tmp/substrate-installer-prod-no-world.wfIoWg/prefix/versions/0.0.0-test/config
[substrate-install] install metadata: /tmp/substrate-installer-prod-no-world.wfIoWg/prefix/config.yaml
[substrate-install] If the global config is missing or needs regeneration, run 'substrate config init' after installing.
[substrate-install] World backend disabled (--no-world). Run '/tmp/substrate-installer-prod-no-world.wfIoWg/prefix/bin/substrate world enable --home "/tmp/substrate-installer-prod-no-world.wfIoWg/prefix"' when you are ready to provision.
[install-smoke] env.sh contains SUBSTRATE_OVERRIDE_* exports:
#!/usr/bin/env bash
export SUBSTRATE_HOME=/tmp/substrate-installer-prod-no-world.wfIoWg/prefix
export SUBSTRATE_OVERRIDE_WORLD=disabled
export SUBSTRATE_OVERRIDE_CAGED=1
export SUBSTRATE_OVERRIDE_ANCHOR_MODE=workspace
file update:
diff --git a/crates/shell/tests/installer_env_wcu4.rs b/crates/shell/tests/installer_env_wcu4.rs
new file mode 100644
index 0000000000000000000000000000000000000000..fed36ed1072e3738bc17f7a1232648f324e1b336
--- /dev/null
+++ b/crates/shell/tests/installer_env_wcu4.rs
@@ -0,0 +1,78 @@
+#![cfg(unix)]
+
+mod common;
+
+use common::{binary_path, ensure_substrate_built, shared_tmpdir, temp_dir};
+use serde_json::Value as JsonValue;
+use std::fs;
+use std::path::PathBuf;
+use std::process::Command;
+
+fn repo_root() -> PathBuf {
+    PathBuf::from(env!("CARGO_MANIFEST_DIR"))
+        .join("../..")
+        .canonicalize()
+        .expect("canonicalize repo root")
+}
+
+fn read_repo_file(relative_path: &str) -> String {
+    fs::read_to_string(repo_root().join(relative_path))
+        .unwrap_or_else(|err| panic!("read {relative_path}: {err}"))
+}
+
+#[test]
+fn installer_scripts_do_not_export_substrate_override_by_default() {
+    for script in [
+        "scripts/substrate/install-substrate.sh",
+        "scripts/substrate/dev-install-substrate.sh",
+    ] {
+        let contents = read_repo_file(script);
+        assert!(
+            !contents.contains("export SUBSTRATE_OVERRIDE_"),
+            "{script} must not export SUBSTRATE_OVERRIDE_* by default (WCU4)"
+        );
+    }
+}
+
+#[test]
+fn config_current_show_is_not_affected_without_override_inputs() {
+    ensure_substrate_built();
+    let temp = temp_dir("substrate-wcu4-clean-env-");
+    let home = temp.path().join("home");
+    let substrate_home = temp.path().join("substrate-home");
+    let cwd = temp.path().join("cwd");
+    fs::create_dir_all(&home).expect("create HOME");
+    fs::create_dir_all(&substrate_home).expect("create SUBSTRATE_HOME");
+    fs::create_dir_all(&cwd).expect("create cwd");
+
+    fs::write(
+        substrate_home.join("config.yaml"),
+        "policy:\n  mode: disabled\n",
+    )
+    .expect("write config.yaml");
+
+    let host_path = std::env::var("PATH").unwrap_or_else(|_| "/usr/bin:/bin".to_string());
+    let output = Command::new(binary_path())
+        .env_clear()
+        .env("PATH", host_path)
+        .env("TMPDIR", shared_tmpdir())
+        .env("HOME", &home)
+        .env("USERPROFILE", &home)
+        .env("SUBSTRATE_HOME", &substrate_home)
+        .current_dir(&cwd)
+        .arg("--no-world")
+        .args(["config", "current", "show", "--json"])
+        .output()
+        .expect("run substrate config current show --json");
+    assert!(
+        output.status.success(),
+        "config current show should succeed: {output:?}"
+    );
+
+    let json: JsonValue = serde_json::from_slice(&output.stdout).expect("current JSON parse");
+    assert_eq!(
+        json.pointer("/policy/mode").and_then(|v| v.as_str()),
+        Some("disabled"),
+        "policy.mode mismatch: {json}"
+    );
+}
diff --git a/tests/installers/install_smoke.sh b/tests/installers/install_smoke.sh
index db133b5c269c4ff975491691e4fb17fc2e2a6e46..76b8a4df24bcd3b603ea8df1c25de70ace488c18
--- a/tests/installers/install_smoke.sh
+++ b/tests/installers/install_smoke.sh
@@ -3,6 +3,7 @@
 
 SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
 REPO_ROOT="$(cd "${SCRIPT_DIR}/../.." && pwd)"
+HOST_PATH="${PATH}"
 
 log() {
   printf '[install-smoke] %s\n' "$*" >&2
@@ -694,6 +695,22 @@
   log "Verified manager manifest at ${manifest}"
 }
 
+assert_env_sh_has_no_override_exports() {
+  local env_sh="${PREFIX}/env.sh"
+  if [[ ! -f "${env_sh}" ]]; then
+    fatal "env.sh missing after install: ${env_sh}"
+  fi
+  if grep -Eq '^export[[:space:]]+SUBSTRATE_OVERRIDE_' "${env_sh}"; then
+    log "env.sh contains SUBSTRATE_OVERRIDE_* exports:"
+    sed -n '1,120p' "${env_sh}" >&2 || true
+    fatal "env.sh must not export SUBSTRATE_OVERRIDE_* by default (WCU4)"
+  fi
+  if env -i PATH="${HOST_PATH}" bash -lc "source \"${env_sh}\"; env | grep -q '^SUBSTRATE_OVERRIDE_'" >/dev/null 2>&1; then
+    fatal "Sourcing env.sh must not export SUBSTRATE_OVERRIDE_* (WCU4)"
+  fi
+  log "Verified env.sh does not export SUBSTRATE_OVERRIDE_* (${env_sh})"
+}
+
 run_health_smoke() {
   local substrate_bin="${PREFIX}/bin/substrate"
   local output
@@ -794,14 +811,15 @@
 prepare_stub_bin
 reset_systemctl_log
 
-if [[ "${SCENARIO_KIND}" == "prod" ]]; then
-  build_fake_release
-  run_prod_install
-  install_systemctl_log="$(capture_systemctl_log install)"
-  assert_config_init_hint_logged
-  assert_install_config
-  assert_manifest_present
-  run_health_smoke
+  if [[ "${SCENARIO_KIND}" == "prod" ]]; then
+    build_fake_release
+    run_prod_install
+    assert_env_sh_has_no_override_exports
+    install_systemctl_log="$(capture_systemctl_log install)"
+    assert_config_init_hint_logged
+    assert_install_config
+    assert_manifest_present
+    run_health_smoke
   if [[ "${SCENARIO_WORLD_ENABLED}" -eq 1 ]]; then
     assert_systemctl_log "install" "${install_systemctl_log}" 1
   else
@@ -810,13 +828,14 @@
   run_prod_uninstall
   uninstall_systemctl_log="$(capture_systemctl_log uninstall)"
   assert_systemctl_log "uninstall" "${uninstall_systemctl_log}" 1
-elif [[ "${SCENARIO_KIND}" == "dev" ]]; then
-  run_dev_install
-  install_systemctl_log="$(capture_systemctl_log install)"
-  assert_systemctl_log "install" "${install_systemctl_log}" 1
-else
-  fatal "Unsupported scenario kind: ${SCENARIO_KIND}"
-fi
+  elif [[ "${SCENARIO_KIND}" == "dev" ]]; then
+    run_dev_install
+    assert_env_sh_has_no_override_exports
+    install_systemctl_log="$(capture_systemctl_log install)"
+    assert_systemctl_log "install" "${install_systemctl_log}" 1
+  else
+    fatal "Unsupported scenario kind: ${SCENARIO_KIND}"
+  fi
 
 log "Scenario ${SCENARIO} completed using PREFIX=${PREFIX}"
 log "Temp root: ${WORK_ROOT}"
diff --git a/tests/installers/install_state_smoke.sh b/tests/installers/install_state_smoke.sh
index 88cecedffc5a6cbba88a97c65c1dea1f9dcb12b5..e94e9017e822b20b2e6dfba5a894e29228c877bc
--- a/tests/installers/install_state_smoke.sh
+++ b/tests/installers/install_state_smoke.sh
@@ -669,6 +669,7 @@
       fatal "metadata install failed (see ${install_log})"
     }
 
+  assert_env_sh_has_no_override_exports "${prefix}" "${HOST_PATH}"
   assert_metadata_file "${state_path}" "${fake_user}" "true" "true" "no"
 
   # Simulate legacy metadata and rerun install to ensure upgrade path rewrites it.
@@ -698,6 +699,7 @@
       fatal "metadata upgrade install failed (see ${install_log})"
     }
 
+  assert_env_sh_has_no_override_exports "${prefix}" "${HOST_PATH}"
   assert_metadata_upgraded "${state_path}" "${fake_user}"
 
   log "Metadata scenario completed (state: ${state_path})"
@@ -786,6 +788,24 @@
   log "Cleanup scenario completed (state: ${state_path})"
 )
 
+assert_env_sh_has_no_override_exports() {
+  local prefix="$1"
+  local host_path="$2"
+  local env_sh="${prefix}/env.sh"
+  if [[ ! -f "${env_sh}" ]]; then
+    fatal "env.sh missing after install: ${env_sh}"
+  fi
+  if grep -Eq '^export[[:space:]]+SUBSTRATE_OVERRIDE_' "${env_sh}"; then
+    log "env.sh contains SUBSTRATE_OVERRIDE_* exports:"
+    sed -n '1,120p' "${env_sh}" >&2 || true
+    fatal "env.sh must not export SUBSTRATE_OVERRIDE_* by default (WCU4)"
+  fi
+  if env -i PATH="${host_path}" bash -lc "source \"${env_sh}\"; env | grep -q '^SUBSTRATE_OVERRIDE_'" >/dev/null 2>&1; then
+    fatal "Sourcing env.sh must not export SUBSTRATE_OVERRIDE_* (WCU4)"
+  fi
+  log "Verified env.sh does not export SUBSTRATE_OVERRIDE_* (${env_sh})"
+}
+
 run_missing_metadata_scenario() (
   local work_root
   work_root="$(mktemp -d "/tmp/substrate-install-state.missing.XXXXXX")"


thinking
**Checking commit messages**

I think I’ll start by using `test:` since there’s no mention against it. I’ll check the git log for existing messages, maybe using `git log -1 --pretty=%s`. However, in this workflow, they might want the message to include a task ID, but that's not specified. I could look into previous tasks, but that’s not in the session log. Maybe I could check `git log --oneline -5` to inspect the latest commit messages.
exec
/bin/bash -lc "git status --porcelain

git log -5 --pretty=format:'%h %s'" in /home/spenser/__Active_code/substrate/wt/workspace_config_policy_unification-wcu4-test succeeded in 47ms:
 M tests/installers/install_smoke.sh
 M tests/installers/install_state_smoke.sh
?? crates/shell/tests/installer_env_wcu4.rs
cbfa6ad docs: start WCU4 code+test
99d1f6a docs: close out WCU3 integ
7ba0d12 merge: WCU3 integ-core into integ
3b7f83f chore(triad): complete WCU3 integ-core
a2b5c48 chore(triad): mark WCU3 platform fixes noop
file update:
diff --git a/crates/shell/tests/installer_env_wcu4.rs b/crates/shell/tests/installer_env_wcu4.rs
new file mode 100644
index 0000000000000000000000000000000000000000..fed36ed1072e3738bc17f7a1232648f324e1b336
--- /dev/null
+++ b/crates/shell/tests/installer_env_wcu4.rs
@@ -0,0 +1,78 @@
+#![cfg(unix)]
+
+mod common;
+
+use common::{binary_path, ensure_substrate_built, shared_tmpdir, temp_dir};
+use serde_json::Value as JsonValue;
+use std::fs;
+use std::path::PathBuf;
+use std::process::Command;
+
+fn repo_root() -> PathBuf {
+    PathBuf::from(env!("CARGO_MANIFEST_DIR"))
+        .join("../..")
+        .canonicalize()
+        .expect("canonicalize repo root")
+}
+
+fn read_repo_file(relative_path: &str) -> String {
+    fs::read_to_string(repo_root().join(relative_path))
+        .unwrap_or_else(|err| panic!("read {relative_path}: {err}"))
+}
+
+#[test]
+fn installer_scripts_do_not_export_substrate_override_by_default() {
+    for script in [
+        "scripts/substrate/install-substrate.sh",
+        "scripts/substrate/dev-install-substrate.sh",
+    ] {
+        let contents = read_repo_file(script);
+        assert!(
+            !contents.contains("export SUBSTRATE_OVERRIDE_"),
+            "{script} must not export SUBSTRATE_OVERRIDE_* by default (WCU4)"
+        );
+    }
+}
+
+#[test]
+fn config_current_show_is_not_affected_without_override_inputs() {
+    ensure_substrate_built();
+    let temp = temp_dir("substrate-wcu4-clean-env-");
+    let home = temp.path().join("home");
+    let substrate_home = temp.path().join("substrate-home");
+    let cwd = temp.path().join("cwd");
+    fs::create_dir_all(&home).expect("create HOME");
+    fs::create_dir_all(&substrate_home).expect("create SUBSTRATE_HOME");
+    fs::create_dir_all(&cwd).expect("create cwd");
+
+    fs::write(
+        substrate_home.join("config.yaml"),
+        "policy:\n  mode: disabled\n",
+    )
+    .expect("write config.yaml");
+
+    let host_path = std::env::var("PATH").unwrap_or_else(|_| "/usr/bin:/bin".to_string());
+    let output = Command::new(binary_path())
+        .env_clear()
+        .env("PATH", host_path)
+        .env("TMPDIR", shared_tmpdir())
+        .env("HOME", &home)
+        .env("USERPROFILE", &home)
+        .env("SUBSTRATE_HOME", &substrate_home)
+        .current_dir(&cwd)
+        .arg("--no-world")
+        .args(["config", "current", "show", "--json"])
+        .output()
+        .expect("run substrate config current show --json");
+    assert!(
+        output.status.success(),
+        "config current show should succeed: {output:?}"
+    );
+
+    let json: JsonValue = serde_json::from_slice(&output.stdout).expect("current JSON parse");
+    assert_eq!(
+        json.pointer("/policy/mode").and_then(|v| v.as_str()),
+        Some("disabled"),
+        "policy.mode mismatch: {json}"
+    );
+}
diff --git a/tests/installers/install_smoke.sh b/tests/installers/install_smoke.sh
index db133b5c269c4ff975491691e4fb17fc2e2a6e46..76b8a4df24bcd3b603ea8df1c25de70ace488c18
--- a/tests/installers/install_smoke.sh
+++ b/tests/installers/install_smoke.sh
@@ -3,6 +3,7 @@
 
 SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
 REPO_ROOT="$(cd "${SCRIPT_DIR}/../.." && pwd)"
+HOST_PATH="${PATH}"
 
 log() {
   printf '[install-smoke] %s\n' "$*" >&2
@@ -694,6 +695,22 @@
   log "Verified manager manifest at ${manifest}"
 }
 
+assert_env_sh_has_no_override_exports() {
+  local env_sh="${PREFIX}/env.sh"
+  if [[ ! -f "${env_sh}" ]]; then
+    fatal "env.sh missing after install: ${env_sh}"
+  fi
+  if grep -Eq '^export[[:space:]]+SUBSTRATE_OVERRIDE_' "${env_sh}"; then
+    log "env.sh contains SUBSTRATE_OVERRIDE_* exports:"
+    sed -n '1,120p' "${env_sh}" >&2 || true
+    fatal "env.sh must not export SUBSTRATE_OVERRIDE_* by default (WCU4)"
+  fi
+  if env -i PATH="${HOST_PATH}" bash -lc "source \"${env_sh}\"; env | grep -q '^SUBSTRATE_OVERRIDE_'" >/dev/null 2>&1; then
+    fatal "Sourcing env.sh must not export SUBSTRATE_OVERRIDE_* (WCU4)"
+  fi
+  log "Verified env.sh does not export SUBSTRATE_OVERRIDE_* (${env_sh})"
+}
+
 run_health_smoke() {
   local substrate_bin="${PREFIX}/bin/substrate"
   local output
@@ -794,14 +811,15 @@
 prepare_stub_bin
 reset_systemctl_log
 
-if [[ "${SCENARIO_KIND}" == "prod" ]]; then
-  build_fake_release
-  run_prod_install
-  install_systemctl_log="$(capture_systemctl_log install)"
-  assert_config_init_hint_logged
-  assert_install_config
-  assert_manifest_present
-  run_health_smoke
+  if [[ "${SCENARIO_KIND}" == "prod" ]]; then
+    build_fake_release
+    run_prod_install
+    assert_env_sh_has_no_override_exports
+    install_systemctl_log="$(capture_systemctl_log install)"
+    assert_config_init_hint_logged
+    assert_install_config
+    assert_manifest_present
+    run_health_smoke
   if [[ "${SCENARIO_WORLD_ENABLED}" -eq 1 ]]; then
     assert_systemctl_log "install" "${install_systemctl_log}" 1
   else
@@ -810,13 +828,14 @@
   run_prod_uninstall
   uninstall_systemctl_log="$(capture_systemctl_log uninstall)"
   assert_systemctl_log "uninstall" "${uninstall_systemctl_log}" 1
-elif [[ "${SCENARIO_KIND}" == "dev" ]]; then
-  run_dev_install
-  install_systemctl_log="$(capture_systemctl_log install)"
-  assert_systemctl_log "install" "${install_systemctl_log}" 1
-else
-  fatal "Unsupported scenario kind: ${SCENARIO_KIND}"
-fi
+  elif [[ "${SCENARIO_KIND}" == "dev" ]]; then
+    run_dev_install
+    assert_env_sh_has_no_override_exports
+    install_systemctl_log="$(capture_systemctl_log install)"
+    assert_systemctl_log "install" "${install_systemctl_log}" 1
+  else
+    fatal "Unsupported scenario kind: ${SCENARIO_KIND}"
+  fi
 
 log "Scenario ${SCENARIO} completed using PREFIX=${PREFIX}"
 log "Temp root: ${WORK_ROOT}"
diff --git a/tests/installers/install_state_smoke.sh b/tests/installers/install_state_smoke.sh
index 88cecedffc5a6cbba88a97c65c1dea1f9dcb12b5..e94e9017e822b20b2e6dfba5a894e29228c877bc
--- a/tests/installers/install_state_smoke.sh
+++ b/tests/installers/install_state_smoke.sh
@@ -669,6 +669,7 @@
       fatal "metadata install failed (see ${install_log})"
     }
 
+  assert_env_sh_has_no_override_exports "${prefix}" "${HOST_PATH}"
   assert_metadata_file "${state_path}" "${fake_user}" "true" "true" "no"
 
   # Simulate legacy metadata and rerun install to ensure upgrade path rewrites it.
@@ -698,6 +699,7 @@
       fatal "metadata upgrade install failed (see ${install_log})"
     }
 
+  assert_env_sh_has_no_override_exports "${prefix}" "${HOST_PATH}"
   assert_metadata_upgraded "${state_path}" "${fake_user}"
 
   log "Metadata scenario completed (state: ${state_path})"
@@ -786,6 +788,24 @@
   log "Cleanup scenario completed (state: ${state_path})"
 )
 
+assert_env_sh_has_no_override_exports() {
+  local prefix="$1"
+  local host_path="$2"
+  local env_sh="${prefix}/env.sh"
+  if [[ ! -f "${env_sh}" ]]; then
+    fatal "env.sh missing after install: ${env_sh}"
+  fi
+  if grep -Eq '^export[[:space:]]+SUBSTRATE_OVERRIDE_' "${env_sh}"; then
+    log "env.sh contains SUBSTRATE_OVERRIDE_* exports:"
+    sed -n '1,120p' "${env_sh}" >&2 || true
+    fatal "env.sh must not export SUBSTRATE_OVERRIDE_* by default (WCU4)"
+  fi
+  if env -i PATH="${host_path}" bash -lc "source \"${env_sh}\"; env | grep -q '^SUBSTRATE_OVERRIDE_'" >/dev/null 2>&1; then
+    fatal "Sourcing env.sh must not export SUBSTRATE_OVERRIDE_* (WCU4)"
+  fi
+  log "Verified env.sh does not export SUBSTRATE_OVERRIDE_* (${env_sh})"
+}
+
 run_missing_metadata_scenario() (
   local work_root
   work_root="$(mktemp -d "/tmp/substrate-install-state.missing.XXXXXX")"


thinking
**Deciding on commit messages**

I'm thinking about how to format my commit messages, particularly with prefixes and scopes, like using `chore(triad)`. For tests, I'm considering `test(wcu4): ...`, but I'm not sure if "test" is allowed since it's not specified. However, conventional commits usually include it, so I'm leaning toward `test(wcu4): add installer override export checks`. I also realize I shouldn't edit planning documents, even if the commit history shows earlier changes. Time to commit!
exec
/bin/bash -lc 'git add crates/shell/tests/installer_env_wcu4.rs tests/installers/install_smoke.sh tests/installers/install_state_smoke.sh
git commit -m "test(wcu4): enforce no override exports"' in /home/spenser/__Active_code/substrate/wt/workspace_config_policy_unification-wcu4-test succeeded in 78ms:
[workspace_config_policy_unification-wcu4-test 4657921] test(wcu4): enforce no override exports
 3 files changed, 132 insertions(+), 15 deletions(-)
 create mode 100644 crates/shell/tests/installer_env_wcu4.rs
file update:
diff --git a/crates/shell/tests/installer_env_wcu4.rs b/crates/shell/tests/installer_env_wcu4.rs
new file mode 100644
index 0000000000000000000000000000000000000000..fed36ed1072e3738bc17f7a1232648f324e1b336
--- /dev/null
+++ b/crates/shell/tests/installer_env_wcu4.rs
@@ -0,0 +1,78 @@
+#![cfg(unix)]
+
+mod common;
+
+use common::{binary_path, ensure_substrate_built, shared_tmpdir, temp_dir};
+use serde_json::Value as JsonValue;
+use std::fs;
+use std::path::PathBuf;
+use std::process::Command;
+
+fn repo_root() -> PathBuf {
+    PathBuf::from(env!("CARGO_MANIFEST_DIR"))
+        .join("../..")
+        .canonicalize()
+        .expect("canonicalize repo root")
+}
+
+fn read_repo_file(relative_path: &str) -> String {
+    fs::read_to_string(repo_root().join(relative_path))
+        .unwrap_or_else(|err| panic!("read {relative_path}: {err}"))
+}
+
+#[test]
+fn installer_scripts_do_not_export_substrate_override_by_default() {
+    for script in [
+        "scripts/substrate/install-substrate.sh",
+        "scripts/substrate/dev-install-substrate.sh",
+    ] {
+        let contents = read_repo_file(script);
+        assert!(
+            !contents.contains("export SUBSTRATE_OVERRIDE_"),
+            "{script} must not export SUBSTRATE_OVERRIDE_* by default (WCU4)"
+        );
+    }
+}
+
+#[test]
+fn config_current_show_is_not_affected_without_override_inputs() {
+    ensure_substrate_built();
+    let temp = temp_dir("substrate-wcu4-clean-env-");
+    let home = temp.path().join("home");
+    let substrate_home = temp.path().join("substrate-home");
+    let cwd = temp.path().join("cwd");
+    fs::create_dir_all(&home).expect("create HOME");
+    fs::create_dir_all(&substrate_home).expect("create SUBSTRATE_HOME");
+    fs::create_dir_all(&cwd).expect("create cwd");
+
+    fs::write(
+        substrate_home.join("config.yaml"),
+        "policy:\n  mode: disabled\n",
+    )
+    .expect("write config.yaml");
+
+    let host_path = std::env::var("PATH").unwrap_or_else(|_| "/usr/bin:/bin".to_string());
+    let output = Command::new(binary_path())
+        .env_clear()
+        .env("PATH", host_path)
+        .env("TMPDIR", shared_tmpdir())
+        .env("HOME", &home)
+        .env("USERPROFILE", &home)
+        .env("SUBSTRATE_HOME", &substrate_home)
+        .current_dir(&cwd)
+        .arg("--no-world")
+        .args(["config", "current", "show", "--json"])
+        .output()
+        .expect("run substrate config current show --json");
+    assert!(
+        output.status.success(),
+        "config current show should succeed: {output:?}"
+    );
+
+    let json: JsonValue = serde_json::from_slice(&output.stdout).expect("current JSON parse");
+    assert_eq!(
+        json.pointer("/policy/mode").and_then(|v| v.as_str()),
+        Some("disabled"),
+        "policy.mode mismatch: {json}"
+    );
+}
diff --git a/tests/installers/install_smoke.sh b/tests/installers/install_smoke.sh
index db133b5c269c4ff975491691e4fb17fc2e2a6e46..76b8a4df24bcd3b603ea8df1c25de70ace488c18
--- a/tests/installers/install_smoke.sh
+++ b/tests/installers/install_smoke.sh
@@ -3,6 +3,7 @@
 
 SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
 REPO_ROOT="$(cd "${SCRIPT_DIR}/../.." && pwd)"
+HOST_PATH="${PATH}"
 
 log() {
   printf '[install-smoke] %s\n' "$*" >&2
@@ -694,6 +695,22 @@
   log "Verified manager manifest at ${manifest}"
 }
 
+assert_env_sh_has_no_override_exports() {
+  local env_sh="${PREFIX}/env.sh"
+  if [[ ! -f "${env_sh}" ]]; then
+    fatal "env.sh missing after install: ${env_sh}"
+  fi
+  if grep -Eq '^export[[:space:]]+SUBSTRATE_OVERRIDE_' "${env_sh}"; then
+    log "env.sh contains SUBSTRATE_OVERRIDE_* exports:"
+    sed -n '1,120p' "${env_sh}" >&2 || true
+    fatal "env.sh must not export SUBSTRATE_OVERRIDE_* by default (WCU4)"
+  fi
+  if env -i PATH="${HOST_PATH}" bash -lc "source \"${env_sh}\"; env | grep -q '^SUBSTRATE_OVERRIDE_'" >/dev/null 2>&1; then
+    fatal "Sourcing env.sh must not export SUBSTRATE_OVERRIDE_* (WCU4)"
+  fi
+  log "Verified env.sh does not export SUBSTRATE_OVERRIDE_* (${env_sh})"
+}
+
 run_health_smoke() {
   local substrate_bin="${PREFIX}/bin/substrate"
   local output
@@ -794,14 +811,15 @@
 prepare_stub_bin
 reset_systemctl_log
 
-if [[ "${SCENARIO_KIND}" == "prod" ]]; then
-  build_fake_release
-  run_prod_install
-  install_systemctl_log="$(capture_systemctl_log install)"
-  assert_config_init_hint_logged
-  assert_install_config
-  assert_manifest_present
-  run_health_smoke
+  if [[ "${SCENARIO_KIND}" == "prod" ]]; then
+    build_fake_release
+    run_prod_install
+    assert_env_sh_has_no_override_exports
+    install_systemctl_log="$(capture_systemctl_log install)"
+    assert_config_init_hint_logged
+    assert_install_config
+    assert_manifest_present
+    run_health_smoke
   if [[ "${SCENARIO_WORLD_ENABLED}" -eq 1 ]]; then
     assert_systemctl_log "install" "${install_systemctl_log}" 1
   else
@@ -810,13 +828,14 @@
   run_prod_uninstall
   uninstall_systemctl_log="$(capture_systemctl_log uninstall)"
   assert_systemctl_log "uninstall" "${uninstall_systemctl_log}" 1
-elif [[ "${SCENARIO_KIND}" == "dev" ]]; then
-  run_dev_install
-  install_systemctl_log="$(capture_systemctl_log install)"
-  assert_systemctl_log "install" "${install_systemctl_log}" 1
-else
-  fatal "Unsupported scenario kind: ${SCENARIO_KIND}"
-fi
+  elif [[ "${SCENARIO_KIND}" == "dev" ]]; then
+    run_dev_install
+    assert_env_sh_has_no_override_exports
+    install_systemctl_log="$(capture_systemctl_log install)"
+    assert_systemctl_log "install" "${install_systemctl_log}" 1
+  else
+    fatal "Unsupported scenario kind: ${SCENARIO_KIND}"
+  fi
 
 log "Scenario ${SCENARIO} completed using PREFIX=${PREFIX}"
 log "Temp root: ${WORK_ROOT}"
diff --git a/tests/installers/install_state_smoke.sh b/tests/installers/install_state_smoke.sh
index 88cecedffc5a6cbba88a97c65c1dea1f9dcb12b5..e94e9017e822b20b2e6dfba5a894e29228c877bc
--- a/tests/installers/install_state_smoke.sh
+++ b/tests/installers/install_state_smoke.sh
@@ -669,6 +669,7 @@
       fatal "metadata install failed (see ${install_log})"
     }
 
+  assert_env_sh_has_no_override_exports "${prefix}" "${HOST_PATH}"
   assert_metadata_file "${state_path}" "${fake_user}" "true" "true" "no"
 
   # Simulate legacy metadata and rerun install to ensure upgrade path rewrites it.
@@ -698,6 +699,7 @@
       fatal "metadata upgrade install failed (see ${install_log})"
     }
 
+  assert_env_sh_has_no_override_exports "${prefix}" "${HOST_PATH}"
   assert_metadata_upgraded "${state_path}" "${fake_user}"
 
   log "Metadata scenario completed (state: ${state_path})"
@@ -786,6 +788,24 @@
   log "Cleanup scenario completed (state: ${state_path})"
 )
 
+assert_env_sh_has_no_override_exports() {
+  local prefix="$1"
+  local host_path="$2"
+  local env_sh="${prefix}/env.sh"
+  if [[ ! -f "${env_sh}" ]]; then
+    fatal "env.sh missing after install: ${env_sh}"
+  fi
+  if grep -Eq '^export[[:space:]]+SUBSTRATE_OVERRIDE_' "${env_sh}"; then
+    log "env.sh contains SUBSTRATE_OVERRIDE_* exports:"
+    sed -n '1,120p' "${env_sh}" >&2 || true
+    fatal "env.sh must not export SUBSTRATE_OVERRIDE_* by default (WCU4)"
+  fi
+  if env -i PATH="${host_path}" bash -lc "source \"${env_sh}\"; env | grep -q '^SUBSTRATE_OVERRIDE_'" >/dev/null 2>&1; then
+    fatal "Sourcing env.sh must not export SUBSTRATE_OVERRIDE_* (WCU4)"
+  fi
+  log "Verified env.sh does not export SUBSTRATE_OVERRIDE_* (${env_sh})"
+}
+
 run_missing_metadata_scenario() (
   local work_root
   work_root="$(mktemp -d "/tmp/substrate-install-state.missing.XXXXXX")"

Plan update
  ✓ Verify task state and specs
  ✓ Add WCU4 installer env tests
  ✓ Run fmt and targeted tests
  → Run triad task finisher
file update:
diff --git a/crates/shell/tests/installer_env_wcu4.rs b/crates/shell/tests/installer_env_wcu4.rs
new file mode 100644
index 0000000000000000000000000000000000000000..fed36ed1072e3738bc17f7a1232648f324e1b336
--- /dev/null
+++ b/crates/shell/tests/installer_env_wcu4.rs
@@ -0,0 +1,78 @@
+#![cfg(unix)]
+
+mod common;
+
+use common::{binary_path, ensure_substrate_built, shared_tmpdir, temp_dir};
+use serde_json::Value as JsonValue;
+use std::fs;
+use std::path::PathBuf;
+use std::process::Command;
+
+fn repo_root() -> PathBuf {
+    PathBuf::from(env!("CARGO_MANIFEST_DIR"))
+        .join("../..")
+        .canonicalize()
+        .expect("canonicalize repo root")
+}
+
+fn read_repo_file(relative_path: &str) -> String {
+    fs::read_to_string(repo_root().join(relative_path))
+        .unwrap_or_else(|err| panic!("read {relative_path}: {err}"))
+}
+
+#[test]
+fn installer_scripts_do_not_export_substrate_override_by_default() {
+    for script in [
+        "scripts/substrate/install-substrate.sh",
+        "scripts/substrate/dev-install-substrate.sh",
+    ] {
+        let contents = read_repo_file(script);
+        assert!(
+            !contents.contains("export SUBSTRATE_OVERRIDE_"),
+            "{script} must not export SUBSTRATE_OVERRIDE_* by default (WCU4)"
+        );
+    }
+}
+
+#[test]
+fn config_current_show_is_not_affected_without_override_inputs() {
+    ensure_substrate_built();
+    let temp = temp_dir("substrate-wcu4-clean-env-");
+    let home = temp.path().join("home");
+    let substrate_home = temp.path().join("substrate-home");
+    let cwd = temp.path().join("cwd");
+    fs::create_dir_all(&home).expect("create HOME");
+    fs::create_dir_all(&substrate_home).expect("create SUBSTRATE_HOME");
+    fs::create_dir_all(&cwd).expect("create cwd");
+
+    fs::write(
+        substrate_home.join("config.yaml"),
+        "policy:\n  mode: disabled\n",
+    )
+    .expect("write config.yaml");
+
+    let host_path = std::env::var("PATH").unwrap_or_else(|_| "/usr/bin:/bin".to_string());
+    let output = Command::new(binary_path())
+        .env_clear()
+        .env("PATH", host_path)
+        .env("TMPDIR", shared_tmpdir())
+        .env("HOME", &home)
+        .env("USERPROFILE", &home)
+        .env("SUBSTRATE_HOME", &substrate_home)
+        .current_dir(&cwd)
+        .arg("--no-world")
+        .args(["config", "current", "show", "--json"])
+        .output()
+        .expect("run substrate config current show --json");
+    assert!(
+        output.status.success(),
+        "config current show should succeed: {output:?}"
+    );
+
+    let json: JsonValue = serde_json::from_slice(&output.stdout).expect("current JSON parse");
+    assert_eq!(
+        json.pointer("/policy/mode").and_then(|v| v.as_str()),
+        Some("disabled"),
+        "policy.mode mismatch: {json}"
+    );
+}
diff --git a/tests/installers/install_smoke.sh b/tests/installers/install_smoke.sh
index db133b5c269c4ff975491691e4fb17fc2e2a6e46..76b8a4df24bcd3b603ea8df1c25de70ace488c18
--- a/tests/installers/install_smoke.sh
+++ b/tests/installers/install_smoke.sh
@@ -3,6 +3,7 @@
 
 SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
 REPO_ROOT="$(cd "${SCRIPT_DIR}/../.." && pwd)"
+HOST_PATH="${PATH}"
 
 log() {
   printf '[install-smoke] %s\n' "$*" >&2
@@ -694,6 +695,22 @@
   log "Verified manager manifest at ${manifest}"
 }
 
+assert_env_sh_has_no_override_exports() {
+  local env_sh="${PREFIX}/env.sh"
+  if [[ ! -f "${env_sh}" ]]; then
+    fatal "env.sh missing after install: ${env_sh}"
+  fi
+  if grep -Eq '^export[[:space:]]+SUBSTRATE_OVERRIDE_' "${env_sh}"; then
+    log "env.sh contains SUBSTRATE_OVERRIDE_* exports:"
+    sed -n '1,120p' "${env_sh}" >&2 || true
+    fatal "env.sh must not export SUBSTRATE_OVERRIDE_* by default (WCU4)"
+  fi
+  if env -i PATH="${HOST_PATH}" bash -lc "source \"${env_sh}\"; env | grep -q '^SUBSTRATE_OVERRIDE_'" >/dev/null 2>&1; then
+    fatal "Sourcing env.sh must not export SUBSTRATE_OVERRIDE_* (WCU4)"
+  fi
+  log "Verified env.sh does not export SUBSTRATE_OVERRIDE_* (${env_sh})"
+}
+
 run_health_smoke() {
   local substrate_bin="${PREFIX}/bin/substrate"
   local output
@@ -794,14 +811,15 @@
 prepare_stub_bin
 reset_systemctl_log
 
-if [[ "${SCENARIO_KIND}" == "prod" ]]; then
-  build_fake_release
-  run_prod_install
-  install_systemctl_log="$(capture_systemctl_log install)"
-  assert_config_init_hint_logged
-  assert_install_config
-  assert_manifest_present
-  run_health_smoke
+  if [[ "${SCENARIO_KIND}" == "prod" ]]; then
+    build_fake_release
+    run_prod_install
+    assert_env_sh_has_no_override_exports
+    install_systemctl_log="$(capture_systemctl_log install)"
+    assert_config_init_hint_logged
+    assert_install_config
+    assert_manifest_present
+    run_health_smoke
   if [[ "${SCENARIO_WORLD_ENABLED}" -eq 1 ]]; then
     assert_systemctl_log "install" "${install_systemctl_log}" 1
   else
@@ -810,13 +828,14 @@
   run_prod_uninstall
   uninstall_systemctl_log="$(capture_systemctl_log uninstall)"
   assert_systemctl_log "uninstall" "${uninstall_systemctl_log}" 1
-elif [[ "${SCENARIO_KIND}" == "dev" ]]; then
-  run_dev_install
-  install_systemctl_log="$(capture_systemctl_log install)"
-  assert_systemctl_log "install" "${install_systemctl_log}" 1
-else
-  fatal "Unsupported scenario kind: ${SCENARIO_KIND}"
-fi
+  elif [[ "${SCENARIO_KIND}" == "dev" ]]; then
+    run_dev_install
+    assert_env_sh_has_no_override_exports
+    install_systemctl_log="$(capture_systemctl_log install)"
+    assert_systemctl_log "install" "${install_systemctl_log}" 1
+  else
+    fatal "Unsupported scenario kind: ${SCENARIO_KIND}"
+  fi
 
 log "Scenario ${SCENARIO} completed using PREFIX=${PREFIX}"
 log "Temp root: ${WORK_ROOT}"
diff --git a/tests/installers/install_state_smoke.sh b/tests/installers/install_state_smoke.sh
index 88cecedffc5a6cbba88a97c65c1dea1f9dcb12b5..e94e9017e822b20b2e6dfba5a894e29228c877bc
--- a/tests/installers/install_state_smoke.sh
+++ b/tests/installers/install_state_smoke.sh
@@ -669,6 +669,7 @@
       fatal "metadata install failed (see ${install_log})"
     }
 
+  assert_env_sh_has_no_override_exports "${prefix}" "${HOST_PATH}"
   assert_metadata_file "${state_path}" "${fake_user}" "true" "true" "no"
 
   # Simulate legacy metadata and rerun install to ensure upgrade path rewrites it.
@@ -698,6 +699,7 @@
       fatal "metadata upgrade install failed (see ${install_log})"
     }
 
+  assert_env_sh_has_no_override_exports "${prefix}" "${HOST_PATH}"
   assert_metadata_upgraded "${state_path}" "${fake_user}"
 
   log "Metadata scenario completed (state: ${state_path})"
@@ -786,6 +788,24 @@
   log "Cleanup scenario completed (state: ${state_path})"
 )
 
+assert_env_sh_has_no_override_exports() {
+  local prefix="$1"
+  local host_path="$2"
+  local env_sh="${prefix}/env.sh"
+  if [[ ! -f "${env_sh}" ]]; then
+    fatal "env.sh missing after install: ${env_sh}"
+  fi
+  if grep -Eq '^export[[:space:]]+SUBSTRATE_OVERRIDE_' "${env_sh}"; then
+    log "env.sh contains SUBSTRATE_OVERRIDE_* exports:"
+    sed -n '1,120p' "${env_sh}" >&2 || true
+    fatal "env.sh must not export SUBSTRATE_OVERRIDE_* by default (WCU4)"
+  fi
+  if env -i PATH="${host_path}" bash -lc "source \"${env_sh}\"; env | grep -q '^SUBSTRATE_OVERRIDE_'" >/dev/null 2>&1; then
+    fatal "Sourcing env.sh must not export SUBSTRATE_OVERRIDE_* (WCU4)"
+  fi
+  log "Verified env.sh does not export SUBSTRATE_OVERRIDE_* (${env_sh})"
+}
+
 run_missing_metadata_scenario() (
   local work_root
   work_root="$(mktemp -d "/tmp/substrate-install-state.missing.XXXXXX")"

exec
/bin/bash -lc 'make triad-task-finish TASK_ID="WCU4-test"' in /home/spenser/__Active_code/substrate/wt/workspace_config_policy_unification-wcu4-test succeeded in 1.34s:
make[1]: Entering directory '/home/spenser/__Active_code/substrate/wt/workspace_config_policy_unification-wcu4-test'
== Running: make triad-test-checks
make[2]: Entering directory '/home/spenser/__Active_code/substrate/wt/workspace_config_policy_unification-wcu4-test'
cargo fmt
make[2]: Leaving directory '/home/spenser/__Active_code/substrate/wt/workspace_config_policy_unification-wcu4-test'
== No changes to commit
TASK_BRANCH=workspace_config_policy_unification-wcu4-test
WORKTREE=/home/spenser/__Active_code/substrate/wt/workspace_config_policy_unification-wcu4-test
HEAD=46579219fb12003558ba9a6fa95ad72a3c7c5de3
COMMITS=1
CHECKS=make triad-test-checks
SMOKE_RUN=
MERGED_TO_ORCH=
make[1]: Leaving directory '/home/spenser/__Active_code/substrate/wt/workspace_config_policy_unification-wcu4-test'
file update:
diff --git a/crates/shell/tests/installer_env_wcu4.rs b/crates/shell/tests/installer_env_wcu4.rs
new file mode 100644
index 0000000000000000000000000000000000000000..fed36ed1072e3738bc17f7a1232648f324e1b336
--- /dev/null
+++ b/crates/shell/tests/installer_env_wcu4.rs
@@ -0,0 +1,78 @@
+#![cfg(unix)]
+
+mod common;
+
+use common::{binary_path, ensure_substrate_built, shared_tmpdir, temp_dir};
+use serde_json::Value as JsonValue;
+use std::fs;
+use std::path::PathBuf;
+use std::process::Command;
+
+fn repo_root() -> PathBuf {
+    PathBuf::from(env!("CARGO_MANIFEST_DIR"))
+        .join("../..")
+        .canonicalize()
+        .expect("canonicalize repo root")
+}
+
+fn read_repo_file(relative_path: &str) -> String {
+    fs::read_to_string(repo_root().join(relative_path))
+        .unwrap_or_else(|err| panic!("read {relative_path}: {err}"))
+}
+
+#[test]
+fn installer_scripts_do_not_export_substrate_override_by_default() {
+    for script in [
+        "scripts/substrate/install-substrate.sh",
+        "scripts/substrate/dev-install-substrate.sh",
+    ] {
+        let contents = read_repo_file(script);
+        assert!(
+            !contents.contains("export SUBSTRATE_OVERRIDE_"),
+            "{script} must not export SUBSTRATE_OVERRIDE_* by default (WCU4)"
+        );
+    }
+}
+
+#[test]
+fn config_current_show_is_not_affected_without_override_inputs() {
+    ensure_substrate_built();
+    let temp = temp_dir("substrate-wcu4-clean-env-");
+    let home = temp.path().join("home");
+    let substrate_home = temp.path().join("substrate-home");
+    let cwd = temp.path().join("cwd");
+    fs::create_dir_all(&home).expect("create HOME");
+    fs::create_dir_all(&substrate_home).expect("create SUBSTRATE_HOME");
+    fs::create_dir_all(&cwd).expect("create cwd");
+
+    fs::write(
+        substrate_home.join("config.yaml"),
+        "policy:\n  mode: disabled\n",
+    )
+    .expect("write config.yaml");
+
+    let host_path = std::env::var("PATH").unwrap_or_else(|_| "/usr/bin:/bin".to_string());
+    let output = Command::new(binary_path())
+        .env_clear()
+        .env("PATH", host_path)
+        .env("TMPDIR", shared_tmpdir())
+        .env("HOME", &home)
+        .env("USERPROFILE", &home)
+        .env("SUBSTRATE_HOME", &substrate_home)
+        .current_dir(&cwd)
+        .arg("--no-world")
+        .args(["config", "current", "show", "--json"])
+        .output()
+        .expect("run substrate config current show --json");
+    assert!(
+        output.status.success(),
+        "config current show should succeed: {output:?}"
+    );
+
+    let json: JsonValue = serde_json::from_slice(&output.stdout).expect("current JSON parse");
+    assert_eq!(
+        json.pointer("/policy/mode").and_then(|v| v.as_str()),
+        Some("disabled"),
+        "policy.mode mismatch: {json}"
+    );
+}
diff --git a/tests/installers/install_smoke.sh b/tests/installers/install_smoke.sh
index db133b5c269c4ff975491691e4fb17fc2e2a6e46..76b8a4df24bcd3b603ea8df1c25de70ace488c18
--- a/tests/installers/install_smoke.sh
+++ b/tests/installers/install_smoke.sh
@@ -3,6 +3,7 @@
 
 SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
 REPO_ROOT="$(cd "${SCRIPT_DIR}/../.." && pwd)"
+HOST_PATH="${PATH}"
 
 log() {
   printf '[install-smoke] %s\n' "$*" >&2
@@ -694,6 +695,22 @@
   log "Verified manager manifest at ${manifest}"
 }
 
+assert_env_sh_has_no_override_exports() {
+  local env_sh="${PREFIX}/env.sh"
+  if [[ ! -f "${env_sh}" ]]; then
+    fatal "env.sh missing after install: ${env_sh}"
+  fi
+  if grep -Eq '^export[[:space:]]+SUBSTRATE_OVERRIDE_' "${env_sh}"; then
+    log "env.sh contains SUBSTRATE_OVERRIDE_* exports:"
+    sed -n '1,120p' "${env_sh}" >&2 || true
+    fatal "env.sh must not export SUBSTRATE_OVERRIDE_* by default (WCU4)"
+  fi
+  if env -i PATH="${HOST_PATH}" bash -lc "source \"${env_sh}\"; env | grep -q '^SUBSTRATE_OVERRIDE_'" >/dev/null 2>&1; then
+    fatal "Sourcing env.sh must not export SUBSTRATE_OVERRIDE_* (WCU4)"
+  fi
+  log "Verified env.sh does not export SUBSTRATE_OVERRIDE_* (${env_sh})"
+}
+
 run_health_smoke() {
   local substrate_bin="${PREFIX}/bin/substrate"
   local output
@@ -794,14 +811,15 @@
 prepare_stub_bin
 reset_systemctl_log
 
-if [[ "${SCENARIO_KIND}" == "prod" ]]; then
-  build_fake_release
-  run_prod_install
-  install_systemctl_log="$(capture_systemctl_log install)"
-  assert_config_init_hint_logged
-  assert_install_config
-  assert_manifest_present
-  run_health_smoke
+  if [[ "${SCENARIO_KIND}" == "prod" ]]; then
+    build_fake_release
+    run_prod_install
+    assert_env_sh_has_no_override_exports
+    install_systemctl_log="$(capture_systemctl_log install)"
+    assert_config_init_hint_logged
+    assert_install_config
+    assert_manifest_present
+    run_health_smoke
   if [[ "${SCENARIO_WORLD_ENABLED}" -eq 1 ]]; then
     assert_systemctl_log "install" "${install_systemctl_log}" 1
   else
@@ -810,13 +828,14 @@
   run_prod_uninstall
   uninstall_systemctl_log="$(capture_systemctl_log uninstall)"
   assert_systemctl_log "uninstall" "${uninstall_systemctl_log}" 1
-elif [[ "${SCENARIO_KIND}" == "dev" ]]; then
-  run_dev_install
-  install_systemctl_log="$(capture_systemctl_log install)"
-  assert_systemctl_log "install" "${install_systemctl_log}" 1
-else
-  fatal "Unsupported scenario kind: ${SCENARIO_KIND}"
-fi
+  elif [[ "${SCENARIO_KIND}" == "dev" ]]; then
+    run_dev_install
+    assert_env_sh_has_no_override_exports
+    install_systemctl_log="$(capture_systemctl_log install)"
+    assert_systemctl_log "install" "${install_systemctl_log}" 1
+  else
+    fatal "Unsupported scenario kind: ${SCENARIO_KIND}"
+  fi
 
 log "Scenario ${SCENARIO} completed using PREFIX=${PREFIX}"
 log "Temp root: ${WORK_ROOT}"
diff --git a/tests/installers/install_state_smoke.sh b/tests/installers/install_state_smoke.sh
index 88cecedffc5a6cbba88a97c65c1dea1f9dcb12b5..e94e9017e822b20b2e6dfba5a894e29228c877bc
--- a/tests/installers/install_state_smoke.sh
+++ b/tests/installers/install_state_smoke.sh
@@ -669,6 +669,7 @@
       fatal "metadata install failed (see ${install_log})"
     }
 
+  assert_env_sh_has_no_override_exports "${prefix}" "${HOST_PATH}"
   assert_metadata_file "${state_path}" "${fake_user}" "true" "true" "no"
 
   # Simulate legacy metadata and rerun install to ensure upgrade path rewrites it.
@@ -698,6 +699,7 @@
       fatal "metadata upgrade install failed (see ${install_log})"
     }
 
+  assert_env_sh_has_no_override_exports "${prefix}" "${HOST_PATH}"
   assert_metadata_upgraded "${state_path}" "${fake_user}"
 
   log "Metadata scenario completed (state: ${state_path})"
@@ -786,6 +788,24 @@
   log "Cleanup scenario completed (state: ${state_path})"
 )
 
+assert_env_sh_has_no_override_exports() {
+  local prefix="$1"
+  local host_path="$2"
+  local env_sh="${prefix}/env.sh"
+  if [[ ! -f "${env_sh}" ]]; then
+    fatal "env.sh missing after install: ${env_sh}"
+  fi
+  if grep -Eq '^export[[:space:]]+SUBSTRATE_OVERRIDE_' "${env_sh}"; then
+    log "env.sh contains SUBSTRATE_OVERRIDE_* exports:"
+    sed -n '1,120p' "${env_sh}" >&2 || true
+    fatal "env.sh must not export SUBSTRATE_OVERRIDE_* by default (WCU4)"
+  fi
+  if env -i PATH="${host_path}" bash -lc "source \"${env_sh}\"; env | grep -q '^SUBSTRATE_OVERRIDE_'" >/dev/null 2>&1; then
+    fatal "Sourcing env.sh must not export SUBSTRATE_OVERRIDE_* (WCU4)"
+  fi
+  log "Verified env.sh does not export SUBSTRATE_OVERRIDE_* (${env_sh})"
+}
+
 run_missing_metadata_scenario() (
   local work_root
   work_root="$(mktemp -d "/tmp/substrate-install-state.missing.XXXXXX")"


thinking
**Updating plans**

I see that the finisher ran successfully—awesome! Now it's time to update my plan since step 4 is completed. I need to evaluate what comes next and make sure everything is aligned for the following steps. This is an important moment to keep track of progress and stay organized. I'm feeling good about moving forward, and I want to make sure to keep everything on track as I proceed!
Plan update
  ✓ Verify task state and specs
  ✓ Add WCU4 installer env tests
  ✓ Run fmt and targeted tests
  ✓ Run triad task finisher
file update:
diff --git a/crates/shell/tests/installer_env_wcu4.rs b/crates/shell/tests/installer_env_wcu4.rs
new file mode 100644
index 0000000000000000000000000000000000000000..fed36ed1072e3738bc17f7a1232648f324e1b336
--- /dev/null
+++ b/crates/shell/tests/installer_env_wcu4.rs
@@ -0,0 +1,78 @@
+#![cfg(unix)]
+
+mod common;
+
+use common::{binary_path, ensure_substrate_built, shared_tmpdir, temp_dir};
+use serde_json::Value as JsonValue;
+use std::fs;
+use std::path::PathBuf;
+use std::process::Command;
+
+fn repo_root() -> PathBuf {
+    PathBuf::from(env!("CARGO_MANIFEST_DIR"))
+        .join("../..")
+        .canonicalize()
+        .expect("canonicalize repo root")
+}
+
+fn read_repo_file(relative_path: &str) -> String {
+    fs::read_to_string(repo_root().join(relative_path))
+        .unwrap_or_else(|err| panic!("read {relative_path}: {err}"))
+}
+
+#[test]
+fn installer_scripts_do_not_export_substrate_override_by_default() {
+    for script in [
+        "scripts/substrate/install-substrate.sh",
+        "scripts/substrate/dev-install-substrate.sh",
+    ] {
+        let contents = read_repo_file(script);
+        assert!(
+            !contents.contains("export SUBSTRATE_OVERRIDE_"),
+            "{script} must not export SUBSTRATE_OVERRIDE_* by default (WCU4)"
+        );
+    }
+}
+
+#[test]
+fn config_current_show_is_not_affected_without_override_inputs() {
+    ensure_substrate_built();
+    let temp = temp_dir("substrate-wcu4-clean-env-");
+    let home = temp.path().join("home");
+    let substrate_home = temp.path().join("substrate-home");
+    let cwd = temp.path().join("cwd");
+    fs::create_dir_all(&home).expect("create HOME");
+    fs::create_dir_all(&substrate_home).expect("create SUBSTRATE_HOME");
+    fs::create_dir_all(&cwd).expect("create cwd");
+
+    fs::write(
+        substrate_home.join("config.yaml"),
+        "policy:\n  mode: disabled\n",
+    )
+    .expect("write config.yaml");
+
+    let host_path = std::env::var("PATH").unwrap_or_else(|_| "/usr/bin:/bin".to_string());
+    let output = Command::new(binary_path())
+        .env_clear()
+        .env("PATH", host_path)
+        .env("TMPDIR", shared_tmpdir())
+        .env("HOME", &home)
+        .env("USERPROFILE", &home)
+        .env("SUBSTRATE_HOME", &substrate_home)
+        .current_dir(&cwd)
+        .arg("--no-world")
+        .args(["config", "current", "show", "--json"])
+        .output()
+        .expect("run substrate config current show --json");
+    assert!(
+        output.status.success(),
+        "config current show should succeed: {output:?}"
+    );
+
+    let json: JsonValue = serde_json::from_slice(&output.stdout).expect("current JSON parse");
+    assert_eq!(
+        json.pointer("/policy/mode").and_then(|v| v.as_str()),
+        Some("disabled"),
+        "policy.mode mismatch: {json}"
+    );
+}
diff --git a/tests/installers/install_smoke.sh b/tests/installers/install_smoke.sh
index db133b5c269c4ff975491691e4fb17fc2e2a6e46..76b8a4df24bcd3b603ea8df1c25de70ace488c18
--- a/tests/installers/install_smoke.sh
+++ b/tests/installers/install_smoke.sh
@@ -3,6 +3,7 @@
 
 SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
 REPO_ROOT="$(cd "${SCRIPT_DIR}/../.." && pwd)"
+HOST_PATH="${PATH}"
 
 log() {
   printf '[install-smoke] %s\n' "$*" >&2
@@ -694,6 +695,22 @@
   log "Verified manager manifest at ${manifest}"
 }
 
+assert_env_sh_has_no_override_exports() {
+  local env_sh="${PREFIX}/env.sh"
+  if [[ ! -f "${env_sh}" ]]; then
+    fatal "env.sh missing after install: ${env_sh}"
+  fi
+  if grep -Eq '^export[[:space:]]+SUBSTRATE_OVERRIDE_' "${env_sh}"; then
+    log "env.sh contains SUBSTRATE_OVERRIDE_* exports:"
+    sed -n '1,120p' "${env_sh}" >&2 || true
+    fatal "env.sh must not export SUBSTRATE_OVERRIDE_* by default (WCU4)"
+  fi
+  if env -i PATH="${HOST_PATH}" bash -lc "source \"${env_sh}\"; env | grep -q '^SUBSTRATE_OVERRIDE_'" >/dev/null 2>&1; then
+    fatal "Sourcing env.sh must not export SUBSTRATE_OVERRIDE_* (WCU4)"
+  fi
+  log "Verified env.sh does not export SUBSTRATE_OVERRIDE_* (${env_sh})"
+}
+
 run_health_smoke() {
   local substrate_bin="${PREFIX}/bin/substrate"
   local output
@@ -794,14 +811,15 @@
 prepare_stub_bin
 reset_systemctl_log
 
-if [[ "${SCENARIO_KIND}" == "prod" ]]; then
-  build_fake_release
-  run_prod_install
-  install_systemctl_log="$(capture_systemctl_log install)"
-  assert_config_init_hint_logged
-  assert_install_config
-  assert_manifest_present
-  run_health_smoke
+  if [[ "${SCENARIO_KIND}" == "prod" ]]; then
+    build_fake_release
+    run_prod_install
+    assert_env_sh_has_no_override_exports
+    install_systemctl_log="$(capture_systemctl_log install)"
+    assert_config_init_hint_logged
+    assert_install_config
+    assert_manifest_present
+    run_health_smoke
   if [[ "${SCENARIO_WORLD_ENABLED}" -eq 1 ]]; then
     assert_systemctl_log "install" "${install_systemctl_log}" 1
   else
@@ -810,13 +828,14 @@
   run_prod_uninstall
   uninstall_systemctl_log="$(capture_systemctl_log uninstall)"
   assert_systemctl_log "uninstall" "${uninstall_systemctl_log}" 1
-elif [[ "${SCENARIO_KIND}" == "dev" ]]; then
-  run_dev_install
-  install_systemctl_log="$(capture_systemctl_log install)"
-  assert_systemctl_log "install" "${install_systemctl_log}" 1
-else
-  fatal "Unsupported scenario kind: ${SCENARIO_KIND}"
-fi
+  elif [[ "${SCENARIO_KIND}" == "dev" ]]; then
+    run_dev_install
+    assert_env_sh_has_no_override_exports
+    install_systemctl_log="$(capture_systemctl_log install)"
+    assert_systemctl_log "install" "${install_systemctl_log}" 1
+  else
+    fatal "Unsupported scenario kind: ${SCENARIO_KIND}"
+  fi
 
 log "Scenario ${SCENARIO} completed using PREFIX=${PREFIX}"
 log "Temp root: ${WORK_ROOT}"
diff --git a/tests/installers/install_state_smoke.sh b/tests/installers/install_state_smoke.sh
index 88cecedffc5a6cbba88a97c65c1dea1f9dcb12b5..e94e9017e822b20b2e6dfba5a894e29228c877bc
--- a/tests/installers/install_state_smoke.sh
+++ b/tests/installers/install_state_smoke.sh
@@ -669,6 +669,7 @@
       fatal "metadata install failed (see ${install_log})"
     }
 
+  assert_env_sh_has_no_override_exports "${prefix}" "${HOST_PATH}"
   assert_metadata_file "${state_path}" "${fake_user}" "true" "true" "no"
 
   # Simulate legacy metadata and rerun install to ensure upgrade path rewrites it.
@@ -698,6 +699,7 @@
       fatal "metadata upgrade install failed (see ${install_log})"
     }
 
+  assert_env_sh_has_no_override_exports "${prefix}" "${HOST_PATH}"
   assert_metadata_upgraded "${state_path}" "${fake_user}"
 
   log "Metadata scenario completed (state: ${state_path})"
@@ -786,6 +788,24 @@
   log "Cleanup scenario completed (state: ${state_path})"
 )
 
+assert_env_sh_has_no_override_exports() {
+  local prefix="$1"
+  local host_path="$2"
+  local env_sh="${prefix}/env.sh"
+  if [[ ! -f "${env_sh}" ]]; then
+    fatal "env.sh missing after install: ${env_sh}"
+  fi
+  if grep -Eq '^export[[:space:]]+SUBSTRATE_OVERRIDE_' "${env_sh}"; then
+    log "env.sh contains SUBSTRATE_OVERRIDE_* exports:"
+    sed -n '1,120p' "${env_sh}" >&2 || true
+    fatal "env.sh must not export SUBSTRATE_OVERRIDE_* by default (WCU4)"
+  fi
+  if env -i PATH="${host_path}" bash -lc "source \"${env_sh}\"; env | grep -q '^SUBSTRATE_OVERRIDE_'" >/dev/null 2>&1; then
+    fatal "Sourcing env.sh must not export SUBSTRATE_OVERRIDE_* (WCU4)"
+  fi
+  log "Verified env.sh does not export SUBSTRATE_OVERRIDE_* (${env_sh})"
+}
+
 run_missing_metadata_scenario() (
   local work_root
   work_root="$(mktemp -d "/tmp/substrate-install-state.missing.XXXXXX")"

exec
/bin/bash -lc 'git status --porcelain' in /home/spenser/__Active_code/substrate/wt/workspace_config_policy_unification-wcu4-test succeeded in 66ms:

file update:
diff --git a/crates/shell/tests/installer_env_wcu4.rs b/crates/shell/tests/installer_env_wcu4.rs
new file mode 100644
index 0000000000000000000000000000000000000000..fed36ed1072e3738bc17f7a1232648f324e1b336
--- /dev/null
+++ b/crates/shell/tests/installer_env_wcu4.rs
@@ -0,0 +1,78 @@
+#![cfg(unix)]
+
+mod common;
+
+use common::{binary_path, ensure_substrate_built, shared_tmpdir, temp_dir};
+use serde_json::Value as JsonValue;
+use std::fs;
+use std::path::PathBuf;
+use std::process::Command;
+
+fn repo_root() -> PathBuf {
+    PathBuf::from(env!("CARGO_MANIFEST_DIR"))
+        .join("../..")
+        .canonicalize()
+        .expect("canonicalize repo root")
+}
+
+fn read_repo_file(relative_path: &str) -> String {
+    fs::read_to_string(repo_root().join(relative_path))
+        .unwrap_or_else(|err| panic!("read {relative_path}: {err}"))
+}
+
+#[test]
+fn installer_scripts_do_not_export_substrate_override_by_default() {
+    for script in [
+        "scripts/substrate/install-substrate.sh",
+        "scripts/substrate/dev-install-substrate.sh",
+    ] {
+        let contents = read_repo_file(script);
+        assert!(
+            !contents.contains("export SUBSTRATE_OVERRIDE_"),
+            "{script} must not export SUBSTRATE_OVERRIDE_* by default (WCU4)"
+        );
+    }
+}
+
+#[test]
+fn config_current_show_is_not_affected_without_override_inputs() {
+    ensure_substrate_built();
+    let temp = temp_dir("substrate-wcu4-clean-env-");
+    let home = temp.path().join("home");
+    let substrate_home = temp.path().join("substrate-home");
+    let cwd = temp.path().join("cwd");
+    fs::create_dir_all(&home).expect("create HOME");
+    fs::create_dir_all(&substrate_home).expect("create SUBSTRATE_HOME");
+    fs::create_dir_all(&cwd).expect("create cwd");
+
+    fs::write(
+        substrate_home.join("config.yaml"),
+        "policy:\n  mode: disabled\n",
+    )
+    .expect("write config.yaml");
+
+    let host_path = std::env::var("PATH").unwrap_or_else(|_| "/usr/bin:/bin".to_string());
+    let output = Command::new(binary_path())
+        .env_clear()
+        .env("PATH", host_path)
+        .env("TMPDIR", shared_tmpdir())
+        .env("HOME", &home)
+        .env("USERPROFILE", &home)
+        .env("SUBSTRATE_HOME", &substrate_home)
+        .current_dir(&cwd)
+        .arg("--no-world")
+        .args(["config", "current", "show", "--json"])
+        .output()
+        .expect("run substrate config current show --json");
+    assert!(
+        output.status.success(),
+        "config current show should succeed: {output:?}"
+    );
+
+    let json: JsonValue = serde_json::from_slice(&output.stdout).expect("current JSON parse");
+    assert_eq!(
+        json.pointer("/policy/mode").and_then(|v| v.as_str()),
+        Some("disabled"),
+        "policy.mode mismatch: {json}"
+    );
+}
diff --git a/tests/installers/install_smoke.sh b/tests/installers/install_smoke.sh
index db133b5c269c4ff975491691e4fb17fc2e2a6e46..76b8a4df24bcd3b603ea8df1c25de70ace488c18
--- a/tests/installers/install_smoke.sh
+++ b/tests/installers/install_smoke.sh
@@ -3,6 +3,7 @@
 
 SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
 REPO_ROOT="$(cd "${SCRIPT_DIR}/../.." && pwd)"
+HOST_PATH="${PATH}"
 
 log() {
   printf '[install-smoke] %s\n' "$*" >&2
@@ -694,6 +695,22 @@
   log "Verified manager manifest at ${manifest}"
 }
 
+assert_env_sh_has_no_override_exports() {
+  local env_sh="${PREFIX}/env.sh"
+  if [[ ! -f "${env_sh}" ]]; then
+    fatal "env.sh missing after install: ${env_sh}"
+  fi
+  if grep -Eq '^export[[:space:]]+SUBSTRATE_OVERRIDE_' "${env_sh}"; then
+    log "env.sh contains SUBSTRATE_OVERRIDE_* exports:"
+    sed -n '1,120p' "${env_sh}" >&2 || true
+    fatal "env.sh must not export SUBSTRATE_OVERRIDE_* by default (WCU4)"
+  fi
+  if env -i PATH="${HOST_PATH}" bash -lc "source \"${env_sh}\"; env | grep -q '^SUBSTRATE_OVERRIDE_'" >/dev/null 2>&1; then
+    fatal "Sourcing env.sh must not export SUBSTRATE_OVERRIDE_* (WCU4)"
+  fi
+  log "Verified env.sh does not export SUBSTRATE_OVERRIDE_* (${env_sh})"
+}
+
 run_health_smoke() {
   local substrate_bin="${PREFIX}/bin/substrate"
   local output
@@ -794,14 +811,15 @@
 prepare_stub_bin
 reset_systemctl_log
 
-if [[ "${SCENARIO_KIND}" == "prod" ]]; then
-  build_fake_release
-  run_prod_install
-  install_systemctl_log="$(capture_systemctl_log install)"
-  assert_config_init_hint_logged
-  assert_install_config
-  assert_manifest_present
-  run_health_smoke
+  if [[ "${SCENARIO_KIND}" == "prod" ]]; then
+    build_fake_release
+    run_prod_install
+    assert_env_sh_has_no_override_exports
+    install_systemctl_log="$(capture_systemctl_log install)"
+    assert_config_init_hint_logged
+    assert_install_config
+    assert_manifest_present
+    run_health_smoke
   if [[ "${SCENARIO_WORLD_ENABLED}" -eq 1 ]]; then
     assert_systemctl_log "install" "${install_systemctl_log}" 1
   else
@@ -810,13 +828,14 @@
   run_prod_uninstall
   uninstall_systemctl_log="$(capture_systemctl_log uninstall)"
   assert_systemctl_log "uninstall" "${uninstall_systemctl_log}" 1
-elif [[ "${SCENARIO_KIND}" == "dev" ]]; then
-  run_dev_install
-  install_systemctl_log="$(capture_systemctl_log install)"
-  assert_systemctl_log "install" "${install_systemctl_log}" 1
-else
-  fatal "Unsupported scenario kind: ${SCENARIO_KIND}"
-fi
+  elif [[ "${SCENARIO_KIND}" == "dev" ]]; then
+    run_dev_install
+    assert_env_sh_has_no_override_exports
+    install_systemctl_log="$(capture_systemctl_log install)"
+    assert_systemctl_log "install" "${install_systemctl_log}" 1
+  else
+    fatal "Unsupported scenario kind: ${SCENARIO_KIND}"
+  fi
 
 log "Scenario ${SCENARIO} completed using PREFIX=${PREFIX}"
 log "Temp root: ${WORK_ROOT}"
diff --git a/tests/installers/install_state_smoke.sh b/tests/installers/install_state_smoke.sh
index 88cecedffc5a6cbba88a97c65c1dea1f9dcb12b5..e94e9017e822b20b2e6dfba5a894e29228c877bc
--- a/tests/installers/install_state_smoke.sh
+++ b/tests/installers/install_state_smoke.sh
@@ -669,6 +669,7 @@
       fatal "metadata install failed (see ${install_log})"
     }
 
+  assert_env_sh_has_no_override_exports "${prefix}" "${HOST_PATH}"
   assert_metadata_file "${state_path}" "${fake_user}" "true" "true" "no"
 
   # Simulate legacy metadata and rerun install to ensure upgrade path rewrites it.
@@ -698,6 +699,7 @@
       fatal "metadata upgrade install failed (see ${install_log})"
     }
 
+  assert_env_sh_has_no_override_exports "${prefix}" "${HOST_PATH}"
   assert_metadata_upgraded "${state_path}" "${fake_user}"
 
   log "Metadata scenario completed (state: ${state_path})"
@@ -786,6 +788,24 @@
   log "Cleanup scenario completed (state: ${state_path})"
 )
 
+assert_env_sh_has_no_override_exports() {
+  local prefix="$1"
+  local host_path="$2"
+  local env_sh="${prefix}/env.sh"
+  if [[ ! -f "${env_sh}" ]]; then
+    fatal "env.sh missing after install: ${env_sh}"
+  fi
+  if grep -Eq '^export[[:space:]]+SUBSTRATE_OVERRIDE_' "${env_sh}"; then
+    log "env.sh contains SUBSTRATE_OVERRIDE_* exports:"
+    sed -n '1,120p' "${env_sh}" >&2 || true
+    fatal "env.sh must not export SUBSTRATE_OVERRIDE_* by default (WCU4)"
+  fi
+  if env -i PATH="${host_path}" bash -lc "source \"${env_sh}\"; env | grep -q '^SUBSTRATE_OVERRIDE_'" >/dev/null 2>&1; then
+    fatal "Sourcing env.sh must not export SUBSTRATE_OVERRIDE_* (WCU4)"
+  fi
+  log "Verified env.sh does not export SUBSTRATE_OVERRIDE_* (${env_sh})"
+}
+
 run_missing_metadata_scenario() (
   local work_root
   work_root="$(mktemp -d "/tmp/substrate-install-state.missing.XXXXXX")"

codex
- Added WCU4 contract tests to ensure install/dev scripts don’t export `SUBSTRATE_OVERRIDE_*` by default: `crates/shell/tests/installer_env_wcu4.rs:1`
- Hardened installer harnesses to fail if `env.sh` exports `SUBSTRATE_OVERRIDE_*`: `tests/installers/install_smoke.sh:1`, `tests/installers/install_state_smoke.sh:1`

Commands run:
- `cargo fmt`
- `cargo test -p substrate-shell --test installer_env_wcu4 -- --nocapture` (expected failure until WCU4 code removes the exports)
- `bash tests/installers/install_state_smoke.sh --scenario metadata` (expected failure until WCU4 code removes the exports)
- `bash tests/installers/install_smoke.sh --scenario prod-no-world` (expected failure until WCU4 code removes the exports)
- `make triad-task-finish TASK_ID="WCU4-test"`
file update:
diff --git a/crates/shell/tests/installer_env_wcu4.rs b/crates/shell/tests/installer_env_wcu4.rs
new file mode 100644
index 0000000000000000000000000000000000000000..fed36ed1072e3738bc17f7a1232648f324e1b336
--- /dev/null
+++ b/crates/shell/tests/installer_env_wcu4.rs
@@ -0,0 +1,78 @@
+#![cfg(unix)]
+
+mod common;
+
+use common::{binary_path, ensure_substrate_built, shared_tmpdir, temp_dir};
+use serde_json::Value as JsonValue;
+use std::fs;
+use std::path::PathBuf;
+use std::process::Command;
+
+fn repo_root() -> PathBuf {
+    PathBuf::from(env!("CARGO_MANIFEST_DIR"))
+        .join("../..")
+        .canonicalize()
+        .expect("canonicalize repo root")
+}
+
+fn read_repo_file(relative_path: &str) -> String {
+    fs::read_to_string(repo_root().join(relative_path))
+        .unwrap_or_else(|err| panic!("read {relative_path}: {err}"))
+}
+
+#[test]
+fn installer_scripts_do_not_export_substrate_override_by_default() {
+    for script in [
+        "scripts/substrate/install-substrate.sh",
+        "scripts/substrate/dev-install-substrate.sh",
+    ] {
+        let contents = read_repo_file(script);
+        assert!(
+            !contents.contains("export SUBSTRATE_OVERRIDE_"),
+            "{script} must not export SUBSTRATE_OVERRIDE_* by default (WCU4)"
+        );
+    }
+}
+
+#[test]
+fn config_current_show_is_not_affected_without_override_inputs() {
+    ensure_substrate_built();
+    let temp = temp_dir("substrate-wcu4-clean-env-");
+    let home = temp.path().join("home");
+    let substrate_home = temp.path().join("substrate-home");
+    let cwd = temp.path().join("cwd");
+    fs::create_dir_all(&home).expect("create HOME");
+    fs::create_dir_all(&substrate_home).expect("create SUBSTRATE_HOME");
+    fs::create_dir_all(&cwd).expect("create cwd");
+
+    fs::write(
+        substrate_home.join("config.yaml"),
+        "policy:\n  mode: disabled\n",
+    )
+    .expect("write config.yaml");
+
+    let host_path = std::env::var("PATH").unwrap_or_else(|_| "/usr/bin:/bin".to_string());
+    let output = Command::new(binary_path())
+        .env_clear()
+        .env("PATH", host_path)
+        .env("TMPDIR", shared_tmpdir())
+        .env("HOME", &home)
+        .env("USERPROFILE", &home)
+        .env("SUBSTRATE_HOME", &substrate_home)
+        .current_dir(&cwd)
+        .arg("--no-world")
+        .args(["config", "current", "show", "--json"])
+        .output()
+        .expect("run substrate config current show --json");
+    assert!(
+        output.status.success(),
+        "config current show should succeed: {output:?}"
+    );
+
+    let json: JsonValue = serde_json::from_slice(&output.stdout).expect("current JSON parse");
+    assert_eq!(
+        json.pointer("/policy/mode").and_then(|v| v.as_str()),
+        Some("disabled"),
+        "policy.mode mismatch: {json}"
+    );
+}
diff --git a/tests/installers/install_smoke.sh b/tests/installers/install_smoke.sh
index db133b5c269c4ff975491691e4fb17fc2e2a6e46..76b8a4df24bcd3b603ea8df1c25de70ace488c18
--- a/tests/installers/install_smoke.sh
+++ b/tests/installers/install_smoke.sh
@@ -3,6 +3,7 @@
 
 SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
 REPO_ROOT="$(cd "${SCRIPT_DIR}/../.." && pwd)"
+HOST_PATH="${PATH}"
 
 log() {
   printf '[install-smoke] %s\n' "$*" >&2
@@ -694,6 +695,22 @@
   log "Verified manager manifest at ${manifest}"
 }
 
+assert_env_sh_has_no_override_exports() {
+  local env_sh="${PREFIX}/env.sh"
+  if [[ ! -f "${env_sh}" ]]; then
+    fatal "env.sh missing after install: ${env_sh}"
+  fi
+  if grep -Eq '^export[[:space:]]+SUBSTRATE_OVERRIDE_' "${env_sh}"; then
+    log "env.sh contains SUBSTRATE_OVERRIDE_* exports:"
+    sed -n '1,120p' "${env_sh}" >&2 || true
+    fatal "env.sh must not export SUBSTRATE_OVERRIDE_* by default (WCU4)"
+  fi
+  if env -i PATH="${HOST_PATH}" bash -lc "source \"${env_sh}\"; env | grep -q '^SUBSTRATE_OVERRIDE_'" >/dev/null 2>&1; then
+    fatal "Sourcing env.sh must not export SUBSTRATE_OVERRIDE_* (WCU4)"
+  fi
+  log "Verified env.sh does not export SUBSTRATE_OVERRIDE_* (${env_sh})"
+}
+
 run_health_smoke() {
   local substrate_bin="${PREFIX}/bin/substrate"
   local output
@@ -794,14 +811,15 @@
 prepare_stub_bin
 reset_systemctl_log
 
-if [[ "${SCENARIO_KIND}" == "prod" ]]; then
-  build_fake_release
-  run_prod_install
-  install_systemctl_log="$(capture_systemctl_log install)"
-  assert_config_init_hint_logged
-  assert_install_config
-  assert_manifest_present
-  run_health_smoke
+  if [[ "${SCENARIO_KIND}" == "prod" ]]; then
+    build_fake_release
+    run_prod_install
+    assert_env_sh_has_no_override_exports
+    install_systemctl_log="$(capture_systemctl_log install)"
+    assert_config_init_hint_logged
+    assert_install_config
+    assert_manifest_present
+    run_health_smoke
   if [[ "${SCENARIO_WORLD_ENABLED}" -eq 1 ]]; then
     assert_systemctl_log "install" "${install_systemctl_log}" 1
   else
@@ -810,13 +828,14 @@
   run_prod_uninstall
   uninstall_systemctl_log="$(capture_systemctl_log uninstall)"
   assert_systemctl_log "uninstall" "${uninstall_systemctl_log}" 1
-elif [[ "${SCENARIO_KIND}" == "dev" ]]; then
-  run_dev_install
-  install_systemctl_log="$(capture_systemctl_log install)"
-  assert_systemctl_log "install" "${install_systemctl_log}" 1
-else
-  fatal "Unsupported scenario kind: ${SCENARIO_KIND}"
-fi
+  elif [[ "${SCENARIO_KIND}" == "dev" ]]; then
+    run_dev_install
+    assert_env_sh_has_no_override_exports
+    install_systemctl_log="$(capture_systemctl_log install)"
+    assert_systemctl_log "install" "${install_systemctl_log}" 1
+  else
+    fatal "Unsupported scenario kind: ${SCENARIO_KIND}"
+  fi
 
 log "Scenario ${SCENARIO} completed using PREFIX=${PREFIX}"
 log "Temp root: ${WORK_ROOT}"
diff --git a/tests/installers/install_state_smoke.sh b/tests/installers/install_state_smoke.sh
index 88cecedffc5a6cbba88a97c65c1dea1f9dcb12b5..e94e9017e822b20b2e6dfba5a894e29228c877bc
--- a/tests/installers/install_state_smoke.sh
+++ b/tests/installers/install_state_smoke.sh
@@ -669,6 +669,7 @@
       fatal "metadata install failed (see ${install_log})"
     }
 
+  assert_env_sh_has_no_override_exports "${prefix}" "${HOST_PATH}"
   assert_metadata_file "${state_path}" "${fake_user}" "true" "true" "no"
 
   # Simulate legacy metadata and rerun install to ensure upgrade path rewrites it.
@@ -698,6 +699,7 @@
       fatal "metadata upgrade install failed (see ${install_log})"
     }
 
+  assert_env_sh_has_no_override_exports "${prefix}" "${HOST_PATH}"
   assert_metadata_upgraded "${state_path}" "${fake_user}"
 
   log "Metadata scenario completed (state: ${state_path})"
@@ -786,6 +788,24 @@
   log "Cleanup scenario completed (state: ${state_path})"
 )
 
+assert_env_sh_has_no_override_exports() {
+  local prefix="$1"
+  local host_path="$2"
+  local env_sh="${prefix}/env.sh"
+  if [[ ! -f "${env_sh}" ]]; then
+    fatal "env.sh missing after install: ${env_sh}"
+  fi
+  if grep -Eq '^export[[:space:]]+SUBSTRATE_OVERRIDE_' "${env_sh}"; then
+    log "env.sh contains SUBSTRATE_OVERRIDE_* exports:"
+    sed -n '1,120p' "${env_sh}" >&2 || true
+    fatal "env.sh must not export SUBSTRATE_OVERRIDE_* by default (WCU4)"
+  fi
+  if env -i PATH="${host_path}" bash -lc "source \"${env_sh}\"; env | grep -q '^SUBSTRATE_OVERRIDE_'" >/dev/null 2>&1; then
+    fatal "Sourcing env.sh must not export SUBSTRATE_OVERRIDE_* (WCU4)"
+  fi
+  log "Verified env.sh does not export SUBSTRATE_OVERRIDE_* (${env_sh})"
+}
+
 run_missing_metadata_scenario() (
   local work_root
   work_root="$(mktemp -d "/tmp/substrate-install-state.missing.XXXXXX")"

tokens used
91,744
