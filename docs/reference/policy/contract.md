# Policy Contract

This file is scaffolding: it will define the stable policy contract:
- policy file locations (global vs workspace),
- policy mode semantics (disabled/observe/enforce),
- strict parsing expectations,
- and how policy interacts with world isolation and filesystem mode.

Existing related docs:
- `docs/BROKER.md`
- `docs/WORLD.md`

