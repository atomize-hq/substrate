# Filesystem Layout Contract

This file is scaffolding: it will enumerate stable filesystem paths that Substrate reads/writes.

Suggested sections:
- Global home (`$SUBSTRATE_HOME`) and required files
- Workspace files under `.substrate/` and `.substrate-git/`
- Trace/log outputs and rotation settings

Existing related docs:
- `docs/TRACE.md`
- `docs/INSTALLATION.md`

