# CLI Reference

This directory is scaffolding: it is intended to hold stable, user-facing CLI contracts.

Suggested contents:
- Command inventory and subcommands (high-level)
- Flag/arg contracts (names, defaults, conflicts)
- Exit codes and error class expectations

Existing related docs (top-level):
- `docs/COMMANDS.md`
- `docs/USAGE.md`

