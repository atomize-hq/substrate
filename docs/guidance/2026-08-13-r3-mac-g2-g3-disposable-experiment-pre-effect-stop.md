# R3 macOS G2/G3 disposable experiment pre-effect stop

**Task:** `R3-MAC-RETIRE-G2-G3-DISPOSABLE-EXPERIMENT`
**Date:** 2026-08-13
**Status:** `G2 BLOCKED PRE-EFFECT`; `G3 BLOCKED`
**Native effects:** none

## Result

The accepted repository checkpoint was rebound cleanly at branch
`feat/internal-host-orchestrator-world-dispatch-bootstrap`, commit
`ae7230a98b5fcccebcf03fb124847dfd213c811b`, and tree
`4dff2d5cdec5bd7e8581c46d7d53af271703de5d`, with local HEAD, upstream, and
the fetched remote ref equal.

The G2 packet could not be frozen for execution. The controlling correction
requires the exact prospective-finalizer identity and exact planned
access-control posture, but intentionally leaves its literal executable,
root-launchd route, endpoint, code and source/build identities, caller
identity, and capability digest unaccepted. It also does not define the
canonical legacy `SecAccess` construction that would grant the finalizer
delete-only authority while denying sign, private export, ACL or
trusted-application mutation, other-key deletion, and target broadening.

Using the current uid-501 task process, `sudo`, setuid, a broad helper, a
product executable, or an invented disposable root route would not test the
exact prospective identity and is prohibited. The accepted G2/G3-before-G6
ordering does not itself authorize inventing the literal identity and trust
mechanism that G6 otherwise defers.

The stop occurred before any experiment source or executable was created and
before any Keychain, `security`, launchd, publisher, Lima, mirror, or product
state operation. Consequently:

| Conclusion | Result |
|---|---|
| Creator-recovery no-UI behavior | `NOT_RUN` |
| Prospective-finalizer capability | `NOT_RUN` |
| SecurityAgent during native effects | no native effect process ran |
| Disposable-scope restoration | `RESTORED_BY_NO_EFFECT` |
| Independent restoration review | `PASS_RESTORED_BY_NO_EFFECT` |
| G3 | `BLOCKED` |

The accepted contract specifies the high-level creator query-only and
fresh-process process-deny arms, but the packet is not fully executable for
that matrix either. It still must freeze how the process-deny arm handles the
prior interaction setting without placing a Security call before the required
first disable, and it must preclassify the wrong-identity result rather than
choosing an expected outcome after observation. The stop receipt's
`exact_creator_matrix_specifiable=true` records that the contract-level matrix
shape is specifiable; it does not claim those executable details were closed.
The combined task's mandatory stop-before-effects rule also bars running any
creator arm after the prospective-finalizer blocker. No result here is
evidence about the preserved signer's unknown saved ACL.

## Durable external receipts

External root:

`/Users/spensermcconnell/.codex/experiments/keychain-disposable/82126b24-a774-477c-b20d-7c2f7f0aea19`

| Artifact | SHA-256 |
|---|---|
| `experiment-packet.md` | `a8a59811472e6c285ce12515559ca70848485dbbd9a6c1cfb1788d0571024d9d` |
| `pre-effect-stop-receipt.json` | `08c26343e49c3698e689dcb81c4072085e730bd51595117f96cad33283647c0a` |
| `creator-recovery-receipt.json` | `15134c8472fd032853f43fa816c96f43192a1b08250cdfe10a14a1769863b28c` |
| `prospective-finalizer-receipt.json` | `a730696c4e24210fac9e3111e74379e5d3d3a10e0c91cb0239af363a19fd11a0` |
| `restoration-manifest.json` | `eb5e03fa9579247047101f6d75cfe05dc5360ea0e4999836855505757c584a8c` |
| `independent-restoration-review.json` | `3bcd7e87dfbad729c5eab27f8e3f7fa46fa79be5e7fd022c615fea22a18f24c6` |
| `artifact-set.json` | `3d7229fe41dfedb5afd9792e76c6b6df6a6f5ab97f932fd49f6bdad5d727f56d` |

The artifact-set wrapper covers exactly nineteen other regular files in the
external root and excludes only itself by definition. Independent review
recomputed every primary hash and size, found no source, executable, binary,
private key, credential material, or raw platform UUID, and confirmed the
repository remained clean and remote-equal.

The allocated but unused surrogate namespace has SHA-256
`2a9ff93e55fe4646ebba8e15401d16093960ff7c0583c95bcbe0546d13929b11`.
It had no exact controlling-document hit or preserved-denylist substring and
was never used in a query, tag, label, executable, code signature, service, or
native object.

## Preservation statement

The preservation proof is non-access rather than a prohibited re-query. The
action ledger records zero Keychain API, `security` CLI, `SecItem` mutation,
experiment build/sign/exec, launchctl, Lima, publisher, mirror, and product
state operations. No preserved signer, scope, prefix, failed-attempt root,
publisher object, Lima state, or mirror identity was passed to a native state
tool. Baseline and final bounded observations both found zero exact
`SecurityAgent` processes and zero on-screen windows owned by
`SecurityAgent`; these observations were outside any native effect because no
arm ran.

## Required next decision

Do not dispatch G4. The next permissible boundary is a bounded G2
packet-amendment decision that accepts:

1. exact surrogate finalizer source/build/bytes/path/physical identity,
   signing identifier, CDHash, full designated requirement, process identity,
   and no-sudo privilege launch and rollback model;
2. the exact relationship to the later G6 finalizer identity, including the
   drift rule that requires a fresh G2 run;
3. the complete signer creation dictionary and canonical `SecAccess` ACL,
   including every authorization, trusted-application requirement,
   delete-only grant, creator/sign policy, ACL-change policy, prompt selector,
   export policy, readback digest, and expected negative-control return;
4. the creator process-deny arm's exact prior-interaction-setting handling,
   while preserving the requirement that successful process denial is its
   first Security-framework interaction;
5. the wrong-identity arm's precommitted expected status classification and
   the ACL fact that supports it, without post-observation reclassification.

Only a newly complete packet that passes independent pre-effect review may
authorize the disposable arms. G4 remains downstream of a genuinely closed
G3 and is not authorized by this stop record.
