# Structured Objective Bug Map — RETIRED / CONSOLIDATED (2026-06-20)

This standalone diagnosis has been consolidated into the single canonical authority:

→ `docs/specs/r5/R5_75/MAP.md` → **R5.75-1 → Remaining Bug Ledger (Structured Objective)**

That ledger is the current, unified (`Issue 1`–`Issue 8`) list of remaining structured-objective
bugs, with their statuses (blocker / non-blocking follow-on / gated follow-on / landed-conditional),
primary code paths, evidence sessions, suggested fix order, and the held promotion gate.

Do not re-add bug content to this file — update the ledger in `MAP.md` instead. This file is kept
only as a redirect for anyone who follows an old link, and can be deleted.
