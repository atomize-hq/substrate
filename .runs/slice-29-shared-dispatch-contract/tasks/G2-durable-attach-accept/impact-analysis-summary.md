G2 acceptance preserved the L1 GitNexus impact evidence rather than rerunning new symbol edits on the parent branch.

- The highest-risk L1 seam remained `AgentRuntimeStateStore.resolve_public_control_target` with `HIGH` risk and `19` impacted symbols.
- The run stayed blocked until that result was escalated and explicit user direction authorized continuation within the frozen `A3` ownership boundary.
- The merged authoritative diff still centered on durable-state files; the only out-of-band path was a compile-only unit-test fixture update in `dispatch_contract.rs`, which did not reopen the shared dispatch semantics frozen at `G1`.
