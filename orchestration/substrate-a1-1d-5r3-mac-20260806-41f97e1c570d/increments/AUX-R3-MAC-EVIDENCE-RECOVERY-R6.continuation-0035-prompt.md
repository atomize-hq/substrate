R6 SAME-TASK CONTINUATION — CLOSED SESSION WIRING FENCE

Bind this continuation exactly:

- task_thread_id: 019fded7-05c4-7c61-9f7d-5fc456bcda6d
- task_host_id: local
- exact_worktree: /Users/spensermcconnell/.codex/worktrees/1bd8/substrate
- orchestration_id: substrate-a1-1d-5r3-mac-20260806-41f97e1c570d
- increment: AUX-R3-MAC-EVIDENCE-RECOVERY-R6
- original_dispatch_nonce: 1b9fe0a5eee31553d83daa75ccdde591ea5a4b797a81f14c3f3d3d839bc9cd70
- continuation_nonce: b718215391e000b0fb0a74927d8e8e224d659c8a42934dcec561a134a590020e
- expected_base_commit: a9626bc10a1737416c1d8f7f48630850118f3e6a
- expected_base_tree: 3f7cdf20125a847a3dfaa472d3797759b5cf6737
- live target at adjudication: origin refs/heads/feat/internal-host-orchestrator-world-dispatch-bootstrap = a9626bc10a1737416c1d8f7f48630850118f3e6a
- prior terminal cursor: 72bc6338-004b-44ac-9181-551e0e60ffb5:2
- authority amendment: authority-amendments/0035-r6-closed-session-wiring-fence.json
- authority amendment sha256: sha256:5d540701bcd947d3c565539a03cb0395ddcc5d2eda7bcff64664c717a17c849b
- task/subagent/reviewer model: gpt-5.6-terra Extra High (xhigh)
- continue_authorized: true

The clean pre-edit `BLOCKED_SCOPE_EXPANSION` receipt is accepted as a non-advancing same-packet
fence correction. The initial R6 contract already authorized the seven owning production paths and
required executable dual-session behavior. Amendment 0035 now names the omitted decoders, closed
tags, CLI/XPC/listener branches, fixed retained-limactl two-child session helpers, and Linux guest
entrypoint boundary. This is not a new transport, selector, principal, endpoint, or review cycle.

Before editing, reverify the exact clean HEAD/tree/index/live remote, complete skill digest, and
amendment digest. Then resume R6 in this same worktree.

Controlling interpretation:

1. Add exactly two internal R6 protocol tags:
   `guest_pairing_data_session` and `guest_pairing_operator_tty_session`. They are fixed protocol
   discriminants, not transport selectors. Only the hidden direct-interactive R6 control path may
   construct them after exact PM/source/artifact/record admission. R5 wrappers, scripts, ordinary
   mapped input, argv/environment, and caller JSON must reject them.
2. Wire those exact operations through the already-allowed shell request/client, fixed macOS XPC
   client relay, audit-before-decode listener, and protected host record issue/advance/consume/CAS
   path. No generic operation string or operation passthrough is allowed.
3. Implement exactly two distinct fixed retained-limactl child sessions joined to the same accepted
   PM instance and staged guest-executor digest. The data child's own stdio carries only typed
   ticket/hello/transcript/guest-anchor frames. The operator child has an independently controlling
   guest TTY and may receive only a bounded shared-binding start frame—not ticket, transcript, key,
   confirmation, path, command, or transport selection.
4. The retained host control terminal remains the sole display source for complete fingerprint,
   challenge ID, challenge, and literal. Guest confirmation is read only from the independent guest
   controlling TTY. Never alias data stdio or accept confirmation from argv, environment, file,
   pipe, frame, ticket, transcript, or automation.
5. Close the Linux guest boundary: R6 pairing may not accept caller-selected `--state-root`,
   `--tty-path`, `--transport`, `--ticket-file`, or `--transcript-file`; it derives fixed state and
   artifact identity from the signed binding and installed layout. The old generic begin/commit
   pairing route must be rejected or unreachable without the exact PM-bound R6 binding. Other Linux
   commands and all Windows paths remain byte/behavior identical.
6. Fixed retained `limactl shell` child invocation is the already-approved same-PM SSH transport
   implementation. It does not authorize direct SSH, VSock, TCP, alternate socket, caller-selected
   instance/tool/command, generic broker, third session, or native execution in this source task.
7. Use the original unused R6 causal review budget. Complete TDD, MAC-only deterministic proof,
   three-lens discovery, different closure, permitted causal supplementation only, and the final
   combined R1-R6 audit. If combined review finds a valid P1/P2 outside the amended R6 fence, stop
   and return it rather than editing R1-R5.

Do not run installation, Keychain, XPC, Lima, code-signing, live pairing, native evidence, or any
Windows/Linux cross-target gate. Do not start `EVIDENCE:R3-MAC-IMP-01`. Preserve this task/worktree
on every stop. The terminal receipt send to meta remains your final tool action.
