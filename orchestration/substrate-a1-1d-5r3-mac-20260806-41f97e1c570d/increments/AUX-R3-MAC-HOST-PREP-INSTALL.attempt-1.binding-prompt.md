GENERAL MACOS HOST-PREPARATION TASK IDENTITY BINDING AND START AUTHORITY

Bind this exact identity before taking any action:

- thread ID: `019fd9ed-4067-7922-8034-da8abbc8717e`
- host ID: `local`
- exact assigned worktree: `/Users/spensermcconnell/.codex/worktrees/abc3/substrate`
- dispatch nonce: `5fa439de8a75a37f6c31b4cc7f758fc8e0d556f86e840d7bd7d48297c3f12e42`
- return meta task: `019fd8c3-b12c-7e73-919f-898600ab0f64` on `local`
- model/reasoning: `gpt-5.6-terra` / Extra High (`xhigh`)
- exact HEAD: `d8a65fc8890dd37584aaeac2984c906188e5f06e`
- exact tree: `d8f25cc993f264f0c67fe3e00180eb393e41b2e3`
- live target: `origin` `refs/heads/feat/internal-host-orchestrator-world-dispatch-bootstrap`
- project: `local-b2016f8a311fef93149e42eaec4d704d` at protected saved path `/Users/spensermcconnell/__Active_Code/atomize-hq/substrate`
- hydrated skill: `/Users/spensermcconnell/.codex/worktrees/abc3/substrate/.agents/skills/orchestrate-top-level-tasks`
- hydration file count/digest: `26` / `61d99190f680d3738fa824348e671e8eb84b2729bf31952ef16031c6ba24d903`

Independent pre-binding checks confirmed the assigned worktree is clean at the exact published
commit/tree and skill hydration verifies. Start authority is now explicit. Load `AGENTS.md` and the
hydrated skill, reverify every bound fact and the live remote before mutation, then execute the full
contract below. Do not ask the user to operate terminals. End only with a structured auxiliary
receipt sent to the return meta task as your final tool action.

---

# General macOS Substrate host preparation and complete install

## Initialization barrier

You are a fresh general top-level macOS host-preparation/install task. You are not an official R3
increment, evidence task, closeout task, or subagent. Do not perform any host, guest, repository,
or installation mutation until a follow-up binds your real task thread ID, host ID, exact assigned
worktree, dispatch nonce, exact published commit/tree/ref, and start authority.

Do not modify the protected saved checkout:
`/Users/spensermcconnell/__Active_Code/atomize-hq/substrate`.

## Objective after identity binding

Prepare this macOS host completely for a later fresh official `EVIDENCE:R3-MAC-IMP-01` run and
perform a complete Substrate macOS/Lima installation from the exact published source. Remove all
safe Substrate-owned permission, ownership, ACL, install, publisher, LaunchDaemon, helper, Lima,
guest-service, socket, mapping, and readiness blockers. Use the repository's canonical install and
macOS lifecycle scripts rather than inventing a parallel installer. Leave the successful installed
state in place for the later official evidence task.

This task is deliberately general and auxiliary. It is not tied to an official packet ID and must
not create or claim native R3 evidence.

## Exact source and host

- project ID: `local-b2016f8a311fef93149e42eaec4d704d`
- project host: `local`
- protected saved project: `/Users/spensermcconnell/__Active_Code/atomize-hq/substrate`
- remote: `origin`
- target ref: `refs/heads/feat/internal-host-orchestrator-world-dispatch-bootstrap`
- published commit: `d8a65fc8890dd37584aaeac2984c906188e5f06e`
- published tree: `d8f25cc993f264f0c67fe3e00180eb393e41b2e3`
- selected Lima instance: `substrate`
- canonical host prefix: `/Users/spensermcconnell/.substrate`
- macOS user/UID: `spensermcconnell` / `501`
- return meta task: `019fd8c3-b12c-7e73-919f-898600ab0f64` on host `local`
- orchestration context: `substrate-a1-1d-5r3-mac-20260806-41f97e1c570d`
- dispatch nonce: `5fa439de8a75a37f6c31b4cc7f758fc8e0d556f86e840d7bd7d48297c3f12e42`

## Authority after binding

You may:

1. Read the exact published repository truth and derive the canonical install sequence.
2. Inventory and hash/record relevant pre-state before mutation.
3. Use host `sudo -n` and guest `limactl shell substrate sudo -n`.
4. Manually correct safe Substrate-owned ownership, permissions, ACLs, and modes. This explicitly
   includes making `/Users/spensermcconnell/.substrate` owner `501` and mode `0700`, provided the
   object identity and ownership are unambiguous and its contents are preserved.
5. Build and install from the exact published source using the repository's canonical installer,
   `scripts/mac` lifecycle/warm/doctor/smoke surfaces, and any prerequisite repository scripts they
   legitimately invoke.
6. Install/update Substrate-owned binaries, publisher helper, LaunchDaemon, Lima guest artifacts,
   units, sockets, known-host state, and mapping-bound lifecycle state.
7. Start/stop/restart/reload only Substrate-owned services and the selected `substrate` Lima VM.
8. Run all non-evidence install, doctor, permission, service, mapping, and smoke/readiness checks
   necessary to establish that a fresh official evidence task can begin without further user or
   system preparation.

Complete all safe independent preparation even if a separate published-source defect remains.
Do not stop after merely reporting the already-known `0755` prefix; repair it under this explicit
authority, verify exact owner-only `0700`, and continue.

## Hard boundaries

- Do not edit, stage, commit, or push product/control/review files.
- Do not mutate the protected saved checkout. Work only from your task-assigned worktree and
  external host/guest installation state.
- Do not run or claim official `EVIDENCE:R3-MAC-IMP-01`; do not create its artifact or receipt.
- Do not fabricate, bypass, or weaken validator, project-identity, private-home, mapping,
  code-signing, XPC, pairing-ticket, or security checks.
- Do not patch installed binaries or source locally to conceal a published-source defect.
- Do not delete unrelated data, unrelated Lima instances, unrelated services, or unrelated
  Keychain items.
- The existing `/etc/sudoers.d/substrate-r3-mac-evidence` file is an immutable pre-existing
  sentinel for this task. You may use the sudo authority it provides but must not edit or remove it.
- If an operation would destroy ambiguous/pre-existing user state, preserve it and return a precise
  blocker rather than guessing.

## Known blockers to adjudicate honestly

At dispatch time, independent verification found:

1. `/Users/spensermcconnell/.substrate` is owner `501` but mode `0755`; this task is explicitly
   authorized to repair the Substrate-owned root to `0700` and validate ACL/ownership requirements.
2. `scripts/ci/validate_r3_native_evidence.py` in the published source hard-codes historical remote
   project ID `2ccb802f-301c-4af4-9bd5-51d22808f0a2`, while the actual local project ID is
   `local-b2016f8a311fef93149e42eaec4d704d`. Do not alter or bypass it; report whether this remains a
   source/control blocker after all host prep.
3. `issue_lima_guest_pairing_ticket_v1` in the published source appeared to return an unconditional
   error after request validation. Independently verify the exact published implementation. Do not
   claim evidence readiness if signed guest-ticket issuance is not genuinely implemented.

## Required workflow

1. After binding, verify task identity, exact worktree, exact HEAD/tree, live remote equality, clean
   tracked source, protected-checkout cleanliness, skill hydration, and sudoers sentinel hash.
2. Read `AGENTS.md`, the hydrated repository-local orchestration skill, install/macOS documentation,
   installer entry points, `scripts/mac` lifecycle scripts, and the R3 MAC/evidence contracts.
3. Produce and retain a pre-state manifest outside the repository before mutation.
4. Determine the canonical full install path from current source. Use script help/source and docs;
   do not guess flags or substitute an ad-hoc installation.
5. Repair safe Substrate-owned host/guest permission and ownership defects.
6. Run the complete canonical install and all prerequisite builds/setup it requires.
7. Validate installed binary identity, private-home contract, publisher/LaunchDaemon/helper state,
   Lima mapping, guest artifacts/units/socket/service state, host forwarding/known-host state,
   doctor/health/smoke readiness, host/guest sudo availability, and source/sentinel parity.
8. Preserve an exact post-state manifest and command/result log outside the repository.
9. Return a structured `codex.auxiliary-host-prep-receipt.v1` receipt to the meta task as your last
   action. Status is `HOST_READY` only if no further host/system preparation or published-source
   correction is required before official evidence. Otherwise use `BLOCKED_SOURCE_CORRECTION` or
   `BLOCKED_SYSTEM_PREPARATION`, enumerate completed preparation, remaining blockers, exact artifact
   paths/digests, and a continuation handoff.

Do not ask the user to perform terminal operations. You are the authorized operator for this task.
