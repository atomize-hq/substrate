# Continuation — fixed executor-owned MAC install sequence

Continue `AUX-R3-MAC-PRESTAGE-EVIDENCE-BLOCKER-CORRECTION` in the preserved task/worktree only.

Authority amendment: `authority-amendments/0047-r3-mac-fixed-install-sequence.json`
SHA-256: `sha256:6bd998d38beb552233b6360ac7c02c482a7c2c096ea6c6bbb6e09ba5f6c222d4`
Continuation nonce: `543448d57169010ad5c3537c650d6d1d8d8510b05c91d45efa1cdb7f40e415a6`

The user explicitly authorizes the privileged macOS executor to derive and execute one fixed install-only post-PM sequence from the signed Stage-1 successor manifest. The caller supplies no selector. Never execute the exhaustive `post_pm_requests_v1` catalogue as a sequence. The install plan may use only the fixed forward Create effects and the existing required Enable/Start socket/service transitions; it must never Stop, Remove, Restore, or perform generic repair. Exact retries replay durable receipts and resume only the next unambiguous step.

Rebind exact HEAD/tree/remote and the preserved RED test hash before editing. Continue the already-authorized four-binary prestage correction; do not build another binary or revive the `world` artifact role. Stay inside the 0046 path/symbol fence. Use repository-local skills, TDD, deterministic locked/offline MAC/AArch64 proof, and the required fresh causal review/closure process. Publish exactly one normal fast-forward commit only if terminal CLEAN.

If a genuine new authority/security/platform contradiction remains, pause with a precise blocked receipt. Do not run native evidence or dispatch a successor.
