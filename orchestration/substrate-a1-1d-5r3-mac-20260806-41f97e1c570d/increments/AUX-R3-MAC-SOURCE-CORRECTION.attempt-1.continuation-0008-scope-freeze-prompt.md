R3 MAC SOURCE-CORRECTION SCOPE FREEZE

Apply amendment `0008-r3-mac-source-correction-scope-freeze` immediately to task
`019fd9fe-670c-7253-bf60-86f78cd373b5` in preserved worktree
`/Users/spensermcconnell/.codex/worktrees/60cc/substrate`.

The user explicitly approved this scope freeze after live inspection of your task transcript,
current diff, the runtime-refactor control pack, and `macos-hardening/`.

1. Let the already-running closure reviewer finish read-only.
2. Freeze every further remediation cycle, editing subagent, production edit, native mutation,
   staging, commit, push, installation, evidence run, and successor dispatch.
3. Preserve the worktree exactly. Do not reset, clean, checkout, reconstruct, archive, or discard.
4. Produce a complete path-and-symbol scope ledger mapping every changed path and materially
   changed production symbol to one of the four original blockers or an explicit amendment-0007
   surface.
5. Identify every Windows change and prove it is compile-only with zero behavior change. Identify
   every Linux change as either exact Lima guest-executor work or backward-compatible shared
   adaptation with zero ordinary Linux host behavior change.
6. Enumerate every new single-stdio-channel, one-child-channel, alternate-channel, TTY,
   operator-confirmation, transport, selector, endpoint, and principal assumption.
7. The single-stdio-channel design is unapproved and is not authority. `no alternate transport`,
   `one canonical transport`, and `single broker endpoint` do not themselves prohibit logically
   distinct channels or sessions joined to the same PM-bound SSH transport.
8. This is not authority to implement a second channel. If the independent guest controlling-TTY
   requirement needs a transport/channel decision, stop rather than adding another architecture
   layer.
9. After the reviewer and ledger, you may only make surgical exact-hunk restorations/deletions that
   reduce scope. Do not add paths, increase production insertions, or widen the changed-symbol set.
10. Return a non-advancing `BLOCKED_SCOPE_EXPANSION` report/receipt with the reduced exact diff,
    ledger, review findings, completed tests, and precise remaining transport/TTY decision.

Observed freeze snapshot at `2026-08-07T04:09:18Z`:

- base `d8a65fc8890dd37584aaeac2984c906188e5f06e`
- tree `d8f25cc993f264f0c67fe3e00180eb393e41b2e3`
- tracked diff SHA-256 `39682fd4415a00ae4099580135a687d3e11858f62f84e9ef6ff3771bafd4b54c`
- 26 tracked changed paths, 28 total status entries, 0 staged paths
- tracked diff `+8821/-681`

Publication remains prohibited until a later explicit meta continuation accepts the reduced scope.
