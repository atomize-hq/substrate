META IDENTITY BINDING AND FRESH MAC AUTHORITY

Bind this exact task identity and dispatch before any work:

- increment_task_thread_id: 019fd97a-3740-7261-a7e8-0008845709b5
- increment_task_host_id: local
- exact_task_worktree: /Users/spensermcconnell/.codex/worktrees/a943/substrate
- orchestration_id: substrate-a1-1d-5r3-mac-20260806-41f97e1c570d
- dispatch_attempt: 4
- dispatch_nonce: 84392e6f217153abafdd033bf2b503ea9f901949c4ff34345ad93a2c06632faf
- authority_amendment: 0003-fresh-mac-review-epoch.json
- contract_sha256: sha256:d4097c22defcee29cd334109ce7d264c10b86cbaa1ec6ee7cea407ce14c06f9b
- rendered_prompt_sha256: sha256:62dcfdc072f909c136a8858e369dde2e2fe7c1f53ab50f034da1fff7106e2aa4
- hydrated_skill_path: /Users/spensermcconnell/.codex/worktrees/a943/substrate/.agents/skills/orchestrate-top-level-tasks
- hydrated_skill_sha256: 61d99190f680d3738fa824348e671e8eb84b2729bf31952ef16031c6ba24d903
- model/reasoning: gpt-5.6-terra / xhigh
- dispatch_authorized: true

The worktree was independently verified clean at commit
`270f6e55e1a94b7e2f9b2667e605980d2e50579c`, tree
`acef6844c15d17aba8cfd18d2fc7ef88d9c3420b`, before skill hydration. The hydrated skill was
verified byte-for-byte and is ignored by Git; product status remained clean.

The recovered `/Users/spensermcconnell/.codex/worktrees/ace6/substrate` worktree is an immutable
read-only donor. Never modify, archive, clean, reset, stage, commit, or publish from it.

You are explicitly authorized to begin only A1.1d-5R3-MAC under the following complete rendered
prompt. Carry all six known P1/P2 findings as mandatory remediation inputs and use the new bounded
review epoch. Do not begin native evidence or any successor.

--- BEGIN COMPLETE RENDERED PROMPT ---

Use the repository-local $orchestrate-top-level-tasks skill and every skill required by the
increment contract. Never use or install a global copy. This initial prompt is self-contained;
before identity binding, the meta orchestrator must hydrate and verify the skill at
`.agents/skills/orchestrate-top-level-tasks` in the assigned worktree. If the bound follow-up
arrives while that local skill is absent or its persisted digest does not match, stop with
BLOCKED_CONTRADICTION before any work.

ROLE

You are the fresh top-level increment orchestrator for A1.1d-5R3-MAC. You own this increment only.
You may use subagents for GitNexus/source analysis, bounded implementation, test/proof execution,
and independent review. You remain responsible for scope, shared-worktree integration,
verification, publication, and the terminal receipt.

DISPATCH IDENTITY

- orchestration_id: substrate-a1-1d-5r3-mac-20260806-41f97e1c570d
- dispatch_nonce: 84392e6f217153abafdd033bf2b503ea9f901949c4ff34345ad93a2c06632faf
- meta_thread_id: 019fd8c3-b12c-7e73-919f-898600ab0f64
- meta_host_id: local
- increment: A1.1d-5R3-MAC
- packet_id: A1.1d-5R3-MAC
- next_increment: EVIDENCE:R3-MAC-IMP-01

INCREMENT-TASK IDENTITY BARRIER

Do not edit, delegate, run implementation checks, or publish until the meta orchestrator sends a
follow-up binding your own real increment-task thread ID and host ID to this dispatch nonce. Echo
those exact IDs in every terminal receipt.

REPOSITORY BOUNDARY

Work only in the task-assigned checkout:

/Users/spensermcconnell/.codex/worktrees/a943/substrate

Never mutate these protected checkouts:

- /Users/spensermcconnell/__Active_Code/atomize-hq/substrate
- /Users/spensermcconnell/.codex/worktrees/eb49/substrate
- /Users/spensermcconnell/.codex/worktrees/ace6/substrate
- /Users/spensermcconnell/.codex/worktrees/867b/substrate

Canonical starting state:

- publication mode: declared by the increment contract
- remote: origin
- target ref: refs/heads/feat/internal-host-orchestrator-world-dispatch-bootstrap
- expected base commit: 270f6e55e1a94b7e2f9b2667e605980d2e50579c
- expected base tree: acef6844c15d17aba8cfd18d2fc7ef88d9c3420b
- required ancestor: 270f6e55e1a94b7e2f9b2667e605980d2e50579c

Before editing, query the publication-mode authority: the live remote for `remote`, or the exact
local branch ref and worktree for `local`. Verify the exact base, tree, ancestry, cleanliness, and
any required index state. If they differ, send BASE_DRIFT or BLOCKED_CONTRADICTION. Do not
reconcile, merge, rebase, reset, clean, or force-push.

SUBAGENT ORCHESTRATION

Use repository-required model/reasoning settings for every subagent. Complete required pre-edit
impact analysis before any subagent edits an existing symbol. Give editing subagents mutually
exclusive ownership when practical. Use fresh read-only subagents for independent review. Do not
allow a reviewer to review implementation it authored.

INCREMENT CONTRACT

# A1.1d-5R3-MAC dispatch contract

## Authority and predecessor

- This fresh attempt owns exactly `A1.1d-5R3-MAC` in the orchestration `substrate-a1-1d-5r3-mac-20260806-41f97e1c570d`.
- The exact remote-equal predecessor is commit `270f6e55e1a94b7e2f9b2667e605980d2e50579c`, tree `acef6844c15d17aba8cfd18d2fc7ef88d9c3420b`, on `refs/heads/feat/internal-host-orchestrator-world-dispatch-bootstrap`.
- `A1.1d-5R3-LINUX-CLOSEOUT` is immutable predecessor truth only. Do not reinterpret, repair, or resume its finished orchestration.
- The only clean successor is `EVIDENCE:R3-MAC-IMP-01`. Do not create, run, or claim native evidence, MAC closeout, Windows, Unix, or final native-gate work.
- The protected saved checkout `/Users/spensermcconnell/__Active_Code/atomize-hq/substrate` and meta worktree `/Users/spensermcconnell/.codex/worktrees/eb49/substrate` are read-only to this task.

## Current-authority reconciliation

The fresh user authority for this corridor supersedes only two stale historical wordings at the exact base:

1. stale Linux-closeout status prose that made the earlier orchestration terminal; for this fresh hierarchy, current corridor truth is `A1.1d-5R3-MAC -> EVIDENCE:R3-MAC-IMP-01 -> A1.1d-5R3-MAC-CLOSEOUT -> AUTHORITY_REQUIRED:A1.1d-5R3-WIN`;
2. stale packet-local wording requiring automatic remediation of all `P1` through `P4`; the controlling causal process remediates valid `P1`/`P2` only, while valid unfixed `P3`/`P4` are recorded or deduplicated in `06-review-finding-inventory.md` and do not create remediation or review cycles.

No other authority, scope, file fence, product claim, platform gate, or predecessor evidence is reinterpreted.

## Meta-adjudicated GitNexus refresh preflight

- Authority record: `orchestration/substrate-a1-1d-5r3-mac-20260806-41f97e1c570d/authority-amendments/0002-gitnexus-context-restoration.json`.
- If a required `npx gitnexus analyze` changes repository bytes, stop unless the complete diff is exactly `AGENTS.md` and `CLAUDE.md` and every changed hunk is confined to their generated `gitnexus:start`/`gitnexus:end` blocks.
- Only in that exact case, restore those two files byte-for-byte from expected base `270f6e55e1a94b7e2f9b2667e605980d2e50579c` using explicit `git show <base>:<path>` output written to each file. Do not use `git reset`, `git checkout`, `git restore`, `git clean`, or `git stash`.
- Verify both restored file blobs equal the expected-base blobs, `git status` is clean, and the refreshed external GitNexus index is usable before any product edit. Never commit the generated context diff. Any other changed path/hunk or failed byte restoration is `BLOCKED_CONTRADICTION`. This is a preflight-only exception and does not widen the implementation allowlist.
- Before sending any terminal receipt, write it to a temporary file and validate it with the orchestration skill's `validate_receipt.py`; use exact field `protocol`, nested `increment_task`, nested `expected_base`, and nested `blocker` for blocked status. Do not send a merely similar custom shape.


## Fresh-authority remediation and review epoch (amendment 0003)

This attempt runs under `authority-amendments/0003-fresh-mac-review-epoch.json`. It is a new
nonce-bound top-level task and a new bounded review epoch because authority and risk changed. The
prior four-cycle `bounded_stop/budget_exhausted` record is immutable predecessor evidence; do not
append a fifth cycle or treat a new task alone as a budget reset.

Before editing, verify the restored donor `/Users/spensermcconnell/.codex/worktrees/ace6/substrate`
read-only against `receipts/A1.1d-5R3-MAC.attempt-3.recovery-verification.json`: exact base/tree,
19-path status, recovered-tree digest `sha256:a023bc6e2f4929277468f5f418e954b0d7b96671d2d8c24f8790a9674763ff88`,
historical subject `sha256:e60963026aa39be67e334119ff7ae61f3f15f19dbfd31539d790f8735b493924`,
and review digest `sha256:bc7258fb432375e50af899f14691c7691980d4f99a8174b8e2d6ee2e1a29e003`.
Never modify, clean, reset, stage, commit, publish, or archive the donor. After required impact
analysis, reconstruct only its exact 19 allowed changed paths in the fresh task worktree as an
unreviewed remediation baseline, then resolve all six mandatory findings from amendment 0003.

The amended fence permits new private MAC-local helpers and bounded call-site/test adjustments only
inside the amendment's authorized surfaces and only when directly necessary for the six findings.
Actual accepted XPC connection peer/audit-token binding, canonical carrier/mapping/role/Stage-1
validation before every mutation, and race-free owned SSH-UDS activation are explicitly authorized.
`new_with_mapping`, other selector families, VSock/TCP, unrelated architectures, native evidence,
and non-MAC packets remain frozen. A required path outside the amendment is
`BLOCKED_SCOPE_EXPANSION`.

After remediation and packet checks, replace the canonical MAC review-cycle record with a new
schema-valid v1 record that cites this amendment and preserves the prior record digest in the
review prose/receipt. Run one fresh discovery review or same-subject burst, then a different fresh
closure review if remediation follows. At most two supplemental causal cycles are available only
for P1/P2 directly caused or unmasked by the immediately preceding remediation under unchanged
amended authority/risk. CLEAN ends the loop. P3/P4 are inventory-only.

## Common R3 packet envelope

- Canonical `R3-DOCS` is exactly `llm-last-mile/runtime-refactor/00-README.md`, `02-seam-crosswalk.md`, `03-phase-slice-map.md`, `04-contracts-and-gates.md`, and `05-debug-regression-ledger.md`. `01-target-architecture.md` is frozen. Append only this packet's bounded status; do not rewrite earlier rows, proof, or architecture.
- The exact `mac` review set is `llm-last-mile/runtime-refactor/review-control/r3-mac-review-cycle-record.json`, `r3-mac-review-authority-security.md`, `r3-mac-review-lifecycle-convergence.md`, and `r3-mac-review-allowlist-evidence.md` in that same directory.
- Before any edit to an existing function/method/symbol, run upstream GitNexus impact with tests for the exact symbol/file, report direct callers, processes/modules, and risk, and manually close script/cfg/generated/platform callers. New symbols record `N/A (new)` plus impact on the existing caller/constructor they feed or replace.
- HIGH/CRITICAL is a warning-and-review checkpoint, not automatic scope expansion. The meta authority permits bounded file-fence and manual-risk reconciliation only when it remains strictly inside this MAC packet. A new authority domain, selector, execution family, destructive primitive, edit to `new_with_mapping`, or new selector/caller is `BLOCKED_SCOPE_EXPANSION`.
- Run `gitnexus_detect_changes()` before commit and stop on any unexpected production symbol, flow, file, dependency, generated artifact, or fence escape.
- Explicit user amendment `substrate-a1-1d-5r3-mac-20260806-41f97e1c570d-model-0001` requires the increment orchestrator and every implementation/review subagent to use model `gpt-5.6-terra` with Extra High (`xhigh`) reasoning. This supersedes only the earlier `gpt-5.4` model requirement for this MAC dispatch. Give editing subagents disjoint ownership where practical. Use fresh read-only reviewers that did not author the implementation.
- Freeze the selected outcome, contracts, allowlists, fingerprint method, lenses, and budget before implementation. Run one discovery review or same-subject burst, consolidate every valid `P1`/`P2`, then one different-fresh delta-focused closure review. Allow at most two supplemental causal cycles only for `P1`/`P2` directly caused or unmasked by the immediately preceding remediation under unchanged authority/risk and after `validate_review_cycle.py --next-cycle` preflight. `CLEAN` ends the loop. Inventory valid unfixed `P3`/`P4`; do not fix them automatically or extend the loop for them.
- Validate the review-cycle record after every returned cycle with `llm-last-mile/runtime-refactor/review-control/validate_review_cycle.py`.
- Before publication, run the exact packet checks, path/symbol/test allowlist comparison, `git diff --check`, staged secret/credential review, change detection, and live remote/base/ancestry check. Publish exactly one product/test/review commit by normal fast-forward push; never force-push, merge, rebase, reset, or clean.
- The terminal receipt must be `codex.top-level-task-receipt.v1`, status `LANDED_CLEAN`, exact changed paths/tree/review digest/subject fingerprint, live remote equality, clean checkout and `0/0`, with `next_increment` exactly `EVIDENCE:R3-MAC-IMP-01`.

## Canonical packet contract from `03-phase-slice-map.md`

### `A1.1d-5R3-MAC`

**Completion claim.** Implement manifest-bound Lima staging/teardown and activate only the
already-fixed PM-bound SSH-UDS path after exact unlink/timeout/drop/retry ownership. Predecessor:
`LINUX-CLOSEOUT`; successor: `EVIDENCE:R3-MAC-IMP-01`.

- Ownership: PI-051, PI-098, PI-099, PI-100, PI-101, PI-113, PI-114,
  `A1D5I-INSTALL-08`, `R3-ACT-MAC-012`, and `R3-ACT-MAC-026`.
- Production allowlist: new typed executor
  `src/bin/substrate-lifecycle-macos.rs` symbols `main`,
  `MacManagedArtifactExecutorV1`, `open_mac_lifecycle_capsule_v1`,
  `join_mac_role_identity_v1`, `execute_mac_managed_action_v1`,
  `restore_mac_managed_role_v1`, `publish_mac_action_receipt_v1`,
  `MacLifecyclePublisherServiceV1`, `open_system_keychain_protected_state_v1`, and
  `compare_and_swap_mac_publisher_protected_state_v1`, `bootstrap_mac_publisher_v1`,
  `export_mac_p256_spki_der_v1`, `normalize_mac_p256_signature_p1363_low_s_v1`,
  `resume_mac_publisher_bootstrap_v1`, `run_mac_xpc_publisher_v1`,
  `accept_mac_xpc_connection_v1`, `attest_mac_xpc_audit_token_v1`,
  `verify_mac_control_designated_requirement_v1`, `handle_mac_publisher_request_v1`, and
  `issue_lima_guest_pairing_ticket_v1`, `consume_lima_guest_pairing_ticket_v1`,
  `open_mac_guest_pairing_record_v1`, `compare_and_swap_mac_guest_pairing_record_v1`,
  `prove_lima_guest_reservation_unused_v1`,
  `commit_lima_guest_reservation_unused_acknowledgement_v1`,
  `retire_mac_test_publisher_v1`, `publish_lima_stage_one_intent_v1`,
  `attach_lima_stage_one_machine_identity_v1`, and `close_lima_stage_one_intent_v1`; its
  unsafe/FFI fence is exactly
  `mac_xpc_listener_ffi_v1`, `mac_audit_token_ffi_v1`, `mac_security_key_ffi_v1`,
  `mac_keychain_anchor_ffi_v1`, and `mac_atomic_file_ffi_v1`, with link declarations only for
  `Security`, `CoreFoundation`, and the system XPC library; new
  `crates/shell/src/execution/managed_lifecycle/macos_client.rs` replacement symbols
  `bootstrap_publisher_v1`, `submit_publisher_request_v1`, `open_mac_xpc_channel_v1`, and
  `attest_mac_publisher_response_v1`, `issue_guest_publisher_pairing_ticket_v1`; no other
  control/core symbol may change; new fixed plist
  `scripts/mac/com.substrate.lifecycle.publisher.v1.plist`; new
  `scripts/mac/lima-lifecycle.sh` functions `load_mapped_lifecycle_v1`,
  `invoke_mac_lifecycle_executor`, `install_mapped_lima_state_v1`,
  `restore_mapped_lima_state_v1`, `install_mac_publisher_v1`,
  `bootstrap_lima_guest_publisher_v1`, `retain_lima_guest_bootstrap_channel_v1`,
  `display_lima_guest_pairing_challenge_v1`,
  `join_lima_guest_bootstrap_transcript_v1`,
  `record_lima_guest_pre_state_v1`, `install_exact_guest_artifacts_v1`,
  `persist_lima_guest_unused_proof_v1`, `acknowledge_lima_guest_unused_proof_v1`,
  `restore_lima_guest_state_v1`, `retire_lima_guest_test_publisher_v1`, and `main`; existing `scripts/mac/lima-stop.sh` parameter/intake
  surface may add required prefix, `InstallBootstrapContextCarrierV1`, and
  `PlatformBootstrapMappingV1` arguments plus `resolve_lima_stop_authority_v1` and
  `invoke_mapped_lima_stop_v1`; its complete executable body after `set -euo pipefail`, including
  the first `limactl list substrate` through final `fi`, is replaced by one exact mapped-executor
  request. No hard-coded/default instance remains on the destructive path;
  `scripts/mac/lima-warm.sh` functions
  `destroy_vm`, `ensure_vm_ready`, `ensure_substrate_group`, `stage_workspace`,
  `install_agent_from_host`, `install_cli_from_host`, `install_gateway_from_host`,
  `build_missing_components_inside_vm`, `install_guest_binaries`,
  `bootstrap_guest_private_home`, `write_systemd_units`, `enable_socket_activation`,
  `write_layout_sentinel`, and `configure_guest`; the exact `configure_guest` mutation range from
  `ensure_substrate_group` through `write_layout_sentinel` is replaced by one
  `lima-lifecycle.sh` call. `build_missing_components_inside_vm` and its nested `fix_dns`, package,
  Rustup, Cargo-build, `/etc/resolv.conf`, and DNS-service mutation branches are tombstoned; absence
  of an exact `ExecutorBuildEvidenceV1`-joined guest artifact stops before guest mutation. Existing mapping observation,
  check-only/status, socket summary, and linger guidance remain read-only and frozen;
  `scripts/mac/lima/units/substrate-world-service.socket` removes exactly
  `PartOf=substrate-world-service.service` and admits no replacement propagation relationship;
  `crates/world-mac-lima/src/forwarding.rs` symbols
  `ForwardingHandle`, `ForwardingHandle::drop`, `ForwardingKind::SshUds`,
  `create_ssh_uds_forwarding`, and new
  `MappedSshUdsAttemptV1`, `create_mapped_ssh_uds_forwarding_v1`,
  `record_mapped_known_hosts_entry_v1`, and `restore_mapped_known_hosts_entry_v1`; and
  `crates/world-mac-lima/src/lib.rs` symbols `MacLimaBackend::ensure_forwarding`,
  `ensure_session_setup`, `get_agent_endpoint`, and removal of the exact
  `r3_forwarding_activation_required` gate. `new_with_mapping`, mapping validation,
  `auto_select`, VSock, TCP, and ambient selectors are frozen.
- Mutable test/fixture allowlist: embedded tests in
  `crates/world-mac-lima/src/forwarding.rs` and
  `crates/world-mac-lima/src/lib.rs`,
  `tests/mac/installer_parity_fixture.sh`, and new `tests/mac/lifecycle_r3.sh`. Run-only regression
  paths are `tests/mac/prefix_mapping_r2_3.sh` and `tests/mac/lima_doctor_fixture.sh`; their bytes
  must not change. Required negatives
  cover exact socket/replacement/symlink identity, `StreamLocalBindUnlink`, timeout kill/wait,
  Drop teardown, handle loss, retry, partial staging/unit/socket state, pre-existing VM,
  protected host-publisher plus guest world/publisher service-state/coupled-endpoint prepared
  transactions and receipts, endpoint
  non-requestability, missing/replaced/ambiguous endpoint zero-change, kill/retry at each activation/
  endpoint-observation/receipt boundary, exact absence of service-to-socket propagation, and
  service stop/restart/restore with socket state and endpoint identity unchanged,
  absent-instance stage-one authorization/create/finalize-PM kill points and preserving ambiguous
  create/finalize failure,
  group/membership/private-home/layout-sentinel/service-state and A-local known-hosts restoration,
  missing or build-evidence-mismatched artifact with zero mutation, prohibited DNS/toolchain/package remediation,
  host-key fingerprint/challenge confirmation only through independent host and guest controlling
  terminals; signed ticket PM/machine/artifact scope, expiry, one-use consumption, exact retry,
  wrong/automated/non-TTY confirmation, malformed/substituted SPKI, wrong curve/encoding,
  SPKI/fingerprint/anchor mismatch, high-S signature, alternate child/key/channel/transcript,
  durable host-record/guest-intent restart, pairing kill points, exact pre-existing versus created
  Lima guest state-root/lifecycle-container/publisher-directory/executor-parent roles, root create/
  identity-CAS preserving gaps, reserved challenge/component tuple consumption; both `Reserved`
  and `TicketIssued` unused revocation with exact pre-existing/absent target-before-state,
  protected-host P-256 proof, external file/parent fsync/hash, harness-Ed25519 acknowledgement,
  Keychain pairing-record restoration/removal, and kill points; liveness/timeout/pathname proof,
  missing/wrong acknowledgement, cross-reservation/replay/file/parent/fsync/digest negatives;
  reverse created-empty parent retirement after guest acknowledgement, and final guest parity,
  preserving rejection of schema-reserved `adopted`,
  unrelated Lima instance/state, and no alternate transport.
- Documentation/control surface: exact `R3-DOCS` and the exact `mac` review set. Evidence bytes
  are not created or committed in this implementation packet.
- Checks: focused crate tests, mac fixture scripts, non-native lifecycle-model/restoration
  assertions, available target static-check only, diff/allowlist/secret/change detection. IMP
  fixtures use a non-executing mock artifact identity; only `EVIDENCE:R3-MAC-IMP-01` may natively
  build/code-sign the host executor and build the Linux guest executor from the exact published
  source under `ExecutorBuildEvidenceV1` before baseline. It supplies those exact digests to the
  disposable exact-absence publisher bootstrap; guest bootstrap additionally requires a live
  operator to pin the full host-key fingerprint/challenge between independent host and guest
  controlling TTYs. Absence of that operator/TTY boundary is
  `BLOCKED_PLATFORM_HANDOFF_REQUIRED`. Final release/dev
  archive staging remains UNIX-owned. Native supported-Lima publisher/lifecycle/product/
  restoration proof is exclusively `EVIDENCE:R3-MAC-IMP-01`. Treat the graph-visible LOW results
  for private/cfg/Drop symbols as manual HIGH review surfaces; any edit to HIGH
  `new_with_mapping` or any new selector/caller stops.
- Platform/privilege: implementation and non-native fixtures only; publisher installation and
  Lima lifecycle are deferred to the evidence task. Exactly one product/test/review commit and one
  normal fast-forward push. Receipt successor is exactly `EVIDENCE:R3-MAC-IMP-01`.
- Non-goals/freeze: Unix uninstall bodies, Linux, Windows, ambient compatibility activation,
  alternate/newly selected VM, endpoint, transport, principal, or prefix, and unowned VM deletion.
  The sole absent-instance create path is the already-selected R2 Stage-1 identity under
  `LimaStageOneAuthorizationV1`.

## Native-evidence boundary from `05-debug-regression-ledger.md`

This implementation packet may run only non-native fixtures and static checks. It must not provision, install, bootstrap, code-sign, or exercise the native publisher/Lima lifecycle. The later evidence task alone may build the exact host and Linux guest executors from the published source, collect baseline, run the packet-local MAC provider actions, perform human-pinned guest pairing from independent controlling TTYs, externally receipt disposable retirement, restore exact baseline, and return `EVIDENCE_CLEAN`. Platform absence or lack of the live operator/TTY boundary is not an implementation workaround.

## Explicit non-goals

No Unix uninstall body, Linux or Windows behavior, passive-health/world-deps remediation, authenticated Codex work, retained-worker/session work, gateway adoption beyond the named MAC fence, direct-member architecture, policy redesign, ambient compatibility activation, alternate VM/endpoint/transport/principal/prefix, unowned VM deletion, native evidence, evidence-byte creation, successor dispatch, or protected-checkout mutation.

COMMON TERMINAL CONTRACT

Before publication:

1. Complete every increment-specific check and proof gate.
2. Verify the exact file/symbol/test allowlist.
3. Run required change detection.
4. Complete and validate the bounded review sequence.
5. Require zero open blocking findings.
6. Fetch/query the live target again and require it still equals the expected base.

For publication mode `remote`, when authorized by the increment contract, create one atomic commit
and perform a normal fast-forward push of HEAD to refs/heads/feat/internal-host-orchestrator-world-dispatch-bootstrap. Never force-push. Verify the live
remote equals the landed commit, refresh required indexes, and finish clean.

For publication mode `local`, create exactly the contract-authorized two-commit chain:
`expected base -> reviewed work commit -> mechanical control commit`. Keep the isolated branch and
worktree clean. Do not merge or push. Record the local branch ref, worktree, both commits/trees,
both exact changed-file inventories, their sorted union, and an unchanged remote observation.

Send a codex.top-level-task-receipt.v1 message to the meta task. For remote success, use
LANDED_CLEAN. For local success, use CLOSED_CLEAN. Include your bound increment-task thread/host
IDs, expected base, exact publication identity, changed paths, subject fingerprint, validated
review record and digest, finding disposition, checks, change detection, clean status, and next
increment.

For failure, send the exact blocked status, evidence, required authority or platform, and a
complete continuation/handoff prompt. Preserve the task-assigned worktree exactly; do not clean,
reset, discard, or request archival of a blocked task.

The send_message_to_thread call is your final tool action. After it succeeds, make no more tool
calls or repository changes. Return only the human-readable final report.

Do not generate the next increment prompt and do not begin EVIDENCE:R3-MAC-IMP-01. The meta
orchestrator owns independent verification and subsequent dispatch.

--- END COMPLETE RENDERED PROMPT ---
