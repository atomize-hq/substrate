CURRENT AUTHORIZED INCREMENT: A1.1d-5R2-3ZD1

This is a documentation-only closeout-policy amendment. It permanently removes the broken
canonical shell-wall wrapper from the blocking R2-3 closeout path, records that proof-runner work
as a separate infrastructure defect, and defines the direct product/native verification path for
the remaining R2-3 increments.

This top-level task and every subagent must run at Standard/default speed, never Fast. Every
subagent must use GPT-5.4 with Extra High reasoning.

MISSION

Land one atomic control-record commit that makes the R2-3 completion boundary truthful and
executable:

1. permanently defer R2-3ZP3 from the R2-3 blocking sequence;
2. preserve its unpublished commit chain and proof artifacts as diagnostic evidence only;
3. replace canonical-runner eligibility with direct Cargo/static/native evidence for R2-3;
4. record the exact remaining sequence R2-3ZT1 -> R2-3ZH1 -> R2-3ZM5 -> native evidence -> R2-3Z;
5. preserve the broader runtime-refactor and all R3 work as explicitly open.

BOUND SOURCE

- commit: `ec05167bd7bcb7be874af48fe33d6edb71492516`
- tree: `b0d399cfb09655d5ff61d47ec68408e52ba5cfec`
- target ref: `refs/heads/feat/internal-host-orchestrator-world-dispatch-bootstrap`
- required ancestor: `0f1e147fb735791b44a65099a65167cbdc1803af`

Before editing, verify exact base/tree/ref/ancestor, clean task checkout, 0 ahead/0 behind, and the
current GitNexus index. Analyzer-only `AGENTS.md`/`CLAUDE.md` count churn must be restored to the
starting HEAD and excluded from publication. Any other pre-existing diff is a hard stop.

EXACT TRACKED FILE ALLOWLIST

- `llm-last-mile/runtime-refactor/00-README.md`
- `llm-last-mile/runtime-refactor/03-phase-slice-map.md`
- `llm-last-mile/runtime-refactor/04-contracts-and-gates.md`
- `llm-last-mile/runtime-refactor/05-debug-regression-ledger.md`

No Rust, test, script, runner, platform, orchestration-state, or other tracked file may change.

REQUIRED POLICY RECORD

The four documents must agree on all of the following:

- R2-3ZP3 is permanently deferred from the R2-3 blocking path. Its unpublished commits
  `2cb796ffef68c2b049376984a90ce0382e5f3980`,
  `50948dbeb921582515a34bb6b7be21c46f29d008`, and
  `868994efaf132bb04c6cdd7c82da9433333c94e5` are not accepted product source and must not be
  cherry-picked, pushed, or represented as landed.
- The recurring canonical-runner defect is separately tracked: authenticated wall execution
  materializes honest shell results but finalizes ineligible with `evidence_write_failed` and
  `mount_teardown_failed`, missing authenticated stage-A completion/teardown proof and retaining
  hidden backing state.
- R2-3 closeout will make no claim of eligible canonical-runner provenance. The defect remains
  open proof infrastructure work outside the R2-3 completion gate.
- After R2-3ZT1, direct `cargo test --locked -p shell --lib -- --nocapture` validation must produce
  exactly `1322 discovered / 1277 passed / 45 failed / 0 ignored`, with the exact frozen 45-name
  inventory SHA-256 `b23bb59ad12833d2c1d37c19c54933cd6bcb1c75e0dab8a70179b9881372be70`
  and normalized-signature SHA-256
  `33c686a6ec9f3a0a4f51e1fca976445e6804da12fbbff50312a03f0042cdfac3`.
- Count-only equivalence is insufficient. Any additional failure, missing frozen failure, or
  changed frozen signature blocks closeout until classified.
- R2-3ZH1 owns only the host-inbox trusted-root test helper. R2-3ZM5 owns only the macOS
  contextless-constructor removal and typed pre-R3 smoke contract. Their focused tests, package
  checks, formatting/clippy gates, and direct shell regression checks remain mandatory.
- Fresh source-bound native macOS and Windows evidence must be collected after R2-3ZM5 and before
  R2-3Z. Existing older clean receipts are historical support only after later product commits.
- R2-3Z may close only with the direct product/static checks and fresh native receipts clean, while
  explicitly recording the canonical-runner provenance limitation.
- This amendment does not complete any R3 lifecycle/forwarding/provisioning row and does not close
  the remaining work in `llm-last-mile/runtime-refactor/` outside the bounded R2-3 packet.

SOURCE AND HISTORY HONESTY

Reconcile the documents with the actual landed history through R2-3ZP2 without rewriting history.
Previously authorized allowlist amendments and the Windows repair landing remain accepted source.
Do not retroactively describe unpublished R2-3ZR1 or R2-3ZP3 commits as landed. Preserve enough
commit IDs, artifact references, and failure signatures for a future proof-infrastructure owner to
resume independently without reopening R2-3.

REQUIRED CHECKS

1. Exact four-file containment and `git diff --check`.
2. Internal cross-reference/source-closure review across the four changed documents.
3. Verify all named commits, hashes, counts, and sequence identifiers against accepted receipts and
   current source-control facts.
4. `gitnexus_detect_changes()` with any documentation-index tooling limitation recorded.
5. Fresh read-only review of policy consistency, history honesty, closeout safety, and non-expansion
   into R3 or the remaining runtime-refactor work.

REVIEW AND PUBLICATION

Use fresh read-only GPT-5.4 Extra High reviewers at Standard/default speed. Validate the bounded
review record. Publication requires terminal CLEAN, P1=0, P2=0, full P3/P4 disposition, exact
allowlist compliance, and the live target still equal to the expected base. Publish one normal
fast-forward commit; never force-push.

COMPLETION BOUNDARY

Success completes only R2-3ZD1, the closeout-policy amendment. It does not repair the retained
worker or host-inbox helpers, does not land the macOS cleanup, does not run native evidence, does
not execute R2-3Z, and does not complete the broader runtime-refactor or any R3 row.

TERMINAL RECEIPT

Use `increment: "R2-3ZD1"`, `packet_id: "A1.1d-5R2-3ZD1"`, and
`next_increment: "R2-3ZT1"`. A LANDED_CLEAN receipt must include the landed commit/tree/ref, exact
four changed paths, subject fingerprint, validated review record/digest, checks, finding
disposition, GitNexus result, remote 0/0, clean checkout, and the exact direct-verification sequence
established by the amendment. For failure, send exact evidence, required authority, and a complete
handoff prompt. Receipt send is the final tool action.
