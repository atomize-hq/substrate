CURRENT AUTHORIZED INCREMENT: A1.1d-5R2-3W1

Use:

- using-agent-skills
- context-engineering
- source-driven-development
- api-and-interface-design
- test-driven-development
- incremental-implementation
- gitnexus-impact-analysis
- security-and-hardening
- code-review-and-quality
- git-workflow-and-versioning

This top-level task and every subagent must run on Standard/default speed, never Fast. Every
subagent must use GPT-5.4 with Extra High reasoning. Ground Windows identity, Known Folder,
PowerShell, path, and process-boundary decisions in current repository truth, official Microsoft
documentation, and the published runtime-refactor control pack. Do not reopen R2-2 or R2-3A
through R2-3M4 without a concrete contradiction.

Load only:

- current status in `llm-last-mile/runtime-refactor/00-README.md`;
- the parent A1.1d-5R2-3 row, common child-subdivision rules, and R2-3W1 section in
  `llm-last-mile/runtime-refactor/03-phase-slice-map.md`;
- PI-042 through PI-046 and PI-068 through PI-070 current-state and required-action rows in
  `llm-last-mile/runtime-refactor/02-seam-crosswalk.md`;
- the host-context path/principal/framing, boundary-carrier, generated-projection,
  install/uninstall symmetry, diagnostic, and R3-exclusive lifecycle contracts in
  `llm-last-mile/runtime-refactor/04-contracts-and-gates.md`;
- R2-MAP-WIN-01, R2-GEN-01, and R2-DIAG-01 in
  `llm-last-mile/runtime-refactor/05-debug-regression-ledger.md`; and
- `06-review-finding-inventory.md` only for actual P3/P4 deduplication.

SELECTED OUTCOME

Construct the same strict Windows `InstallBootstrapContextCarrierV1` at all four Windows
installer/uninstaller public entries and carry it explicitly through the existing
profile/shim/doctor/guarded-WSL child call sections, including release `-NoAutoSource`, without
changing any install, replacement, removal, stop, unregister, or cleanup action.

EXACT COMPLETION CLAIM

R2-3W1 completes only PI-042 through PI-046 and PI-068 through PI-070. It establishes Windows
installer context construction and explicit child propagation. It does not construct or complete
the WSL platform mapping, select or activate a pipe/forwarder/backend, enable WSL provisioning,
claim native Windows evidence, add deletion ownership, complete any R3 row, or complete the R2-3
parent packet.

EXACT ALLOWLIST

Production changes are limited to:

- prefix/context setup, generated profile projection, and existing shim child call section in
  `scripts/windows/dev-install-substrate.ps1`;
- prefix/context setup and only the existing shim-remove child call section in
  `scripts/windows/dev-uninstall-substrate.ps1`;
- prefix/context setup, generated profile projection, and existing shim, guarded-WSL, and
  world-doctor child call sections in `scripts/windows/install-substrate.ps1`; and
- prefix/context setup only in `scripts/windows/uninstall-substrate.ps1`.

Test changes are limited to one new file:

- `scripts/windows/prefix-mapping-r2-3.Tests.ps1`

Do not edit any Rust file, manifest, `Cargo.lock`, Unix/macOS script, WSL helper, forwarder helper,
PowerShell profile outside a test sandbox, control-pack document, generated analyzer file,
`AGENTS.md`, or `CLAUDE.md`. Do not edit any other section of the four production scripts. If
another file or production section is required, stop with `BLOCKED_SCOPE_EXPANSION`.

EXACT SYMBOL AND SECTION SCOPE

The only new production functions permitted, duplicated locally only where the four-file boundary
requires them, are:

- `Resolve-InstallBootstrapContextV1`
- `Assert-InstallBootstrapContextV1`
- `Invoke-SubstrateWithInstallContextV1`

They may only:

- observe the current Windows account and SID;
- resolve the current token's `LocalApplicationData` Known Folder;
- select and normalize the declared `-Prefix` or the principal-derived default;
- implement the canonical Windows eight-line commitment and nine-line outer carrier framing;
- decode, canonicalize, recompute, and authenticate that same carrier;
- require current account/SID and declared-prefix equality;
- produce the checked non-secret H/R/account/commitment/carrier projections; and
- invoke an already-existing Substrate child with the exact carrier on explicit argv while
  preserving its existing action arguments, exit behavior, and output posture.

No new function may become a platform mapping constructor, pipe or distro selector, artifact
owner, cleanup helper, fallback path, side table, persistence record, or lifecycle seam.

Only these existing top-level sections may change:

- each script's `param`/prefix/context intake before its first mutation, build, download, shim,
  profile, doctor, WSL, or deletion action;
- dev and release generated-profile content, only to project the exact validated
  `SUBSTRATE_HOME`, `SUBSTRATE_ROOT`, `SUBSTRATE_INSTALL_HOST_CONTEXT_COMMITMENT`,
  `SUBSTRATE_INSTALL_PRIMARY_USER`, and `SUBSTRATE_INSTALL_BOOTSTRAP_CONTEXT_V1`;
- the current dev/release shim-deploy invocation;
- the current dev shim-remove invocation;
- the current release world-doctor invocation; and
- the current release `wsl-warm.ps1` invocation, only to transport the exact selected prefix,
  encoded carrier, declared distro, and existing project-path arguments toward W2's guarded
  intake.

Every other statement is byte-frozen. In particular:

- release archive/download/checksum/version resolution and installed payload selection;
- every `New-Item`, `Copy-Item`, `Set-Content`, `Add-Content`, `Remove-Item`, recursive, wildcard,
  replacement, profile-snippet, PATH, temp, version, bin, and finalizer action except the exact
  generated-profile value additions above;
- dev build/cargo and config-warning behavior;
- uninstaller profile-target discovery, regex replacement, profile helper removal, prefix child
  enumeration, prefix removal, and shim-cache cleanup;
- WSL stop/unregister behavior in uninstall;
- error/warning/exit predicates, action order, dry-run behavior, and user-facing completion
  semantics except bounded redaction-safe context mismatch diagnostics; and
- every downstream WSL guard, helper, backend, pipe, forwarder, and lifecycle byte.

PRE-EDIT SOURCE CLOSURE AND IMPACT

Before editing, perform exact source/caller closure over all four scripts:

- every `-Prefix` default/read and every path derived from it;
- every public and internal entry discriminator;
- every environment projection read/write/restore;
- every generated profile writer and profile-source branch;
- every shim deploy/remove call;
- every world-doctor call;
- every `wsl-warm.ps1` call; and
- every deletion, recursive, wildcard, replacement, stop, unregister, or cleanup block whose
  bytes must remain frozen.

Run file-qualified upstream GitNexus impact with tests included for the already-landed shell
Windows observation symbols invoked by the scripts:

- `construct_windows_install_bootstrap_context`
- `decode_and_bind_windows_install_bootstrap_context`
- `bind_windows_install_bootstrap_context`
- `current_windows_principal_and_known_folder`
- `windows_known_folder_for_principal`
- `resolve_windows_install_prefix_from_invocation`
- `require_same_windows_file_identity`

These Rust symbols remain read-only in W1. Record direct callers, affected processes/modules, and
risk. PowerShell blocks/functions that GitNexus does not index require the same exact textual
source closure. Treat public prefix selection, profile generation, `-NoAutoSource`, installed-copy
witnessing, and child carrier propagation as HIGH posture even when graph coverage under-reports
them. Warn before proceeding. Any unexpected action/lifecycle reachability stops W1.

AUTHORITATIVE WINDOWS CONTEXT

Each public script entry constructs exactly one IH before any mutation or context-sensitive child:

1. Read `[System.Security.Principal.WindowsIdentity]::GetCurrent()` once.
2. Require a non-anonymous current account and canonical `S-1-...` SID.
3. If `-Prefix` was explicitly supplied, normalize that value under the frozen Windows V1 rules.
4. Otherwise resolve `LocalApplicationData` for the current Windows token through an OS Known
   Folder API and append `Substrate`; do not read `$env:LOCALAPPDATA`, `$env:USERPROFILE`, `$HOME`,
   profile state, CWD, repository location, or a generated record.
5. Require exact Windows path normalization: fully-qualified drive or UNC path, canonical
   separators/drive letter, no device namespace, relative/root-only/bare-root path, ADS, dot
   component, invalid/control/reserved component, trailing dot/space, NUL, or unpaired surrogate.
6. Populate selected prefix, H, and R with that one identical normalized value.
7. Encode the exact Windows commitment record with canonical unpadded base64url fields and SHA-256,
   then encode the exact nine-line carrier once as canonical unpadded base64url.

Public mode must ignore and scrub outer
`SUBSTRATE_INSTALL_BOOTSTRAP_CONTEXT_V1`,
`SUBSTRATE_INSTALL_HOST_CONTEXT_COMMITMENT`,
`SUBSTRATE_INSTALL_PRIMARY_USER`, `SUBSTRATE_HOME`, and `SUBSTRATE_ROOT` as authority. An explicit
declared prefix remains authoritative even under conflicting ambient B.

Where an explicit internal carrier parameter is accepted, argv presence is the sole child-mode
discriminator. Internal mode must decode/re-encode canonically, recompute the commitment, bind the
carrier to the current account/SID, require any declared `-Prefix` to match, validate any inherited
projection as equal, and reject malformed, duplicate, tampered, reordered, forged-principal, or
conflicting input before action. Invalid internal mode may not fall through to public
construction.

No script may log, print, persist, interpolate into an error, or expose the encoded carrier or SID.
Only the normalized prefix, lowercase commitment, and canonical account may appear in already
authorized non-secret diagnostics.

INSTALL/UNINSTALL SYMMETRY AND INSTALLED WITNESS

Dev install, dev uninstall, release install, and release uninstall must use the same normalization,
default Known Folder, principal binding, commitment, and carrier for the same current token and A.
Repeat construction must be byte-identical.

Release installation must not derive A from the downloaded artifact, version directory, ambient
profile, or CWD. After the installed release copy exists, any installed-product child call must
use the already-selected carrier and the exact installed `A\\bin\\substrate.exe` witness. Do not
accept another executable ancestor, PATH candidate, symlink/reparse alias, case variant, or file
identity. W1 may transport the binding to the existing child; it may not edit Rust witness
validation or create a second installed-witness algorithm.

Uninstall constructs A by the same public rule. Existing files/records may confirm or reject the
selected context but may not select another prefix. Missing or malformed projection cannot trigger
ambient fallback. W1 changes no removal target, ownership classification, action predicate, or
convergence behavior.

PROFILE AND CHILD PROPAGATION

Generated dev/release profile helpers are consistency projections only. They must contain exact
escaped PowerShell literals for:

- `SUBSTRATE_HOME=A`
- `SUBSTRATE_ROOT=A`
- `SUBSTRATE_INSTALL_HOST_CONTEXT_COMMITMENT=<commitment>`
- `SUBSTRATE_INSTALL_PRIMARY_USER=<canonical account>`
- `SUBSTRATE_INSTALL_BOOTSTRAP_CONTEXT_V1=<carrier>`

Profile contents and file existence never select A. Escape all values for PowerShell single-quoted
literal safety; a value that cannot be represented under the validated grammar fails before
writing. Do not add SID as a separate environment authority.

`Invoke-SubstrateWithInstallContextV1` must prepend the exact hidden
`--install-bootstrap-context-v1 <carrier>` argv to existing substrate child calls, establish the
matching checked process projections for that invocation, and restore every prior process
environment value exactly, including prior absence, after the child returns or throws. It may not
use an environment carrier instead of argv, disclose the carrier in dry-run output, use
`Start-Process` command-string re-evaluation, or change the child's remaining argv/order/exit
handling.

Release `-NoAutoSource` must affect only whether the generated profile is sourced. Shim deploy,
world doctor, and guarded WSL propagation must still receive the exact carrier explicitly and may
not depend on profile side effects.

The `wsl-warm.ps1` call site may pass only the exact already-selected prefix/carrier and its
existing distro/project values. W1 does not construct PM, select a pipe, edit WSL helper intake,
move the capability guard, or claim WSL action. Until W2 lands, an unknown-parameter/fail-closed
child result remains a guarded failure and must not be reinterpreted as provisioning success.

R3 LIFECYCLE FREEZE

W1 has no cleanup authority. No W1 code or test may:

- broaden or execute a recursive/wildcard/prefix/profile/version/bin removal;
- change replacement, preservation, rollback, retry, finalizer, or convergence predicates;
- stop a forwarder, terminate/unregister WSL, remove a PID/pipe/config/log, or create a deletion
  manifest;
- count deletion or protected-path behavior as W1 proof; or
- edit or execute a downstream WSL provisioning path as success evidence.

Capture exact base snippets or hashes for every frozen action block in all four production files
before editing, and prove them byte-identical before staging and again before publication.

TEST-DRIVEN PROOF

Add deterministic tests only in `scripts/windows/prefix-mapping-r2-3.Tests.ps1` covering at least:

- the exact Windows IH golden vector and canonical outer carrier;
- default A from token Known Folder and custom declared A;
- conflicting ambient `LOCALAPPDATA`, `USERPROFILE`, H, R, account, commitment, and carrier B
  cannot retarget public construction;
- repeat construction and all four install/uninstall entry surfaces produce the same context for
  the same token/A;
- malformed, duplicate, tampered, reordered, noncanonical-base64url, wrong-prefix, and
  forged-account/SID carriers reject before child/action;
- strict drive and UNC normalization plus rejection of relative, device, ADS, dot, reserved,
  invalid/control, trailing-dot/space, NUL, and alias spellings;
- generated dev/release profile projections contain exact A/account/commitment/carrier values and
  no ambient B selection;
- shim deploy/remove and world-doctor calls receive the carrier on explicit argv;
- `-NoAutoSource` does not remove carrier propagation to shim/doctor/guarded WSL calls;
- child process projections are exact and prior process environment values/absence are restored;
- release child invocation uses the selected installed `A\\bin\\substrate.exe` path;
- guarded WSL call propagation does not move or bypass the downstream guard; and
- zero read/write/remove/stop/unregister action occurs under ambient B.

Tests must use temporary sandbox copies/stubs and must never modify the real PowerShell profile,
registry, Known Folder, installed prefix, WSL distro, forwarder, pipe, process environment outside
the bounded test process, or any protected checkout. Pure framing/path tests may inject an explicit
test observation object only into test-only harness code; production functions must always observe
the real current token and Known Folder.

No test may prove W1 by deleting a real/sandbox install tree, changing a real user profile,
starting/stopping/unregistering WSL, launching a forwarder, creating a pipe, or reaching the
currently unreachable WSL provisioning body.

REQUIRED VERIFICATION

Run:

- PowerShell parser validation over all four changed production scripts and the test file using
  the available supported PowerShell parser/runtime;
- the focused `scripts/windows/prefix-mapping-r2-3.Tests.ps1` suite under the narrowest supported
  static/test mode;
- any existing Windows installer script tests or static fixtures that cover the four touched call
  sections without lifecycle action;
- static source assertions for canonical framing, explicit child argv, `-NoAutoSource`
  independence, no carrier/SID disclosure, and exact selected installed binary;
- exact before/after hashes or snippets for every frozen recursive, wildcard, replacement,
  profile-removal, stop/unregister, cleanup, action-order, and WSL-guard-related block;
- a repository search proving no outer `LOCALAPPDATA`, `USERPROFILE`, profile, H/R, or environment
  carrier remains a normal authority source in the four selected prefix/context sections;
- `git diff --check` and staged `git diff --cached --check`;
- exact five-file unstaged and staged allowlist/status checks;
- confirmation that every Rust file, manifest, lockfile, WSL/forwarder helper, non-Windows file,
  control document, and generated artifact is unchanged; and
- `gitnexus_detect_changes()` before commit, with every reported affected flow inspected.

Do not install or simulate a missing PowerShell/Windows runtime. If this Linux host lacks `pwsh` or
Windows APIs, preserve the exact unavailability and use only honest static/parser alternatives
already present in the repository. Do not label a Linux text check as PowerShell execution or
native Windows evidence. If an exact required command exposes only a pre-existing failure in
untouched forbidden code, preserve it and run a narrowly scoped subject supplement without
weakening the required command.

SUBJECT FINGERPRINT

After deterministic formatting/checks and before discovery review:

1. record the pre-edit base commit;
2. build a sorted manifest containing that commit plus every exact changed subject path, its Git
   mode (or `NEW`), and `git hash-object --no-filters` blob ID (or `MISSING`);
3. include only paths inside the exact W1 production/test allowlist; and
4. SHA-256 the manifest and use `sha256:<digest>` as the review subject fingerprint.

BOUNDED REVIEW

Open and validate one V1 review record with packet ID `A1.1d-5R2-3W1`. Use fresh read-only lenses
for:

1. Windows token/SID/Known-Folder context construction, strict carrier framing, prefix
   normalization, and ambient non-authority;
2. profile/shim/doctor/guarded-WSL propagation, installed-copy witness, `-NoAutoSource`, exact
   environment restoration, and carrier non-disclosure; and
3. exact file/section/test allowlist, frozen cleanup/action predicates, WSL guard/capability
   preservation, static-proof honesty, and regression sufficiency.

Apply the common causal bounded-review contract. P1/P2 block. Valid unfixed P3/P4 must be
deduplicated/inventoried before completion; if that requires a file outside the exact W1 allowlist,
stop with `BLOCKED_SCOPE_EXPANSION`. Validate the record after every returned cycle. A CLEAN cycle
is terminal; do not launch another review after CLEAN.

PUBLICATION

Before publication require:

- exact staged five-file allowlist and frozen-section proof;
- every required check and change-detection result;
- validated CLEAN review with zero P1/P2 and complete P3/P4 disposition;
- live target still equal to the expected base;
- one atomic Conventional Commit;
- normal fast-forward push only; and
- post-push live equality, clean checkout, current GitNexus index, and 0 ahead/0 behind.

The successful completion claim must say that W1 completes only PI-042 through PI-046 and PI-068
through PI-070 by constructing and explicitly propagating one Windows IH across the four installer
and uninstaller entry surfaces. It must explicitly disclaim WSL mapping completion, pipe or
forwarder selection/activation, WSL provisioning, deletion/lifecycle authority, native Windows
evidence, later Windows consumer migration, and R2-3 parent completion.
