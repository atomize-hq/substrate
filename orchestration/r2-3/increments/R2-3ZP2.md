CURRENT AUTHORIZED INCREMENT: A1.1d-5R2-3ZP2

This is a fresh prerequisite increment before `R2-3ZR1`. It owns only the canonical shell-wall
descendant-authority repair diagnosed by `R2-3ZP1`. It must not resume, copy, edit, stage, archive,
or otherwise mutate the preserved three-file `R2-3ZR1` WIP.

This top-level task and every subagent must run on Standard/default speed, never Fast. Every
subagent must use GPT-5.4 with Extra High reasoning.

MISSION

Implement a security-preserving authority model that allows the canonical shell-wall runner to
execute at an ordinary descendant commit while remaining anchored to a reviewed immutable
authority commit. Land exactly one atomic prerequisite commit, make that landed commit the new
reviewed authority commit, and prove a later disposable descendant can use it without becoming
self-authorizing.

BOUND SOURCE

- commit: `3a6490ff391453535168491b3f8d26adf9e20938`
- tree: `fca816eaed26407ea89d352c7a087a201391bd49`
- target ref: `refs/heads/feat/internal-host-orchestrator-world-dispatch-bootstrap`
- required ancestor: `0f1e147fb735791b44a65099a65167cbdc1803af`
- prior reviewed authority commit:
  `8c46135c861a468dea316cf9fd7d6c6bb15bddac`
- diagnostic artifact:
  `/home/spenser/.codex/visualizations/2026/07/30/019fb3ac-2abd-7e90-9510-7d411862ee14/r2-3zp1-invocation-authority-diagnostic.json`
- diagnostic SHA-256:
  `c349dd88767fdb4346ec0483bd609cd51ca9fbd5f9e313332af88d1fd314fff5`

Before any edit, verify exact base/tree/ref/ancestor, a clean task checkout, 0 ahead/0 behind, and
a current GitNexus index for the exact checkout. If analyzer refresh changes only generated count
blocks in `AGENTS.md` and `CLAUDE.md`, restore only those blocks to the verified starting HEAD and
record the before/after counts. Any other pre-existing diff is a hard stop.

PRESERVED WIP

The blocked `R2-3ZR1` worktree is:

`/home/spenser/.codex/worktrees/e847/substrate-r2-3`

It must remain untouched and unarchived. Its exact unstaged paths are:

- `crates/shell/src/execution/host_inbox_materialization.rs`
- `crates/world-mac-lima/examples/mac_backend_smoke.rs`
- `crates/world-mac-lima/src/lib.rs`

Do not run Cargo, formatters, analyzers, tests, or any command in that checkout that can create or
change files. Do not reset, clean, restore, stage, commit, hand off, or remove it.

EXACT FILE ALLOWLIST

Production/test-harness code:

- `scripts/ci/canonical_shell_wall_runner.py`
- `scripts/ci/test_canonical_shell_wall_runner.py`

Authority/governance records:

- `llm-last-mile/runtime-refactor/02-seam-crosswalk.md`
- `llm-last-mile/runtime-refactor/04-contracts-and-gates.md`
- `llm-last-mile/runtime-refactor/05-debug-regression-ledger.md`

No other tracked file may change. `AGENTS.md` and `CLAUDE.md` analyzer-only count churn must be
restored and excluded from publication.

AUTHORIZED AUTHORITY MODEL

The repair must represent two distinct immutable identities:

1. `expected_head`: the exact live descendant commit being tested; and
2. `authority_commit_oid`: the exact reviewed authority commit authorizing the runner.

An authenticated descendant is eligible only when all of the following hold:

- `authority_commit_oid` is a verified ancestor of `expected_head` in the held repository object
  graph;
- the authority commit contains exactly one value for each required P1 trailer and the values
  match the authenticated bootstrap and argv templates;
- the live descendant tree carries the exact runner blob and exact self-test blob from the
  authority commit at the authenticated paths;
- the live ref, expected head, tree, ancestry, index, worktree cleanliness, Git configuration, held
  objects, and repository identity all pass the existing fail-closed checks;
- stage authority and provenance bind both identities without conflating them;
- descendant provenance records `authority_commit_matches_live_head=false`,
  `authority_commit_trailers_verified=true`, and `reviewed_oid_matches=true`; and
- an unrelated commit, rewritten ancestry, substituted blob, wrong trailer, wrong template,
  wrong argv, dirty state, or mutable side artifact cannot confer authority.

The final R2-3ZP2 commit must carry the five exact P1 trailers computed from its final runner bytes
and templates:

- `P1-Bootstrap-Bytes`
- `P1-Bootstrap-SHA256`
- `P1-Host-Argv-Template-SHA256`
- `P1-Stage-A-Argv-Template-SHA256`
- `P1-Stage-B-Argv-Template-SHA256`

That commit becomes the new reviewed authority commit for later descendants. Do not copy stale
trailer values from the historical authority commit if the authenticated bytes changed.

LINKED-WORKTREE BOUNDARY

Linked-worktree support is not authorized. Preserve the current fail-closed rejection of
worktree-pointer `.git`, `commondir`, and `config.worktree` authority.

Implementation and unit tests may run in the task-assigned Codex worktree where supported. The
authenticated runner self-test and canonical wall must run in a disposable full non-worktree
checkout with an ordinary `.git` directory, created outside every protected checkout. This
increment may create and delete only its own disposable proof checkout and external proof roots.
It must never redirect evidence into the product worktree.

REQUIRED IMPACT AND SOURCE CLOSURE

Before editing each existing symbol, run GitNexus upstream impact and record direct callers,
affected processes/modules, and risk. The diagnostic already identified:

- `verify_expected_head_object_chain`: HIGH
- `_verify_commit_trailers`: LOW
- `host_main`: LOW
- `_default_provenance`: HIGH
- `_validate_provenance_record`: MEDIUM

Inspect all callers and provenance consumers before changing these or any equivalent sites. Treat
GitNexus undercoverage as requiring textual/source closure, not as proof of low risk.

MINIMUM IMPLEMENTATION SCOPE

The implementation may update only the allowlisted files and only as needed to:

- separate authority commit from live expected head at bootstrap, stage, host, self-test, and
  provenance boundaries;
- prove authority ancestry and exact runner/self-test blob continuity;
- verify P1 trailers on the reviewed authority commit object;
- preserve exact live-head/ref/tree/cleanliness verification;
- update disposable-repository fixtures and all assertions that assumed authority commit equals
  expected head;
- add descendant-positive and fail-closed negative tests; and
- reconcile the three named authority documents with the implemented descendant model, the new
  authority commit/trailers, and the canonical baseline.

Do not change product Rust, shell, PowerShell, platform, installer, replay, shim, trace, or native
evidence behavior.

REQUIRED RED/GREEN PROOF MATRIX

RED or fail-closed cases:

- old single-commit-only code rejects an ordinary descendant lacking P1 trailers;
- authority commit not in descendant ancestry;
- authority commit with wrong runner blob;
- authority commit with wrong self-test blob;
- live descendant with a changed runner or self-test blob;
- missing, duplicate, or mismatched P1 trailers;
- wrong `authority_commit_oid` in stage authority or provenance;
- wrong host/stage-A/stage-B template or substituted argv hashes;
- dirty index, tracked modification, or disallowed untracked input;
- unrelated or history-rewritten descendant; and
- linked worktree remains rejected with `repository_identity_mismatch`.

GREEN cases:

- exact authority commit remains eligible;
- ordinary descendant with unchanged authenticated blobs and authority commit in ancestry passes
  bootstrap, self-test, host preflight, and provenance validation;
- eligible descendant provenance distinguishes authority commit from live head exactly;
- all existing canonical runner unit/self-tests pass;
- the final canonical shell wall runs from a disposable full checkout and produces exactly:
  `1309 discovered / 1264 passed / 45 failed / 0 ignored`;
- failure-name SHA-256:
  `b23bb59ad12833d2c1d37c19c54933cd6bcb1c75e0dab8a70179b9881372be70`;
- normalized-signature SHA-256:
  `33c686a6ec9f3a0a4f51e1fca976445e6804da12fbb50312a03f0042cdfac3`.

PUBLICATION SEQUENCE

1. Complete implementation, unit/self-tests, exact allowlist verification, GitNexus change
   detection, and fresh bounded review with zero open P1/P2 and all P3/P4 dispositioned.
2. Recompute the five P1 trailer values from the final authenticated bytes/templates.
3. Reverify the live target still equals the expected base.
4. Create one local atomic commit with the exact computed P1 trailers.
5. Create a disposable full non-worktree checkout at that local commit and prove:
   - the authority commit itself is eligible;
   - a disposable ordinary descendant with unchanged authenticated blobs is eligible;
   - the complete RED/GREEN matrix;
   - the canonical shell wall exact baseline and hashes.
6. If any post-commit proof fails, do not amend, reset, or publish. Return a blocked receipt with
   the local commit and proof evidence for a fresh bounded continuation.
7. If all proof passes, reverify the live target still equals the expected base and perform one
   normal fast-forward push. Never force-push.
8. Verify remote commit/tree, 0 ahead/0 behind, clean task checkout, and refreshed GitNexus index.

REVIEW CONTRACT

Use fresh read-only GPT-5.4 Extra High reviewers on Standard/default speed. At minimum cover:

1. ancestry/object/blob/trailer authority and anti-self-authorization;
2. stage/provenance/schema continuity plus negative substitution cases; and
3. exact allowlist, governance reconciliation, proof honesty, linked-worktree non-expansion, and
   canonical baseline integrity.

Validate the review-cycle record. Publication requires terminal CLEAN, P1=0, P2=0, and complete
P3/P4 disposition.

COMPLETION BOUNDARY

Success completes only `R2-3ZP2`, the canonical-runner descendant-authority prerequisite. It does
not complete `R2-3ZR1`, does not land or supersede its preserved WIP, does not run or claim native
macOS/Windows evidence, does not complete `R2-3Z`, and does not complete the parent packet.

TERMINAL RECEIPT

Send a `codex.top-level-task-receipt.v1` receipt to the meta task using:

- `increment: "R2-3ZP2"`
- `packet_id: "A1.1d-5R2-3ZP2"`
- `next_increment: "R2-3ZR1"`

For `LANDED_CLEAN`, include the landed commit/tree/ref, exact five changed paths, subject
fingerprint, exact five P1 trailer values, disposable full-checkout proof identity, canonical wall
counts/hashes, validated review record/digest, finding disposition, GitNexus result, and final
clean/0-0 verification.

For failure, send the exact blocked status, evidence, required authority, and a complete handoff
prompt. The receipt send must be the final tool action. After it succeeds, make no further tool
calls.
