Use $orchestrate-top-level-tasks and every skill required by the increment contract.

ROLE

You are the fresh top-level increment orchestrator for R2-3ZR1. You own this increment only.
You may use subagents for GitNexus/source analysis, bounded implementation, test/proof execution,
and independent review. You remain responsible for scope, shared-worktree integration,
verification, publication, and the terminal receipt.

DISPATCH IDENTITY

- orchestration_id: substrate-r2-3
- dispatch_nonce: 81dd798cbdb999c8a5b64b4c74cb0e3172a1e1d2ef5d4391015ab75c123c8959
- meta_thread_id: 019fa3f7-c447-7132-9126-82e2cf38bd9d
- meta_host_id: remote-ssh-discovered:spenser-linux-codex
- increment: R2-3ZR1
- packet_id: A1.1d-5R2-3ZR1
- next_increment: EVIDENCE:R2-3Z

INCREMENT-TASK IDENTITY BARRIER

Do not edit, delegate, run implementation checks, or publish until the meta orchestrator sends a
follow-up binding your own real increment-task thread ID and host ID to this dispatch nonce. Echo
those exact IDs in every terminal receipt.

REPOSITORY BOUNDARY

Work only in the task-assigned checkout:

the fresh retry task-assigned Linux Codex worktree provisioned from the remote-tracking product ref

Never mutate these protected checkouts:

- /home/spenser/__Active_code/substrate
- /home/spenser/__Active_code/substrate-r2-3
- /home/spenser/__Active_code/substrate-r2-3-meta-orchestration
- /home/spenser/.codex/worktrees/0499/substrate-r2-3
- /home/spenser/.codex/worktrees/b5aa/substrate-r2-3
- /home/spenser/.codex/worktrees/bb3f/substrate-r2-3
- C:\Users\spmcc\Documents\__Project_Code\substrate-r2-3

Canonical starting state:

- remote: origin
- target ref: refs/heads/feat/internal-host-orchestrator-world-dispatch-bootstrap
- expected base commit: 3a6490ff391453535168491b3f8d26adf9e20938
- expected base tree: fca816eaed26407ea89d352c7a087a201391bd49
- required ancestor: 0f1e147fb735791b44a65099a65167cbdc1803af

Before editing, fetch/query the live remote and verify the exact base, tree, ancestry, cleanliness,
0 ahead/0 behind, and current GitNexus index. If they differ, send BASE_DRIFT or
BLOCKED_CONTRADICTION. Do not reconcile, merge, rebase, reset, clean, or force-push.

SUBAGENT ORCHESTRATION

Use repository-required model/reasoning settings for every subagent. Complete required pre-edit
impact analysis before any subagent edits an existing symbol. Give editing subagents mutually
exclusive ownership when practical. Use fresh read-only subagents for independent review. Do not
allow a reviewer to review implementation it authored.

INCREMENT CONTRACT

CURRENT AUTHORIZED INCREMENT: A1.1d-5R2-3ZR1

This is the user-authorized bounded repair and reconciliation increment inserted after the
blocked R2-3Z closeout audit and before a fresh native-evidence gate and R2-3Z retry.

Use:

- using-agent-skills
- context-engineering
- source-driven-development
- api-and-interface-design
- test-driven-development
- incremental-implementation
- gitnexus-impact-analysis
- security-and-hardening
- code-review-and-quality
- documentation-and-adrs
- git-workflow-and-versioning

This top-level task and every subagent must run on Standard/default speed, never Fast. Every
subagent must use GPT-5.4 with Extra High reasoning.

Load only:

- the R2-3 parent row, common child-subdivision rules, R2-3M2, R2-3F, R2-3R, R2-3S1,
  R2-3T, and R2-3Z sections in `llm-last-mile/runtime-refactor/03-phase-slice-map.md`;
- PI-041, PI-054 through PI-059, PI-075, PI-081, PI-090 through PI-091, and PI-118 in
  `llm-last-mile/runtime-refactor/02-seam-crosswalk.md`;
- the mapping/factory, explicit-product trace, bounded-review, and R3-exclusive lifecycle
  contracts in `llm-last-mile/runtime-refactor/04-contracts-and-gates.md`;
- R2-SHIM-01, R2-GEN-01, R2-DIAG-01, R2-MAP-MAC-01, and applicable R2-RUNTIME-01 in
  `llm-last-mile/runtime-refactor/05-debug-regression-ledger.md`;
- `06-review-finding-inventory.md` only for actual P3/P4 deduplication; and
- the embedded accepted facts from the blocked R2-3Z receipt below.

AUTHORITY AND SELECTED OUTCOME

The user explicitly authorized a fresh bounded `R2-3ZR1` repair/reconciliation increment after
R2-3Z stopped with `BLOCKED_CONTRADICTION`.

R2-3ZR1 owns exactly three outcomes:

1. eliminate the remaining contextless macOS `MacLimaBackend` constructor surface and migrate the
   harness/example to explicit typed carrier/mapping input without activating lifecycle;
2. reproduce and classify the shell proof failures exposed by the R2-3Z proof wall, then repair
   only test/test-helper expectations demonstrably caused by accepted R2-3 typed factory,
   unbound-trace, or Windows cfg-build behavior; and
3. reconcile the already-authorized M4/F/R/S1 scope amendments plus the R2-3ZWR1 repair landing
   into the control-pack history without rewriting or blessing unauthorized behavior.

This is not authority to weaken R2-3Z, treat ambient compatibility as acceptable, change normal
shell/platform behavior, or absorb R3 lifecycle work.

ACCEPTED BASE AND BLOCKED CLOSEOUT FACTS

Start only from:

- product commit `3a6490ff391453535168491b3f8d26adf9e20938`;
- tree `fca816eaed26407ea89d352c7a087a201391bd49`;
- target `refs/heads/feat/internal-host-orchestrator-world-dispatch-bootstrap`; and
- ancestor `0f1e147fb735791b44a65099a65167cbdc1803af`.

The validated R2-3Z receipt established:

- `MacLimaBackend::new` at `crates/world-mac-lima/src/lib.rs:112` selects VM/socket/transport from
  ambient state;
- `MacLimaBackend::new_with_vm_name` at the same file's former line 175 delegates to that ambient
  constructor;
- `crates/world-mac-lima/examples/mac_backend_smoke.rs` consumes the contextless constructor;
- production factory/shell/shim/replay callers are otherwise typed or fail closed;
- PI-077/PI-078 guards remain byte-frozen and fail closed;
- native macOS and Windows receipts were clean and source-bound to the old accepted source;
- the R2-3Z Linux proof wall surfaced shell world-deps/authority/retained-worker/orchestrator/repl
  test failures involving unbound global trace initialization and legacy authority-writer
  preflights; and
- previously landed M4/F/R/S1 paths and the post-T `3a6490ff...` repair need explicit
  control-pack reconciliation.

The prior clean native receipts remain regression reference only. Because R2-3ZR1 changes the
accepted source, they cannot satisfy the post-ZR1 evidence gate; fresh macOS and Windows evidence
must run after this increment lands.

EXACT COMPLETION CLAIM

R2-3ZR1 completes only:

- the final no-contextless macOS backend-constructor condition supporting PI-041/PI-054/PI-059;
- test-only alignment required to prove already-landed R2-3 typed factory, final unbound-trace,
  and Windows cfg behavior without ambient compatibility;
- accurate historical reconciliation of explicitly authorized child amendments and R2-3ZWR1; and
- a clean repair proof/review subject suitable for fresh source-bound native evidence.

It does not complete R2-3Z or the R2-3 parent. It does not claim new native evidence, activate
forwarding or provisioning, change platform mapping semantics, modify runtime lifecycle, replace
or stop processes, remove sockets/PIDs/artifacts, perform cleanup/rollback/convergence, or
complete any R3 row.

EXACT PRODUCTION ALLOWLIST

Production/harness changes are limited to:

- `crates/world-mac-lima/src/lib.rs`
- `crates/world-mac-lima/examples/mac_backend_smoke.rs`

Inside `lib.rs`, changes are limited to:

- removing the public contextless `MacLimaBackend::new`;
- removing the public contextless `MacLimaBackend::new_with_vm_name`;
- mechanically migrating colocated tests/helpers that called those functions to
  `MacLimaBackend::new_with_mapping`; and
- minimum private cleanup made unreachable by those removals.

Inside `mac_backend_smoke.rs`, changes are limited to:

- accepting an explicit encoded `InstallBootstrapContextCarrierV1` plus
  `PlatformBootstrapMappingV1` through explicit CLI arguments or an equally explicit
  non-ambient test harness boundary;
- validating/decoding them with shared transport types; and
- constructing only through `MacLimaBackend::new_with_mapping`.

The example must not reconstruct A, home, Lima control root, VM, guest identity, socket, or
transport from environment, CWD, repository paths, `dirs`, defaults, or tool availability. It
must not start a forwarder or make any R3 lifecycle path newly reachable.

No manifest or `Cargo.lock` change is authorized. If the existing dependency surface cannot
support explicit decoding, stop with `BLOCKED_SCOPE_EXPANSION` rather than adding a dependency.

CONDITIONAL TEST-ONLY SHELL ALLOWLIST

After exact reproduction and causal classification, test/test-helper-only changes are permitted
inside these existing files:

- `crates/shell/src/builtins/world_deps/surfaces.rs`
- `crates/shell/src/execution/agent_runtime/control.rs`
- `crates/shell/src/execution/agent_runtime/state_store.rs`
- `crates/shell/src/execution/agent_runtime/host_session_authority/transition_tests.rs`
- `crates/shell/src/execution/host_inbox_materialization.rs`
- `crates/shell/src/execution/orchestrator_world_dispatch.rs`
- `crates/shell/src/repl/async_repl.rs`

This conditional authority applies only to `#[cfg(test)]` modules, test functions, and private
test helpers whose failing assertion/preflight is proven to be caused by:

- R2-3T's final global unbound-`init_trace(None)` failure rule;
- the already-landed explicit-product trace/factory input requirement; or
- R2-3ZWR1's Windows cfg/test-build alignment.

Valid repairs must install or inject explicit typed test authority, update an obsolete ambient
success expectation to the final fail-closed contract, or align a test-only cfg/import. Do not
change a production body, public API, runtime branch, authority selection, lifecycle behavior, or
error behavior in these shell files. Do not disable, ignore, rename, delete, filter around, or
weaken a test.

For each changed test, record:

- the exact failing command and test name before edit;
- the accepted R2-3 landing that changed the expected contract;
- why the repair is test-only and not a production regression mask; and
- the exact passing command after edit.

An unrelated pre-existing failure is not editable under this increment. A required shell file
outside this list or any required production change stops with `BLOCKED_SCOPE_EXPANSION`.

CONTROL-PACK RECONCILIATION ALLOWLIST

Historical reconciliation may edit only:

- `llm-last-mile/runtime-refactor/00-README.md`
- `llm-last-mile/runtime-refactor/02-seam-crosswalk.md`
- `llm-last-mile/runtime-refactor/03-phase-slice-map.md`
- `llm-last-mile/runtime-refactor/04-contracts-and-gates.md`
- `llm-last-mile/runtime-refactor/05-debug-regression-ledger.md`
- `llm-last-mile/runtime-refactor/06-review-finding-inventory.md`

Record only verified facts:

- `scripts/mac/lima-doctor.sh` was an explicitly authorized M4 parity/backslash-safety expansion;
- `crates/shell/src/execution/routing/dispatch/world_ops.rs` was an explicitly authorized F
  live-dispatch expansion;
- `crates/replay/src/state.rs`, `crates/shell/src/execution/auto_sync.rs`,
  `crates/shell/src/execution/cli.rs`, and `crates/world-backend-factory/src/lib.rs` were
  explicitly authorized R expansions/handoff surfaces;
- `crates/world-backend-factory/src/lib.rs` was the explicitly handed-off S1 final cutover
  surface; and
- commit `3a6490ff391453535168491b3f8d26adf9e20938` is the explicit R2-3ZWR1 native-Windows proof
  repair, with its exact twelve-file landed set and narrow cfg/test-build completion boundary.

Do not characterize those prior paths as unauthorized merely because their amendments lived in
the orchestration ledger. Do not retroactively broaden their semantic claims, erase their blocked
attempts, close R2-3Z, or change an R3 ownership rule.

PRE-EDIT SOURCE CLOSURE AND IMPACT

Before any edit:

1. Verify exact base/tree/ref/ancestor, clean checkout, 0/0 divergence, and current GitNexus index
   for the exact task worktree.
   - Check cleanliness before any GitNexus refresh.
   - If the exact checkout is not indexed or is stale, run the repository-required analysis.
   - GitNexus is known to refresh generated count blocks in tracked `AGENTS.md` and `CLAUDE.md`.
     If and only if a post-analysis diff is confined to those generated count blocks, restore
     those two files to the verified starting `HEAD`, record their before/after counts, and
     reverify exact cleanliness. This is authorized analyzer hygiene, not product reconciliation.
   - Any other pre-existing or analyzer-produced diff remains a hard stop. Do not reset, clean,
     or restore any other path.
2. Run file-qualified upstream GitNexus impact with tests for
   `MacLimaBackend::new`, `MacLimaBackend::new_with_vm_name`,
   `MacLimaBackend::new_with_mapping`, and every shell test helper proposed for edit.
3. Warn in commentary before editing any HIGH/CRITICAL symbol and enumerate direct callers,
   affected processes, and affected modules.
4. Query every source caller of the two contextless constructors. Require all legitimate
   production consumers already use typed mapping and every remaining caller be inside the exact
   allowlist.
5. Hash/freeze `MacLimaBackend::new_with_mapping`, mapping validation, transport/forwarding
   lifecycle bodies, PI-077/PI-078 guards, trace writer/rotation/retention, and accepted
   factory/shim/replay behavior.
6. Reproduce the exact R2-3Z shell failures one focused group at a time. Do not begin shell edits
   from the broad failure summary alone.
7. Build a commit/path ledger from accepted child commits and the orchestration-authorized
   amendments. Distinguish actual over-scope from authorized amendment bookkeeping.

Any remaining contextless caller outside the allowlist, required production shell change,
unrelated test repair, lifecycle change, or need to weaken the final R2-3 contract stops.

IMPLEMENTATION CONTRACT

The final source must satisfy:

- there is no callable contextless `MacLimaBackend` constructor in production or examples;
- the smoke harness receives one explicit validated carrier/mapping pair and never reconstructs
  it;
- `MacLimaBackend::new_with_mapping` and its validation semantics remain unchanged except
  mechanical visibility/import cleanup if strictly required;
- production factory, shell, shim, replay, transport, forwarding, and lifecycle behavior remains
  byte/behavior frozen;
- shell tests use explicit authority or assert final fail-closed behavior; no test restores
  ambient/default compatibility;
- every edited test failure is causally owned by an accepted R2-3 transition;
- prior scope amendments are accurately transcribed without altering their semantic boundaries;
  and
- R3 remains the sole owner of forwarding activation, provisioning, process/socket/PID/artifact
  lifecycle, cleanup, rollback, and convergence.

TEST AND PROOF CONTRACT

Run at minimum:

- `cargo fmt --all -- --check`;
- `cargo check --locked -p world-mac-lima`;
- `cargo test --locked -p world-mac-lima -- --nocapture`;
- `cargo clippy --locked -p world-mac-lima --all-targets -- -D warnings`;
- the existing external/cfg supplement harness needed to execute macOS-gated constructor tests on
  Linux, with exact source-copy provenance and no behavior substitution;
- `cargo check --locked -p shell`;
- each focused previously failing shell test before and after repair;
- `cargo test --locked -p shell -- --nocapture` after focused repairs;
- `cargo clippy --locked -p shell --all-targets -- -D warnings`, with any pre-existing
  warnings-denied baseline distinguished from subject failures rather than silently allowed;
- exact source closure proving no contextless macOS/Windows factory consumer remains;
- exact hashes for every frozen body/guard named above;
- semantic manifest and `Cargo.lock` unchanged proof;
- `git diff --check` and `git diff --cached --check`;
- exact unstaged/staged allowlist checks; and
- `gitnexus_detect_changes()` with every affected flow inspected and graph undercoverage
  supplemented by textual closure.

Attempt Apple-target compile/static proof honestly. Report a missing Apple SDK/toolchain as an
environment limitation, not native evidence. Do not rerun or claim native R2-3 evidence here.

SUBJECT FINGERPRINT AND BOUNDED REVIEW

After the final subject settles:

1. Build a deterministic manifest containing the exact expected base plus each allowed staged
   path's mode, blob, stage, and pathname.
2. SHA-256 that manifest and use `sha256:<digest>` as the subject fingerprint.
3. Run fresh independent read-only GPT-5.4 Extra High lenses covering:
   - contextless-constructor removal, typed mapping security, and no lifecycle activation;
   - shell failure causality, explicit-authority test honesty, and production behavior freeze;
   - historical amendment reconciliation, allowlist containment, proof completeness, and
     R2-versus-R3 ownership.
4. Remediate all valid P1/P2 in scope, recompute the fingerprint, and rerun closure review.
5. Causally disposition all P3/P4 against `06-review-finding-inventory.md`.
6. Validate the final review-cycle JSON and require zero open P1/P2 before publication.

STOP CONDITIONS

Stop with the matching blocked receipt if:

- live target/base/tree or task checkout differs;
- any contextless constructor caller requires a file outside the allowlist;
- typed mapping validation or production shell/platform/factory/shim/replay behavior must change;
- a shell failure is unrelated to R2-3 or requires production behavior change;
- a required test/test-helper file is outside the conditional list;
- a manifest, lockfile, other production, script, or generated file requires change;
- a prior amendment cannot be proved from accepted git/orchestration truth;
- any test is weakened, ignored, removed, renamed, or filtered away;
- lifecycle, forwarding, provisioning, deletion, rollback, or convergence becomes reachable;
- a required check cannot run without dishonest substitution; or
- review has any open P1/P2.

PUBLICATION AND RECEIPT

Before commit, require the live product ref still equals
`3a6490ff391453535168491b3f8d26adf9e20938` with tree
`fca816eaed26407ea89d352c7a087a201391bd49`. Stage only exact authorized files, inspect the
staged diff, rerun allowlist/diff/fingerprint/review checks, create one Conventional Commit, and
normal fast-forward push `HEAD` to
`refs/heads/feat/internal-host-orchestrator-world-dispatch-bootstrap`. Never force-push.

Verify live commit/tree, refresh GitNexus, restore only analyzer-generated count churn, and finish
clean with 0/0 divergence.

Send one `codex.top-level-task-receipt.v1` terminal message to the meta task. Success must use
`LANDED_CLEAN`, include exact identity/nonce/base/landed commit/tree/changed paths/fingerprint,
validated review record/digest and finding disposition, proof commands and limitations,
GitNexus/source closure, exact test-failure disposition, control-pack reconciliation summary,
clean status, explicit non-claims, and `next_increment: EVIDENCE:R2-3Z`.

The send to the meta task is your final tool action. Do not dispatch native evidence, do not begin
R2-3Z, and do not begin R2-4 or R3.

COMMON TERMINAL CONTRACT

Before publication:

1. Complete every increment-specific check and proof gate.
2. Verify the exact file/symbol/test allowlist.
3. Run `gitnexus_detect_changes()` and inspect every affected flow.
4. Complete and validate the bounded review sequence.
5. Require zero open blocking findings.
6. Fetch/query the live target again and require it still equals the expected base.

When authorized by the increment contract, create one atomic commit and perform a normal
fast-forward push of `HEAD` to refs/heads/feat/internal-host-orchestrator-world-dispatch-bootstrap. Never force-push. Verify the live remote equals the
landed commit, refresh GitNexus, restore analyzer-only generated count changes if necessary, and
finish clean.

Send a `codex.top-level-task-receipt.v1` message to the meta task. For success, use
`LANDED_CLEAN` and include your bound increment-task thread/host IDs, expected base, landed
commit/tree, changed paths, subject fingerprint, validated review record and digest, finding
disposition, checks, GitNexus result, clean status, and next increment.

For failure, send the exact blocked status, evidence, required authority or platform, and a
complete continuation/handoff prompt.

The `send_message_to_thread` call is your final tool action. After it succeeds, make no more tool
calls or repository changes. Return only the human-readable final report.

Do not generate the next increment prompt and do not begin EVIDENCE:R2-3Z. The meta
orchestrator owns independent verification and subsequent dispatch.
