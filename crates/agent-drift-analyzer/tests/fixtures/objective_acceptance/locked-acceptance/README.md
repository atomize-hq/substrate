# locked-acceptance

Packet `SO-5.1` seeds the required WDAP kickoff cases here:

- `wdap0-integ-linux-kickoff/`
- `wdap0-integ-macos-kickoff/`

These locked acceptance fixtures must keep the `## Scope` mission as the canonical objective while
explicitly forbidding the subordinate `Run this task on a <platform> machine.` checklist line from
being promoted to `goal`.
