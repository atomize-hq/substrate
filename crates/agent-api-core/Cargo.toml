[package]
name = "agent-api-core"
version = "0.1.0"
edition = "2021"

[dependencies]
axum = "0.7"
http = "1.1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tower = "0.5"
agent-api-types = { path = "../agent-api-types" }
thiserror = "1"
async-trait = "0.1"
