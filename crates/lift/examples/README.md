# Examples

This directory is reserved for user-facing example inputs, outputs, and adoption patterns for the `lift` toolchain.

Examples are intentionally separate from fixtures so test assets and documentation assets can evolve independently.
