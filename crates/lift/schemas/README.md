# Schemas

This directory is reserved for hand-authored JSON schemas for engine and app contracts.

The first concrete schema work belongs to the kernel seam, which will introduce `schemas/kernel/primitives.v1.json` as the source of truth for shared primitives. Later app schemas can reference those definitions instead of redefining them.

Commit 1 reserves the directory only and does not add schema files yet.
