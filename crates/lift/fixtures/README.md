# Fixtures

This directory is reserved for deterministic fixture repos, golden artifacts, parse-failure cases, and perf inputs.

Under the corrected engine-first architecture, fixtures are a cross-cutting seam that supports kernel, packs, repo substrate, adapters, graph/query, facts/derive, apps, and export behavior.

Commit 1 does not add runtime fixture content here. It only reserves the directory for later seam work.
