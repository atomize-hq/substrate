version: 1
name: nftables
description: Linux-only example (platform-filtered).
runnable: true
entrypoints: ["nft"]
platforms: [linux]
install:
  method: apt
  apt:
    - name: nftables
probe:
  command: "nft --version"

