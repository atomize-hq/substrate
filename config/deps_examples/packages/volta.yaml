version: 1
name: volta
description: Volta toolchain manager (script install into world-deps prefix).
runnable: true
entrypoints: ["volta"]
install:
  method: script
  script_path: ../scripts/volta.sh
probe:
  command: "volta --version"

