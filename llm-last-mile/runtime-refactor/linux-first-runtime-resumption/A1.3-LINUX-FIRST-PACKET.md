# A1.3 Linux-First Implementation Packet

## Status and authority

- **Status:** selected and separately authorized only as the later Linux-first implementation
  packet that resumes after A1.3-P0 exits; no runtime code, test, installer, or native action is
  performed by this packet record.
- **Selection record:** this record closes
  `AUTHORITY_REQUIRED:RUNTIME_REFACTOR_REENTRY` only as a documentation/control-plane rebind.
- **Active next packet:** [`A1.3-P0-LINUX-FIRST-PREPARATORY-PACKET.md`](A1.3-P0-LINUX-FIRST-PREPARATORY-PACKET.md).
- **Held-later packet:** this A1.3 packet remains the later public/successor adoption and
  startup-result completion packet. It is held open behind A1.3-P0, does not resume here, and may
  not claim `RG-BASE-01` until A1.3-P0 is implemented, independently reviewed, and exits on its
  exact Linux proof wall.
- **Authority boundary:** the later A1.3 implementation task owns only the code/test fence below.
  It must begin from a clean worktree and rebind this record before editing. It must run required
  impact analysis before changing any code symbol and stop on an authority, ancestry, digest,
  ownership, or restoration contradiction.

## Rebound source and evidence

The selection was rebound on 2026-08-20 against the current product branch:

- branch `feat/internal-host-orchestrator-world-dispatch-bootstrap`;
- commit `1ea68308bb79e387fcb874e647ba183d6da7cc4d`;
- tree `e87174a9d0bd4acb736ca82d8233f61e670a795f`;
- parent `3f609602bc4ef58298fdb9c18a000a6cdecec409`; and
- live `origin/main` `7028089742ea6a936d9dd5cd4a4582f8715fd6dd`, which is an ancestor of
  the bound branch head.

An earlier handoff observed `655d3f431cad…` as its `main` merge base. Live rebind proves that it
is an ancestor of the current `origin/main`; the bound branch head and tree remain exact. This is a
remote-tracking advancement, not a source-tree substitution.

The historical corridor is complete through B1/B2.1 joint closeout, B3.1, C1, and the bounded
internal A1.2b packet. The source/evidence references that establish that limit are:

- [`../00-README.md`](../00-README.md) — current control conclusion and corridor chronology;
- [`../03-phase-slice-map.md`](../03-phase-slice-map.md) — the A1.3 row in **A1 bounded packet
  decomposition**;
- [`../04-contracts-and-gates.md`](../04-contracts-and-gates.md) — the reentry contract and
  durable authority contracts;
- [`../05-debug-regression-ledger.md`](../05-debug-regression-ledger.md) — **A1.2b recorded
  result** and `RG-BASE-01`; and
- [`../review-control/a1-2b-differential-evidence.json`](../review-control/a1-2b-differential-evidence.json)
  — accepted A1.2b differential, bound source `658dc5996c50a8d8edcd55111191b37605393490`, and
  required ancestor `83101dcbcc750e6e8fb8979bea19f1f777792188`.

## A1.3-P0 hold amendment

The later frozen evidence packet at remote path
`llm-last-mile/runtime-refactor/review-control/a1-3-p0-scope-expansion-evidence.md`, bound to
commit `6bb9901e9c0f030224e4e6208ac5a399ebc65d85`, proved that the then-current
public Start/turn/reattach paths still entered the legacy hidden-owner launcher corridor and that
the current authority-managed route still crossed readiness, timeout-reconciliation, and legacy
authority-write boundaries while `agent_runtime/auto_attach.rs` remained a separate frozen caller
on the same launcher.

This packet therefore remains preserved but is not the active next implementation packet. The
controlling preparatory split is now
[`A1.3-P0-LINUX-FIRST-PREPARATORY-PACKET.md`](A1.3-P0-LINUX-FIRST-PREPARATORY-PACKET.md). A1.3
retains only the later public/successor adoption and startup-result completion work after that
preparatory packet exits cleanly.

## Selected A1.3 outcome

A1.3 routes public CLI issuance, `run_owner_helper` consumption, owner-helper application, and
real REPL `Attach`/`ResumeOneTurn` through the already-landed authority and claimed intent. It
finishes public Start adoption around the bounded A1.2a-S bootstrap without changing its applied
identity; transports exact actor-bound startup/post-turn protocol events; resolves the still-Pending
startup result; performs real-path parked-successor adoption; and closes `RG-BASE-01`.

`HostSessionAuthority` remains the authority owner. `SurfaceAdapter` transports only exact
actor-bound events; `HostExecutionEpisode` remains observational. No adapter may turn PID, helper,
socket, readiness, timeout, EOF, or local-error posture into startup or post-turn protocol truth.

## Exact implementation fence

### Production edits allowed only in

- `crates/shell/src/execution/agents_cmd.rs`
  - `run_start`, `run_turn`, `run_reattach`, `run_owner_helper`, and
    `build_attach_launch_plan_with_store`;
- `crates/shell/src/repl/async_repl.rs`
  - `prepare_hidden_owner_helper_runtime`, `run_hidden_owner_helper`, and only directly required
    real-REPL adoption/startup/post-turn adapter regions; and
- only those explicit-bootstrap-home resolver call paths proven necessary to thread the already
  opened home through the named CLI/REPL paths. They may not change resolver precedence,
  configuration, policy, or inventory interpretation.

The existing authority API is **consumption-only** in this packet:
`resolve_current_exact`, `issue_successor`, `claim_successor`, `apply_successor`,
`resolve_startup_ownership`, `accept_transition_input`, and `resolve_post_turn`. Any required
change to their semantics, schemas, or persistence is outside A1.3 and stops for separate
authority.

### Tests allowed only in

- `crates/shell/tests/agent_public_control_surface_v1.rs` for focused public Start, parked turn,
  explicit reattach, same-session stop, and REPL-owned-stop coverage; and
- directly required focused CLI/helper/REPL integration fixtures.

`crates/shell/src/execution/agent_runtime/host_session_authority/transition_tests.rs` is a required
regression source for successor issue/claim/apply, startup ownership, input acceptance, post-turn,
and released-transport behavior. Do not weaken, remove, rename, substitute, or mask its existing
assertions or test identities.

## Required acceptance and verification

The implementation may claim A1.3 only if it proves all of the following on Linux:

1. public Start resolves Pending only from exact actor-bound evidence;
2. a parked public turn applies exactly one `ResumeOneTurn`, and explicit reattach applies exactly
   one `Attach`;
3. helper/REPL launch follows durable application; exact retry joins rather than reapplies;
4. exact `OwnershipAccepted`, `RuntimeCreationRejected`, and
   `StartupFailedBeforeOwnership` reconcile only with matching actor/evidence/result identity;
5. timeout, drop, EOF, readiness, process posture, and local adapter errors remain Pending;
6. each applied Resume terminal outcome has an immutable exact-scope protocol event, matching
   completion/post-turn application, and closed input terminalization; and
7. the real public Start → turn/reattach → stop path plus the `RG-BASE-01` unsafe-home negative
   and owner-only-private-home (`0700`) positive smoke are green.

Run focused public control-surface and authority transition tests, both `make shell-lib-wall` and
`make shell-lib-wall-serial` with differential review, formatting, clippy, workspace tests, and the
required Linux real-path smoke/doctor/shim/health evidence. A failed or ambiguous required check
keeps A1.3 open.

## Explicit exclusions and shared-surface rule

A1.3 does **not** authorize A1.4 auto-attach producer adoption; A2/A3 lifecycle or persistence
work; resolver semantics; endpoint redesign; receipts, supervisor, or member-runtime work; Linux
R3 reopening; Windows; or any macOS installer, Lima, Keychain, lifecycle, archive, or native proof
operation.

The named files are shared Unix runtime surfaces. This packet is the required explicit
shared-surface integration packet for its named fence: it assigns ownership, requires
Linux-behavior preservation proof, and requires any cross-platform effect to be named. This rule
does not make macOS parity blocking, does not authorize macOS work, and does not require native
macOS proof.
