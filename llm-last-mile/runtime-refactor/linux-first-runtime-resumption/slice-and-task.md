**Kind:** slice/task
**Stable ID:** `A1.3-family`
**Canonical for:** A1.3-P1, A1.3-P0, and A1.3 slice and task description
**Status:** canonical
**Authority scope:** exact extracted A1.3-family row bodies only
**Source span:** [`../03-phase-slice-map.md#a1-bounded-packet-decomposition`](../03-phase-slice-map.md#a1-bounded-packet-decomposition), D6 pre-extraction lines 142–144
**Supersedes:** canonical ownership of those source rows; the source section remains a compatibility surface
**Superseded by:** none
**Projection consumers:** [`README.md`](README.md), [`../03-phase-slice-map.md#a1-bounded-packet-decomposition`](../03-phase-slice-map.md#a1-bounded-packet-decomposition)
**Source span SHA-256:** `fe0cff009fa8ddfe71eab5fad985ead1c921009e3249ba7f03515d6f1761ae6e`

### A1 bounded packet decomposition — A1.3 family

Current disposition: A1.3-P1 is terminally complete under the exact closure identities in
[`A1.3-P1-LINUX-FIRST-ATOMIC-PUBLIC-ADOPTION-PACKET.md`](A1.3-P1-LINUX-FIRST-ATOMIC-PUBLIC-ADOPTION-PACKET.md).
A1.4 and the enclosing A1 slice are also terminally complete under their separate closure
identities. A2, A3, and Track A are terminally complete under their separate closure identities.
E2 awaits fresh admission and explicit dispatch and receives no authority here.

| Packet | Goal and authority boundary | Exact code and test areas | Contract fields or transitions | Explicit non-goals | Test-first or test-alongside proof and packet exit | Slice gates not yet claimable |
|---|---|---|---|---|---|---|
| **A1.3-P0 — held preparatory split** | Preserved scope-expansion evidence only. This row correctly records the legacy launcher corridor and the frozen auto-attach caller but is not an executable prerequisite. The real public path must land transport adoption, exact actor-event resolution, and the mechanical B1 acceptance-context projection atomically. | Documentation/control only; no implementation authority. | Scope-expansion evidence and exclusions only. | No implementation dispatch, no silent widening, no reinterpretation as active successor. | Never dispatch this row as implementation authority; use A1.3-P1 instead. | Nothing claimable. |
| **A1.3 — held historical public/successor fence** | Preserved older public-adoption fence only. Its omitted `agent_runtime/control.rs` public transport work and omitted acceptance-context projection seam make it non-executable; correcting it here would silently widen the fence. | Documentation/control only; no implementation authority. | Historical public/successor boundary only. | No silent widening; no use as active successor. | Never dispatch this row as implementation authority; use A1.3-P1 instead. | Nothing claimable. |
| **A1.3-P1 — terminally complete atomic public adoption and startup/post-turn completion** | In one bounded packet, land the new authority-managed public transport path for public `Start`, turn, and explicit reattach; consume exact A1.2 authority on the real public CLI/helper/REPL path; correct Start continuity with additive `EstablishStartContinuation` and `SettleStartTurn` HSA phases around the caller's single real inaugural exchange; resolve exact startup and post-turn actor events; and mechanically project already HSA-authorized `host_transition_correlation` into the existing B1 acceptance-context seam. | Only the narrowly required, explicitly versioned HSA Start-continuation schema/protocol, transition, facade/persistence/validation, exports, and focused tests under `agent_runtime/host_session_authority/` and `agent_runtime/mod.rs`; only the new public transport/shared real-Start execution and typed continuation/settlement result in `agent_runtime/control.rs`; only the named public `run_start`/`run_turn`/`run_reattach`/`run_owner_helper` regions in `execution/agents_cmd.rs`; only the exact retained-turn acceptance-context construction/projection seam in `execution/orchestrator_world_dispatch.rs`; only owner-helper validation/application, exact startup/post-turn and real-Start continuation/completion event submission, remaining public/real REPL adoption, and explicit-bootstrap-home threading in `repl/async_repl.rs`; only the A1.1 explicit-home resolver entry points needed by those named CLI/REPL paths; focused HSA, CLI, helper, and REPL integration tests. | Existing Start application may transiently be `ActiveAttached` without a handle. `PromptNotSubmitted` alone permits automatic provider submission; the durable `PromptSubmissionNoReplayBarrier` immediately before handoff permits no replay and proves no acceptance; loss before authenticated acceptance becomes durable `PromptSubmissionIndeterminate`, which is unsuccessful, nonresumable, and exact-joins with zero provider submissions. `EstablishStartContinuation` authenticates and registers an immutable handle against the exact authority store/session/participant/backend/protocol/Start intent/application/run/revision while preserving `ActiveAttached`; `SettleStartTurn` preserves that handle and moves the same inaugural turn to `ParkedResumable`, `AwaitingAttention`, or `Terminal` only from exact Substrate-owned completion and canonical obligation evidence; exact mechanical B1 acceptance-context projection of already HSA-authorized correlation. | No reinterpretation of historical V1/V2/V3 records; no synthetic/empty/hidden/bootstrap/warm-up/substitute prompt; no provider-specific submit-or-join or durable submission supervisor; no backend ID, PID, process, readiness, helper/socket/endpoint/plan, timeout, EOF, delivery, adapter, compatibility, or legacy StateStore posture authority; no legacy StateStore authority/schema/persistence-semantics change; no B1/C1/B3.1 semantic change; no legacy helper or auto-attach change; no macOS, Windows, or R3 work; no `orchestrator_world_dispatch.rs` change outside its exact projection seam. | Prove Start submits the caller's real prompt at most once automatically; successful nonterminal Start returns only after authenticated continuation registration and inaugural-turn settlement; clean, attention-bearing, and terminal outcomes settle to the exact three postures; success without a handle is impossible; backend ID alone cannot settle posture; pre-handoff retry can submit once; ambiguous-handoff retry returns `submission_outcome_unknown` with zero submissions; registration-before-settlement remains unsuccessful and never replays; settlement-before-response joins committed success without replay; stale/substituted/mismatched evidence fails closed; HSA `ResumeOneTurn` issue/application accepts the Start handle; existing Attach/Resume, compatibility, auto-attach, exact startup actor-event behavior, and mechanical B1 projection remain green. | Bounded auto-attach producer adoption; complete A1 regression closure; A1 completion. |

The Start-continuity correction owned only public `Start`; it did not own public `Turn`/`run_turn`
adoption or legacy StateStore authority writes. Its successor-readiness gate was only that Start
produced a valid durable handle and HSA could use that handle to issue/apply the existing
`ResumeOneTurn` protocol. The completed A1.3-P1 public-adoption work later routed public Turn
through that protocol.
