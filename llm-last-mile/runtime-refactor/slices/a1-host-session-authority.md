**Kind:** slice
**Stable ID:** `a1-host-session-authority`
**Canonical for:** extracted A1 slice row plus bounded packet-decomposition projection only
**Status:** terminally complete
**Authority scope:** exact extracted Track A A1 row plus the extracted A1 bounded packet-decomposition framing/projection only; no schedule, dispatch, or implementation authority
**Source span:** composite of [`../03-phase-slice-map.md`](../03-phase-slice-map.md) line 130 and lines 134–146
**Supersedes:** canonical ownership of the extracted `A1 — HostSessionAuthority facade` row and bounded packet-decomposition framing/projection
**Superseded by:** none
**Projection consumers:** [`track-a-authority-and-surface-neutrality.md`](track-a-authority-and-surface-neutrality.md), [`tasks/a1-4-auto-attach-producer-adoption.md`](tasks/a1-4-auto-attach-producer-adoption.md), [`../03-phase-slice-map.md`](../03-phase-slice-map.md)

# A1 — HostSessionAuthority facade

> **Authority boundary:** This file owns only the extracted Track A A1 row and the bounded packet-decomposition framing/projection below. A1.1–A1.4 remain substeps/checkpoints inside A1 rather than independent slices; A1.1d and historical entries remain checkpoints, not slices; held `A1.3-P0` and `A1.3` remain preserved history only; [`../linux-first-runtime-resumption/A1.3-P1-LINUX-FIRST-ATOMIC-PUBLIC-ADOPTION-PACKET.md`](../linux-first-runtime-resumption/A1.3-P1-LINUX-FIRST-ATOMIC-PUBLIC-ADOPTION-PACKET.md) and [A1.4](tasks/a1-4-auto-attach-producer-adoption.md) are terminally complete. This record closes A1 without dispatching A2/A3 or widening any completed packet.

Current disposition: A1.3-P1 and A1.4 are terminally complete under their exact closure identities.
The final A1 wall makes `RG-BASE-01`, `RG-BASE-02`, the A1-scoped clauses of `RG-AUTH-01` and
`RG-AUTH-02`, and all of `RG-AUTH-03` green. Earlier A1 packets are complete, the Linux A1.1d
closeout is landed, and native macOS is a separate non-predecessor lane under the controlling
Linux-first decision. No other A1 condition remains, so A1 is terminally complete. The
ledger-wide remainder of `RG-AUTH-01`/`RG-AUTH-02` and all A2/A3 work remained open and
undispatched at A1 close. A2 and A3 later completed under their own authority and closure
identities; Track A is now terminally complete, and E2 remains undispatched.

| Slice | Goal | Must-read docs | Sibling context | Allowed code areas | Explicit non-goals | Exit gate | Regression gates |
|---|---|---|---|---|---|---|---|
| **A1 — HostSessionAuthority facade** | Establish the only API that resolves exact durable session/caller/binding truth, owns revision-checked posture transitions, preserves strict V1/V2/V3 record meaning, uses strict `HostSessionTransitionIntentV2` for production Start plus additive Start-continuation registration/settlement, and uses the independently reviewed strict V3 successor intent for real `Attach` and `ResumeOneTurn` paths. | `01` Authority map + invariants 1–3; `02` HostSessionAuthority and helper-plan/mode/parent-control/private-stop/auto-park rows; `04` `DurableSessionAuthorityV1` + strict V1/V2 Start and V3 successor-intent contract; corrected A1.3-P1 Start-continuity protocol; `DESIGN-host-orchestrator-world-dispatch-contract.md` exact identity rules | StateStore; CompatibilityReadModel; HostExecutionEpisode; WorldDispatchControl; AutoAttachProjection | Bounded modules under `crates/shell/src/execution/agent_runtime/host_session_authority/`, including the narrowly versioned A1.3-P1 Start-continuation schema/protocol, transitions, facade/persistence/validation, and focused tests; bounded facade types/re-exports and integration only in `agent_runtime/{orchestration_session,mod}.rs`; `agent_runtime/state_store.rs` only as the bounded persistence/integration surface; bounded explicit-bootstrap-home entry points only in `crates/common/src/paths.rs` and `crates/shell/src/execution/{config_model,policy_model,policy_snapshot,agent_inventory}.rs`; in `agent_runtime/control.rs`, only `HiddenOwnerHelperLaunchPlan` and its write/load/remove/launch integration plus the separately bounded A1.3-P1 atomic public-adoption and shared real-Start execution path; in `execution/agents_cmd.rs`, only `Start`/`Attach`/`ResumeOneTurn` intent issuance and `run_owner_helper` consumption; in `repl/async_repl.rs`, only owner-helper validation/application, exact real-Start continuation/completion submission, and real REPL adoption; in `agent_runtime/auto_attach.rs`, only `build_auto_attach_launch_plan` and directly required plan integration; focused tests | No reinterpretation or backfill of historical V1/V2/V3 records; no synthetic Start prompt; no provider-specific submit-or-join or durable submission supervisor; no backend/process/transport or legacy compatibility posture authority; no legacy StateStore authority/schema/persistence-semantics change; no general `HostExecutionEpisode` demotion; no endpoint/path redesign; no helper removal; no auto-attach policy, eligibility, or settlement redesign; no receipt/supervisor work; no config/policy/inventory precedence, merge, or interpretation changes; no unrelated lifecycle, transport, or schema cleanup. | All real CLI and REPL transitions route through `HostSessionAuthority`: Start uses a durable revision-bound V2 intent, persists a no-replay barrier immediately before the at-most-once automatic submission, treats an ambiguous handoff as durable `PromptSubmissionIndeterminate` rather than acceptance or success, durably establishes an exact authenticated provider continuation while transiently `ActiveAttached`, and durably settles that same inaugural turn to `ParkedResumable`, `AwaitingAttention`, or `Terminal` before success; indeterminate retry performs zero submissions, and exact registration/settlement retry joins without prompt replay; Attach/Resume use the V3 successor intent; stale, substituted, mismatched, expired, or conflicting replay fails closed; stale observations cannot overwrite newer authority; no backend identifier or process observation owns posture; `RG-BASE-01` and `RG-BASE-02` remain green. Do not claim A1 before A1.1–A1.4, A1.3-P1, and the final proof wall complete. | `RG-AUTH-01`, `RG-AUTH-02`, `RG-AUTH-03`, `RG-BASE-01`, `RG-BASE-02` |

For the current Start-continuity correction, “later ResumeOneTurn” means HSA issue/application proof
against the Start-produced handle only. Public `Turn`/`run_turn` routing is excluded and remains the
responsibility of the later A1.3-P1 public-adoption work. This correction also does not restore or
add legacy StateStore authority writes.

### A1 bounded packet decomposition

A1 is one independently reviewable slice implemented in the following order. Each packet must remain green and reviewable before the next begins; prerequisites established by an early packet do not satisfy later production-adoption or slice-closeout gates.

| Packet | Goal and authority boundary | Exact code and test areas | Contract fields or transitions | Explicit non-goals | Test-first or test-alongside proof and packet exit | Slice gates not yet claimable |
|---|---|---|---|---|---|---|
| **A1.1 — authority-store core** | [Canonical A1.2/earlier-histories family row](../a1-2-earlier-histories/slice-and-task.md#a11-aggregate-packet-row). | — | — | — | — | — |
| **A1.2 — intent issuance/claim/application** | [Canonical A1.2/earlier-histories family row](../a1-2-earlier-histories/slice-and-task.md#a12-aggregate-packet-row). | — | — | — | — | — |
| **A1.3-P0 — held preparatory split** | Canonical content: [`linux-first-runtime-resumption/slice-and-task.md#a1-bounded-packet-decomposition--a13-family`](../linux-first-runtime-resumption/slice-and-task.md#a1-bounded-packet-decomposition--a13-family). | — | — | — | — | — |
| **A1.3 — held historical public/successor fence** | Canonical content: [`linux-first-runtime-resumption/slice-and-task.md#a1-bounded-packet-decomposition--a13-family`](../linux-first-runtime-resumption/slice-and-task.md#a1-bounded-packet-decomposition--a13-family). | — | — | — | — | — |
| **A1.3-P1 — terminally complete atomic public adoption and startup/post-turn completion** | Canonical content: [`linux-first-runtime-resumption/slice-and-task.md#a1-bounded-packet-decomposition--a13-family`](../linux-first-runtime-resumption/slice-and-task.md#a1-bounded-packet-decomposition--a13-family). | — | — | — | — | — |
| [**A1.4 — bounded auto-attach producer adoption and regression closure**](tasks/a1-4-auto-attach-producer-adoption.md) | Make the existing auto-attach launch-plan producer issue/reference the same exact `Attach` intent, then close A1 without changing projection policy or settlement ownership. | Only `build_auto_attach_launch_plan` and directly required plan integration in `agent_runtime/auto_attach.rs`; focused auto-attach producer/consumer tests; final A1 CLI/REPL/regression/smoke wall. | Exact `Attach` precondition/revision, claim identity, immutable payload hash, host/session/binding/descriptor commitments, and idempotent applied result. | No auto-attach policy, eligibility, claim, or settlement redesign; no router responsibility expansion; no endpoint/path redesign; no A2 demotion; no subsequent-slice work. | Prove manual and auto-attach cannot substitute or double-apply an intent, retry/restart converges, every A1-scoped clause of `RG-AUTH-01`/`RG-AUTH-02`, all of `RG-AUTH-03`, and `RG-BASE-01`/`RG-BASE-02` pass on real CLI and REPL paths; only then may A1 close. | No A1 gate remains claimable until this packet's final wall passes; the ledger-wide `RG-AUTH-01`/`RG-AUTH-02` rows remain unresolved for A2/A3, and later sibling-seam gates remain out of scope. |
