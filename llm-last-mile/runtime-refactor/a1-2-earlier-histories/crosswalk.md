**Kind:** crosswalk and inventory record
**Stable ID:** `A1.2-earlier-histories-family`
**Status:** canonical historical/completed-family record
**Authority scope:** exact extracted family-local source bodies only; no implementation authority
**Supersedes:** canonical ownership of the extracted source bodies; source headings/rows remain compatibility anchors
**Superseded by:** none
**Projection consumers:** [`README.md`](README.md)
**Canonical for:** A1.2 HostSessionAuthority seam row and earlier A0/A1.1d crosswalk inventories only
**Source span:** composite of the four preserved root compatibility spans listed in the extraction ledger

# A1.2 and earlier packet-histories crosswalk

## HostSessionAuthority family row

| Seam | Current code artifacts | Current semantic status | Authority boundary correct? | Enforcement point correct? | Proven by smoke/e2e? | Refactor action | Sibling seams that must stay in context |
|---|---|---|---|---|---|---|---|
| HostSessionAuthority | component-only R0 reservation/application/proof maps and strict object persistence in `agent_runtime/host_session_authority`; `agent_runtime/orchestration_session.rs`; resolution and posture logic spread across `state_store.rs`, `agents_cmd.rs`, `control.rs`, and `async_repl.rs` | `MislandedWrongModel` | no | not applicable | no | A1.1 establishes safe bootstrap/home/object/key/greenfield-certification/resolution/CAS primitives only and refuses pre-A1 authority state. A1.1d additionally requires every pre-A1 session/participant read-decide-write transaction to enter one retained opened physical-root capability whose trusted root, descendant handles, exact identity, root lock, and activation observation survive through the final file and directory `fsync`; no guarded operation may reread `SUBSTRATE_HOME`, use ambient CWD, reconstruct an absolute descendant path, or follow a rebound lexical root. A1.1e resolves config, effective policy, inventory, and a bound StateStore capability from the accepted bootstrap-home identity, but by itself its exact reader requires an already-existing `SessionNamespaceRecordV1::Authority` and omits the caller backend/role projection required by the prepared dispatcher. That historical gap selected Case B; A1.2a and A1.2a-S now boundedly create and adopt the authority for the ordinary internal greenfield host path without closing the broader seam. A1.2a is the smallest authority prerequisite: one strict greenfield-only V1-to-V2 root upgrade followed by greenfield Start reservation/issuance/claim/application, initial authority birth with startup ownership Pending, exact retry, and a typed read result joining the applied descriptor to exact store/session/participant/workspace/world/revision/policy truth. V1 remains strict and a nonempty/unsafe V1 root is not converted. A1.2a has no Attach/Resume, startup resolution, post-turn, obligation-cut, correlation-supply, or public-consumer adoption. A1.2a-WB corrects only the Start write/read matrix: descriptor and launch scope remain equal; `Host` accepts `None` or exact `Some`, `World` requires exact `Some`, and the exact current-authority reader must accept the same persisted combinations while any present binding remains session authority rather than participant placement. A1.2a-S is the separate smallest SurfaceAdapter prerequisite: it adopts only the ordinary internal greenfield host bootstrap, constructs compatibility projection from the applied result, carries the bound capability to the live toolbox, suppresses activated legacy writes, and leaves startup ownership Pending. R0 now provides a distinct HSA-owned non-transition CAS that appends exactly one retained lineage member/ref and immutable registration proof while preserving active caller, posture, binding, policy, and origin; it is review-clean component proof with no production ingress and does not promote this seam. Pending startup continues to name the original application revision; A1.2b now completes the bounded internal durable successor/post-turn protocol by preserving the strict V2 Start and R0 state under V3 publication and keeping successor `Attach`/`ResumeOneTurn`, startup/post-turn reconciliation, ledger-snapshot consumption, `ReleaseEligible` transport state, Released-state exact join validation, and retry inside HostSessionAuthority without interpreting obligations. `ObligationLedger` remains the snapshot/cut authority. The active A1.3-P1 packet, not the held historical A1.3 fence, owns the remaining public CLI/helper/successor adoption and exact startup-result completion; A1.4 retains bounded auto-attach producer adoption. A2, not early B work, performs general episode demotion; A3 still owns persistence/compatibility separation. | SurfaceAdapter; StateStore; CompatibilityReadModel; WorldDispatchControl; ObligationLedger |

## A1.1d-5R2 canonical prefix-propagation inventory

This is the implementation inventory frozen by A1.1d-5R2-0 at
`6ab2a515e13946324d0aac25b144e1c3408cb2c1`. It is source truth, not a claim that the
current behavior is correct. `A` means the selected normalized host prefix;
`H`/`R`/`P` mean the current `SUBSTRATE_HOME`/`SUBSTRATE_ROOT`/intended-principal
source. `amb` means an ambient process value; `acct` means the platform account database or
Windows access-token identity; `none` means no carrier exists. `IH` is the authoritative
`InstallBootstrapContextV1`; `PM` is the verified `PlatformBootstrapMappingV1`. A generated
projection may be consumed, but it never becomes selection authority.

### Live current-state edges

| ID | Public entry and live source/section | Current context sources (`A`; `H`; `R`; `P`) | Child/helper and privilege boundary | Current explicit carrier; ambient fallback | Projection consumed; platform mapping |
|---|---|---|---|---|---|
| PI-001 | Unix dev install; `scripts/substrate/dev-install-substrate.sh` option parsing and `bootstrap_private_home` | `--prefix` else `$HOME/.substrate`; `A`; unset; `id`/`SUDO_USER` mix | shell bootstrap; none | `A` argument plus `H=A`; `$HOME` selects default | none; host |
| PI-002 | Unix dev install; `scripts/substrate/dev-install-substrate.sh` shim-deploy block | inherited `A`; outer/unset; `A`; inherited | `substrate --shim-deploy`; none | only `R=A`; shim path code may resolve `H` from amb | shim tree; host |
| PI-003 | Unix dev install; `scripts/substrate/dev-install-substrate.sh::{generate_env_file,write_manager_env_script}` and helper writer | inherited `A`; env writes A but manager consumes outer H before A; unset; inherited | generated `env.sh`, manager and helper; none | literal/self path is partial; generated manager accepts outer `H` | env/manager/helper; host |
| PI-004 | Unix dev install; config/version/install-state/dependency projection writers | inherited `A`; `A`; `A`; inherited | file writers; none | prefix-relative destinations; consumers can later reread amb | config/version/state/deps; host |
| PI-005 | Unix dev install; runtime dependency add loop | inherited `A`; `A`; `A`; inherited | installed `substrate deps add`; none | `H=A,R=A`; command children otherwise inherit amb | dependency registry; host |
| PI-006 | Unix dev install; runtime current sync/list block | inherited `A`; `A`; `A`; inherited | installed `substrate deps current/list`; none | `H=A,R=A`; none at this edge | dependency/runtime manager; host |
| PI-007 | Unix dev install; failure rollback block | inherited `A`; inconsistent; partial `A`; inherited | rollback helper/dependency remove; none | partial tuple; helper may reconstruct from amb | dependency state; host |
| PI-008 | Unix dev install; Linux world provisioning branch | inherited `A`; `A`; unset; `SUDO_USER`/`id` | `scripts/linux/world-provision.sh`; sudo inside child | only `H=A`; child reconstructs root/principal | units/socket/drop-in; Linux host |
| PI-009 | Unix dev install; macOS provisioning branch | inherited `A`; unset; unset; host `id` | `scripts/mac/lima-warm.sh`; host-to-guest | no context carrier; host/guest defaults | Lima config/unit; Lima |
| PI-010 | Unix dev uninstall; `scripts/substrate/dev-uninstall-substrate.sh` parsing/bootstrap | `--prefix` else `$HOME/.substrate`; `A`; unset; `id` | bootstrap; none | `A` plus `H=A`; `$HOME` selects default | install state opportunistically; host |
| PI-011 | Unix dev uninstall; `scripts/substrate/dev-uninstall-substrate.sh` shim-remove block | inherited `A`; outer/unset; `A`; inherited | `substrate --shim-remove`; none | only `R=A`; shim path code may resolve `H` from amb | shim tree; host |
| PI-012 | Unix dev uninstall; prefix/system/socket removal sections | inherited `A`; inconsistent; inconsistent; inherited | `rm`, link/socket helpers; sudo/system boundary | paths and globs, not `IH`; amb home used by mac socket | installed/managed artifacts; host/Linux/macOS |
| PI-013 | Unix release install; `scripts/substrate/install.sh` prefix detection/delegation | `--prefix` else `$HOME/.substrate`; outer; child arg; `SUDO_USER`/`id` | `install-substrate.sh`; possible sudo/setup boundary | public `--prefix=A`; child receives no `IH` | release archive/source; host |
| PI-014 | Unix release install; `scripts/substrate/install-substrate.sh` parsing and bootstrap | prefix arg; `A`; unset; primary-user heuristic | bootstrap; none | `H=A` plus separate user string; root absent | none; host |
| PI-015 | Unix release install; `scripts/substrate/install-substrate.sh::deploy_shims` | inherited `A`; outer/unset; outer/unset; inherited | installed `substrate --shim-deploy`; none | no per-call context; amb/default resolution can win | shim tree; host |
| PI-016 | Unix release install; `scripts/substrate/install-substrate.sh` env/manager/config/state/version/deps writers | inherited `A`; env writes A but manager consumes outer H before A; unset/derived; inherited | generated projections; none | literal/self path partial; manager accepts outer `H` | env/manager/config/state/version/deps; host |
| PI-017 | Unix release install; dependency add/runtime provision | inherited `A`; `A`; `A`; inherited | `substrate deps add` and runtime installer; subprocesses | tuple at selected calls; nested children may reconstruct | dependency/runtime projections; host |
| PI-018 | Unix release install; dependency current/list/sync | inherited `A`; `A`; `A`; inherited | `substrate deps current/list`; subprocesses | tuple at selected calls; no commitment | dependency/runtime state; host |
| PI-019 | Unix release install; failure rollback | inherited `A`; inconsistent; inconsistent; inherited | dependency/helper rollback; none/sudo by child | no complete carrier; amb reconstruction | installed candidates; host |
| PI-020 | Unix release install; embedded Linux service/unit generation | inherited `A`; `A` in unit; partial; primary user | unit/socket/drop-in writer; root-owned files | literal `H=A`; `ReadWritePaths` uses amb `$HOME` | systemd projections; Linux host |
| PI-021 | Unix release install; package/user/service privileged sections | inherited `A`; partial; partial; `SUDO_USER`/primary user | package-manager commands, `sudo groupadd/usermod/install/systemctl`; sudo | separate args/env, no complete carrier | packages/group/membership/units/service state; Linux host |
| PI-022 | Unix release install; macOS Lima branch | inherited `A`; partial; partial; host user | `lima-warm.sh`; host-to-guest | no verified mapping carrier | Lima config/unit; Lima |
| PI-023 | Unix release install; final doctor/dependency checks | inherited `A`; `A`; `A`; inherited | `world doctor`, `shim doctor`, `health`, deps checks | tuple only; individual diagnostics can reread amb | doctor JSON/runtime state; host/platform |
| PI-024 | Unix release uninstall; `scripts/substrate/uninstall.sh` installed-child selection | explicit/outer/default search; outer; `A`; current user | selected `uninstall-substrate.sh`; none | child path selected by `R`; no `IH` | installed uninstaller; host |
| PI-025 | Unix release uninstall; `scripts/substrate/uninstall-substrate.sh` parsing/bootstrap | reconstructed/ambient; amb; partial; `SUDO_USER`/`id` | bootstrap and removal helpers; sudo later | no matching authoritative carrier; `$HOME` can retarget | install state/config; host |
| PI-026 | Unix release uninstall; prefix/system removal sections | reconstructed/ambient; amb; partial; inherited | Python/shell deletion, sudo service helpers | paths from amb/home; no managed identity | prefix and managed artifacts; Linux/macOS |
| PI-027 | `substrate world enable`; `crates/shell/src/builtins/world_enable/runner.rs` and helper-script launch | CLI process-selected amb; amb; amb; process account | generated `world-enable.sh`; subprocesses/sudo later | inherited env plus helper args; no `IH` | manager/dependency state; host |
| PI-028 | direct Linux provision; `scripts/linux/world-provision.sh` option/bootstrap section | `$SUBSTRATE_HOME` else account home `/.substrate`; `A`; unset; `SUDO_USER`/`id` | bootstrap; sudo crossings follow | `H=A` and user strings; amb/account fallback selects | none; Linux host |
| PI-029 | direct Linux provision; unit/socket/drop-in generation | inherited/reconstructed `A`; `A`; partial; resolved user | template/install helpers; root-owned files | literals and separate env; no commitment | systemd units/socket/drop-in; Linux host |
| PI-030 | direct Linux provision; group/member/ACL/linger/restart sections and installed `scripts/linux/substrate-apply-socket-acl.sh` child | inherited/reconstructed `A`; partial; partial; installer uses `SUDO_USER`/UID lookup while helper independently enumerates `substrate` group members | `sudo groupadd/usermod/setfacl/loginctl/systemctl`; ACL helper receives only mode, target, and group, including later systemd `ExecStartPost` | separate user/home values across sudo; helper receives no account, UID, carrier, or commitment | account/ACL/systemd state; Linux host |
| PI-031 | Linux provision disable/removal/legacy replacement sections | inherited/reconstructed `A`; partial; partial; resolved user | `rm`, `systemctl disable`, replacement cleanup; sudo | literal paths/legacy names; no ownership carrier | managed and legacy artifacts; Linux host |
| PI-032 | `substrate --shim-deploy/--shim-remove`; `crates/shell/src/execution/invocation/plan.rs` and `ShimDeployer` calls | no public prefix option; `substrate_home()`; `substrate_root()`/binary; process account | shim deployer/remove; filesystem | process env only; `H` falls back to `dirs::home_dir()` | shim state; host |
| PI-033 | `substrate --shim-status`; invocation plan/status renderer | same process resolution; amb/fallback; amb/derived; process account | shim status/deployer inspection; none | process env only | shim state; host |
| PI-034 | `substrate shim doctor`; `builtins/shim_doctor/report.rs::{build_report,trace_log_path}` | process resolution; amb/fallback; amb/derived; process account | shim/status/trace checks; none | process env; trace path directly falls back to default home | shim and trace projections; host |
| PI-035 | `substrate world doctor` and `substrate health`; doctor aggregation/platform checks | process resolution; amb/fallback; amb/derived; process account | shim/world/service/platform doctors | inherited env and backend defaults | doctor JSON/service state; host/platform |
| PI-036 | direct `substrate deps add/remove/current/list`; world-enable provision-deps runner | process resolution; amb/fallback; amb/derived; process account | dependency manager and child installer | inherited tuple, manager/helper can self/amb resolve | dependency registry/install state; host |
| PI-037 | runtime-family/Codex provisioning; world-enable `provision_deps.rs` and generated helper | process resolution; amb/fallback; amb/derived; account/guest user | provider installer, manager and world helper | inherited env plus script args; nested context not committed | runtime/dependency projections; host/world |
| PI-038 | generated runtime manager/env consumers; `execution/{env_scripts.rs,manager.rs}` and world-enable manager env | installed/self location; manager prefers outer `H`; partial `R`; process account | source/exec generated scripts | literal/self values plus conflicting amb acceptance | env/manager/helper; host |
| PI-039 | macOS mapping; `scripts/mac/lima-warm.sh` configure/provision sections | host selection absent; guest `$HOME/.substrate`; guest unset; host/guest `id` independently | `limactl shell`/guest provision; host-to-Lima | VM name/project args only; guest home reconstructed | Lima config and guest unit; Lima |
| PI-040 | macOS unit/doctor; `lima-warm.sh` service templates and `lima-doctor.sh` | no host commitment; guest account home; partial; guest `id` | guest systemd/doctor via Lima | VM name only; guest defaults | unit/socket/doctor state; Lima |
| PI-041 | macOS selected socket/control store; `world-mac-lima/{lib.rs,forwarding.rs,transport.rs}` and invocation-plan mac socket logic | process selection; `substrate_home()`/`dirs`; derived; host account | `LIMA_HOME`/`HOME` selects SSH config store; SSH UDS/VSock/TCP selector, forwarder, and socket status/remove | process env only; `LIMA_HOME`, default host home, and tool availability fallbacks | Lima store, host socket/forwarding state; Lima |
| PI-042 | Windows dev install; `scripts/windows/dev-install-substrate.ps1` prefix/context entry | `-Prefix` else `$env:LOCALAPPDATA\Substrate`; partial; `A`; current token | same-script install graph | prefix parameter; ambient LOCALAPPDATA default | profile/shim/config/state; Windows host |
| PI-043 | Windows dev uninstall; `dev-uninstall-substrate.ps1` prefix/context entry | `-Prefix` else `$env:LOCALAPPDATA\Substrate`; partial; `A`; current token | same-script uninstall graph | prefix parameter; ambient LOCALAPPDATA default | profile/shim/prefix state; Windows host |
| PI-044 | Windows release install; `install-substrate.ps1` prefix/context entry | `-Prefix` else `$env:LOCALAPPDATA\Substrate`; profile would set `H=R=A`; `A`; current token | same-script install graph | prefix parameter; ambient LOCALAPPDATA default | profile/shim/doctor; Windows host |
| PI-045 | Windows release install; WSL warm call | inherited `A`; partial; partial; current token | `wsl-warm.ps1` reaches PI-077 guard before forwarder/provision; host-to-WSL | distro/project only; host context absent | no realized state at live guard; WSL |
| PI-046 | Windows release uninstall; `uninstall-substrate.ps1` prefix selection | `-Prefix` else `$env:LOCALAPPDATA\Substrate`; partial; partial; current token | profile/forwarder/WSL/prefix removal | prefix parameter plus ambient paths; no `IH` | install/profile/forwarder/WSL state; Windows/WSL |
| PI-047 | Windows uninstall; wildcard/forwarder/WSL deletion sections | selected `A` plus USERPROFILE wildcard; amb; partial; current token | `Remove-Item`, stop forwarder, `wsl --unregister` | wildcard/LOCALAPPDATA/distro name; no ownership carrier | prefix/shared/platform artifacts; Windows/WSL |
| PI-048 | Windows host-to-WSL; `wsl-doctor.ps1`, `world-windows-wsl/{backend.rs,paths.rs,transport.rs}`; warm is guarded by PI-077 | host selection absent; guest/account fallback; partial; Windows and guest principals separate | `wsl.exe`, named pipe/TCP transport | distro/project/port; no verified host commitment | distro/runtime/doctor state; WSL |
| PI-049 | Windows forwarder/shim shared state; `start-forwarder.ps1`, `forwarder/src/{config.rs,logging.rs,windows.rs}`, shim scripts | prefix-independent paths; LOCALAPPDATA/USERPROFILE; partial; current SID not recorded | process/PID/pipe/log/shim management | ambient Windows folders and global names | forwarder PID/log/pipe/shims; Windows host |
| PI-050 | additional production consumers; `crates/common/src/paths.rs::substrate_home`, invocation/doctor/platform callers | no public context object; amb `H` else `dirs::home_dir()/.substrate`; derived `R`; process account | config/trace/socket/shim/world consumers | process env only; default-home fallback | config/trace/socket/state projections; host/platform |
| PI-051 | macOS public stop helper; `scripts/mac/lima-stop.sh` whole script | no host selection; guest not read; unset; no principal | `limactl list/stop substrate`; host-to-Lima action | hard-coded VM name; no carrier or host commitment | Lima instance status; Lima |
| PI-052 | Windows public pipe diagnostic; `scripts/windows/pipe-status.ps1` parameter/connection sections | no prefix; no H; no R; current token unused | named-pipe connect/status; none | explicit/default global pipe path; no SID/instance commitment | pipe status; Windows host/WSL forwarder |
| PI-053 | Windows public WSL stop; `scripts/windows/wsl-stop.ps1` whole script | no prefix; LOCALAPPDATA PID; unset; current token not recorded | stop processes, remove PID, `wsl --terminate`; destructive/shared boundary | distro name plus ambient PID/global process scan/pipe; no carrier | forwarder PID/process/pipe/distro state; Windows/WSL |
| PI-054 | generic backend factory; `crates/world-backend-factory/src/lib.rs::factory` | no prefix; backend constructors read amb/default; derived; process account | `MacLimaBackend::new`/`WindowsWslBackend::new`; platform boundary | no context/mapping parameter | backend/socket/pipe defaults; Lima/WSL |
| PI-055 | macOS platform adapter; `crates/shell/src/execution/platform/macos.rs` backend/socket/doctor call sites | process resolution; `substrate_home()`/default; derived; host account | managed socket plus `MacLimaBackend::new`; host-to-Lima | process env and contextless constructor | socket/backend/doctor state; Lima |
| PI-056 | macOS platform-world adapter; `crates/shell/src/execution/platform_world/mod.rs::{connect_world_transport,build_world_backend}` macOS sections | process resolution; default host socket; derived; host account | managed socket and `MacLimaBackend::new`; host-to-Lima | no mapping parameter | transport/backend state; Lima |
| PI-057 | Windows platform-world adapter; `crates/shell/src/execution/platform_world/windows.rs::build_world_backend` | no prefix; backend reads env/default; derived; Windows token not carried | `WindowsWslBackend::new`; host-to-WSL | no mapping parameter | pipe/backend state; WSL |
| PI-058 | world gateway platform transport; `crates/shell/src/builtins/world_gateway.rs::gateway_transport` | process resolution; default host socket/pipe; derived; process account | platform transport selection/backend | process env/default path | socket/pipe gateway state; Lima/WSL |
| PI-059 | macOS backend native proof entry; `crates/world-mac-lima/examples/mac_backend_smoke.rs::main` | no prefix; constructor reads amb/default; derived; host account | `MacLimaBackend::new`; host-to-Lima | no mapping parameter | smoke/backend/socket state; Lima |
| PI-060 | Windows named-pipe owner; `crates/forwarder/src/pipe.rs::{PipeListener::new,run_pipe_forwarder}` | no prefix; config pipe; no R; process token not bound | first named-pipe instance/listener | caller pipe string; no SID/instance/commitment | live shared pipe; Windows host |
| PI-061 | Linux dev shim bootstrap; `scripts/substrate/dev-shim-bootstrap.sh` option/context section | `--prefix` else `SUBSTRATE_DEV_PREFIX` else `$HOME/.substrate`; unset; unset; `id` not resolved | install/uninstall dispatch; none | prefix/bin arguments; ambient default | none; Linux host |
| PI-062 | Linux dev shim bootstrap; same script `run_substrate`/install/remove calls | inherited prefix; unset; unset; unresolved | `substrate --shim-deploy/--shim-remove`; none | only original PATH; child reconstructs H/R | shim tree; Linux host |
| PI-063 | Linux dev shim bootstrap; same script `write_env_file` | inherited prefix; no H/R encoded; unset; unresolved | generated `dev-shim-env.sh`; none | literal shim PATH only | dev shim helper; Linux host |
| PI-064 | Linux dev shim bootstrap; same script `uninstall_shims`/`remove_env_file` | inherited prefix; unset; unset; unresolved | recursive shim/helper removal; none | direct paths and `rm -rf`; no ownership carrier | shim/helper state; Linux host |
| PI-065 | direct Unix release installer; `scripts/substrate/install-substrate.sh` public option parse | `--prefix` else `$HOME/.substrate`; bootstrap later H=A; unset; `SUDO_USER`/`id` | same-script install graph; later sudo/platform children | public args only; ambient HOME default | none initially; host |
| PI-066 | direct Unix release uninstaller; `scripts/substrate/uninstall-substrate.sh` public entry | no prefix option; Python/shell home; amb; partial; `SUDO_USER`/`USER`/`id` | same-script cleanup graph; sudo later | no prefix/context carrier; ambient HOME selects target | install state and shell profiles; host |
| PI-067 | standalone product CLI; `crates/shell/src/execution/cli.rs` plus invocation/doctor/world/deps/config/policy/gateway dispatch | no install-prefix option; H/R from process env/default; derived; process account | shim/status/doctor/world/deps/config/policy/gateway leaves | ambient env only; no public context | shim/trace/world/deps/config/policy/gateway state; host/platform |
| PI-068 | Windows release install child propagation; `scripts/windows/install-substrate.ps1` profile-source/`-NoAutoSource`, shim, doctor, WSL call sections | inherited A; profile may set H/R=A; otherwise partial; current token | installed CLI, WSL warm, doctors | profile side effect or partial env/args | profile/shim/doctor/WSL state; Windows/WSL |
| PI-069 | Windows dev install child propagation; `scripts/windows/dev-install-substrate.ps1` shim/profile sections | inherited A; partial; R=A; current token | `--shim-deploy` and profile writer | only R on shim call | profile/shim state; Windows host |
| PI-070 | Windows dev uninstall child propagation; `scripts/windows/dev-uninstall-substrate.ps1` shim/profile sections | inherited A; partial; R=A; current token | existing `--shim-remove` and profile removal | only R on shim call | profile/shim state; Windows host |
| PI-071 | world-enable child edge; `scripts/substrate/world-enable.sh` bootstrap/platform/deps sections | `--home` or amb; H=A after parse; partial R; detected Unix user | bootstrap, platform provisioner, dependency/runtime CLI | separate `--home`/env tuple; no commitment | world/dependency/runtime state; host/platform |
| PI-072 | Unix release PATH projection; `scripts/substrate/install-substrate.sh::{update_shell_path,upsert_path_snippet}` | inherited A; target uses ambient `$HOME`; unset; primary user separate | shell RC/profile writers; possible root-to-user mismatch | bin path explicit; profile target ambient | shell PATH snippets; Unix host |
| PI-073 | Unix release wrapper PATH diagnostic; `scripts/substrate/install.sh` post-install RC target/reporting | inherited A; target uses ambient `$HOME`; partial; current/root process | wrapper messaging/profile path selection; possible sudo | child prefix explicit; diagnostic target ambient | shell RC projection; Unix host |
| PI-074 | Unix release PATH cleanup; `scripts/substrate/uninstall-substrate.sh::{remove_path_snippet,remove_shell_path_snippets}` | target reconstructed; ambient HOME; partial; heuristic user | shell RC removal; possible root-to-user mismatch | direct ambient paths; no context/principal carrier | shell PATH snippets; Unix host |
| PI-075 | macOS doctor projection; `crates/shell/src/execution/platform/macos.rs` world-doctor report builders | process resolution; default socket/home; derived; host account | doctor report/backend probe | process env/contextless backend | doctor JSON/socket/backend; Lima |
| PI-076 | Windows doctor projection; `crates/shell/src/execution/platform/windows.rs` world-doctor report builders | process resolution; env/default pipe/home; derived; current token | doctor report/backend probe | process env/contextless backend | doctor JSON/pipe/backend; WSL |
| PI-077 | WSL warm capability guard; `scripts/windows/wsl-warm.ps1` fail-closed block at the live entry | public distro/project/pipe; no H/R commitment; current token | exits before WSL mutation | explicit args/defaults only | none; WSL intentionally disabled |
| PI-078 | WSL guest provision capability guard; `scripts/wsl/provision.sh` complete live body | no host context; no guest mapping; unset; guest/root if reached | exits 4 before provisioning | none | none; WSL intentionally disabled |
| PI-079 | Windows pipe selector boundary; `install-substrate.ps1`, `wsl-warm.ps1`, `start-forwarder.ps1`, `pipe-status.ps1`, `wsl-doctor.ps1`, `WindowsWslBackend::new` | no prefix binding; no H/R; current token not bound | pipe producer/consumer/doctor/backend | independent `-PipePath`, hard-coded default, or `SUBSTRATE_FORWARDER_PIPE` | shared named pipe; Windows/WSL |
| PI-080 | Linux operational socket recreation; release/provision service restart sections around `/run/substrate.sock` | inherited A; partial service tuple; resolved Unix user | remove stale runtime socket then systemd restart; sudo | direct fixed socket path; no deletion manifest | runtime socket/service state; Linux host |
| PI-081 | direct platform helpers; macOS `lima-warm.sh`/`lima-doctor.sh` and Windows `wsl-warm.ps1`/`start-forwarder.ps1`/`pipe-status.ps1`/`wsl-doctor.ps1` public entries | no host prefix parameter/context; platform selectors/defaults only; process token/`id` | platform mapping/diagnostic/forwarder child graph; Lima create/start currently precedes guest observation | no host carrier; independent instance/pipe/default selection | platform projections; Lima/WSL |
| PI-082 | standalone CLI pre-dispatch bootstrap; `execution/routing.rs::{run_shell,run_shell_with_cli}` and `home_bootstrap.rs::ensure_substrate_home_deps_scaffold` | no A before call; H from amb/default; R unset; UID resolved after H | private-home/dependency scaffold before `ShellConfig::from_cli`; filesystem mutation | no carrier; parse errors/help can also bootstrap ambient H | dependency scaffold; host |
| PI-083 | normal CLI automatic shim deploy; `execution/routing.rs::run_shell_with_cli` post-config block | config paths already derived from amb H/R; process account | `ShimDeployer::with_skip(...).ensure_deployed`; filesystem | no explicit context; deployer reconstructs common paths | shim version/tree; host |
| PI-084 | shim subcommand dispatch; `execution/routing.rs::handle_shim_command` and `builtins/shim_doctor/mod.rs::{run_doctor,run_repair}` | CLI process resolution; amb/default H; derived R; process account | doctor/report or repair child | booleans/manager name only; no context | shim/trace/manager projections; host/platform |
| PI-085 | shim repair target; `builtins/shim_doctor/{report.rs,repair.rs}` manifest, legacy-bashenv, backup, write, and trace-path helpers | manifest H from amb; bashenv from `dirs::home_dir`; R derived; process account | manifest load, intended-user bashenv/backup writer, trace logger | no context; ambient/default home independently selects paths | manager manifest/bashenv/backup/trace; host |
| PI-086 | Unix dev install direct privilege crossings; `dev-install-substrate.sh::{run_privileged,install_packages,ensure_substrate_group_membership,ensure_socket_group_alignment}` and provisioning preflight | inherited A; partial H/R; `SUDO_USER`/`id` heuristics | package manager, `groupadd`, `usermod`, unit rewrite/install, `systemctl`, sudo validation | command argv and separate user; no immutable context/principal tuple | packages/group/membership/unit/service state; Linux host |
| PI-087 | Linux provision config/policy proof reads; `world-provision.sh::evaluate_gateway_lifecycle_proof_eligibility` | reconstructed A; H partial; R unset; invoking user/home separately resolved | installed `substrate config current show` and `policy current show`; none | CLI path only; children inherit ambient process context | config/policy diagnostic JSON; Linux host |
| PI-088 | Linux provision gateway proof lifecycle children; `world-provision.sh::run_gateway_lifecycle_proof` | same reconstructed A and invoking home; partial H/R; invoking user | `substrate world gateway sync/status/restart` and health probe; service boundary | CLI path only; no context carrier | gateway status/service state; Linux world |
| PI-089 | Linux provision synthetic Codex-auth target; `world-provision.sh::prepare_gateway_smoke_auth` | A unrelated; target is `${INVOKING_HOME}/.codex`; R unset; detected invoking user | temporary auth-file install before gateway proof | invoking-home string only; may derive from sudo/ambient account state | Codex auth projection; Linux host/world proof |
| PI-090 | shim world-telemetry backend construction; `shim/src/{context.rs,exec/mod.rs,exec/logging.rs::collect_world_telemetry}` | shim path plus process env; no IH/PM; process account | `world_backend_factory::factory`; platform backend | `SUBSTRATE_WORLD_ID` only; factory/backend selects socket/pipe/default | trace fs-diff/backend state; host/Lima/WSL |
| PI-091 | replay world-backend construction; shell `execution/routing/replay.rs` into `replay::{lib.rs,replay/mod.rs,replay/planner.rs,replay/executor.rs::try_world_backend}` | shell process amb/default; replay state carries recorded env but no IH/PM; process account | `world_backend_factory::factory`; platform backend | no bootstrap parameter; factory/backend selects default | replay state/recorded transport; host/Lima/WSL |
| PI-092 | shim deployment replacement/migration; `execution/shim_deploy.rs::{ensure_deployed,deploy_shims,migrate_old_shims}` | same ambient-derived shim paths as PI-032/PI-083 | recursive old-tree removal, atomic replacement, legacy rename | path members only; no managed-artifact identity | shim tree/version/legacy tree; host |
| PI-093 | CLI shim removal action; `execution/invocation/plan.rs` `cli.shim_remove` block | common-path H fallback; derived shim path; process account | recursive shim-tree deletion | no managed identity/context authority | shim tree; host |
| PI-094 | dev installer managed replacement cleanup; `dev-install-substrate.sh::{stage_managed_bundle_symlink,stage_managed_linux_binary_copy,clear_managed_prefix_linux_binary_cache,cleanup_legacy_world_enable_helper_bridge}` | inherited A; partial H/R; host user | remove/replace cached binaries, symlinks, manifest, legacy helper bridge | path/heuristic manifest checks; no R3 ownership manifest | dev managed bundle/cache; host/macOS |
| PI-095 | Unix release install replacement cleanup; `install-substrate.sh::{prepare_bundle_payload,link_binaries,deploy_shims,harden_shim_symlinks,provision_linux_world}` replacement/legacy sections | inherited A; partial H/R; primary user | recursive version/payload removal, bin/shim replacement, legacy unit delete | path/version names and sudo; no R3 ownership carrier | release/bin/shim/unit state; Unix/Linux |
| PI-096 | Windows release install replacement cleanup; `install-substrate.ps1` version/bin install block | inherited A; partial H/R; current token | `Remove-Item -Recurse -Force` for version/bin before copy | prefix paths only; no ownership carrier | release version/bin state; Windows host |
| PI-097 | Windows dev uninstall deletion; `dev-uninstall-substrate.ps1` shim/profile deletion blocks | inherited A; H partial; R=A; current token | recursive shim and profile removal after PI-070 | direct paths; no ownership carrier | dev shim/profile state; Windows host |
| PI-098 | Lima warm destroy/recreate branch; `lima-warm.sh::{destroy_vm,ensure_vm_ready}` layout-mismatch path | VM selector only before PM; no host commitment at action | `limactl stop/delete/start`; shared platform lifecycle | hard-coded/ambient VM selector; no exact ownership/mapping | whole Lima instance; macOS/Lima |
| PI-099 | Lima staged-workspace replacement; `lima-warm.sh::stage_workspace` | VM selector; guest paths/constants; host/guest principals separately inferred | recursive guest staging/current replacement and temp removal | shell variables; no PM or managed ownership | guest staged workspace; Lima |
| PI-100 | Lima unit/socket/staging cleanup; `lima-warm.sh::{write_systemd_units,enable_socket_activation}` | guest home inferred after entry; partial paths/principal | rendered temp removal, legacy service/socket stop-disable-delete, staged unit removal, runtime-socket unlink | direct paths/names; no deletion ownership carrier | guest units/socket/temp state; Lima |
| PI-101 | macOS forwarded-socket replacement; `world-mac-lima/src/forwarding.rs::create_ssh_uds_forwarding` explicit removal plus SSH `StreamLocalBindUnlink` | socket from ambient/default host home; no PM; host account | direct or SSH-side unlink of an existing host socket before forwarding | derived path/SSH option only; no ownership carrier | host forwarded socket; Lima |
| PI-102 | Windows forwarder current-attempt rollback; `start-forwarder.ps1` `-WaitForExit` timeout branch | pipe/distro parameters; log path ambient; current token | kill spawned forwarder after timeout | process handle only; no mapping/attempt ownership record | forwarder process; Windows host |
| PI-103 | Linux provision synthetic Codex-auth cleanup; `world-provision.sh::cleanup_gateway_smoke_auth` and proof cleanup branch | target `${INVOKING_HOME}/.codex/auth.json`; no IH; detected invoking user | delete same-attempt synthetic auth file | path plus boolean; no durable attempt/ownership record | Codex auth projection; Linux host |
| PI-104 | generated Bash preexec projection; `scripts/bash_preexec.rs::BASH_PREEXEC_SCRIPT` plus `invocation/plan.rs::ShellConfig::from_cli` helper-path selection | no A at writer/consumer; H from outer `SUBSTRATE_HOME` or `$HOME`; R unset; process account | write/source `manager_env.sh` and per-user `.substrate_preexec`; none | generated script and helper path accept ambient H/home | manager/preexec/trace projection; host |
| PI-105 | Linux member-dispatch Codex credential projection; `routing/dispatch/world_ops.rs::{resolve_host_codex_seed_home,maybe_inject_codex_auth_seed_home_for_policy,maybe_inject_codex_auth_seed_home_for_member_dispatch}` | A unrelated; H/R unused; intended account reconstructed from `dirs::home_dir`/HOME/USERPROFILE | inject host `.codex` seed directory into an accepted world request; host-to-world credential boundary | environment value only; ambient process home selects principal credential source | Codex auth seed; Linux host/world |
| PI-106 | world-deps command dispatch; `execution/platform/mod.rs::handle_world_command` to `builtins/world_deps::run` | CLI H/R from ambient/default; process account; no typed IH | global/current add/remove/list/show/install/sync leaves; same process/world backend | CLI options only; no context parameter | dependency/config/runtime projections; host/world |
| PI-107 | world-deps global/current consumers; `builtins/world_deps/surfaces.rs::{run_global_list,run_global_add,run_global_remove,run_global_reset,resolve_global_available_inventory_view,resolve_current_inventory_view}` | global deps/config paths from `substrate_home`/common ambient resolver; workspace CWD separately valid | inventory/config read-write and current sync/list/doctor helpers; same process | no IH/path parameter; ambient H selects global layer | deps inventory/config/applied-state projections; host/world |
| PI-108 | world/host/health doctor dependency projection; `execution/platform/mod.rs::{handle_world_command,handle_host_command,handle_health_command}` and `builtins/world_deps/mod.rs::{collect_doctor_snapshot_v1,resolve_effective_enabled_provisioning_requirements_v1}` | H/R from ambient/default resolvers; process account; no typed IH | world/host/health doctor into effective config and global dependency inventory; same process | no context parameter; ambient H may select diagnostic source | doctor/config/deps projection; host/platform |
| PI-109 | provision config-proof consumer; `execution/config_cmd.rs::{handle_config_command,run_current_show}` | global config layer resolves through ambient H/R; CWD selects workspace layer; process account | `substrate config current show`; installer-managed child process | no internal argv carrier; ambient process tuple is effective | merged config diagnostic projection; host |
| PI-110 | provision policy-proof consumer; `execution/policy_cmd.rs::{handle_policy_command,run_current_show}` | global policy layer resolves through ambient H/R; CWD selects workspace layer; process account | `substrate policy current show`; installer-managed child process | no internal argv carrier; ambient process tuple is effective | merged policy diagnostic projection; host |
| PI-111 | provision gateway-proof consumer; `builtins/world_gateway.rs::{run,build_gateway_request_context,macos_default_world_socket_path,codex_auth_state_path}` | config/policy/socket from ambient H/R/default; Codex account from `dirs::home_dir`/HOME/USERPROFILE | gateway sync/status/restart, transport and integrated-auth children; service/world boundary | no internal argv/typed context; multiple ambient fallbacks | gateway/config/policy/socket/Codex auth projections; host/platform |
| PI-112 | macOS SSH known-hosts projection; `world-mac-lima/src/forwarding.rs::create_ssh_uds_forwarding` `UserKnownHostsFile` | host home from `dirs::home_dir`; no IH/PM; host account | SSH forwarding child writes/reads scoped known-hosts file | derived ambient path only | `.substrate/lima_known_hosts`; macOS/Lima |
| PI-113 | macOS active forwarding-handle teardown; `world-mac-lima/src/forwarding.rs::ForwardingHandle::drop` | stored child and socket path; no mapping/attempt ownership | kill/wait forwarding child and remove UDS on drop | process handle/path only; no ownership carrier | forwarder process/socket; macOS/Lima |
| PI-114 | macOS SSH-forwarding current-attempt timeout; `world-mac-lima/src/forwarding.rs::create_ssh_uds_forwarding` timeout branch | stored child; socket path ambient-derived; no mapping/attempt ownership | kill/wait timed-out SSH child | process handle only; no attempt ownership record | forwarder process/socket; macOS/Lima |
| PI-115 | Windows forwarder-to-WSL child; `forwarder/src/bridge.rs::spawn_bridge` to `forwarder/src/wsl.rs::spawn` | no A/H/R; distro and target come from `ForwarderConfig`; host/guest principals are not checked at this edge | `wsl -d <distro> -- python3 -c <BRIDGE_SCRIPT>`; host-to-WSL process boundary | distro argv plus target environment and `WSLENV`; config/environment/defaults may select a different target | forwarder target and WSL guest socket; Windows/WSL |
| PI-116 | physical shim manager-hint manifest consumer; `shim/src/exec/mod.rs::run_shim` to `exec/logging.rs::{ManagerHintEngine::new,manifest_paths}` | A is not passed; H comes from common ambient resolver; R derived/unused; process account | same-process manifest and overlay load before command execution | `SUBSTRATE_MANAGER_MANIFEST`, ambient `substrate_home()`, then compile-time repo fallback | manager hint manifest/overlay; host |
| PI-117 | product-shell trace and policy-commit projection; `shell/src/execution/{routing.rs,invocation/plan.rs,manager.rs,routing/telemetry.rs}` and `builtins/shim_doctor/repair.rs` into `trace/src/context.rs::{TraceContext::init_trace,build_replay_context}` and `trace/src/util.rs::get_policy_git_hash` | no typed A at trace boundary; H/R come from process configuration/defaults; process account | process-global trace context, trace writer/rotation, and `git rev-parse`; no privilege boundary | `TraceContext::default()` plus `init_trace(None)`; `SHIM_TRACE_LOG` then `dirs::home_dir()` select output and `dirs::home_dir()` separately selects policy Git directory | trace JSONL and policy-commit metadata; host |
| PI-118 | physical-shim trace and policy-commit projection; `shim/src/exec/mod.rs::run_shim`, `exec/policy.rs::evaluate_policy`, and `logger.rs::write_log_entry` into the same trace context/util functions | invocation path is observed but no IH reaches trace; H/R are ambient/default; process account | policy span and execution-log writers plus policy Git metadata; no privilege boundary | default global context and `init_trace(None)`; trace/policy paths independently fall back through `SHIM_TRACE_LOG`/`dirs::home_dir()` | physical-shim trace JSONL and policy-commit metadata; host |

### Required disposition, implementation surface, and proof

Each `Class` cell contains exactly one approved edge class. `R2 carrier only; R3 action` means R2
may make the later target unambiguous but may not perform, authorize, or define deletion.

| ID | Class | Expected authority behavior and R2/R3 owner | Required implementation file/symbol or section | Regression proof; evidence posture | Packet |
|---|---|---|---|---|---|
| PI-001 | `ContextConstruction` | Construct `IH` once from public `--prefix`; R2 | dev installer parse/bootstrap | R2-UDEV-01; native Linux | R2-1 |
| PI-002 | `ExplicitHostPropagation` | pass complete `IH`; R2 | dev installer shim block plus invocation/deployer leaf | R2-SHIM-01; native Linux | R2-1 |
| PI-003 | `GeneratedProjectionConsumption` | encode `IH`, self-bind A, reject B as authority, and copy the selected dev version manifest as generated base `A/manager_hooks.yaml` only after IH fixes A; R2 | dev projection writers; `env_scripts.rs`, `manager.rs` | R2-GEN-01; native Linux/static Unix | R2-1 |
| PI-004 | `GeneratedProjectionConsumption` | keep prefix-relative projections subordinate to `IH`; R2 | dev config/version/state/deps writers | R2-GEN-01; native Linux | R2-1 |
| PI-005 | `ExplicitHostPropagation` | pass complete `IH` to dependency child; R2 | dev dependency-add loop | R2-RUNTIME-01; native Linux | R2-2 |
| PI-006 | `ExplicitHostPropagation` | keep same `IH` for current/list/sync; R2 | dev runtime-sync block | R2-RUNTIME-01; native Linux | R2-2 |
| PI-007 | `ExplicitHostPropagation` | R2 carrier only; R3 action | dev failure path: context arguments only | R2-RUNTIME-01 then R3-LIFE-01; native Linux | R2-2 |
| PI-008 | `PrivilegeBoundaryPropagation` | pass `IH` and exact Unix principal into provisioner; R2 | dev Linux call site, `world-provision.sh` intake | R2-LINUX-01; native Linux | R2-2 |
| PI-009 | `PlatformMapping` | construct/verify `PM`; R2 | dev mac call site, `lima-warm.sh` intake | R2-MAP-MAC-01; static plus assigned native macOS | R2-3 |
| PI-010 | `ContextConstruction` | construct identical uninstall `IH` from declared prefix; R2 | dev uninstaller parse/bootstrap | R2-UDEV-01; native Linux | R2-1 |
| PI-011 | `ExplicitHostPropagation` | pass complete `IH` to shim remove; R2 | dev uninstaller shim block/invocation leaf | R2-SHIM-01; native Linux | R2-1 |
| PI-012 | `R3CleanupOnly` | deletion, rollback, socket/system cleanup exclusively R3 | dev removal sections, future R3 exact-ownership logic | R3-LIFE-01; native platform required | R3 |
| PI-013 | `ContextConstruction` | public release wrapper constructs/forwards one `IH`; R2 | `install.sh` prefix/delegation | R2-UREL-01; native Linux | R2-2 |
| PI-014 | `ExplicitHostPropagation` | child verifies, never reconstructs or reselects; R2 | `install-substrate.sh` intake/bootstrap | R2-UREL-01; native Linux | R2-2 |
| PI-015 | `ExplicitHostPropagation` | complete `IH` reaches shim deployment; R2 | release `deploy_shims` and invocation leaf | R2-SHIM-01; native Linux/static Unix | R2-2 |
| PI-016 | `GeneratedProjectionConsumption` | all generated consumers bind A and project both H/R; copy the selected release manifest as generated base `A/manager_hooks.yaml` only after IH fixes A; R2 | release projection writers, `env_scripts.rs`, `manager.rs` | R2-GEN-01; native Linux/static Unix | R2-2 |
| PI-017 | `ExplicitHostPropagation` | same `IH` reaches dependency/runtime children; R2 | release dependency/runtime provision sections | R2-RUNTIME-01; native Linux | R2-2 |
| PI-018 | `ExplicitHostPropagation` | same `IH` reaches current/list/sync; R2 | release runtime-sync/check sections | R2-RUNTIME-01; native Linux | R2-2 |
| PI-019 | `ExplicitHostPropagation` | R2 carrier only; R3 action | release failure path: context arguments only | R2-UREL-01 then R3-LIFE-01; native Linux | R2-2 |
| PI-020 | `GeneratedProjectionConsumption` | unit carries H=A, R=A, commitment, and intended-principal projection; `ReadWritePaths` derives only from `IH`; R2 | release Linux service template section | R2-LINUX-01; native Linux | R2-2 |
| PI-021 | `PrivilegeBoundaryPropagation` | Substrate-owned sudo child validates full IH; arbitrary package/group/install/systemctl tools receive only exact parent-revalidated context-derived argv and no preserved environment; R2 | release privileged argument construction/install/reload/restart sections | R2-LINUX-01; native Linux dedicated host | R2-2 |
| PI-022 | `PlatformMapping` | host `IH` binds verified Lima `PM`; R2 | release mac branch, `lima-warm.sh` | R2-MAP-MAC-01; static plus assigned native macOS | R2-3 |
| PI-023 | `DiagnosticProjection` | release call sites pass context; shared/Linux doctors report selected commitment and A projections; R2 | release final checks plus shared/Linux doctor reports | R2-DIAG-01; native Linux, platform rows PI-075–PI-076 | R2-2 |
| PI-024 | `ContextConstruction` | wrapper selects installed target then constructs matching uninstall `IH`; R2 | `uninstall.sh` selection/delegation | R2-UREL-01; native Linux | R2-2 |
| PI-025 | `ExplicitHostPropagation` | child verifies same `IH`; no ambient retarget; R2 | `uninstall-substrate.sh` intake/bootstrap only | R2-UREL-01; native Linux | R2-2 |
| PI-026 | `R3CleanupOnly` | all prefix/system removal and convergence exclusively R3 | release removal sections/future R3 ownership logic | R3-LIFE-01; native Linux/platform | R3 |
| PI-027 | `ContextConstruction` | public CLI reconciles global `--install-prefix` with nested `world enable --home` before dispatch, constructs from the one equal normalized declared selector or a verified installed layout when neither is present, and fails before mutation on conflict or when a repo/dev invocation has neither source; an authenticated internal carrier remains authoritative and any declared selector must match it; R2 | `run_shell_with_cli` alone owns pre-dispatch selector reconciliation and the existing R2-1 decode/construct path; typed IH then crosses `ShellConfig::from_cli`'s existing `SubCommands::World` branch into `handle_world_command` and remains the same-process authority through `run_enable`; `run_enable`, including its existing provision-deps branch, derives A only from typed IH and passes explicit A to `update_manager_env_exports`; helper public parse | R2-RUNTIME-01; native Linux | R2-2 |
| PI-028 | `ContextConstruction` | direct public provision constructs one `IH` and Unix principal; R2 | `world-provision.sh` option/bootstrap | R2-LINUX-01; native Linux | R2-2 |
| PI-029 | `GeneratedProjectionConsumption` | unit carries H=A, R=A, commitment, and intended-principal projection; socket/drop-in/allowlist values derive from `IH`; R2 | Linux provision template/install sections | R2-LINUX-01; native Linux | R2-2 |
| PI-030 | `PrivilegeBoundaryPropagation` | context-aware Substrate children validate full IH, while arbitrary tools receive exact derived argv. The ACL helper is instead a non-context system leaf: it may accept only `--socket /run/substrate.sock substrate`, `--directory-traverse /var/lib/substrate substrate`, or `--tree-readonly /var/lib/substrate/world-deps substrate`; it receives no A/H/R/principal authority, and group enumeration remains the unchanged ACL subject-set rule; R2 | Linux group/ACL/linger/restart call sites plus ACL-helper argument parser/closed tuple validation only; ACL application/enumeration bodies frozen | R2-LINUX-01; native Linux dedicated host | R2-2 |
| PI-031 | `R3CleanupOnly` | disable/removal/legacy replacement ownership exclusively R3 | Linux deletion/disable/replacement sections | R3-LIFE-01; native Linux dedicated host | R3 |
| PI-032 | `ExplicitHostPropagation` | shim actions consume caller `IH`, with legacy tuple accepted only as checked compatibility input; R2 | invocation plan and `ShimDeployer::{deploy,remove}` call path | R2-SHIM-01; native Linux/static Unix/Windows | R2-1 |
| PI-033 | `DiagnosticProjection` | status reports the caller-selected A/commitment; R2 | shim status invocation/rendering | R2-SHIM-01; native Linux | R2-1 |
| PI-034 | `DiagnosticProjection` | doctor derives trace and shim paths from `IH`; R2 | `shim_doctor/report.rs::{build_report,trace_log_path}` | R2-DIAG-01; native Linux | R2-1 |
| PI-035 | `DiagnosticProjection` | Route C carries already-authenticated typed IH through the existing Host/World doctor call paths and reports only optional/defaulted selected-prefix and commitment fields. The representation is non-authoritative: ambient/generated state cannot establish or replace A; the world-service producer emits host fields absent and reads no host authority; no hidden carrier, credential, request, secret, or sensitive principal material is returned or logged. Route D separately owns Health/shim world-deps snapshot propagation; platform fields remain PI-075/PI-076; R2 | exactly `crates/shell/src/execution/platform/linux.rs`, `crates/shell/src/execution/platform/mod.rs`, `crates/shell/tests/doctor_scopes_ds0.rs`, `crates/transport-api-types/src/lib.rs`, and `crates/world-service/src/handlers.rs`; all detected process labels map only to `handle_host_command`, `host_doctor_main`, `handle_world_command`, or `world_doctor_main`, while the legacy adapter, optional representation, mechanical `doctor_world` `None` producer, and tests create no execution-family root | R2-DIAG-01; native Linux | R2-2 Route C |
| PI-036 | `ExplicitHostPropagation` | dependency commands/manager use caller `IH`; R2 | deps call sites and world-enable provision runner | R2-RUNTIME-01; native Linux | R2-2 |
| PI-037 | `ExplicitHostPropagation` | runtime-family/Codex provisioning inherits verified carrier; no runtime seam change; R2 | `world_enable/runner/provision_deps.rs`, helper launch | R2-RUNTIME-01; native Linux | R2-2 |
| PI-038 | `GeneratedProjectionConsumption` | encoded/self-derived A controls runtime/world-enable generated consumers; R2 | world-enable manager-env/helper consumers | R2-GEN-01/R2-RUNTIME-01; native Linux/static Unix | R2-2 |
| PI-039 | `PlatformMapping` | after IH+VM-selector and committed account-database Lima-control-root validation, Stage 1 may create/start only the declared instance with scrubbed/overwritten child `HOME`/`LIMA_HOME`; Stage 2 finalizes PM from the running guest before R2-owned guest projection; delete/rebuild/staging cleanup are PI-098–PI-100; R2 | `lima-warm.sh` intake plus non-destructive instance-realization/mapping and exact child-environment sections only | R2-MAP-MAC-01; static plus assigned native existing-instance macOS | R2-3 |
| PI-040 | `DiagnosticProjection` | Lima unit/doctor verifies the same `PM`, typed control root, and scrubbed child environment; R2 | `lima-warm.sh` unit and `lima-doctor.sh` | R2-MAP-MAC-01/R2-DIAG-01; native macOS assigned | R2-3 |
| PI-041 | `PlatformMapping` | R2 commits `host_platform_control_root=<host account-database home>/.lima`, makes `lima_home_dir`/SSH config and every Lima child consume the typed/scrubbed projection, fixes the future V1 PM target to SSH UDS from `A/sock/agent.sock` to `/run/substrate.sock`, removes ambient auto-selection, and fails before forwarder launch with the R3 lifecycle prerequisite. Directly injected conflicting internal `HOME`/`LIMA_HOME` rejects; outer public B is scrubbed. R3 activates only after PI-101/PI-113/PI-114. VSock/SSH-TCP remain direct diagnostic/test-only paths; R2 | typed control-root/mapping/fail-closed sections in `world-mac-lima` forwarding/transport and mac invocation socket code; lifecycle bodies frozen | R2-MAP-MAC-01; static plus assigned native macOS no-forwarder A/B mapping-only proof | R2-3 |
| PI-042 | `ContextConstruction` | PowerShell entry constructs one Windows-principal `IH`; R2 | dev Windows prefix/shim/profile sections | R2-MAP-WIN-01; native Windows assigned | R2-3 |
| PI-043 | `ContextConstruction` | Windows dev uninstall constructs one `IH` from `-Prefix`/Known Folder default; action propagation is PI-070; R2 | dev Windows prefix/context intake only | R2-MAP-WIN-01; native Windows assigned | R2-3 |
| PI-044 | `ContextConstruction` | Windows release install constructs one `IH`; `-NoAutoSource` child propagation is PI-068; R2 | release Windows prefix/context intake only | R2-MAP-WIN-01; native Windows assigned | R2-3 |
| PI-045 | `PlatformMapping` | release passes host carrier/instance/pipe to the guarded warm child, which validates then preserves fail-closed behavior; existing-instance mapping is PI-048; R2 | Windows WSL warm call arguments and pre-guard intake only | R2-MAP-WIN-01; native Windows guard proof | R2-3 |
| PI-046 | `ContextConstruction` | uninstall constructs same Windows `IH`; selection only R2 | Windows uninstaller prefix/context intake | R2-MAP-WIN-01; native Windows assigned | R2-3 |
| PI-047 | `R3CleanupOnly` | wildcard/prefix/shared/WSL deletion and convergence exclusively R3 | Windows deletion/stop/unregister sections | R3-WIN-01; native Windows required | R3 |
| PI-048 | `PlatformMapping` | WSL adapter verifies commitment, instance identity, token Known Folder control root, guest home, and guest principal; R2 | WSL warm/doctor and `world-windows-wsl` backend/paths/transport | R2-MAP-WIN-01; static plus native Windows/WSL assigned | R2-3 |
| PI-049 | `PlatformMapping` | R2 derives the canonical SID+registered-distro+machine-ID+pipe scope digest and token-Known-Folder shared PID root, generates config/log projections only under A after IH, passes every path explicitly, rejects conflicting host commitments, and treats `LOCALAPPDATA`/`USERPROFILE` only as conflicting diagnostic inputs; R3 owns replacement/rollback/removal/termination and any ownership manifest | `start-forwarder.ps1`; forwarder `windows.rs`, config/logging and shim/forwarder call sites | R2-MAP-WIN-01 then R3-WIN-01; native Windows A/B assigned | R2-3 |
| PI-050 | `OutOfScope` | do not edit the common ambient resolver in R2; every relevant leaf is separately inventoried and receives context at that leaf | none: `common::paths::substrate_home` is a guardrail, not an R2 implementation surface | R2-4 inventory audit; static plus each caller's native owner | R2-4 |
| PI-051 | `R3CleanupOnly` | stopping a possibly shared Lima instance is lifecycle/cleanup authority; R3 consumes the R2 mapping but R2 does not edit the action | `scripts/mac/lima-stop.sh` future R3 exact-target/ownership sections | R3-LIFE-01; native macOS | R3 |
| PI-052 | `DiagnosticProjection` | pipe status accepts/verifies the SID+instance+pipe mapping scope, canonical control root, and active commitment without ambient Known Folder variables; R2 | `scripts/windows/pipe-status.ps1` mapping intake and diagnostic projection | R2-MAP-WIN-01/R2-DIAG-01; native Windows assigned | R2-3 |
| PI-053 | `R3CleanupOnly` | PID removal, broad process stop, distro termination, and convergence are R3; R2-3 supplies the mapping contract only | `scripts/windows/wsl-stop.ps1` future R3 exact-ownership/targeting sections | R3-WIN-01; native Windows required | R3 |
| PI-054 | `PlatformMapping` | factory accepts a verified `PM`; no contextless platform constructor; R2 | `world-backend-factory/src/lib.rs::factory` signature/call only | mapping/backend tests; static plus native assignments | R2-3 |
| PI-055 | `PlatformMapping` | mac platform call sites receive verified host context/mapping and stop before forwarding until the R3 lifecycle prerequisite; R2 | `execution/platform/macos.rs` backend/socket call sites | R2-MAP-MAC-01/R2-DIAG-01; native macOS no-forwarder mapping-only assignment | R2-3 |
| PI-056 | `PlatformMapping` | platform-world Mac constructor/socket consume verified mapping and cannot bypass the R3 activation prerequisite; R2 | `execution/platform_world/mod.rs` macOS sections | R2-MAP-MAC-01; native macOS no-forwarder mapping-only assignment | R2-3 |
| PI-057 | `PlatformMapping` | platform-world Windows constructor consumes verified mapping; R2 | `execution/platform_world/windows.rs::build_world_backend` | R2-MAP-WIN-01; native Windows assigned | R2-3 |
| PI-058 | `PlatformMapping` | gateway transport consumes verified platform transport from `PM`; R2 | `builtins/world_gateway.rs::gateway_transport` platform branches | mapping/gateway differential; native assignments | R2-3 |
| PI-059 | `OutOfScope` | production implementation is owned above; update this proof harness only if constructor signature requires it, but do not launch forwarding or claim product proof in R2 | `world-mac-lima/examples/mac_backend_smoke.rs::main` test/example call | R2-MAP-MAC-01; native macOS no-forwarder mapping-only evidence | R2-3 |
| PI-060 | `PlatformMapping` | forwarder listener consumes normalized pipe identity from `PM` and rejects a different commitment; R2 | `forwarder/src/pipe.rs::{PipeListener::new,run_pipe_forwarder}` and colocated tests | R2-MAP-WIN-01; native Windows | R2-3 |
| PI-061 | `ContextConstruction` | public helper constructs one Unix `IH` from declared prefix/account default, ignoring ambient dev prefix as authority; R2 | `dev-shim-bootstrap.sh` parse/context only | R2-UDEV-01/R2-SHIM-01; native Linux | R2-1 |
| PI-062 | `ExplicitHostPropagation` | existing deploy/remove invocations receive complete `IH`; action semantics unchanged; R2 | same script `run_substrate` and shim calls | R2-SHIM-01; native Linux | R2-1 |
| PI-063 | `GeneratedProjectionConsumption` | helper encodes H/R/carrier/commitment and binds A; R2 | same script `write_env_file` only | R2-GEN-01; native Linux | R2-1 |
| PI-064 | `R3CleanupOnly` | recursive shim/helper removal and preservation/convergence exclusively R3 | same script `uninstall_shims`/`remove_env_file` deletion sections | R3-LIFE-01; native Linux | R3 |
| PI-065 | `ContextConstruction` | direct installer constructs one `IH`; internal-child mode validates argv carrier plus current-principal binding instead; R2 | `install-substrate.sh` public parse/context discriminator | R2-UREL-01; native Linux | R2-2 |
| PI-066 | `ContextConstruction` | add declared prefix to direct uninstaller and construct one symmetric `IH`; internal-child mode validates carrier plus current-principal binding; R2 selection only | `uninstall-substrate.sh` public parse/context intake only | R2-UREL-01; native Linux | R2-2 |
| PI-067 | `ContextConstruction` | standalone CLI uses declared `--install-prefix` or the unique exact Unix release-link, Unix dev-symlink, or Windows release-copy invocation witness; absent/ambiguous/unverifiable selection fails without ambient fallback. Internal child additionally binds the committed principal to the current OS identity; R2 | `execution/cli.rs` install-prefix/hidden-child fields and the one invocation-witness/context constructor | R2-SHIM-01/R2-DIAG-01/R2-RUNTIME-01; native platform owner | R2-1 |
| PI-068 | `ExplicitHostPropagation` | Windows release children receive `IH` regardless of profile source; R2 | release Windows profile/shim/doctor/WSL call arguments | R2-MAP-WIN-01/R2-DIAG-01; native Windows | R2-3 |
| PI-069 | `ExplicitHostPropagation` | Windows dev install shim/profile receive complete `IH`; R2 | dev Windows shim/profile call arguments | R2-MAP-WIN-01; native Windows | R2-3 |
| PI-070 | `ExplicitHostPropagation` | existing dev shim-remove receives carrier; profile/deletion action unchanged and R3-owned; R2 carrier only | dev Windows existing shim-remove call arguments | R2-MAP-WIN-01 then R3-WIN-01; native Windows | R2-3 |
| PI-071 | `ExplicitHostPropagation` | at the process boundary, `run_enable` and its existing provision-deps path canonically encode their typed IH; `run_helper_script` transports only that exact authenticated carrier on hidden child argv while preserving `--home` as a required matching projection; `world-enable.sh` discriminates child mode by carrier-argv presence, authenticates the carrier/current principal or exact sudo origin, requires normalized `--home` equality, installs checked projections, and only then passes the same authority to existing platform/dependency/runtime children; R2 | `world_enable/runner/helper_script.rs::run_helper_script` transport only; `world-enable.sh` carrier/bootstrap validation and existing child-call arguments | R2-RUNTIME-01/R2-LINUX-01/platform gates | R2-2 |
| PI-072 | `GeneratedProjectionConsumption` | PATH target derives from intended account-database home, not invoking/root HOME; R2 upsert only | release installer PATH upsert/target selection | R2-GEN-01/R2-UREL-01; native Linux/static Unix | R2-2 |
| PI-073 | `DiagnosticProjection` | wrapper reports the same intended-principal projection target; R2 | `install.sh` post-install reporting only | R2-DIAG-01/R2-UREL-01; native Linux | R2-2 |
| PI-074 | `R3CleanupOnly` | shell-profile removal and preservation/convergence exclusively R3; use R2-carried intended principal | release uninstaller path-snippet deletion sections | R3-LIFE-01; native Unix | R3 |
| PI-075 | `DiagnosticProjection` | mac doctor fields consume verified `PM`, report the account-database-derived Lima control root and R3 forwarding prerequisite, and do not launch or clean a forwarder; R2 | `execution/platform/macos.rs` doctor report symbols | R2-DIAG-01/R2-MAP-MAC-01; native macOS mapping-only | R2-3 |
| PI-076 | `DiagnosticProjection` | Windows doctor fields consume verified `PM`; R2 | `execution/platform/windows.rs` doctor report symbols | R2-DIAG-01/R2-MAP-WIN-01; native Windows | R2-3 |
| PI-077 | `OutOfScope` | preserve fail-closed WSL warm guard byte-for-byte; mapping may be observed against an existing instance only; R2 | no editable guard/provision code; guard is a stop condition | static assertion plus native existing-instance diagnostic only | R2-3 |
| PI-078 | `OutOfScope` | preserve guest provision exit-4 guard byte-for-byte; enabling capability requires architecture review | no R2 implementation surface in `scripts/wsl/provision.sh` | static exact-guard regression | R2-3 |
| PI-079 | `PlatformMapping` | first public Windows mapping entry selects normalized pipe once; every child consumes `PM`; env/hard-coded alternatives are checks only | Windows script pipe parameters, backend constructor, `forwarder/config.rs` | R2-MAP-WIN-01; native Windows | R2-3 |
| PI-080 | `PrivilegeBoundaryPropagation` | permit only unchanged removal of the fixed runtime socket immediately before same-attempt restart; full H/R/commitment/principal service projection required; legacy/uninstall cleanup remains R3 | release/Linux provision restart block, excluding legacy cleanup | R2-LINUX-01; native dedicated Linux | R2-2 |
| PI-081 | `ContextConstruction` | direct helper first constructs `IH`, resolves/commits the OS-derived platform control root, validates the public instance and Windows pipe selectors, and fixes Lima's V1 target without an ambient selector. Windows and already-running Lima then construct `PM` before action; an absent/stopped Lima may perform only PI-039 Stage 1 with the committed Lima control root. Internal-child mode only validates the carrier/stage and matching scrubbed projections; R2 | named platform-script parameter/context discriminators before action/diagnostic, exact control-root derivation, explicit Lima Stage-1 discriminator, fixed Lima target, and pre-guard Windows intake | R2-MAP-MAC-01/R2-MAP-WIN-01/R2-DIAG-01; native owners | R2-3 |
| PI-082 | `ContextConstruction` | successful mutating/projection routes construct or validate+principal-bind `IH` before scaffold; parse failures, help, `--version`, and `--version-json` are non-mutating; the hidden `--install-bootstrap-home-v1` action is accepted only with the authenticated hidden carrier, current-principal equality, and checked H/R before it calls only the explicit-path bootstrap and returns; R1 physical/security semantics remain unchanged; R2 | `cli.rs` hidden action field, `routing.rs` parse/order/action dispatch/principal binding, `install_bootstrap.rs` validation, and `home_bootstrap.rs` explicit context-path intake | R2-UDEV-01/R2-SHIM-01; native Linux | R2-1 |
| PI-083 | `ExplicitHostPropagation` | automatic deploy receives the already-constructed `IH`; no common-path reconstruction; R2 carrier only | `routing.rs` auto-deploy call and `ShimDeployer` context constructor | R2-SHIM-01; native Linux | R2-1 |
| PI-084 | `ExplicitHostPropagation` | doctor/repair dispatch passes `IH`; child cannot reread ambient home as authority; R2 | `routing.rs::handle_shim_command`, `shim_doctor/mod.rs` signatures | R2-SHIM-01/R2-DIAG-01; native Linux | R2-1 |
| PI-085 | `GeneratedProjectionConsumption` | manifest/trace paths derive from A and repair targets the committed Unix principal's account-database home; write/backup semantics unchanged; R2 | `shim_doctor/{report.rs,repair.rs,output.rs}` context-derived path arguments | R2-SHIM-01/R2-DIAG-01; native Linux | R2-1 |
| PI-086 | `PrivilegeBoundaryPropagation` | a Substrate-owned privileged child receives/validates the full argv carrier; an arbitrary privileged tool receives only exact context-derived target/principal argv from the still-owning installer, never carrier-as-tool-argv or preserved ambient env; R2 | named dev-installer privilege/package/group/unit/service functions | R2-LINUX-01; native Linux dedicated host | R2-2 |
| PI-087 | `DiagnosticProjection` | config/policy proof reads receive the verified leaf tuple and report A/commitment; no policy semantics change; R2 | `world-provision.sh::evaluate_gateway_lifecycle_proof_eligibility` child argv/env | R2-DIAG-01/R2-LINUX-01; native Linux | R2-2 |
| PI-088 | `ExplicitHostPropagation` | gateway sync/status/restart children receive the same leaf tuple; lifecycle behavior is unchanged; R2 | `world-provision.sh::run_gateway_lifecycle_proof` child argv/env | R2-RUNTIME-01/R2-LINUX-01; native Linux product proof | R2-2 |
| PI-089 | `ExplicitHostPropagation` | synthetic-auth creation target is the committed intended principal's account-database home, never invoking/root HOME; cleanup is PI-103; R2 | `prepare_gateway_smoke_auth` target argument only | R2-RUNTIME-01/R2-LINUX-01 then R3-LIFE-01; native Linux | R2-2 |
| PI-090 | `PlatformMapping` | shim derives A from an explicit invocation pathname or the unique no-follow candidate matching its running target when bare `argv[0]` requires PATH enumeration; PATH/CWD only resolve the invocation witness and never supply prefix precedence. Zero/multiple candidates fail; current principal/default platform selector then bind the verified factory projection; R2 | shim `context.rs` invocation-witness resolution plus exec/logging factory call chain | R2-SHIM-01/R2-MAP-MAC-01/R2-MAP-WIN-01; static plus native owners | R2-3 |
| PI-091 | `PlatformMapping` | shell replay passes its verified mapping; direct replay-library world execution requires explicit bootstrap input on macOS/Windows and otherwise fails before factory construction; replay command/origin/policy semantics remain frozen; R2 | shell replay route; replay public config/planner/executor parameter plumbing | replay differential plus platform mapping gates; static plus native owners | R2-3 |
| PI-092 | `R3CleanupOnly` | recursive shim replacement and legacy migration ownership are R3; R2 may change constructor/carrier plumbing only | `ShimDeployer::{ensure_deployed,deploy_shims,migrate_old_shims}` action predicates/body frozen in R2 | R3-LIFE-01; native platform required | R3 |
| PI-093 | `R3CleanupOnly` | recursive shim-remove action/ownership and convergence are R3; R2 passes its target only | invocation-plan remove predicate/action frozen in R2 | R3-LIFE-01; native platform required | R3 |
| PI-094 | `R3CleanupOnly` | dev managed replacement/cache/legacy-helper deletion and preservation belong exclusively to R3 | named dev installer removal/replacement functions | R3-LIFE-01; native macOS/Linux as applicable | R3 |
| PI-095 | `R3CleanupOnly` | Unix release payload/version/bin/shim replacement and legacy-unit deletion belong exclusively to R3 | named release installer replacement/legacy sections | R3-LIFE-01; native Linux/Unix | R3 |
| PI-096 | `R3CleanupOnly` | Windows version/bin recursive replacement and preservation belong exclusively to R3 | Windows release install replacement block | R3-WIN-01; native Windows | R3 |
| PI-097 | `R3CleanupOnly` | Windows dev shim/profile deletion and convergence belong exclusively to R3; PI-070 transports the target only | Windows dev uninstall deletion blocks | R3-WIN-01; native Windows | R3 |
| PI-098 | `R3CleanupOnly` | VM destroy/rebuild decision, deletion, and convergence belong exclusively to R3; R2 may realize only an absent/stopped declared instance without delete | `destroy_vm` and layout-mismatch call branch frozen in R2 | R3-LIFE-01; native macOS | R3 |
| PI-099 | `R3CleanupOnly` | guest staged-tree recursive replacement/temp cleanup and unrelated-tree preservation belong exclusively to R3 | `lima-warm.sh::stage_workspace` deletion/replacement bodies frozen in R2 | R3-LIFE-01; native macOS | R3 |
| PI-100 | `R3CleanupOnly` | rendered/staged temp cleanup, legacy unit/socket deletion, and guest runtime-socket cleanup belong exclusively to R3 | named Lima unit/socket cleanup sections frozen in R2 | R3-LIFE-01; native macOS | R3 |
| PI-101 | `R3CleanupOnly` | existing forwarded-socket unlink/ownership and convergence, including the SSH `StreamLocalBindUnlink` action, belong exclusively to R3; R2 only computes the A-scoped target | `create_ssh_uds_forwarding` explicit unlink and SSH-option sections frozen in R2 | R3-LIFE-01; native macOS | R3 |
| PI-102 | `R3CleanupOnly` | timeout process kill/current-attempt rollback belongs exclusively to R3; R2 only transports mapping into process launch | `start-forwarder.ps1` timeout kill block frozen in R2 | R3-WIN-01; native Windows | R3 |
| PI-103 | `R3CleanupOnly` | synthetic-auth deletion/current-attempt rollback and pre-existing-auth preservation belong exclusively to R3 | `cleanup_gateway_smoke_auth` and cleanup branch frozen in R2 | R3-LIFE-01; native Linux | R3 |
| PI-104 | `GeneratedProjectionConsumption` | the dispatcher supplies A explicitly; helper placement and generated preexec self-bind A, encode/match IH, and never read outer H/home as authority; R2 | `scripts/bash_preexec.rs::{BASH_PREEXEC_SCRIPT,write_bash_preexec_script}` and invocation-plan preexec path/intake | R2-GEN-01/R2-UDEV-01; native Linux | R2-1 |
| PI-105 | `ExplicitHostPropagation` | derive immutable `PlatformPrincipalV1` from validated IH at the root, carry it explicitly and process/request-scoped through normal REPL, hidden owner-helper, and direct/prepared Spawn paths alongside (not inside) prepared world-dispatch values, then require Unix account+UID database round-trip and derive only that account's `.codex`; clear the reserved seed key before every backend/policy/principal decision so denied, incompatible, and error paths cannot retain ambient poison; Unix account resolution is compiled for Linux production and Unix tests only; the projection is never persisted into HSA, session/receipt/supervisor/retained-worker authority, logs, traces, or globals; policy and credential contents stay unchanged; R2 | `routing.rs::run_shell_with_cli`; `invocation/plan.rs::ShellConfig::from_cli` exact hidden `AgentAction::OwnerHelper` hop; `agents_cmd.rs::{handle_agent_command,run_owner_helper}`; named `async_repl.rs` runtime/principal transport symbols; `orchestrator_world_dispatch.rs::{dispatch_orchestrator_world_request,dispatch_orchestrator_world_request_for_principal,dispatch_prepared_orchestrator_world_request,dispatch_prepared_orchestrator_world_request_for_principal,spawn_world_worker,spawn_prepared_world_worker,execute_spawn_world_worker_stream}` exact separate-argument transport; `world_ops.rs::{build_agent_client_and_member_dispatch_request,build_agent_client_and_member_dispatch_request_for_cwd,build_agent_client_and_member_dispatch_request_impl,resolve_host_codex_seed_home,maybe_inject_codex_auth_seed_home_for_policy,maybe_inject_codex_auth_seed_home_for_member_dispatch}`; unchanged terminal `install_bootstrap.rs::unix_account_home_for_principal` | R2-RUNTIME-01; native Linux dispatch differential plus manual large-file source closure | R2-2 |
| PI-106 | `ExplicitHostPropagation` | Routes A–D did not close normal world-deps dispatch. R2-2F binds one `AuthenticatedWorldDepsContextV1`-equivalent from already-validated A and threads it through current/global/workspace operations, provision-deps, post-provision sync, and authenticated world requests on the reviewed Unix/Linux route. Internal child argv remains authoritative and standalone public selection remains PI-067. Non-Unix compatibility paths remain R2-3-owned/unproven and cannot claim A-bound success. | `platform/mod.rs::{handle_world_command,handle_host_command}`; `world_deps/mod.rs` shared context/snapshot/provision resolver; `world_deps/surfaces.rs` exact R2-2F list; `world_enable/runner.rs` and `runner/provision_deps.rs`; additive authenticated request builder plus export-only routing wiring | R2-RUNTIME-01/R2-DIAG-01; focused native Linux, non-Unix static/fail-closed only, privileged product proof deferred | R2-2F |
| PI-107 | `GeneratedProjectionConsumption` | Routes A–D closed diagnostic snapshot roots but not every normal production path. R2-2F derives global dependency/config roots only from A, carries launch CWD explicitly for workspace scope, and binds every probe/install/sync request to the same context. Doctor constituents require matching non-secret A identity evidence. | `world_deps/surfaces.rs::{run,run_current,run_global,run_workspace,run_current_list,run_current_show,run_current_install,run_current_sync,run_global_list,run_global_add,run_global_remove,run_global_reset,run_workspace_list,run_workspace_add,run_workspace_remove,run_workspace_reset,resolve_global_available_inventory_view,build_current_show_explain_v1,run_current_list_applied,compute_current_applied_items_v1,preflight_runtime_system_requirements_v1,probe_world_apt_requirements_v1,probe_world_pacman_requirements_v1,apply_install_plan_v1,reconcile_world_deps_bin_v1,apply_apt_entrypoint_wrappers_v1,apply_script_package_v1,resolve_script_body_for_package_v1,current_codex_runtime_target_triple_v1,run_world_command_output_for_deps,run_world_command_output_for_deps_with_profile,run_world_command_checked_for_deps,query_world_package_presence,query_world_package_entrypoint_presence,run_world_presence_check_v1,ensure_world_backend_available,run_world_command_for_deps,run_world_command_for_deps_at}` | R2-RUNTIME-01/R2-GEN-01/R2-DIAG-01; focused native Linux | R2-2F |
| PI-108 | `DiagnosticProjection` | World/Host/Health/shim doctor receives typed IH and passes A-derived config/deps paths to the existing report collector. Health and direct Unix shim doctor continue through `collect_report_for_context` → `build_report`. The existing World-disabled branch returns its frozen disabled snapshot without a child or fixture lookup. The embedded world-deps branch may use only the A-rooted world-deps fixture or its existing collector. Under F5-PD, whenever the authenticated Linux World-enabled branch reaches `gather_world_doctor_snapshot`, it uses the hidden passive child; `A/health/world_doctor.json` is `cfg(test)` evidence only on that path. Existing non-Linux fixture/public-child mechanics remain frozen compatibility, cannot supply F5-PD/F5 authority or proof, and cannot claim A-bound/native truth. No authenticated Linux diagnostic reconstructs default H, consults B, invokes a contextless child, mutates the parent environment, or discloses carrier/credential/request/sensitive-principal bytes. The context-aware exposure and checked-projection compatibility collector remain behavior-frozen and R2-3-owned; R2 | named platform/health handlers; exact Route D world-deps carrier/fixture mechanics plus the exact Linux F5-PD World Doctor files/symbols below; no authenticated Linux production World Doctor fixture authority | R2-DIAG-01; native Linux | R2-2 Route D plus bounded F5-PD prerequisite |
| PI-109 | `DiagnosticProjection` | installer-managed config proof receives the hidden argv carrier; root dispatch validates it and `ShellConfig::from_cli` explicitly forwards typed IH only to the Config branch, where `run_current_show` resolves the global layer through the existing opened-bootstrap-home contract with explain parity; workspace/Cli/config semantics stay frozen; R2 | `config_cmd.rs::{handle_config_command,run_current_show}` and `config_model.rs::{resolve_effective_config_for_bootstrap_home,resolve_effective_config_with_explain_for_bootstrap_home}` exact context intake/delegation only | R2-DIAG-01/R2-LINUX-01; native Linux | R2-2 |
| PI-110 | `DiagnosticProjection` | installer-managed policy proof uses the same hidden argv carrier/typed IH; `ShellConfig::from_cli` explicitly forwards only to the Policy branch and `run_current_show` resolves the A-global layer through the existing opened-bootstrap-home contract with explain parity; workspace/policy semantics stay frozen and ambient B cannot select the global layer; R2 | `policy_cmd.rs::{handle_policy_command,run_current_show}` and `policy_model.rs::{resolve_effective_policy_for_bootstrap_home,resolve_effective_policy_with_explain_for_bootstrap_home}` exact context intake/delegation only | R2-DIAG-01/R2-LINUX-01; native Linux | R2-2 |
| PI-111 | `ExplicitHostPropagation` | **R2-2E implementation/proof clause complete.** Routes A–D left gateway configuration, effective policy, network policy, runtime-family inventory, Codex home, disabled routing, and client selection ambient. R2-2E now validates A before status/mutation/launch, keeps existing config/effective-policy owners and `policy_snapshot.rs` as the single snapshot/network owner, removes contextless unavailable synthesis, and fixes the authenticated Linux socket to `/run/substrate.sock`. Because E has no authenticated platform-endpoint source, macOS fails before client construction/`auto_select`, and Windows/other contextless entries fail before config/policy/inventory/disabled/client selection; frozen ambient compatibility remains R2-3-owned and unreachable from E. Existing policy/network/credential semantics remain unchanged. This does not complete direct-member Codex/UAA gateway adoption or the full credential/config architecture; E3/D1/D3 and the open RG-CONFIG/RG-UAA gates retain that work. | `platform/mod.rs::handle_world_command` Gateway arm; exact R2-2E symbols in `world_gateway.rs`, `policy_snapshot.rs`, and `agent_inventory.rs`; lower platform adapters and schemas frozen | R2-RUNTIME-01/R2-DIAG-01; focused Linux plus non-Unix build/static fail-closed cfg, native platform product proof deferred | R2-2E and F are complete; at that checkpoint the next proof work was remediation planning → R1 → P1 → fresh canonical baseline → renewed production-fix-free closeout. Source publication later completed, after which the accepted R2-3 suffix and refreshed native evidence closed R2-3. |
| PI-112 | `GeneratedProjectionConsumption` | `UserKnownHostsFile` is an A-relative generated helper projection carried into the verified Lima mapping; existence/content never selects A or PM; R2 | `create_ssh_uds_forwarding` known-hosts path parameter only | R2-MAP-MAC-01/R2-GEN-01; static plus assigned native macOS | R2-3 |
| PI-113 | `R3CleanupOnly` | forwarder-child kill/wait and socket removal on handle drop are teardown/cleanup authority reserved exclusively to R3; R2 mapping edits freeze the body | `ForwardingHandle::drop` body frozen in R2 | R3-LIFE-01; native macOS | R3 |
| PI-114 | `R3CleanupOnly` | timed-out SSH child kill/wait is current-attempt rollback reserved exclusively to R3; R2 mapping edits freeze the branch | `create_ssh_uds_forwarding` timeout-kill branch frozen in R2 | R3-LIFE-01; native macOS | R3 |
| PI-115 | `PlatformMapping` | the forwarder first validates IH/PM, current Windows account+SID, registered distro, pipe, and guest target; only then may `spawn_bridge` pass the exact PM-derived distro, target fields, and host commitment to `wsl::spawn`, which overwrites `WSLENV` projections and never accepts ambient target values as normal authority; R2 | `forwarder/src/{bridge.rs,wsl.rs}` spawn/config projection and colocated tests only | R2-MAP-WIN-01; static plus native Windows/WSL assigned | R2-3 |
| PI-116 | `GeneratedProjectionConsumption` | after PI-090 derives and principal-binds IH, `ManagerHintEngine::new` receives typed IH and consumes only generated `A/manager_hooks.yaml` plus optional `A/manager_hooks.local.yaml`; ambient H, `SUBSTRATE_MANAGER_MANIFEST`, and compile-time repo paths cannot select a normal product manifest, while an explicitly labeled diagnostic input cannot satisfy product proof; R2 | shim `exec/{mod.rs,logging.rs}` manager-hint intake/path selection; base projection writers remain PI-003/PI-016 and Windows R2-3 | R2-GEN-01/R2-SHIM-01; native Linux plus platform owners | R2-3 |
| PI-117 | `DiagnosticProjection` | after PI-067/PI-082 constructs and principal-binds IH, the shell entry constructs the additive explicit product-bound trace posture with immutable `A/trace.jsonl` and policy Git directory A before any shell trace consumer. Bound `init_trace(None)` initializes/reuses A, a conflicting explicit path rejects, and additive `get_policy_git_hash_at(A)` returns none for missing/unreadable/invalid metadata without fallback. The neutral global setter and every existing unmigrated default/unbound caller remain behaviorally unchanged; `LegacyAmbientCompatibility` is named, temporary, non-promotable, and forbidden on migrated shell product paths. Trace lifecycle bodies are frozen; R2 | shell trace construction/reuse call sites plus additive/default-preserving `trace/context.rs` binding representation/constructor and `trace/util.rs::get_policy_git_hash_at` | R2-DIAG-01/R2-SHIM-01; native Linux shell A/B zero-access proof plus unchanged shim/replay differential | R2-1 |
| PI-118 | `DiagnosticProjection` | after PI-090 constructs and principal-binds IH, `run_shim` migrates from `LegacyAmbientCompatibility` to the explicit product-bound trace projection before manager, policy, span, telemetry, or execution logging. Policy/logger then reuse A and policy Git reads only A. Joined with the already-frozen PI-091 replay/platform call-site migration, R2-3 removes or makes unreachable the temporary compatibility posture and proves the final global unbound-`init_trace(None)` failure rule. The neutral setter remains a registration primitive and no caller-identity side table is permitted. Trace lifecycle bodies remain PI-117-frozen; R2 | shim `exec/mod.rs`, `exec/policy.rs`, and `logger.rs` trace binding/reuse; already-frozen replay/platform call sites under PI-091; `trace/context.rs` and `trace/util.rs` only for compatibility-posture removal/final unbound rule after all owned callers migrate; common additive representation is PI-117 | R2-SHIM-01/R2-DIAG-01; native Linux plus unchanged-to-migrated shim/replay/platform differential and static/native platform assignments | R2-3 |

The remaining-seam ownership is singular: PI-111's R2-2E clause is complete and remains owned only
by R2-2E; the unresolved production parts of PI-106 and PI-107 belong only to R2-2F. F0, F0a, and F0b own
no production inventory item or architecture seam: they are the bounded test-proof prerequisites
described below. PI-108's Route D world-deps fixture/carrier mechanics remain review-clean. F5-PD
selectively replaces the production World Doctor fixture/public-child mechanics with the test-only
fixture plus authenticated passive child; F later adds broader constituent identity validation. No
row is transferred to R2-3 because a compatibility caller exists. R2-3 retains physical-shim,
replay/global-trace, platform-native mapping, and ambient compatibility migration; R2-4 retains
privileged joined product proof; R3 retains deletion, rollback, replacement cleanup, recursive or
wildcard removal, service/helper/socket cleanup, ownership manifests, account-state restoration,
crash cleanup, and uninstall convergence. The resulting inventory DAG is Routes A–E →
F0/F0a/F0b/F0-HC complete → F1–F4 complete → F5-PD → F5 → final F walls → F closeout →
remediation-planning authority → R1 → P1 → fresh canonical baseline → renewed R2-2 integration
closeout → final six-file closeout docs → one ordinary fast-forward publication → R2-3 → R2-4
→ R3.
The E/F authenticated success claim is Unix/Linux-only. macOS, Windows, fallback, and other
non-Unix compatibility paths remain explicitly unproven R2-3 ownership; build/static preservation
or unavailable/fail-closed output is not A-bound product proof.

R2-2F0/F0a were authorized but incomplete at this historical checkpoint. Their exact candidates are preserved
rather than landed; focused socket/HOME proof is green, but the post-fork candidate's three
parallel broad walls were `1134/146`, `1134/146`, and `1133/147`. The third alone added the stdout
renderer fallback test. The later exact combined F0/F0a/F0b/F0-HC candidate has now passed the
corrected differential gate, fresh containment review, and canonical closeout. The
post-E `1114 passed / 149 failed / 0 ignored` result remains genuine but nondeterministic before F0;
the same source can produce `1113 passed / 150 failed / 0 ignored`. Combined F0/F0a/F0b closeout
has established the deterministic F comparison baseline, with Routes A–E immutable. The distinct
**R2-2 historical starting baseline** remains `1089 passed / 149 failed`, and the distinct **clean
Route D comparison baseline** remains `1101 passed / 149 failed / 0 ignored`. R2-2F follows combined
F0/F0a/F0b closeout; renewed R2-2 integration closeout follows F.

## A1.1d-5 private-home prerequisite crosswalk

`PrivateSubstrateHomeV1` is prerequisite scaffolding for the existing A1 authority work. Correct
creation or rejection of the root is not proof that any target semantic seam owns its full
contract, that the real production path has adopted that seam, or that its runtime gate has passed.

| Applicable seam | A1.1d-5 prerequisite effect | Status after private-home proof | Boundary preserved |
|---|---|---|---|
| HostSessionAuthority | Authority bootstrap may begin only from one no-follow-opened, exact-owner, exact-`0700` physical `SUBSTRATE_HOME` with no effective other-principal authority. Linux rejects observable final-root access/default ACLs; qualified `ENODATA` is mode-authority evidence, never physical-absence proof. Unsafe, unavailable, unsupported, malformed, or replaced roots fail before authority markers or roots are created. | At the A1.1d-5 checkpoint: no promotion; A1.1e facade integration and later transition-intent adoption were unstarted. Current A1.1e status is recorded below. | Private-home validation selects a trusted container; it does not issue, claim, apply, or reconcile host-session transitions. |
| StateStore | Every supported creator supplies the same private per-user root before legacy or A1 persistence can write beneath it; existing invalid roots are never repaired or adopted. | No promotion. StateStore remains persistence, and A3 separation remains pending. | No persistence schema, migration, compatibility conversion, revision rule, or semantic write policy changes. |
| AgentConfigProjectionService | Config, policy, inventory, and future runtime projections originate from the bound private root and reach world members only through Substrate-owned projection/mediation, not direct unprivileged traversal. | No promotion. Direct world Codex projection/gateway adoption remains unresolved under `RG-CONFIG-04`. | Precedence, inventory interpretation, credential posture, and projection identity do not change in A1.1d-5. |
| RuntimeFamilyRealizationAdapter | Provider runtimes retain current availability and receive only the already-landed runtime/config/gateway inputs; private-home creation cannot remove a runtime family or bypass the gateway carrier. | No promotion. Adapter ownership and contract-correct consumer realization remain later work. | No launch semantics, provider parsing, credential path, world request, policy snapshot, or enforcement-plan change. |

For this prerequisite, `mkdirat` success creates only a candidate name. The accepted child identity
begins at the first no-follow directory open and descriptor validation beneath a descriptor-bound,
already-validated trusted parent. Legitimate same-UID Substrate creators converge through create
versus `AlreadyExists`, then independently open and validate the same candidate. All operations
after acceptance remain descriptor-relative, with path-to-descriptor identity revalidation at
required publication boundaries. A different principal is excluded by the trusted parent's owner,
mode, and ACL posture; malicious root or malicious same-UID substitution before the first child
descriptor is acquired is outside the A1 V1 threat model. This correction adds no privileged
broker, changes no StateStore or HostSessionAuthority semantics, and promotes no seam.

## A0 authority-leak inventory contract

Slice A0 populates a repo-grounded inventory in this file before authority code moves. Do not create a separate control-pack file. Each inventoried use of helper/PID/socket/heartbeat/attached-client/owner-process/cwd/env state records:

| Code location and decision | Observed value | Current classification | Current durable effect | Proposed owner seam | First migration target | Proof needed |
|---|---|---|---|---|---|---|
| `AgentRuntimeStateStore::new`; `substrate_paths::substrate_home` select the durable state namespace | Any non-empty ambient `SUBSTRATE_HOME` is accepted verbatim, including a relative path; empty/unset falls back to `$HOME/.substrate` | `AuthorityDecision` | Selects the filesystem root for session, participant, obligation, inbox, binding, and active-task state. Different ambient homes select different authority namespaces, and the same relative home can resolve to different namespaces after a CWD change. | StateStore is configured with one normalized bootstrap-home handle; initialization marker and root persist its physical identity; HostSessionAuthority operates only when both match the trusted handle | A1.1: bind and persist one explicit normalized bootstrap home, distinguish genuinely absent from invalid/partial/unreadable/unsafe/unsupported authority roots, reject copied/rebound homes, and supply serialized initialization/recovery; A3 later separates general persistence and compatibility projection. | `RG-AUTH-01`, `RG-AUTH-02`, `RG-BASE-03`; add absolute/relative/empty cross-CWD, copied-home, ancestor-symlink/unsafe-ACL/scan-replacement, and absent/partial/malformed/permission/symlink/unsupported bootstrap proof |
| `substrate_paths::{config_file,policy_file}`; effective config/policy resolution; `agent_inventory.rs::{discover_agent_inventory_roots,load_effective_agent_inventory}` select launch inputs from the same home | Ambient `SUBSTRATE_HOME` -> global `config.yaml`, `policy.yaml`, and `agents/`, with empty/unset `dirs::home_dir()` fallback or failure | `AuthorityDecision` | Selects effective configuration, policy, backend, capabilities, and inventory material later frozen into the session descriptor and `HostAttachContract`. Changing home can therefore change launch/attach semantics independently of the StateStore namespace already created under another home. | Host bootstrap binds one normalized home; EffectivePolicyResolver and AgentConfigProjectionService resolve versioned inputs from that same handle; HostSessionAuthority persists their accepted identities/snapshots | A1.1 adds bounded explicit-bootstrap-home APIs to the existing path/config/policy/inventory resolver entry points and binds that identical physical home to StateStore without changing precedence or interpretation; A1.2 commits exact already-resolved inputs into typed objects/intents; A1.3 threads the opened handle through the named real CLI/REPL paths with no ambient reread. Later E2/E3 own policy/config snapshot and projection semantics. | `RG-AUTH-01`, `RG-AUTH-02`, `RG-CONFIG-01`, `RG-POLICY-03`; add conflicting homes plus empty/unset/no-home create/control/restart proof |
| `config_model.rs::{parse_env_overrides,resolve_effective_config}` rereads ambient effective-config overrides | `SUBSTRATE_OVERRIDE_WORLD`, `SUBSTRATE_OVERRIDE_ANCHOR_MODE`, `SUBSTRATE_OVERRIDE_ANCHOR_PATH`, `SUBSTRATE_OVERRIDE_CAGED`, `SUBSTRATE_OVERRIDE_WORLD_NET_FILTER`, `SUBSTRATE_OVERRIDE_POLICY_MODE`, and `SUBSTRATE_OVERRIDE_SYNC_{AUTO_SYNC,DIRECTION,CONFLICT_POLICY,EXCLUDE}` | `AuthorityDecision` | Each resolution can change execution scope, world root, caging, network filter, policy posture, and sync behavior used to select/materialize launch and attach contracts; changed ambient values across start/control/restart are not joined to an immutable accepted authority/policy revision. | Host bootstrap captures normalized inputs; EffectivePolicyResolver and AgentConfigProjectionService own versioned resolution; HostSessionAuthority persists accepted references/snapshots | A1.1 parameterizes only the existing effective-config home/path entry points with the explicit bootstrap home; A1.2 binds in-scope intent issuance/application to the exact accepted effective-config identity; A1.3 threads the opened handle through named CLI/REPL paths. Do not change override parsing, precedence, or unrelated control paths. Later E2/E3/E4 own immutable policy/config and runtime enforcement. | `RG-AUTH-01`, `RG-AUTH-02`, `RG-POLICY-03`, `RG-SYNC-01`; add create/control/restart with changed, invalid, empty, and removed overrides |
| `agent_runtime/session.rs::new_participant`; `orchestration_session.rs::new`; `async_repl.rs::owner_helper_orchestration_session` record an owner process | `std::process::id()` -> participant/session `shell_owner_pid` | `SignalOnly` | Persists the episode PID beside durable session and participant rows; the producer itself makes no lifecycle decision, but consumers below treat it as authority. | SurfaceAdapter / HostExecutionEpisode | A2: move PID into episode observation state without changing current launch mechanics. | `RG-AUTH-01`, `RG-AUTH-02` |
| `session.rs::build_participant`; `orchestration_session.rs::new`; `async_repl.rs::prepare_hidden_owner_helper_runtime` create initial host attachment truth | Host role/scope constructor defaults set `attached_client_present`, attachment timestamp, `resume_eligible`, `ActiveAttached`, and attached participant before runtime ownership is established | `AuthorityDecision` | Creates durable attached posture from constructor context. The hidden-helper path persists the parent session before gateway construction, so later construction failure can leave an `ActiveAttached` session without an owning episode or revision-checked reconciliation. | HostSessionAuthority owns atomic intent-backed session birth/posture; HostExecutionEpisode supplies an explicit initial attachment observation | A1.2 owns greenfield-certificate-checked `ExpectedAbsent`, Start reservation/application, and initial authority birth; A1.3 adopts it without editing general participant constructors. A1.1 supplies bootstrap/object/key/resolution/CAS primitives only. A2 later separates episode establishment/failure observations outside the in-scope owner-helper path. | `RG-AUTH-01`, `RG-AUTH-02`, `RG-AUTH-03`; add uncertified/pre-A1-state rejection, fail-after-reservation/issue/authority-commit, restart, and stale-rewrite proof |
| `agent_runtime/session.rs::{can_advertise_live,is_authoritative_live}` derives ownership truth | `uaa_session_id`, `control_owner_retained`, `event_stream_active`, `completion_observer_retained`, `ownership_valid`, terminal observation, participant state | `AuthorityDecision` | Defines the persisted `authoritative_live` predicate consumed by host-session routing and retained-worker lifecycle decisions. | HostSessionAuthority for host posture; RetainedWorkerRuntime for worker lifecycle | A1.1 exact resolution ignores the composite; A1.2/A1.3 do not use it to authorize in-scope host transition intents. A2 owns general host-episode demotion and B3.2 later owns retained-worker liveness. | `RG-AUTH-01`, `RG-AUTH-02`, `RG-AUTH-03` |
| Host and member heartbeat loops in `repl/async_repl.rs` refresh runtime observations | `last_heartbeat_at`, `last_active_at`, current `is_authoritative_live()` | `SignalOnly` | Persists observation timestamps while the in-memory runtime still advertises ownership; no timeout currently terminalizes a row directly. | SurfaceAdapter / HostExecutionEpisode; RetainedWorkerRuntime for member observations | A2: report heartbeat as an episode observation only. | `RG-AUTH-02` |
| `state_store.rs::{persist_participant,should_persist_participant_snapshot}` accepts or rejects a durable write | Maximum of transition, event, heartbeat, ownership-verified, and terminal timestamps | `AuthorityDecision` | A newer heartbeat or other timestamp can make an incoming snapshot overwrite durable participant truth without an authority revision/CAS check. | HostSessionAuthority revision rules with StateStore atomic persistence | A1.1 supplies generic cross-process revision-CAS persistence; A1.2/A1.3 use it for in-scope semantic writes before A3 separates persistence and compatibility. | `RG-AUTH-02` |
| `state_store.rs::{persist_orchestration_session,should_persist_orchestration_session_snapshot}` accepts or rejects a whole parent-session write | Session state rank plus maximum of `closed_at` and `last_active_at` | `AuthorityDecision` | A same-state snapshot with a newer heartbeat-derived `last_active_at` can replace the entire durable session, including older binding, attach-contract, and active-participant truth, without an authority revision/CAS check. | HostSessionAuthority revision rules with StateStore atomic persistence | A1.1 supplies exact resolution and generic root/authority revision-CAS; A1.2/A1.3 reject stale semantic writes. A3 retains only atomic persistence and schema-evolution behavior in StateStore. | `RG-AUTH-01`, `RG-AUTH-02`; add conflicting-active-snapshot stale-heartbeat proof |
| `state_store.rs::snapshot_write_lock` and its guarded read-decide-write paths arbitrate competing snapshot writers | Process-local `OnceLock<Mutex<()>>` | `AuthorityDecision` | Serializes threads in one process only. Independent CLI, helper, and router processes can load the same snapshot, make incompatible lifecycle, binding, claim, inbox, or obligation decisions, and atomically replace one another's updates without a shared revision/CAS boundary. | StateStore provides cross-process atomic persistence/CAS; HostSessionAuthority and the applicable obligation/receipt owner authorize semantic transitions | A1.1 supplies the cross-process root lock, object transaction, revision-CAS, greenfield certificate, and fail-closed pre-A1 write guard. A1.2 puts reservation/intent/application writes behind them. A3 later owns the general persistence split. | `RG-AUTH-01`, `RG-AUTH-02`, `RG-AUTH-03`; add two-process bootstrap, pre-A1 write rejection after init-marker/root publication, conflicting-write, stale-overwrite, and duplicate-claim proof |
| `state_store.rs::{AgentRuntimeSessionRecord::live_participants,list_live_participants}` selects live rows | `is_authoritative_live() && owner_process_is_alive(shell_owner_pid)` | `AuthorityDecision` | Removes durable rows from live discovery; feeds parent selection, retained-member reuse, toolbox discovery, and unreadable-metadata binding fallback. | HostSessionAuthority; RetainedWorkerRuntime for retained member availability | A1.1 supplies exact durable resolution without PID; A1.2/A1.3 use it for in-scope transition issuance/application. A2 owns general host episode availability and B3.2 owns retained-member availability. | `RG-AUTH-01`, `RG-AUTH-02`, `RG-AUTH-03` |
| `state_store.rs::resolve_single_live_session_for_agent` selects one session | Active parent state plus session PID liveness and PID-filtered participant liveness | `AuthorityDecision` | Permits or blocks live orchestrator/toolbox discovery and can report no authoritative session solely because an owner process exited. | HostSessionAuthority | A1.1 supplies exact durable session resolution; A1.2/A1.3 use it for in-scope transition issuance/application. A2 owns unrelated discovery consumers and channel availability. | `RG-AUTH-01`, `RG-AUTH-02`, `RG-AUTH-03` |
| `state_store.rs::{classify_public_session_posture,recoverable_stale_host_attachment}` classifies public posture | Attached participant/client, `is_authoritative_live()`, owner PID liveness, resume eligibility, replacement-owner scan | `AuthorityDecision` | Produces `Active`, `DetachedReattachable`, or `Terminal`; gates stop, attach, turn, internal dispatch, status, and router eligibility. | HostSessionAuthority | A1.1 supplies exact persisted authority resolution only; A1.2 defines revisioned transition preconditions, and A1.3 adopts them for `Start`/`Attach`/`ResumeOneTurn`. A2 demotes the general liveness-derived classifier. | `RG-AUTH-01`, `RG-AUTH-02`, `RG-AUTH-03` |
| `state_store.rs::valid_detached_host_continuity_posture` accepts detached continuity | No attached participant, `attached_client_present == false`, live participant state, resume handle/attach contract, pending inbox posture | `AuthorityDecision` | Treats absence of an attached episode as part of the authority test for parked/attention continuity. | HostSessionAuthority | A1.1 resolves the exact persisted authority/object refs; A1.2 defines the Attach/Resume intent precondition, and A1.3 consumes it instead of episode absence. A2 owns general episode demotion. | `RG-AUTH-01`, `RG-AUTH-02`, `RG-AUTH-03` |
| `state_store.rs::classify_hidden_owner_helper_launch_continuity` decides helper readiness | Attached-client flag, ownership composite, owner PID, detached continuity, stale attached truth | `AuthorityDecision` | Permits helper launch success, blocks readiness, drives timeout reconciliation, and is reused by detached stop fallback. | HostSessionAuthority with HostExecutionEpisode observations | A1.2/A1.3 expose the exact intent/result only through the bounded helper launch integration; A2 supplies typed episode observations and migrates detached-stop reuse. | `RG-AUTH-01`, `RG-AUTH-02`, `RG-CLOSE-01` |
| `agents_cmd.rs::run_stop` gates public host-session closeout on private owner delivery | Resolved public posture; private stop socket path/connect outcome; `Accepted`, `AlreadyTerminal`, `OwnerUnreachable`, or protocol error; terminal-state wait | `AuthorityDecision` | A detached session receives direct durable closeout, but an active session with missing/refused/protocol-failing delivery returns `owner_unreachable` without closeout; accepted delivery still fails if terminal proof does not arrive. Socket availability therefore permits or blocks the durable public stop result. | HostSessionAuthority owns exact stop intent and idempotent closeout; HostExecutionEpisode reports delivery/terminal observations | A2, not A1, adopts public `run_stop` through the authority API while typing private delivery as an episode observation and preserving fail-closed outcomes. | `RG-AUTH-01`, `RG-AUTH-02`, `RG-CLOSE-01`; preserve missing/refused/timeout distinctions and add restart/repeated-closeout proof |
| `control.rs::joined_hidden_owner_helper_attach_receipt`; `agents_cmd.rs::verify_manual_reattach_restored_ownership`; `auto_attach.rs::{ensure_auto_attach_restored_session,finalize_session_auto_attach_after_launch}` accept or reject restored ownership | `ActiveAttached`, live orchestrator, attached client, non-zero `shell_owner_pid`, exact participant | `AuthorityDecision` | Declares manual/auto attach restored and settles obligations on success; verification failure releases a manual claim or marks automatic attach failed closed. The launch receipt exposes the process PID. | HostSessionAuthority owns attach transition; SurfaceAdapter / HostExecutionEpisode reports observations; RouterAttachTrigger and ObligationLedger own claim settlement | A1.3 makes manual attach consume the exact intent result; A1.4 changes only bounded auto-attach plan production/integration and does not edit policy, eligibility, verification settlement, or claim settlement. A2 retains PID only on the episode receipt; C2/C3 own projection/router settlement. | `RG-AUTH-01`, `RG-AUTH-02`, `RG-ATTACH-01` |
| `control.rs::{try_acquire_hidden_owner_helper_attach_launch_guard,wait_for_inflight_hidden_owner_helper_attach_launch}` coordinates attach launch | Filesystem advisory lock, current readiness classification, lock release, and timeout | `AuthorityDecision` | Chooses leader spawn versus joining an existing helper; lock/process loss can retry as leader, while joined success is accepted from current PID/attachment-derived state. | HostSessionAuthority owns the exact attach transition; SurfaceAdapter owns idempotent launch/join mechanics; HostExecutionEpisode reports lock/process observations only | A1.2/A1.3 expose and join a revisioned attach intent/result only in the bounded helper launch integration; A2 makes leader episode loss and retry explicit and idempotent. | `RG-AUTH-01`, `RG-AUTH-02`, `RG-ATTACH-01`; add process-loss/duplicate-leader proof |
| `orchestrator_world_dispatch.rs::{persist_continue_world_worker_obligation,resolve_local_obligation_host_id,run_router_owned_auto_attach_discovery_once}`; `auto_attach.rs::{ensure_router_auto_attach_targets_local_host,resolve_router_auto_attach_local_host_id}` target router-owned attach | Ambient `gethostname()` at obligation creation, inbox materialization, and attach routing; missing target is accepted as untargeted | `AuthorityDecision` | Persists hostname as origin/target when available; hostname failure creates an untargeted obligation, skips the host-inbox materialization pre-pass, and still permits discovery from canonical obligations without exact-host comparison. Later hostname change or collision can deny or misroute attach. | ObligationLedger owns explicit durable target identity; InboxProjection consumes it; RouterAttachTrigger validates it against stable host identity supplied by host bootstrap | C1/C2: require explicit versioned host targeting or a named compatibility rule and project from it; C3 routes only an exact validated target. | `RG-AUTH-02`, `RG-OBL-02`, `RG-ATTACH-01`, `RG-ATTACH-02`; add hostname absence/change/collision/restart proof |
| `control.rs::stabilize_hidden_owner_helper_start_return`; `agents_cmd.rs::recoverable_detached_start_retry` react to helper exit | Child `try_wait`, helper PID liveness, startup-stream failure, replay state, elapsed timeout | `AuthorityDecision` | Chooses success, prompt replay, persisted reconciliation, terminal startup failure, or caller-visible failure after the helper exits. | HostSessionAuthority owns reconciliation/idempotency; SurfaceAdapter owns retry mechanics; HostExecutionEpisode reports exit/transport observations only | A1.2/A1.3 own intent reconciliation/idempotency inside the bounded Start launch/issuance integration; A2 owns general process-exit and transport observations. | `RG-AUTH-01`, `RG-AUTH-02` |
| `async_repl.rs::{connect_startup_prompt_backchannel,terminalize_startup_prompt_failure}` and startup event/completion branches convert prompt-channel state into durable truth | Socket connect, accepted envelope, startup turn phase, EOF before/after acceptance, completion data, and replay state | `AuthorityDecision` | Connection immediately persists `Accepted`; later phase/EOF persists `Completed` or `Failed`, drives terminalization or parking, and changes whether public start may replay or return success. | HostExecutionEpisode reports channel/envelope observations; HostSessionAuthority owns revisioned startup transition and idempotency | A1.3 applies monotonic startup transitions only in owner-helper validation/application and real-REPL adoption; A2 supplies general episode/envelope observations and rejects stale/duplicate input. | `RG-AUTH-01`, `RG-AUTH-02`; add connect/EOF/duplicate/stale/restart phase matrix |
| `control.rs::{reconcile_hidden_owner_helper_start_timeout,build_detached_start_reconciliation}`; `async_repl.rs::{can_park_host_runtime_after_detach,build_parked_host_runtime_snapshots}` write durable posture | Helper/stream exit, attached-client truth, competing live owner PID, ownership flags, inbox count | `AuthorityDecision` | Zeros owner PID, releases ownership, marks detached, and persists parked/attention or terminal state. | HostSessionAuthority | A1.2/A1.3 route in-scope helper reconciliation and posture application through revision-checked authority; A2 owns general episode demotion. | `RG-AUTH-01`, `RG-AUTH-02` |
| `agents_cmd.rs::allocate_fork_successor` allocates a durable successor without a live episode | Literal `shell_owner_pid = 0`; cleared UAA, ownership, stream, observer, attachment, and resume fields | `AuthorityDecision` | Creates an exact successor session and lineage, binds its active participant, and persists it as `ParkedResumable` before any helper owns the successor. The PID-zero sentinel is part of the current durable allocation protocol. | HostSessionAuthority for successor allocation; HostExecutionEpisode may later attach observations | A2, not A1, adopts public fork-successor allocation through the authority API while removing the process sentinel from durable meaning; A1.1 supplies only the prerequisite authority primitive. | `RG-AUTH-01`, `RG-AUTH-02`, `RG-BASE-01` |
| `agents_cmd.rs::{resumed_public_turn_detach_stable_after_grace,resumed_public_turn_runtime_released}` accepts resumed-turn release | Invalid ownership plus `shell_owner_pid == 0`, after detached target readiness and a fixed grace period | `AuthorityDecision` | Declares one-turn detach normalization stable and permits the public turn to return success; a non-zero or still-owned participant blocks that completion path. | HostSessionAuthority decides detached posture; HostExecutionEpisode reports release/grace observations | A1.3 makes `ResumeOneTurn` result joining depend on revisioned authority within the bounded issuance path; A2 replaces the PID-zero release predicate with a typed episode observation. | `RG-AUTH-01`, `RG-AUTH-02` |
| `tool_invocation_contract.rs::resolve_follow_up_dispatch_authority_v1` for `CancelWorldWork`; `state_store.rs::resolve_internal_cancel_world_dispatch_target` decide retained cancelability | Toolbox preflight `record.live_participants()` plus target `is_authoritative_live()`, owner PID, `latest_run_id`, `Running`, and cancel support | `AuthorityDecision` | The real runtime-toolbox path can return `stale_linkage` from PID-filtered `live_participants()` before reaching the inner cancel resolver; the latter can independently reclassify an exact nonterminal worker or permit cancel from episode ownership/PID. | WorldWorkReceiptRegistry and RetainedWorkerRuntime | B4: resolve toolbox and inner cancel once against an active receipt/worker manifest; do not use process liveness as active-work truth. | `RG-AUTH-02`, `RG-CANCEL-01`, `RG-CANCEL-02`; add exact runtime-toolbox cancel after owner-PID loss with distinct no-active/unreachable/terminal outcomes |
| `state_store.rs::count_authoritative_live_retained_workers` computes concurrency occupancy | Retained participant ownership composite plus owner PID | `AuthorityDecision` | Changes whether dispatch concurrency policy sees a worker slot as occupied. | RetainedWorkerRuntime with SteeringPolicyEngine | B3.2: count durable worker manifests/accepted work, with episode availability separate. | `RG-AUTH-02`, `RG-BASE-04` |
| `orchestrator_world_dispatch.rs::{WorldDispatchConcurrencyTracker,acquire_world_dispatch_concurrency_guard}` reserves dispatch capacity | Process-local per-session ephemeral and retained-bootstrap counters plus PID-filtered persisted retained count; RAII guard lifetime | `AuthorityDecision` | Permits or denies ephemeral launch/spawn/fork against policy caps; process exit resets reservations and separate processes do not share occupancy. | SteeringPolicyEngine evaluates caps over durable WorldWorkReceiptRegistry and RetainedWorkerRuntime reservations | B1/B3.2: reserve exact accepted work atomically in durable registries and release idempotently on terminal reconciliation. | `RG-AUTH-02`, `RG-BASE-04`; add multi-process/restart cap proof |
| `state_store.rs::ActiveEphemeralWorldTaskGuard::drop`; `orchestrator_world_dispatch.rs::{ActiveEphemeralTerminalWaitTracker,ActiveEphemeralTerminalTruthGuard}` own active ephemeral identity/closeout | Foreground guard lifetime, process-local waiter registration/watch channel, stream terminal observation | `AuthorityDecision` | Guard drop deletes the persisted active task record; in-memory wait registration alone carries cancel terminal truth and is lost on caller/process exit, making inspect/cancel/closeout foreground-scoped. | WorldWorkReceiptRegistry owns durable active identity; WorldWorkExecutionSupervisor owns terminal observation and restart reconciliation | B1 persists exact accepted identity independently of caller guard; B2.1 moves wait/terminal closeout to the supervisor. | `RG-AUTH-02`, `RG-RECEIPT-01`, `RG-RECEIPT-03`; add caller-drop/restart inspect/cancel proof |
| `state_store.rs::invalidate_stale_world_members_for_session` chooses rows to terminalize | `is_authoritative_live()` plus older world generation | `AuthorityDecision` | Only ownership-advertising older-generation workers are invalidated; parked/non-advertising rows can escape the sweep. | HostSessionAuthority world binding and RetainedWorkerRuntime | A1.1 supplies the binding-revision primitive and in-scope transitions use it; B3.2, not A1, adopts worker invalidation. | `RG-AUTH-02`, `RG-BASE-02` |
| `state_store.rs::{shared_world_metadata_root,recover_active_shared_world_binding_from_local_metadata_only,recover_active_shared_world_binding_from_authoritative_live_participants}`; `agents_cmd.rs::refresh_public_session_world_binding`; `async_repl.rs::{synchronize_repl_authoritative_world_binding,persist_repl_authoritative_world_binding}` repair binding | Effective UID; `XDG_RUNTIME_DIR`; existence of `/run/user/<uid>`; `/tmp/substrate-worlds-<uid>` fallback; persisted/in-memory binding; PID/ownership-filtered live members when metadata is unreadable | `AuthorityDecision` | Ambient process credentials and runtime-directory posture select the metadata namespace; code can then choose metadata, persisted state, or process-liveness-filtered rows as authoritative, persist the selected binding, update the in-memory world session, and invalidate older-generation members. | HostSessionAuthority consumes an explicitly bound metadata source; CompatibilityReadModel may discover/diagnose candidates but cannot author binding | A1.1 binds the metadata namespace and supplies revision-CAS only; A1.2/A1.3 use it for in-scope `Start`/`Attach`/`ResumeOneTurn` and real REPL adoption. A3 owns general compatibility discovery, and B3.2 owns worker invalidation. | `RG-AUTH-01`, `RG-AUTH-02`, `RG-BASE-02`; add XDG-unset, `/run/user`, `/tmp`, and cross-UID proof |
| `async_repl.rs::start_host_orchestrator_runtime_with_prepared_prompt_and_toolbox_request_tx`; `finalize_runtime_startup_failure`; `control.rs::persist_world_binding_authority` establish or clear startup binding | Initial world-ready binding or absence; world close success during startup failure | `AuthorityDecision` | Persists or clears the exact session world binding before runtime ownership is established; persistence failure terminalizes the parent, and successful close during failed startup clears binding after marking the session failed. | HostSessionAuthority owns binding/posture transition; HostExecutionEpisode reports ready/close observations; StateStore persists atomically | A1.3 makes only the in-scope owner-helper/real-REPL startup binding and failure reconciliation revision checked; A2 owns general episode observations. | `RG-AUTH-01`, `RG-AUTH-02`, `RG-BASE-01`, `RG-BASE-02` |
| `async_repl.rs::{ensure_no_policy_drift,handle_detected_world_drift,restart_world_session}`; `control.rs::{persist_world_binding_authority,invalidate_stale_world_members_after_binding}` replace a drifted world | World cwd and workspace root, policy snapshot hash, configured auto-restart/fail-closed mode, old/new world generation | `AuthorityDecision` | Closes or replaces the world, persists or clears the session's exact world binding, and then invalidates older-generation workers. A failure between replacement, binding persistence, and invalidation can leave partially reconciled durable truth without an authority revision/CAS protocol. | EffectivePolicyResolver classifies policy drift; HostSessionAuthority owns binding transition/reconciliation; StateStore persists atomically | Not A1: A1.1 supplies only the generic binding-revision prerequisite. Production drift-handler adoption requires a separately bounded later policy/reconciliation slice; no current A1 code area or packet is widened for it. | `RG-AUTH-01`, `RG-AUTH-02`, `RG-BASE-02`; add crash-between-steps reconciliation proof |
| `state_store.rs::{build_session_record,synthesize_session_record,born_unattached_status_anchor}` builds status/torn-root views | PID/ownership-filtered rows and latest transition/event/heartbeat timestamp | `CompatibilityRead` | Determines read-model completeness, synthesized active participant/latest run, warnings, and status anchor; authoritative control rejects a missing parent but consumers still see the projection. | CompatibilityReadModel | A3: extract versioned read-only synthesis and prevent it from writing newer lifecycle truth. | `RG-AUTH-02`, `RG-BASE-03` |
| `ResolvedInternalInspectWorldDispatchTarget::project_snapshot` projects runtime status | Target ownership composite plus owner PID | `CompatibilityRead` | Reports `authoritative_live` to inspect callers without changing the target row. | CompatibilityReadModel over HostSessionAuthority/RetainedWorkerRuntime truth | A3: project authority and availability as distinct fields. | `RG-AUTH-02` |
| `async_repl.rs::{live_member_for_generation,parked_member_runtime_matches_generation,ensure_member_runtime_ready_for_descriptor}` reuses or replaces a retained member | PID-filtered persisted live list, in-memory/persisted `is_authoritative_live()`, terminal/session handle and world generation | `AuthorityDecision` | Chooses existing runtime reuse, parked resume, shutdown/replacement, or new member launch. | RetainedWorkerRuntime | B3.2 resolves one durable worker manifest and treats process/channel state as runtime availability. | `RG-AUTH-02`, `RG-BASE-04` |
| `async_repl.rs::{PendingMemberReplacementMap,reconcile_member_runtime_generation,ensure_member_runtime_ready_for_descriptor}` carries replacement lineage | Episode-local map from replacement key to invalidated participant snapshot after durable invalidation | `AuthorityDecision` | Supplies `resumed_from_participant_id` only while the REPL survives; exit/restart between invalidation and replacement loses predecessor identity and can re-root or duplicate durable lineage. | RetainedWorkerRuntime owns revisioned invalidation/replacement intent and exact predecessor lineage | B3.2 persists replacement intent before invalidation and reconciles it idempotently after restart. | `RG-AUTH-02`, `RG-BASE-04`; add restart-between-invalidation-and-replacement proof |
| Event/completion closeout branches in `repl/async_repl.rs` mutate lifecycle | Prior `is_authoritative_live()`, stream exit, completion status/data, surfaced UAA-session identity, shutdown/cancel/owner-helper-exited flags | `AuthorityDecision` | Invalidates or fails a retained member, fails bootstrap, applies cancelled/stopped closeout, or parks a host session based on process/stream observations. | HostExecutionEpisode for host observations; HostSessionAuthority and RetainedWorkerRuntime for transitions | A2 demotes host stream/completion facts to revision-bound observations; B3.2 later owns member closeout/reconciliation. | `RG-AUTH-01`, `RG-AUTH-02`, `RG-WORKER-EXIT-01` |
| `async_repl.rs::{spawn_prompt_terminal_loss_monitor,classify_prompt_worker_error,shutdown_disposition_for_termination_cause}` and host shutdown choose terminal-loss closeout | Controlling-terminal/Reedline error, normal exit versus abnormal terminal loss, prompt-worker drop | `AuthorityDecision` | Normal exit stops the host runtime, while abnormal terminal loss selects `ParkIfResumable` and can preserve durable session continuity solely from an episode-local terminal observation. | HostExecutionEpisode reports termination cause; HostSessionAuthority owns park/stop transition | A2: submit typed terminal-loss observation against an authority revision and make repeated shutdown idempotent. | `RG-AUTH-01`, `RG-AUTH-02`, `RG-BASE-03`; add terminal-loss/restart closeout proof |
| `world-service/member_runtime.rs::{should_preserve_retained_member_after_clean_bootstrap,finish_bootstrap,RetainedMemberSlot::stale_fork_child_participant_id,prune_stale_fork_child_slot}` preserves or replaces retained slots | `participant_id.starts_with("ash_")`, process exit code, surfaced UAA-session identity, bootstrap presence, and resumable/bootstrapping state | `AuthorityDecision` | Only an `ash_`-prefixed clean exit with surfaced identity preserves later routability in the in-memory retained slot; action/identity mismatch, missing/nonzero continuity, or stale child posture unregisters/evicts it. Daemon restart loses this authority entirely. | RetainedWorkerRuntime owns typed durable worker kind, manifest, and restart-safe reconciliation; string prefix and process completion are observations/compatibility inputs only | B3.2 drives preservation/replacement from revisioned durable worker truth rather than naming convention, with idempotent daemon-restart reconciliation. | `RG-AUTH-02`, `RG-BASE-04`, `RG-WORKER-EXIT-01`; add retained/ephemeral action-prefix mismatch, malformed identity, clean/nonzero/cancel/missing-identity, retry/restart, and stale-observer proof |
| `world-service/member_runtime.rs::{ActiveMemberRegistry,register_member,find_submit_target,validate_submit_target_slot}` admits and routes retained workers/turns | Process-local maps by participant ID and retained key, including source/direct-child slot membership | `AuthorityDecision` | Permits or rejects duplicate member registration and exact turn submission. Daemon restart empties both maps, so durable worker truth can survive while exact continuation becomes “not retained”; retry can be admitted or rejected solely from reconstructed process memory. | RetainedWorkerRuntime owns a durable revisioned worker registry and restart reconciliation; world-service memory is a cache/availability view | B3.2 rehydrates/reconciles exact retained manifests and slot lineage before admission or turn routing. | `RG-AUTH-02`, `RG-BASE-04`; add daemon-restart exact continue, duplicate participant/key retry, and stale-fork-slot reconciliation proof |
| `world-service/member_runtime.rs::{active_turns_by_span_id,ActiveMemberRuntime::active_turn_span_id,reserve_turn_slot,register_turn,unregister_turn,cancel}` owns submitted turns | Process-local span map, one active span per member, cancel handle, and last signal | `AuthorityDecision` | Permits or denies a submitted turn, decides cancelability, records cancel signal only in memory, and releases the slot on completion; daemon/stream loss erases active-turn truth and repeated cancel semantics. | RetainedWorkerRuntime owns turn admission; WorldWorkReceiptRegistry/Supervisor own active-run identity, cancel outcome, and terminal reconciliation | B1/B2.1/B3.2/B4 persist accepted active-run identity, reconcile exact observation and turn lifecycle across restart, and keep cancel distinct. | `RG-AUTH-02`, `RG-CANCEL-01`, `RG-CANCEL-02`; add immediate/repeated cancel, stream-drop, stale-span, and daemon-restart proof |
| `control.rs` startup/toolbox/prompt/stop/cancel path builders and listeners publish private endpoints | Unix socket path, bind/connect/accept availability, stream acceptance | `FastPathTransport` | Delivers episode-local prompt, toolbox, stop, cancel, and readiness traffic; path publication alone is not durable success. | InternalToolboxTransport and SurfaceAdapter / HostExecutionEpisode | A2: return explicit transport availability without lifecycle mutation. | `RG-AUTH-02`, `RG-CLOSE-01` |
| `control.rs::{compact_stop_transport_fragment,hidden_owner_helper_plan_path,hidden_owner_helper_attach_lock_path,hidden_owner_helper_startup_prompt_stream_path,toolbox_transport_path_for_home,private_stop_transport_path,private_cancel_transport_path,private_prompt_transport_path}` and identity-free request handlers address private artifacts/endpoints | Non-injective first/last-six alphanumeric fragments; preferred Unix path over 100 bytes switches startup/toolbox/stop/cancel/prompt to shared `/tmp/substrate-agent-*` directories without store-root or effective-UID namespace; existing-path removal before bind; stop/cancel/prompt payloads omit exact identity | `AuthorityDecision` | Colliding IDs or long distinct homes can overwrite a plan, serialize unrelated attach launches, alias/rebind another user's or store's socket, deny registration, or deliver control to the wrong runtime; registration failure can then feed durable startup failure. | SurfaceAdapter and InternalToolboxTransport own injective, UID/store-namespaced episode addressing; HostExecutionEpisode reports availability only; durable lifecycle remains with owning authority | A2: make helper artifacts/endpoints injective, namespace-preserving, permission-safe, and identity-validated; B4 later joins exact target/receipt to stop/cancel closeout. | `RG-AUTH-02`, `RG-CLOSE-01`; add colliding IDs and two long homes across UID/store roots, stale path/symlink, permissions, and cross-route rejection for startup/toolbox/prompt/stop/cancel |
| `async_repl.rs::register_internal_toolbox_transport_for_session`; `control.rs::toolbox_transport_path_for_home` publish the session toolbox | Session-only socket path, removal of any existing listener path, process-local caller participant injected by the latest listener | `AuthorityDecision` | A stale or competing episode can rebind the same session endpoint and become the effective toolbox caller without an authority-revision check; transport success then reaches whichever episode bound last. | InternalToolboxTransport resolves the current exact episode through HostSessionAuthority; SurfaceAdapter supplies episode identity | A2: bind toolbox publication to an accepted authority revision and reject stale rebind/requests. | `RG-AUTH-01`, `RG-AUTH-02`, `RG-ATTACH-02`; add competing/stale episode rebind proof |
| Runtime launch branches in `repl/async_repl.rs`; `mark_runtime_startup_failed` convert endpoint registration into startup truth | Success/failure registering toolbox, prompt, stop, and cancel transports; `host_toolbox_surface_authoritative` | `AuthorityDecision` | A registration failure marks the runtime failed and can terminalize the controlling parent session; successful toolbox publication also determines whether the endpoint is injected into the UAA launch env. | HostExecutionEpisode reports startup-protocol events and channel availability; HostSessionAuthority durably captures the actor/event and owns host startup acceptance/terminal reconciliation; RetainedWorkerRuntime owns member lifecycle | A1.3's bounded host helper/REPL adapter may transport only an exact actor-bound `OwnershipAccepted`, `RuntimeCreationRejected`, or `StartupFailedBeforeOwnership` protocol event into the A1.2 decision; endpoint registration/readiness/local failure cannot be relabeled and ambiguity stays Pending. The committed StartupOwnershipResult, not an episode row, is the durable A1 proof. A2 later generalizes episode availability observations without changing that owner; B3.2 owns retained-worker startup reconciliation. | `RG-AUTH-01`, `RG-AUTH-02`, `RG-AUTH-03`; add exact actor/accepted/terminal-event identity, endpoint-registration ambiguity, retry, and conflicting-replay proof |
| `async_repl.rs::{RuntimeStartupSignal,signal_runtime_startup}` and local/remote startup wait branches accept control ownership | Process-local one-shot `Running`/`Failed`, sender drop, fixed timeout, and post-signal persisted-state revalidation | `AuthorityDecision` | Accepts a runtime or aborts it and persists startup failure; channel loss/timeout can decide durable host/member state even when episode events or daemon state survive. | HostExecutionEpisode reports the exact actor-bound startup-protocol event; HostSessionAuthority durably captures it and owns revision-bound host acceptance/failure; RetainedWorkerRuntime owns member acceptance/failure | A1.3 replaces host signal inference only in the bounded real helper/REPL path with exact protocol-event submission: target-participant ownership acknowledgement accepts, exact target-participant pre-ownership failure or exact persisted launch-claimant rejection reconciles terminally, and timeout/drop/EOF/readiness/process posture remains Pending. A2 later generalizes episode observation; B3.2 makes member acceptance/retry/restart idempotent. | `RG-AUTH-01`, `RG-AUTH-02`, `RG-AUTH-03`; add actor-bound accepted/terminal/ambiguous, signal-loss/timeout/duplicate/stale/restart proof |
| `agents_cmd.rs::run_owner_helper`; `control.rs::load_hidden_owner_helper_launch_plan`; `async_repl.rs::{owner_helper_manifest,owner_helper_orchestration_session,prepare_hidden_owner_helper_runtime}` consume the helper-plan payload | Arbitrary `--plan-file` JSON carries mode, session/participant/run/trace IDs, lineage, workspace/world binding, descriptor, attach contract, resume handle, and startup prompt; only resolved backend ID is revalidated before persistence | `AuthorityDecision` | Constructs and persists durable identity, lineage, binding, attach/posture, and resume truth from file contents without an accepted authority revision/provenance join; stale, replayed, or substituted plan identity can become current truth. | HostSessionAuthority validates exact revision-bound launch intent; SurfaceAdapter owns private plan-file creation/read/delete mechanics and reports delivery observations | A1.2/A1.3: after the strict greenfield upgrade, durably issue `HostSessionTransitionIntentV2` only for Start before plan creation; preserve strict V1 intent bytes as read-only and never default V2 state into them. Only after the later A1.2b strict V2-to-V3 extension may Attach/Resume use a V3 successor intent carrying B1/C1-owned post-turn types. Carry the selected intent's exact ID/hash plus a hash-committed transport projection; retain a durable reprojection source through terminal handoff; claim, apply, and reconcile without destructive authority consumption. Exact applied retry joins the result; stale, replayed, substituted, mismatched, expired, or conflicting use fails closed. A2 retains the plan only as adapter transport. | `RG-AUTH-01`, `RG-AUTH-02`, `RG-AUTH-03`; add arbitrary path, load/remove-before-consumption crash, helper loss after claim, stale/replayed/substituted plan, identity/binding/hash mismatch, exact retry, restart, and stale-revision proof |
| `HiddenOwnerHelperLaunchPlan.mode` consumers in `control.rs`, `agents_cmd.rs`, and `async_repl.rs` select helper lifecycle intent | `OwnerHelperMode::{Start,Attach,ResumeOneTurn}` -> attach lock, readiness/reconciliation rules, new versus replacement lineage, internal-session requirement, `control_only_attach`, and `auto_park_after_public_turn` | `AuthorityDecision` | Selects whether to allocate or resume durable identity, which readiness can return success, which timeout may reconcile state, whether prompts run, and whether the session parks after one turn. A serialized launch-mode value therefore selects durable lifecycle transitions and retry behavior. | HostSessionAuthority owns exact start/attach/resume intent, transition, and idempotency; SurfaceAdapter maps accepted intent to launch/join mechanics; HostExecutionEpisode reports observations only | A1.2/A1.3 validate mode-specific preconditions and immutable identity/binding/descriptor commitments against expected absence or the exact authority revision, then apply the durable intent once. A2 replaces remaining launch booleans with typed adapter/episode inputs. | `RG-AUTH-01`, `RG-AUTH-02`, `RG-AUTH-03`, `RG-CLOSE-01`; add Start/Attach/ResumeOneTurn lifecycle, helper loss, exact retry, restart, stale-revision, and repeated-closeout matrix |
| `control.rs::runtime_controls_parent_session` and its `async_repl.rs` consumers select parent authority by runtime role | String equality with `ORCHESTRATOR_ROLE` | `AuthorityDecision` | Gates startup binding, parent activation/failure, stop request/closeout, event/completion loss handling, parking, and shutdown mutation; a non-orchestrator instead leaves/touches parent state while member lifecycle changes. | HostSessionAuthority resolves exact authoritative participant role and owns parent transitions; RetainedWorkerRuntime owns member transitions; HostExecutionEpisode reports stream/process observations | A1.3 validates parent-control capability only for in-scope owner-helper/real-REPL application as an immutable intent commitment against exact authority identity/revision; B3.2 later owns member lifecycle. | `RG-AUTH-01`, `RG-AUTH-02`, `RG-AUTH-03`; add host/member role mismatch, stale identity, loss, restart, and closeout matrix |
| `async_repl.rs::start_host_orchestrator_runtime_with_prepared_prompt_and_toolbox_request_tx` and event/completion/parking branches apply `runtime_owns_private_stop` | Launch-mode boolean: `true` for the hidden owner helper and `false` for ordinary REPL/turn episodes | `AuthorityDecision` | Hidden-helper mode bypasses post-`Running` persisted-state revalidation, directly owns the private stop receiver, preserves active posture on event-stream loss, and parks only after helper exit or the completed one-turn exception. Ordinary mode revalidates startup, delegates stop ownership, and may park immediately on detach. Episode wiring therefore changes durable preserve/park/invalidate behavior. | HostExecutionEpisode owns stop-channel capability and termination observations; HostSessionAuthority owns revisioned startup acceptance and preserve/park/invalidate transitions | A1.3 makes the in-scope modes apply the same claimed intent through revision-checked authority transitions while preserving exact-stop fail-closed behavior; A2 later replaces the boolean with typed episode capability and observations. | `RG-AUTH-01`, `RG-AUTH-02`, `RG-AUTH-03`, `RG-CLOSE-01`; add helper/non-helper stale-`Running`, event/completion ordering, loss, exact retry, restart, and outcome-equivalence matrix |
| `async_repl.rs::{completed_hidden_owner_helper_should_auto_park,run_hidden_owner_helper,start_host_orchestrator_runtime_with_prepared_prompt_and_toolbox_request_tx}` deliver one-turn auto-park | Optional process-local unbounded `auto_park` channel; event-task send after completed/failed phase; `try_recv`, disconnect, or fixed grace-timeout receive after completion | `AuthorityDecision` | Message presence selects durable `ParkIfResumable`; lost, disconnected, delayed, or restart-erased delivery selects a different shutdown/closeout despite the same completed turn. Duplicate/reordered signals have no revision/idempotency owner. | HostExecutionEpisode reports exact turn completion; SurfaceAdapter transports the signal; HostSessionAuthority owns revisioned post-turn posture/idempotency | A1.3 binds post-turn disposition to the accepted `ResumeOneTurn` intent and exact completion/revision, making lost/delayed transport replay-safe; A2 later demotes the queue to episode delivery only. | `RG-AUTH-01`, `RG-AUTH-02`, `RG-AUTH-03`; add lost/disconnected/duplicate/delayed signal, completion ordering, helper loss, exact retry, restart, and stale-revision proof |
| `orchestrator_world_dispatch.rs::{wait_for_fork_child_durable_publication,wait_for_fork_child_durable_publication_with_timeouts,persist_fork_child_lineage,rollback_failed_fork_child_launch}` gates fork lineage on transport publication | Durable child row plus existence of its private stop socket within separate visibility budgets | `AuthorityDecision` | Persists explicit source-to-child lineage only after the stop socket appears; a missing socket triggers rollback of an already-persisted child, and failed rollback can leave a live child while the fork reports failure. | RetainedWorkerRuntime owns durable fork/lineage lifecycle; private stop availability remains a transport observation | B3.2 persists exact child identity and lineage independently of channel publication, with idempotent fail-closed reconciliation; B4 later owns stop closeout. | `RG-AUTH-02`, `RG-BASE-04`; add durable-child/channel-unavailable retry and rollback proof |
| `orchestrator_world_dispatch.rs::{detached_stop_world_worker_closeout_available,stop_world_worker_sanctioned_recovery_retry_available,refused_exact_target_stop_transport_proves_current_attached_owner_stale}` handles stop transport failure | Stop socket exists/missing, `ConnectionRefused`/`NotFound`, helper continuity, exact refreshed owner, retained runtime flags | `AuthorityDecision` | Permits a bounded retry or directly persists durable `Stopped` closeout on the narrow exact-target detached path; otherwise fails closed. | WorldWorkReceiptRegistry and RetainedWorkerRuntime under HostSessionAuthority | B4: keep the resolved exact-target baseline but make receipt/worker state, not socket posture, own closeout. | `RG-CLOSE-01`, `RG-AUTH-02` |
| `orchestrator_world_dispatch.rs::connect_world_agent_client`; `routing/dispatch/world_ops.rs::{ensure_world_service_ready,build_agent_client_and_request_impl}` select and bootstrap world transport | `SUBSTRATE_WORLD_SOCKET` or `/run/substrate.sock`, readiness probe, live `SUBSTRATE_SOCKET_ACTIVATION_OVERRIDE`/detected activation posture, `SUBSTRATE_WORLD_AGENT_BIN`/PATH candidate existence | `FastPathTransport` | Selects the delivery channel; the activation override can force wait/manual cleanup/spawn behavior. Connection/readiness failure blocks the request but does not itself create, rebind, or terminalize durable session truth. | WorldDispatchControl transport adapter | B1: bind accepted work to durable receipt identity independently of socket, activation override, or helper-process availability. | `RG-AUTH-02`, `RG-RECEIPT-03`; add production-ingress override and live transport proof |
| `async_repl.rs::start_remote_member_runtime_with_prepared` consumes world transport after publishing an allocating member | Client construction, `execute_stream` open/acceptance, and post-open retained-ownership persistence after the participant row and registered/task-start events are persisted/emitted | `AuthorityDecision` | Client-build or stream-open failure returns without startup reconciliation; after daemon acceptance, ownership-persistence failure returns before observer creation or remote abort. Either window can leave an exact local `Allocating` row, live daemon slot/process, or duplicate retry. | RetainedWorkerRuntime owns accepted startup/reconciliation; world transport and the member execution episode report availability only | B1 provides the accepted-work anchor, B2.1 owns observer handoff/reconciliation, and B3.2 owns member lifecycle plus abort/retry; none may infer success from stream availability. | `RG-AUTH-02`, `RG-BASE-04`; add pre-open and post-accept persistence-failure retry/restart proof |
| `agents_cmd.rs` start/context builders and `async_repl.rs::resolve_host_orchestrator_bootstrap` establish session context | Repeated process-CWD reads select effective config/policy/inventory and persisted `workspace_root`; read failure silently becomes relative `.` | `AuthorityDecision` | Host start resolves its contract from one CWD read but persists workspace from a later read, so CWD change or lookup failure can bind a session/attach contract to a different or unresolved relative workspace than the selected backend/policy. | HostSessionAuthority receives one normalized absolute workspace identity from fail-closed host bootstrap | A1.3: resolve once within the allowed `Start` issuance and owner-helper/real-REPL adoption regions, reject unresolved/relative ambiguity, and persist the exact workspace binding at session birth. | `RG-AUTH-01`, `RG-AUTH-02`; add deleted/unresolvable-CWD and CWD-change-between-resolution-and-persistence proof |
| `async_repl.rs::owner_helper_shell_config`; host UAA request builders in `control.rs` and `async_repl.rs` reread process cwd | Child cwd with fallback to plan workspace; repeated current CWD with error fallback to relative `.` in `AgentWrapperRunRequest.working_dir` | `AuthorityDecision` | Chooses world-root/caging and host UAA side-effect location even when a persisted session workspace already exists; a changed or unresolvable CWD can make the episode execute relative to a different location without a binding revision. | HostSessionAuthority supplies one normalized workspace binding; RuntimeFamilyRealizationAdapter consumes it without ambient fallback | A1.2/A1.3 make only the bounded helper plan/application and real-REPL paths consume the persisted binding and fail closed on missing identity; later adapter slices carry it explicitly elsewhere. | `RG-AUTH-01`, `RG-AUTH-02`; add existing-session CWD-change/failure negative proof |
| `agent_inventory.rs::{discover_agent_inventory_roots,load_effective_agent_inventory,inventory_path_origin}` applies cwd-relative precedence | Workspace root discovered from cwd; workspace inventory shadows home-global/legacy entries | `AuthorityDecision` | Selects the backend/config material persisted in the launch descriptor and `HostAttachContract`; rereading from another cwd or conflicting global/workspace inventory can author a different durable contract. | Host bootstrap supplies exact normalized home/workspace binding; AgentConfigProjectionService resolves versioned logical config; HostSessionAuthority accepts/persists its identity | A1.1 parameterizes only the existing inventory-root entry points with the explicit bootstrap home; A1.3 threads that handle and freezes the already-resolved binding and inventory/config commitment at `Start` issuance without changing inventory precedence; E3 later makes per-worker projection canonical and rebuildable. | `RG-AUTH-01`, `RG-AUTH-02`, `RG-CONFIG-01`, `RG-CONFIG-02`; add conflicting global/workspace inventory and cross-CWD exact-contract proof |
| `routing/dispatch/world_ops.rs::resolve_host_codex_seed_home`; `member_runtime.rs::prepare_codex_runtime_env` bridge direct world Codex config | `HOME`/`USERPROFILE` -> `SUBSTRATE_INTERNAL_CODEX_AUTH_SEED_HOME`; isolated `CODEX_HOME`; copied auth and bounded host `config.toml` | `CompatibilityRead` | Selects a host seed-home compatibility source and copies runtime-native auth/config for direct Codex. It does not use the already-landed managed gateway carrier. | HostCredentialAuthority and AgentConfigProjectionService | D1: name the compatibility posture in the envelope; E3 adopts the existing gateway and Substrate-owned projection. | `RG-CONFIG-02`, `RG-CONFIG-04` |
| `world_ops.rs::preserve_world_project_dir_override`; `member_runtime.rs::{codex_authoritative_project_dir,member_runtime_process_working_dir}` select world project/cwd | Existing staged `SUBSTRATE_WORLD_PROJECT_DIR` wins; otherwise parsed `SUBSTRATE_ANCHOR_MODE` selects `Project` (path or cwd), `FollowCwd` (cwd), or `Custom` (path or settings fallback); invalid/missing mode uses settings fallback; empty `SUBSTRATE_ANCHOR_PATH` is absent; result materializes `SUBSTRATE_WORLD_PROJECT_DIR` | `CompatibilityRead` | Chooses Codex trust-demotion key, runtime cwd, and resume cwd from env/request projection; it does not mutate host-session truth but invalid/empty values and CWD changes can alter the projected root without a target envelope/projection join. | WorldRuntimeAdapterExecutionEnvelope and AgentConfigProjectionService | D1: bind exact mode/path/cwd projection to envelope identity; E3 makes it rebuildable. | `RG-UAA-01`, `RG-CONFIG-04`; add invalid/empty, Project/FollowCwd/Custom, explicit override, fallback, and CWD-change proof |
| `control.rs::maybe_build_runtime_owned_toolbox_env` injects the model-visible endpoint | `SUBSTRATE_AGENT_TOOLBOX_ENDPOINT` and version derived from exact session ID | `FastPathTransport` | Makes the session-scoped toolbox reachable for a runtime turn; the env value is not persisted as session authority. | RuntimeToolInvocationAdapter -> InternalToolboxTransport | A2: keep endpoint availability episode-local while authority is resolved independently. | `RG-AUTH-02` |

The inventory is complete only when every current durable decision that depends on process/socket/helper/owner/cwd/env posture is either listed or explicitly proven outside scope. A0 changes diagnostics/tests and this inventory only; it does not introduce the authority facade.

### A0 coverage and explicit exclusions

A0 traced production callers across `common/src/paths.rs`, `agent_runtime/{session,orchestration_session,state_store,control,auto_attach,tool_invocation_contract}.rs`, `execution/{agents_cmd,orchestrator_world_dispatch,agent_inventory,config_model}.rs`, broker global-policy path resolution, `execution/routing/dispatch/world_ops.rs`, `repl/async_repl.rs`, and `world-service/member_runtime.rs`. Grouped rows above cover repeated identical predicates rather than duplicating every call site.

| Audited value family | Why it is not an additional durable-decision row |
|---|---|
| `SUBSTRATE_STOP_ORDER_DEBUG_*`, `SUBSTRATE_WS_DEBUG`, `SHIM_TRACE_LOG`, and `SUBSTRATE_TEST_*` | Diagnostics or `#[cfg(test)]` injection only; they do not decide production identity, posture, binding, or terminal truth. |
| `SUBSTRATE_WORLD_EXEC_FORCE_DIRECT` | Live production execution-strategy override: when permitted it suppresses heuristic/read-only overlay execution, and it fails when network isolation requires cgroup attachment. This affects world side-effect enforcement under D2/E4, not host-session durable authority; it is not test-only and A0 makes no enforcement change. |
| `SHIM_SESSION_ID`, `SHIM_PARENT_CMD_ID`, `SUBSTRATE_PARENT_SPAN_ID`, `SUBSTRATE_WORLD`/`SUBSTRATE_WORLD_ID`, and ambient `SUBSTRATE_AGENT_ID` in generic world requests | Trace propagation, telemetry/world-context marking, or request attribution only in the audited paths. Exact orchestration/participant/world binding for retained dispatch comes from typed request fields, not these ambient values. |
| `PATH`, `SHELL`, `XDG_DATA_HOME`, `SUBSTRATE_FORCE_REEDLINE`, `SUBSTRATE_WSL_AGENT_EXEC_TIMEOUT_MS`, `CI`, `GITHUB_ACTIONS`, host-shell child PID, and local kill/wait state | Episode configuration, rendering, transport timeout, or local cancellation only. The durable effects of helper PID/exit are already captured in the helper rows above. |
| `SUBSTRATE_POLICY_MODE`, `SUBSTRATE_WORLD_FS_MODE`, `SUBSTRATE_WORLD_REQUEST_PROFILE`, `SUBSTRATE_CAGED`, world-deps PATH, and network/fs request fields | Execution-policy or runtime-realization inputs, not additional host-session authority in the audited paths. Their enforcement ownership remains explicitly scoped to D2/E1 and cannot be used as A0 proof. |
| Test-only/dead-code helpers such as `find_active_orchestration_session_for_pid` and `list_live_sessions` | No current production caller. Their PID predicates are still covered by the grouped StateStore compatibility/read rows so later activation cannot bypass the inventory. |
| `GatewayAuthBundleV1`, `SUBSTRATE_LLM_AUTH_BUNDLE_FD`, and gateway one-time consumption | Positive landed carrier under `RG-CONFIG-03`, not an authority leak or missing primitive. A0 makes no carrier change; only the direct-Codex compatibility env/projection path above remains unresolved. |

A0 records current semantics only. It moves no authority, changes no enforcement point, adds no smoke claim, and promotes no seam.

**A0 closeout:** the affected crosswalk surface is this inventory contract plus the unchanged SurfaceAdapter / HostExecutionEpisode, HostSessionAuthority, StateStore, and CompatibilityReadModel rows. The real production path remains distributed across the inventoried callers. Existing compatibility reads, helper episodes, private transports, and the direct-Codex compatibility copy bridge are retained without being treated as contract-correct. Exact verification evidence and remaining proof gaps are recorded under A0 in `05-debug-regression-ledger.md`; sibling seams were observed but not edited.

**A1.1e closeout:** commits `8826270a` and `d2aac84f` establish the exact-resolution
`HostSessionAuthority` facade and stale-observation proof. Commit `49876412` adds broker API
`resolve_effective_policy_with_explain_from_global_source`; both ambient and explicit entry points
reuse the same broker-owned parse/layer/explain/validate/finalize core. Remediations `80b0f9e0`,
`839f78b4`, `182dd4af`, `1e356654`, and `cd676614` close descriptor, inventory-provenance, and
StateStore lexical-rebind findings; `for_bootstrap_home` now returns a distinct bound StateStore
capability with retained-transaction operations and no descendant-path API. Focused proof is broker
differential `4/4`, broker `75/75`, config `20/20`, inventory `16/16`, policy `13/13`, policy
snapshot `9/9`, HostSessionAuthority `95/95`, StateStore `188/188`, and explicit-home `19/19`;
format, check, Clippy-with-warnings-denied, and diff checks pass. The broad shell wall is honestly
`843 passed / 171 failed`; its exact 171-test failure set is unchanged from the accepted deferred
A1.1d lifecycle baseline. Exact command/filter and output-hash provenance is recorded in
`05-debug-regression-ledger.md`. This closes A1.1e only: real CLI/helper/REPL adoption, transition intents,
dispatch narrowing/enforcement, A1.1d integrated Linux and native-macOS closeout, and
`RG-BASE-01` remain open. No seam row is promoted. The broad A1.2 checkpoint remains preserved
outside the source branch and must not be restored. The
[B1/B2.1 production-ingress ownership audit](../b1-b2-1/crosswalk.md#b1b21-dispatch-authority-ownership-audit)
decomposed A1.2 into the bounded A1.2a current-authority prerequisite and later A1.2b
successor/post-turn completion.
A1.2a is landed and independently review-clean through `b5f2b4f8`; A1.2a-WB is landed and
independently review-clean through `275f9fa2`; and A1.2a-S is landed and independently review-clean
through `2f2fecb3`. B1/B2.1-R0 is landed and independently review-clean through `bb3eefba`; B3.2a
plus B3.2a-WA are independently review-clean through
`d0a70727c2bec2b2d6fe0754ea469c4682684dda`. The B1 receipt core, B2.1 supervisor core,
B2.1 replay/startup activation, authority-store binding correction, and B1/B2.1-0 action-scoped
join are review-clean through `6436289f`, `c519024b`, `de727091`, `717579b0`, and `83101dcb`
respectively. Their joint production integration closeout is now complete on the frozen
2026-08-03 source without additional product bytes, B3.1 is dependency-ready, and no seam is
promoted by these bounded prerequisites.

**A1.1d-5I ownership audit:** this investigation adds no seam and changes no row classification.
It localizes the compatibility failures to existing boundaries:

| Existing owner/boundary | A1.1d-5I repo truth | Bounded remediation responsibility |
|---|---|---|
| HostSessionAuthority / `PrivateSubstrateHomeV1` | `trusted_fs.rs` owns ancestor and final-root acceptance. At audit baseline `b29897e0`, it rejected any effective named ACL permission and reported only the requested final home, while a zero-effective default ACL could pass the ancestor check, be inherited, and fail only after candidate creation. R1 now corrects that behavior through `4d0acff68e20d86b97fe5367b8a4617554f33ef4`. | R1 enforces masked non-writing Linux POSIX access ACLs under strict parsing and `ACL_MASK`/group-mode correspondence, while rejecting any effective write, every ancestor default ACL before creation, every observable final-root access/default ACL, and every malformed/unreadable/unsupported/ambiguous state. `ENODATA` is typed `NoData` and is accepted only under descriptor-bound owner/type/identity/replacement/mode authority; it never proves physical absence. Diagnostics report the exact offending path, role, ACL kind, reason class, and candidate-creation state without principals. Final-root exact owner/`0700` and owner-only descendant rules do not change. |
| StateStore plus config/policy/inventory consumers | These consumers are entitled only to the already-accepted physical home capability. They do not own prefix selection, ACL repair, installer fallback, or a second ambient-home lookup. | R1/R2 must not move bootstrap or prefix authority into persistence or projections; every consumer continues from the one accepted home. |
| Product lifecycle entry/child adapters (not a new semantic seam) | Install/uninstall entry points select host context; shim, doctor, service, and helper subprocesses consume it. Current Unix dev/release and Windows dev/release paths contain ambient-home or missing-context fallbacks. | R2 transports one explicit host context across every child and sudo edge. A child cannot reinterpret it or make an outer environment override part of the product contract. This does not assign installer behavior to `SurfaceAdapter / HostExecutionEpisode`. |
| Generated installation projections (not authority) | `env.sh` encodes selected values; manager scripts may self-derive their install directory; configuration/install-state/version/dependency artifacts may be prefix-relative without encoding the prefix. Current manager scripts prefer a conflicting ambient home over self-location. | R2 proves encoded values, self-derived locations, prefix-relative placement, and every consumer agree with the selected host context. R3 proves accepted-home/later-stage partial failure, exact synchronous rollback, uninstall, and reinstall convergence without deleting unrelated pre-existing state. An abrupt unaccepted invalid candidate remains fail-closed. This does not promote `CompatibilityReadModel`. |
| RuntimeFamilyRealizationAdapter / platform provisioning boundary | Linux world provisioning consumes the selected host home, but release unit sandbox allowlisting is derived from ambient home. macOS maps the host selection to a distinct Lima guest home/principal; Windows has its own principal/path domain. Static comparison is not native proof. | R2 preserves the host-context commitment and validates an explicit platform-realization mapping rather than equating host and guest paths/principals. The adapter gains no home-selection authority; later Linux/native platform smoke supplies proof. |

The A1.1d-5I failures are installation/bootstrap availability and authority-home correctness defects,
not evidence of lost world filesystem/network capability, changed policy enforcement, or regressed
B1 receipt/B2.1 supervisor semantics.

This R1 correction changes no seam classification. The supported Linux POSIX-ACL proof is bounded
to effective authority: `ACL_MASK` corresponds to group-class mode bits. Unknown ACL models,
`ENOTSUP`, malformed bytes, or other distinguishable retrieval failures fail closed rather than
being projected into HostSessionAuthority truth. Physical ACL-xattr absence would require a
separately approved privileged platform-attestation boundary; R1 neither implies nor adds one.
The bounded R1 implementation and reviews are clean; R2-0 planning and R2-1 implementation are
complete, while R2-2 Routes A–D remain individually review-clean and R2-2E is implementation-,
proof-, and review-complete. At that historical checkpoint F0/F0a/F0b/F0-HC were also canonically
complete and F was the exact next packet. Renewed closeout, R2-3, R2-4, and R3 were still
unstarted; the R1 evidence did not promote this seam, and all later lifecycle/product gates
remained open.

**A1.2a-WB/A1.2a-S closeout:** A1.2a-WB makes Start issuance, application/persistence, and exact
current-authority resolution enforce the same Host/world-binding matrix while leaving exact binding
as session-level truth. A1.2a-S applies or exact-joins that authority before transport or legacy
persistence on the ordinary internal host path, materializes `PreparedAgentRuntime` only from the
applied result, supplies the exact bound capability to the internal toolbox, performs zero activated
legacy session/participant/snapshot writes, and leaves startup ownership Pending. Fork/member
preparation, public/helper/successor adoption, startup/post-turn reconciliation, and world
capability/policy semantics remain unchanged. This evidence does not make HostSessionAuthority,
SurfaceAdapter, StateStore, CompatibilityReadModel, InternalToolboxTransport, or
WorldDispatchControl `ContractCorrectAndProven`.
