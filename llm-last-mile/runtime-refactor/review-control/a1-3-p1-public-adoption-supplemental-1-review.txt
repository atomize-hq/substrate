VERDICT: FINDINGS

A13P1-P2-006 — P2 — `crates/shell/src/execution/agent_runtime/control.rs:423`, `:649`, `:743`; `crates/shell/src/execution/agent_runtime/control.rs:2594`, `:3418`; `crates/shell/src/repl/async_repl.rs:8768` — The launch guard is owned only by the public launcher’s receipt, not the spawned helper. If that launcher exits, is killed, or times out before HSA settlement, the lock releases while the helper remains live. A retry is promoted to leader and launches a duplicate helper; its transport registration removes and replaces the first helper’s live sockets. Contract effect: violates exact-duplicate convergence, single-helper ownership, and no transport replacement. Bounded remedy: transfer a child-owned authenticated launch lease/lock across exec until exact HSA settlement; kill and join the helper before releasing it on launcher errors; refuse to unlink a live exact-owner transport. Add a launcher-death retry test asserting one helper and unchanged socket identities.

A13P1-P2-007 — P2 — `crates/shell/src/execution/agents_cmd.rs:2287`, `:2383`, `:2408`, `:2451`, `:2481`; `crates/shell/src/execution/agents_cmd.rs:1033`, `:1040`, `:1251`; `crates/shell/src/execution/agent_runtime/control.rs:694` — Duplicate coordination begins only after successor issuance, claim, and application, and the guard is keyed by the newly randomized intent ID. Two identical concurrent calls can therefore issue and claim distinct intents against the same authority snapshot; one applies, while the other fails stale and leaves a nonterminal intent instead of joining the winner. Contract effect: exact duplicate public Attach/Resume calls do not reliably converge. Bounded remedy: acquire a stable per-authority/session/request guard before reading or issuing HSA state, re-read under that guard, join equivalent Issued/Claimed/Applied work, and retain coordination through helper settlement.

Repository review was read-only. Exact live subject fingerprint matched `sha256:5a64889b5e9ec5c90271c7953e1574edb7c80b5486427f9a4f3f18d887222652`.
