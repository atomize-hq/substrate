VERDICT: FINDINGS

A13P1-P2-008 — P2 — `crates/shell/src/execution/agents_cmd.rs:2399-2455`, `:2569-2614`, `:2725-2772`; `crates/shell/src/execution/agent_runtime/control.rs:657-690`; `crates/shell/src/execution/agents_cmd.rs:5502-5542`; `crates/shell/tests/agent_public_control_surface_v1.rs:4036-4044` — The persisted public-successor identity is read only when the retry observed live flock contention. Launcher death releases that session/request lock, so a retry acquires it uncontended, ignores and overwrites the persisted identity, and cannot join the prior Issued or Claimed intent because generic recovery considers only Applied intents. The inherited child lock starts only after application and spawn, so it cannot close this pre-application crash window. A death after `issue_successor` or `claim_successor` therefore permits a distinct successor to be issued and applied while leaving the original nonterminal. The tests cover live simultaneous contention and generic FD inheritance, but never kill the actual launcher and retry across these HSA phases. Contract effect: violates stable pre-issuance identity, exact-duplicate convergence, and launcher-death recovery; it can strand nonterminal HSA work and defeats the required one-intent/one-helper proof. Bounded remedy: durably distinguish unfinished exact requests from acknowledged sequential requests, always inspect and exact-validate that state under the session guard regardless of whether acquisition was observed contended, and reuse/join the exact Issued, Claimed, or Applied identity through settlement. Add actual launcher-death retry tests around pre-apply and live-helper phases that assert one HSA intent, one helper, and unchanged transport socket identities.

Repository review was read-only. Exact live subject fingerprint matched `sha256:986cb6ed03475c41427131dcb82ca46ac2ab820a8f4603c7eb949c60d3d9e2fa`.
